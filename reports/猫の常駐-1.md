# 猫の常駐-1 — 題字の右に RunCat の猫を置いた（panel v103）

**VAIO 2026-08-30 17:40 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 1. 絵の複製

`~/Desktop/neko-koma/` から6枚をリポジトリ直下（公開ページから読める場所）へ複製し、名を揃えた。

| 元 | 複製先 | 大きさ |
|---|---|---|
| `cat-frame-0.imageset_cat-frame-0.png` | **`cat0.png`** | 4,423 byte・56×36px |
| `cat-frame-1…` 〜 `cat-frame-4…` | **`cat1.png`〜`cat4.png`** | 各 4,338〜4,403 byte・56×36px |
| `sleeping-cat.imageset_sleeping-cat.png` | **`cat-sleep.png`** | 3,434 byte・56×36px |

## 2. 動き

**パネルが既に読んでいる現況（`lastStat`）で切り替える。新しい取得はしない。**

| 現況 | 猫 |
|---|---|
| **作業中** | `cat0→1→2→3→4→0` のコマ送り（**1コマ100ミリ秒**・`setInterval(runCatTick, 100)`） |
| **それ以外全部**（次の指示待ち・ヨシ待ち・連絡なし・読めない） | `cat-sleep` の静止絵 |

- 六枚は最初に一度だけ先読み。毎コマ `src` を替えるだけで通信は起きない
- 眠りへ入るとき一度だけ `src` を替え、眠っている間は書き換えない

## 3. 見た目 ── 等倍・透過・ヘッダーを崩さない

**一度、崩れかけた。** 猫の36pxは h1 の行（約30px）より高く、そのまま置くと
ヘッダーが **65.4px → 71px に 5.6px 伸びた**（実測）。
`#runcat{margin:-3px 0}` で箱の上では30pxぶんとして数えさせ、
絵は等倍のまま上下へ3pxずつはみ出して描く形にした（header の余白の中なので切れない）。

**直したあとの実測** ── 猫 **56×36px（等倍）**／h1 **30.4px**／header **65.4px** ＝ **v102 と同じ**。

出典は二箇所のコメントに入れた（CSS 側 L38・script 側 L1236）。

```
Cat sprites: RunCat Neo (runcat-dev, Apache-2.0)
```

## 4. 切り替えの実測（作法14「写しに probe」）

```
【作業中】10コマの並び
  cat4 → cat0 → cat1 → cat2 → cat3 → cat4 → cat0 → cat1 → cat2 → cat3   ← 順どおり循環

【手待ちへ】 一度目のコマで cat-sleep.png ／ 眠っている間は書き換えない : はい
【ヨシ待ち】 cat-sleep.png（眠ったまま）
【また作業中】走り出した
```

## 5. 公開側の検収

```
panel-ver.txt = 103 ／ verTag = panel v103（8月30日） ／ 手元と完全一致
cat0〜cat4・cat-sleep : すべて HTTP 200・手元と同一（byte 一致）
出典の一行 : 2件 ／ runCatTick : 1件
```

### 画面の写し（`shots/`）

| 写し | 状態 | 猫 |
|---|---|---|
| **`shots/cat-run.png`** | **公開URLの実物**（現況＝作業中） | **走りの姿**で題字の右に居る |
| **`shots/cat-sleep-state.png`** | 指示待ち相当（公開側の実物の HTML に、状態だけ手待ちを固定して描画） | **眠りの姿（Zzz）** |

＊眠りの側は、いまの現況が作業中のため公開URLそのままでは撮れない。
　**公開側から落とした実物の HTML** に `lastStat = "手待ち"` を毎50ミリ秒で固定する一片だけを
　足して描いた（本物の `pullState` が現況で上書きするのを抑えるため。一度目はこれに負けて
　走りの姿が写った）。

## 版と検査

`PANEL_VER='103'`／`verTag`／`panel-ver.txt` の三箇所を同時更新。
`<script>` を切り出して `node --check` → OK。commit `040b53f`。

---

## 実機で見るところ

1. 題字「返事パネル」の右に**黒い猫**
2. **作業中は走り**（コマ送り）、**それ以外は眠る**（Zzz の静止絵）
3. ヘッダーの高さも他の要素の位置も**変わっていない**
4. 版の字が **panel v103**

## 残り

`orders-open.tsv` の未了 ── **残り0件。**
