# 写しの一回-3 — 読み返しをやめ、未写しの分だけにした（panel v89）

**VAIO 2026-08-30 11:05 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 1. 釦もまとめ写しも、未写しの分だけ

`panel.html` **L910-917**。写す札の選び方を戻した。

```js
/* ---- 写すのは未写しの分だけ（v89）----
   ＊v87/v88 は「写した印の付いた札」も混ぜて一本にしていた（読み返し）。
     押すたびに同じ札が何度も入り、写しが太るだけだった。**v89 で読み返しをやめた。**
   ＊写した札を後から見たいときは、**「終わりました」の箱の個別の「写す」**で足りる。
     あちらは v84 から作業中でも押せる。 */
var todo = (shown || []).filter(function (d) { return !isDone(d); });
```

**釦の字は一つだけ**（**L776-782**）。

```js
var n = buildBundle().count;
if (n > 0) { b.textContent = 'まとめて写す（' + n + '件）'; … }
```

**頭の断り書き（`未写し N件＋読み返し M件`）は消した**（L924 に一行の注記だけ残した）。

## 2. 未写し0件のとき

| 状態 | 釦 |
|---|---|
| **作業中** | **`作業中、HH時MM分終了予定`**（押せない・**出ている**） |
| **手待ち** | **畳む**（`まとめて写す（0件）`・押せない・`hidden`） |

**v84 で釦ごと消えた形とは別**——あのときは「終わりましたに14件並んでいるのに畳まれた」。
いまは**写せる物が本当に無いとき**だけ畳まれ、写した札は
**「終わりました」の箱の個別の「写す」**で取れる（v84 から作業中でも押せる）。

## 3. 読み返しの仕組みは消した

| 何 | 前 | 後 |
|---|---|---|
| 読み返しの枝（`allRows` から拾う） | L920-931 | **消した** |
| 断り書き（`未写し N件＋読み返し M件`） | L936-942 | **消した** |
| `buildBundle()` の戻り | `{ text, todo, count, reread, unread, back }` | **`{ text, todo, count }`** |
| 釦の三分岐（v88） | `新N件＋読み返しM件` ／ `（N件）` ／ `読み返す（M件）` | **`まとめて写す（N件）` の一つ** |

**字面で確かめた** ── `panel.html` に残る「読み返し」は**注記の中の字だけ**（L165・L777・L917-918・L924）。
動く処理には一つも残っていない。

## 4. 通知の N と釦の N

**同じ物を指す**——どちらも「まだ写していない分」。

| | 何を数えるか | 場所 |
|---|---|---|
| **釦の N** | `shown` のうち写した印の付いていない札 | `panel.html` L917（`buildBundle().count`） |
| **通知の N** | 前に知らせたときより後に立った札 | `~/.claude/watch-notify.ps1` L1714-1740（写しの一回-2 のまま） |

＊数える場所は違う（端末の `localStorage` と、機械の `copy-ready.txt` の `upto`）が、
　**指している物は同じ**。ふだんの流れ——知らせが来る → 貼る → 印が付く——では一致する。

## 5. 実測（作法14「写しに probe」）

`panel.html` の写しを一時の場に作り、IIFE の内側へ一片を差し込んで headless の Edge を
`--dump-dom` で回した。**本体には何も入れていない**（写しごと消した）。

| | 状態 | `shown` | 釦の字 | 押せるか | 描かれるか | `count` |
|---|---|---|---|---|---|---|
| **甲** | 作業中・**未写しあり** | 20枚 | **`まとめて写す（20件）`** | **押せる** | 出ている | 20 |
| **丙** | **手待ち・未写しあり** | 20枚 | **`まとめて写す（20件）`** | **押せる** | 出ている | 20 |
| **丁** | **手待ち・未写し0件** | 0枚 | `まとめて写す（0件）` | 押せない | **畳んでいる** | 0 |
| 戊 | 手待ち・**未写し2件**（残り18件は写した印） | 2枚 | **`まとめて写す（2件）`** | 押せる | 出ている | **2** |
| 乙 | **作業中・未写し0件** | 0枚 | **`作業中、11時10分終了予定`** | 押せない | 出ている | 0 |

**戊が眼目** ── 18件に写した印が付いていても、**釦は「2件」＝未写しだけ**。
v88 なら「まとめて写す（新2件＋読み返し18件）」で20件が入っていた。

個別の「写す」は **33個中0個が押せない**＝全部押せる。お知らせの一覧も出ている。

### 甲の写しの頭（5461字・20件）

```
https://kfsr148-art.github.io/koushu-handan/notices.json
作業中 1件：y0830-1058 にヨシ。写しの一回-3 の
現況：作業中 ／ 本体v1431 ／ stable v1427 ／ 最終確認11:01
10:59 ／ ヨシしてください ／ 写しの一回-3（断片を受領・続き待ち）
…（以下、未写しの札が続く）
```

**「未写し N件＋読み返し M件」の行が消えている。**

＊二行目の題が `y0830-1058 にヨシ。写しの一回-3 の` になっているのは、この枠の一行目が
　`印: …` で始まっていないため、**頭24字を使う落とし方**（札の題-1 で入れた備え）が
　働いた結果。控えの件名を書き直せば、そちらへ戻る。

## 版と検査

| 何 | 値 |
|---|---|
| `PANEL_VER` | **`'89'`**（L374） |
| `verTag` | **`panel v89（8月30日）`**（L352） |
| `panel-ver.txt` | **`89`** |
| panel の構文 | `<script>` を切り出して `node --check` → **OK** |

公開側の実読みは下の「公開の確かめ」に。

## 併せて — ヨシ待ちを二つ落とした

| 印 | 立てた刻 | 落とした理由 |
|---|---|---|
| `y0830-utsuseru` | 08-29 22:50 | **甲案で実装済み**（写せます-1）。ご指示により閉じた |
| `y0830-1058` | 08-30 10:59 | 枠の続きが届いた（この枠） |

残るヨシ待ちは **7件**（`y0828-mailbox` ／ `y0829-0744` ／ `y0829-notice` ／ `y0829-tora` ／
`y0829-futatsu` ／ `y0829-fukusan` ／ `y0829-usagi4`）。

---

## 実機で見るところ

1. **未写しがあるとき** … 釦が `まとめて写す（N件）`。**N は未写しの数だけ**（読み返しの分は入らない）
2. **押したあと** … 手待ちなら**釦が畳まれる**／作業中なら `作業中、HH時MM分終了予定`
3. 写した札を見たいときは **「終わりました」の箱の個別の「写す」**
4. 貼った文の頭に **`未写し…＋読み返し…` の行が無い**
5. **版の字が `panel v89`**

## 残り

`orders-open.tsv` の未了 ── **残り5件**（穴の直し-1 ②③／通知の宛先-1 ①②③）。
