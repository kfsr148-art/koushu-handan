# 写しの独立-4 — 読み返しのときの釦の字を分けた（panel v86）

**VAIO 2026-08-30 10:40 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 1. 直した所（`panel.html` v86・二箇所）

| 何 | 行 | 内容 |
|---|---|---|
| `buildBundle()` の戻り | **L957** | `reread` を足した（`return { text, todo, count, reread }`） |
| `setCopyBoardLabel()` | **L776-783** | 釦の字を二つに分けた |

```js
var bb = buildBundle();
var n = bb.count;
if (n > 0) {
  b.textContent = (bb.reread ? '読み返す（' : 'まとめて写す（') + n + '件）';
  …
}
```

| 場合 | 釦の字 | 押せるか |
|---|---|---|
| 未写しが**1件以上** | **`まとめて写す（N件）`** | 押せる |
| 未写し**0件**で、終わりましたの箱を読み返せる | **`読み返す（N件）`** | 押せる |
| **両方0件・作業中** | `作業中、HH時MM分終了予定` | 押せない |
| **両方0件・手待ち** | 畳む | — |

**押したときの中身は v85 のまま。** 読み返しの回は、頭に
`読み返し N件：未写しは0件なので、終わりましたの箱をそのまま写します` が立ちます。

## 2. 実測 ── 両方の釦の字（作法14「写しに probe」）

`panel.html` の写しを一時の場に作り、IIFE の内側へ一片を差し込んで headless の Edge を
`--dump-dom` で回した。**本体には何も入れていない**（写しごと消した）。

```
状態     : 手待ち
shown 20枚 ／ todo 20枚 ／ 釦の数 count=20

【甲】作業中・写した印なし
  釦の字   : まとめて写す（20件） ／ 押せる ／ 出ている
  個別の写す : 34個中0個が押せない
  お知らせの一覧 : 出ている

【丙】手待ち・写した印なし（未写しが在る）
  釦の字   : まとめて写す（20件） ／ 押せる ／ 出ている

【丁】手待ち・全部に写した印（未写し0件・読み返せる）
  釦の字   : 読み返す（14件） ／ 押せる ／ 出ている
  shown 0枚 ／ 控え全部 20枚
  終わりましたの見出し : ✅ 終わりました 14件 ▼
```

**「読み返す（14件）」が「終わりました 14件」と一致している。**

## 3. 版と公開側

| 何 | 値 |
|---|---|
| `PANEL_VER` | **`'86'`**（L374） |
| `verTag` | **`panel v86（8月30日）`**（L352） |
| `panel-ver.txt` | **`86`** |
| 構文検査 | `<script>` を切り出して `node --check` → **OK** |

公開側の実読みは下の「公開の確かめ」に。

---

## 実機で見るところ

1. **未写しが在るとき** … 釦が **`まとめて写す（N件）`**
2. **全部写したあと** … 釦が **`読み返す（14件）`**（畳まれず、押せる）
3. **版の字が `panel v86`**

## 残り

`orders-open.tsv` の未了 ── **残り5件**（穴の直し-1 ②③／通知の宛先-1 ①②③）。
