# 使用量の配分-2 — 「19:59まで N%」の行は VAIO の側に無い。名と場所を報告して止まる

**VAIO 2026-08-30 19:52 ／ 本体（`koushu-handan.html`）にも `panel.html` にも触っていない**

---

## ① 行を作っている場所の特定

**VAIO の側の物を全部当てたが、この行を作っている者は一つも無い。**

| 当てた先 | 「まで N%」の行 |
|---|---|
| `panel.html`（`paintUsage` ほか全文） | **無い**（出すのは「セッション／週／Fable の割合」「N分前取得」だけ） |
| `usage-widget.js`（公開している差し替え台本・履歴の全1版） | **無い**（出すのは 割合・棒・「N時間後」の戻るまで・更新時刻） |
| `daily-notice.ps1` | **無い**（配分の行は「配分: 全体 あとN%/日…」で別物） |
| `watch-notify.ps1` / `home-backup.ps1` / `board-emit.ps1` / `notify-record.ps1` / `state-line.js` | **無い** |
| リポジトリ全体の `git grep`・履歴 | 当たるのは**この指示そのものの控え**（`state.json` の `order`）だけ |

**残る出どころは一つ ── iPhone の Scriptable に貼ってある古いウィジェット台本。**

台帳にも符合する経緯が残っている。

```
2026-08-27 15:20  ウィジェット-1   iPhone のウィジェットから状態の行と本数を削り…
2026-08-27 15:45  再枠2-6         取消：ウィジェット-1 で台本を公開済み（**貼り替え待ち**）
```

**公開した差し替え台本（`usage-widget.js`）には「まで N%」の行が無い**ので、
いまお手元に見えている「19:59まで N%」は、**貼り替え前の古い台本**が出している行と判断する。

**Scriptable の中身は VAIO から触れない**（iPhone のアプリの中にあり、機械からは届かない）。
**指示のとおり、名と場所を報告してここで止まる。**

- **名** … Scriptable の台本（おそらく「Claude 使用量」の名で貼られている、公開前の版）
- **場所** … iPhone の Scriptable アプリの中

## いまの式 ── VAIO の側には写しが無い

貼り替え前の台本は機械に残っていない（`usage-widget.js` の履歴は公開した1版だけ）。
**いまの式を実物から写すことはできない**ため、ここは「写せない」と明記する。

＊「19:59まで」という言い方から推せば、**リセット刻（毎週 20:00）の直前までに使ってよい割合**を
　出している行で、端数の日まで数えているか、分母の取り方が違うために
　ご指示の式とずれているものと思われる——が、これは推測なので式としては書かない。

## ② の準備 ── 正しい式と値（置換そのものは止まっている）

**式** … 直近リセット刻（毎週月曜 20:00。`usage.json` の `resets_at` 2026-08-31T11:00Z＝8/31 月 20:00 と符合）
からの**経過24時間の数 × 100 ÷ 7**。全体も Fable も各自の物差しで同じ値。

**実測（式を回した値）**

```
今日 8/30 20:00   経過24時間の数=6 → 6×100÷7 = 85.7%
明日 8/31 20:00   経過24時間の数=7 → 7×100÷7 = 100%
（参考）いま 19:45 → 5 → 71.4%
```

**ご指示の 85.7%・100% と一致する。**

## 進め方（ご指示があれば）

**公開している `usage-widget.js` へ、この式の一行を足すことはできます。**
そうすれば、iPhone で**一度だけ貼り替えて**いただくことで、
「19:59まで N%」が正しい式（今日 85.7%・明日 100%）に変わり、
以後の直しも公開側の差し替えで届くようになります（貼り替えはその都度要りますが）。

---

## 残り

`orders-open.tsv` の未了 ── **残り0件**（①は報告で済・②③は触れない物のため止まり）。
