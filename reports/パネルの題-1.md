# パネルの題-1 — 済んだ札の見出しを書き換えた（panel v76）

**VAIO 2026-08-29 23:2x ／ commit `53861a8`**

`panel.html` の **1717-1718行**（`paintCopied` の表紙）だけを書き換えた。

```
- head.textContent = '済んだ札 ' + items.length + '件'
-                  + (got ? '（届いた ' + got + '件）' : '')
+ head.textContent = 'ヨシした数 ' + items.length + '件'
+                  + (got ? '（ヨシ済み確認 ' + got + '件）' : '')
```

- **件数は `items.length` と `got` のまま。** 数え方も中身も開閉の動きも触っていない。
- `（読んだ N件）` の行と、ほかの行は無変更。
- 版は三箇所同時（`verTag` L325／`PANEL_VER` L347／`panel-ver.txt`）。

**公開側の実読み**

```
$ curl .../main/panel-ver.txt → 76
325:  <p class="ver" id="verTag">panel v76（8月29日）</p>
347:  var PANEL_VER = '76';
1717:    head.textContent = 'ヨシした数 ' + items.length + '件'
1718:                     + (got ? '（ヨシ済み確認 ' + got + '件）' : '')
$ 旧の字（'済んだ札 ' + items.length ／ '（届いた ' + got）→ 0件
```

＊各札の中の小さな印（L1706 の `<span class="got">届いた</span>`）は見出しではないので据え置いた。

＊この控えは `53861a8` の内容から起こした（印 `パネルの題-1`）。
