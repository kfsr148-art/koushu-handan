# 公開側への押しが通らない元（index.lock）を直す・札の全文を ntfy へ（r0926-1127）

**終わり（残り0件）** — 2026-09-26 11:36ごろ（VAIO）。本体には触っていない。窓は落としていない。

1. **今朝の読むだけの札（r0926-1040）の全文を、ntfy へ直に一通送った。** 3259バイトで4096バイトに収まるので、一通で足りた（HTTP 200・題「📄 札の全文（r0926-1040・読むだけ 10:44 の枠）」）

2. **押しが通らない元は、git push ではなくその前の commit だった。**
   - 実際の失敗の字（git-push.log の末尾。state・usage・board・notices とも同じ）：`終了コード 128：fatal: Unable to create 'C:/Users/user/Desktop/mahjong/koushu-handan/.git/index.lock': File exists. Another git process seems to be running in this repository`
   - 錠は **09-24 15:32:13** に作られ、そのまま残っていた。15:32:33 に board の `git commit` が20秒の上限で切られ（Git-Run の Kill）、錠を消さずに終わった。以後 **426回**、commit が落ちた
   - **成功として飲み込まれていた**：commit が 128 で落ちても、git-push.ps1 はそのまま push へ進んでいた。押す物が無いので push は 0 を返し、押し手はどれも「届いた」と数えた。push-retry.log にも何も積まれなかった（最後は 09-25 22:22 の「押し直した」）
   - ＊push そのものの失敗（20秒の時間切れ・GitHub の 500・rejected）は 09-24 に散発していたが、どれも次の回で通っていた。止まりの元ではない
   - **直したこと**
     - 走っている git が0本なのを確かめ、置き去りの `.git/index.lock` を消した。そのあと `git status` と `git fsck --connectivity-only` とも異常なし
     - **git-push.ps1**：(a) 押しの錠を握った直後に、`.git/index.lock` が**2分より古く、走っている git が0本**なら消して記録する。(b) commit が 0／1 以外（128・時間切れ）で終わったら、push へ進まず**失敗として返す**
     - 常駐（inbox-watch）を 11:35:52 に起こし直した（git-push.ps1 を読み込み直すため・台本 11:33:29 より後）
   - **実読み**：錠を消したあと、公開側へ 11:30:21（usage）・11:31:29（state.json）の commit が出た。**Pages の state.json の `at` は 1790389889 ＝ 09-26 11:31:29**（11:32:16 に `https://kfsr148-art.github.io/koushu-handan/state.json` を読んだ）。raw も同じ 11:31:29
   - 作り値（使い捨ての repo と裸の遠い側。印の置き場も scratchpad）：
     | 例 | 戻り | 錠 | 押しが通らない印 |
     |---|---|---|---|
     | A 10分前の錠・git 0本 | **0**（錠を消して commit・push） | 消えた | 無し |
     | B 作ったばかりの錠 | **128**（前は0で返っていた所） | 残る（触らない） | **書いた**（`commit 128`） |
     | C 錠なし | **0** | — | **消えた** |

3. **押しが通らない間は、終わりの札の全文を ntfy にも載せる形にした。**
   - git-push.ps1 が、押しが落ちた回に **~/.claude/push-stuck.txt**（最初に落ちた刻・押し手・訳）を書き、どれかの押しが通れば消す
   - watch-notify.ps1 の終わり（😽）の押し送り：印が**10分より古い**間は、本文に手元の notices.json の**いちばん新しい札の題と全文**を載せる（頭に「公開側へ押せていないので、札の全文をここに載せます」）。3800バイトを超えたら行の切れ目で割り、続きは「（続き n/N）」の題で送る。印が無い・新しい間は、今までどおり題だけ
   - 作り値（本物へは送らない）：印なし→題だけ／印1分→題だけ／**印15分・終わり→全文（3444バイト・一通）**／印15分・ヨシ待ち→題だけ／**長い札（120行）→4通（3771・3779・3787・1377バイト）**
   - watch-notify は予定表が毎分起こすので、次の走りから効く

### 手元で回る段・雲で回る段（作法36）
- 手元：git-push.ps1（常駐・watch-notify・push-mine などの押しがみな通る）に錠の片付けと失敗の返しと印／watch-notify（毎分）に全文の載せ
- 雲：変わりなし（check.yml は本体の押しのとき、job.yml は本体・パネル・台本の押しのとき）

### 触った物
~/.claude/git-push.ps1（写し .bak-20260926）・watch-notify.ps1（.bak-20260926）・orders-open.tsv・work-note.txt／リポジトリの .git/index.lock（置き去りを消した）・reports/r0926-1040.md（前の枠の分。ここで初めて commit）・reports/r0926-1127.md・report-latest.md

### 残り
残り0件
