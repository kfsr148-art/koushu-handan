# パネルの整理-9 — 時間の刻を黄色の行へ移した（panel v98）

**VAIO 2026-08-30 13:42 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 1. 黄色の行の字（作業中）

```
作業中、HH時MM分終了予定（開始HH:MM・経過N分・指示HH:MM）
```

`panel.html` **L630-648**。

```js
if (lastStat === '作業中') {
  var bits = [];
  if (lastStartHM) { bits.push('開始' + lastStartHM); }
  if (lastStartedAt > 0) {
    bits.push('経過' + Math.max(0, Math.floor((Date.now() / 1000 - lastStartedAt) / 60)) + '分');
  }
  if (lastOrderHM) { bits.push('指示' + lastOrderHM); }
  return endPredText() + (bits.length ? ('（' + bits.join('・') + '）') : '');
}
```

**取れない項目は落とす。**「—」で埋めない（無いことを字で埋めない決め）。
**次の指示待ち・ヨシ待ちは今のまま。**

## 2. 上の札は題だけにした

`panel.html` **L2145-2149**。

```js
/* ＊上の札は**題だけ**にする（v98）。時間の二段（mik / ord）は黄色の行へ移した。
     同じ刻を二箇所に出さないため、ここでは空を渡す（渡す先で行ごと畳まれる）。
   ＊mik と ord の**組み立てそのものは残してある**——lastEndAt を決めるのに要り、
     まとめて写しの中でも使う。画面へ出す口だけを閉じた。 */
paintState(cls, word, subj, '', d.mailUnread, '');
```

＊黄色の行が使う三つ（開始の HH:MM ／ 指示の HH:MM ／ 開始の epoch）は、
　`pullState` が取得のたびに `lastStartHM` / `lastOrderHM` / `lastStartedAt` へ控える（L2104-2116）。

＊刻の字を作る `pad2` は **`pad2s()`（L625-628）へ寄せた**。同じ作り方を二箇所に置かない。

## 3. 経過は今までどおり動く

**黄色の行を塗るたびに数え直す。** 塗るのは `pull()`（お知らせの取得）と
`pullState()`（状態の取得）で、どちらも**15秒ごと**。
v97 まで上の札の `mik` が 15秒ごとに進んでいたのと**同じ速さ**。

## 4. 実測（作法14「写しに probe」）

`panel.html` の写しを一時の場に作り、IIFE の内側へ一片を差し込んで headless の Edge を
`--dump-dom` で回した。**本体には何も入れていない**（写しごと消した）。

```
■ 作業中
   state  ｜ 出ている ｜ 作業中 パネルの整理-9
   stLine ｜ 出ている ｜ 作業中、13時55分終了予定（開始13:40・経過2分・指示13:40）

■ 手待ち
   stLine ｜ 出ている ｜ 次の指示待ち

■ ヨシ待ち
   stLine ｜ 出ている ｜ ヨシを返してください
```

- **上の札から時間の二段が消えた**（`作業中 パネルの整理-9` の二語だけ）
- **黄色の行に三つとも入っている**（開始13:40・経過2分・指示13:40）
- **経過が動く**（一度目の測りで1分、二度目で2分）
- 手待ち・ヨシ待ちは v97 のまま

## 版と検査

| 何 | 値 |
|---|---|
| `PANEL_VER` | **`'98'`**（L374） |
| `verTag` | **`panel v98（8月30日）`**（L352） |
| `panel-ver.txt` | **`98`** |
| 構文 | `<script>` を切り出して `node --check` → **OK** |

---

## 実機で見るところ

1. 上の札が **状態の字と題だけ**（時間の行が無い）
2. 黄色の行が **`作業中、HH時MM分終了予定（開始HH:MM・経過N分・指示HH:MM）`**
3. **経過が15秒ごとに進む**
4. **版の字が `panel v98`**

## 残り

`orders-open.tsv` の未了 ── **残り5件**（穴の直し-1 ②③／通知の宛先-1 ①②③）。
