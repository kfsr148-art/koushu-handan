# パネルと受け口-1（二度目）— 釦の字に終了予定を出す（panel v80）

**VAIO 2026-08-30 02:xx ／ panel v80 ／ 本体（`koushu-handan.html`）には触っていない**

＊この印は**三度届いた**。**2〜4 は前の便の 1〜3 と同じ内容で、既に panel v79 と受け口で済んでいる**
　（控えは `reports/パネルと受け口-1.md`）。**新しいのは1件目だけ**なので、そこを足して版を上げた。
＊二度目以降の控えは、作法33 のとおり末尾に `-2` を付けてある。

---

## 1（新）. 釦の字に終了予定の刻を出す

| 行 | 中身 |
|---|---|
| **L394-403** | `lastEndAt`（終了予定の刻・秒）と `noteEnd(sec)` を足した。**候補が複数あればいちばん遅いものだけを残す** |
| **L713-724**（`setCopyBoardLabel`） | 作業中のとき、刻があれば **`作業中、HH時MM分終了予定`**、無ければ **`作業中、終了予定不明`** |
| **L1873-1876** | 状態を塗るときに `startedAt ＋ 見込み` を秒で組み、`noteEnd()` へ渡す（三行目の「終了予定HH:MM」と**同じ値**） |
| **L1886-1887** | 作業中でなくなったら `lastEndAt` を 0 へ戻し、その場で釦の字も組み直す |

**押せない形と薄い見た目は据え置き**（`#btnCopyBoard:disabled`・不透明度 .55）。
**作業中が0件になれば「まとめて写す（N件）」へ戻る。**

＊いま終了予定を持っているのは `state.json` の `startedAt ＋ 見込み` の**一つだけ**。
　複数になったときに備えて、`noteEnd()` が**いちばん遅いものを残す**形で書いてある（下の実測で確かめた）。

## 2〜4（前の便で済み）

| 件 | どこ | 版 |
|---|---|---|
| 写しの一行目に `notices.json` の道 | `buildBundle` L806-809（`splice(1,0,…)`）／`withPage()` L1465-1467／`copyBoxed` L1476／`copyOne` L1482 | v79 |
| 作業中の札に「指示を受けた HH:MM」 | 器 L255-257／`paintState` L1559-1568／組む所 L1878-1886 | v79 |
| 同じ印の二度受けを本文で返す | `inbox-watch.ps1` L98-102・L626-655・L746-760／`watch-notify.ps1` L1577-1598 | 受け口 |

---

## 実測（写しに probe・作法14）

### 釦の字

```
終了予定が取れているとき : "作業中、22時00分終了予定"（押せない）
候補が三つのとき         : "作業中、22時30分終了予定"   ← いちばん遅い候補が残る
終了予定が取れないとき   : "作業中、終了予定不明"（押せない）
作業中が0件のとき        : "まとめて写す（5件）"（押せる）
薄い見た目               : 不透明度 0.55／地 rgb(29, 51, 40)
```

＊「候補が三つ」は `noteEnd(+30分)` `noteEnd(+120分)` `noteEnd(+60分)` の順で渡した結果。
　**遅い +120分 が残った**（着手20:30 → 22:30）。

### 「まとめて写す」で作られる文の一行目

**作業中が2件あるとき**
```
1行目 : https://kfsr148-art.github.io/koushu-handan/notices.json
2行目 : 未了 2件：兎の攻守-11 の下調べ、パネルと受け口-1 の実測
3行目 : 現況：作業中 ／ 本体v1431 ／ stable v1427 ／ 最終確認21:12
```

**作業中が0件のとき**
```
1行目 : https://kfsr148-art.github.io/koushu-handan/notices.json
2行目 : 現況：作業中 ／ 本体v1431 ／ stable v1427 ／ 最終確認21:12
```

**個別の「写す」の一行目**
```
https://kfsr148-art.github.io/koushu-handan/notices.json
```
「URLを写す」の釦（`#btnCopyUrl`・L319）は**そのまま残っている**。

### 作業中の札の四行

```
作業中
パネルと受け口-1（四件）
開始20:30・終了予定22:00・経過45分
指示を受けた 20:30                    class=k
```
**作業中でないとき** … 三行目 `class="k hide"`／四行目 `class="k hide"` ＝ **行ごと畳む**。

### 4の一行が出る形の見本（二重送りは試していない）

```
題   : 🔎 調べました
本文 : 既に受領済み：パネルと受け口-1 ／ 01:40 に受領
```

---

## 版と公開

`PANEL_VER`（L356）／`verTag`（L334）／`panel-ver.txt` を同時に **79 → 80**。構文検査OK。

```
$ curl .../main/panel-ver.txt → 80
257:        <span class="k hide" id="stOrder"></span>
319:    <button type="button" class="cpall url" id="btnCopyUrl">URLを写す</button>
334:  <p class="ver" id="verTag">panel v80（8月30日）</p>
356:  var PANEL_VER = '80';
399:  var lastEndAt = 0;
401:  function noteEnd(sec) {
719:        b.textContent = '作業中、' + e.getHours() + '時'
722:        b.textContent = '作業中、終了予定不明';
809:      out.splice(1, 0, '未了 ' + working.length + '件：'
1876:          lastEndAt = 0; noteEnd(endSec);
$ diff 公開物 手元 → 完全一致
```
