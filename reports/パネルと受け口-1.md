# パネルと受け口-1 — 写しの一行目・指示を受けた刻・同じ印の二度受け

**VAIO 2026-08-30 02:xx ／ panel v79 ／ 受け口（`inbox-watch.ps1`・`watch-notify.ps1`）**

**本体（`koushu-handan.html`）には触っていない。**

＊この印は**二度届いた**（二通目は中身を細かくした版）。**三件目がまさに扱う形**なので、
　一つの指示として通し、台帳には一行だけ積んである。

---

## 1. 写しの一行目に notices.json の道を必ず置く

| 直した所 | 中身 |
|---|---|
| **L789-792**（`buildBundle`） | `out.unshift` を **`out.splice(1, 0, …)`** へ。v78 は未了の行を頭へ入れていたので URL が二行目へ落ちていた。**URL が一行目、未了が二行目**になる |
| **L1447-1449** | `withPage(t)` を足した（`NOTICES_PAGE` ＋ 改行 ＋ 本文） |
| **L1458**（`copyBoxed`） | 箱の中の札の写しに `withPage()` を通した |
| **L1464**（`copyOne`） | 一覧の札の写しに `withPage()` を通した |

**「URLを写す」の釦（`#btnCopyUrl`・L319）はそのまま**。触っていない。

## 2. 作業中の札に「指示を受けた HH:MM」

| 直した所 | 中身 |
|---|---|
| **L255-257** | 三段目の器 `<span class="k hide" id="stOrder">` を足した |
| **L1541-1550**（`paintState`） | 引数に `ord` を足し、空なら `hide` で畳む |
| **L1860-1868** | `w === '作業中'` のときだけ `state.json` の **`orderAt`** から `HH:MM` を取って組む |
| L1885・L1903 | `paintState(...)` の呼び出し二箇所に `ord` を渡す |

**読む元は `state.json` の `orderAt`**（`inbox-feed.ps1` → `last-order.txt` が書く値）。
刻が読めない回は行ごと出さない（「—」で埋めない）。

## 3. 同じ印の枠を二度受けたら一行返す

**受け口（`~/.claude/inbox-watch.ps1`）**

| 行 | 中身 |
|---|---|
| **L98-102** | `orders-recv.tsv`（受け取った印の台帳）と `dup-order.txt`（返す依頼の一枚）の道 |
| **L626-655** | `Get-Mark` … 文から `印: <名>` を拾う／`Find-Mark` … 台帳に同じ印があれば受領時刻を返す／`Add-Mark` … 台帳へ積む |
| **L746-760** | 取り込みの輪で、**同じ印なら受信箱へ入れず・起こさず**、`dup-order.txt` を置いて次の行へ進む |

**知らせ側（`~/.claude/watch-notify.ps1`）**

| 行 | 中身 |
|---|---|
| **L1577-1598** | `dup-order.txt` があれば **`Send-Ntfy`** で本文を返し、読んだら消す（同じ枠で二度鳴らさない） |

**返す一行の形**

```
既に受領済み：パネルと受け口-1 ／ 01:40 に受領
```

題は **`🔎 調べました`**（`$T_LOOK`）で出るので、パネルの一覧にそのまま並ぶ。
**二度目は実施せず、取消として台帳へ書くのは今までどおり。**

**作法29 の順を踏んだ** ── 写しを取る（`.bak-20260830`）→ 直す → 構文検査
（`PSParser::Tokenize`：`inbox-watch.ps1` **870行**・`watch-notify.ps1` **1884行** とも **構文OK**）
→ 常駐を起こし直す（**PID 8380 → 3216**・起動 20:55:46 ＞ 台本の更新 20:54:54）。
BOM は両方とも**有り**のまま。関数の重複も無し。

---

## 実測（写しに probe・作法14）

### まとめて写す（作業中が2件あるとき）

```
釦の字 : "作業中 — 終わるまで待つ"（押せない）
1行目 : "https://kfsr148-art.github.io/koushu-handan/notices.json"
2行目 : "未了 2件：兎の攻守-11 の下調べ、パネルと受け口-1 の実測"
3行目 : "現況：作業中 ／ 本体v1431 ／ stable v1427 ／ 最終確認20:50"
```

### まとめて写す（作業中が0件のとき）

```
釦の字 : "まとめて写す（5件）"（押せる）
1行目 : "https://kfsr148-art.github.io/koushu-handan/notices.json"
2行目 : "現況：作業中 ／ 本体v1431 ／ stable v1427 ／ 最終確認20:50"
```

### 個別の「写す」の一行目

```
"https://kfsr148-art.github.io/koushu-handan/notices.json"
```

「URLを写す」の釦は残っている（`id="btnCopyUrl"` が1つ・字は6箇所）。

### 作業中の札の四行

```
一行目（状態） : "作業中"
二行目（題）   : "パネルと受け口-1（三件）"
三行目（刻）   : "開始01:40・終了予定02:25・経過45分"   class=k
四行目（指示） : "指示を受けた 01:40"                  class=k
```

**作業中でないとき** … 三行目 `class="k hide"`／四行目 `class="k hide"`（字は空）。**行ごと畳む。**

### 3の一行が出る形の見本（二重送りは試していない）

```
題   : 🔎 調べました
本文 : 既に受領済み：パネルと受け口-1 ／ 01:40 に受領
```

---

## 版と公開

`PANEL_VER`（L356）／`verTag`（L334）／`panel-ver.txt` を同時に **78 → 79**。構文検査OK。

```
$ curl .../main/panel-ver.txt → 79
257:        <span class="k hide" id="stOrder"></span>
319:    <button type="button" class="cpall url" id="btnCopyUrl">URLを写す</button>
334:  <p class="ver" id="verTag">panel v79（8月30日）</p>
356:  var PANEL_VER = '79';
791:      out.splice(1, 0, '未了 ' + working.length + '件：'
1458:    copyInto(btn, withPage(text), '写す');
1464:    var text = withPage(sixLines(d));
1868:          if (om) { ord = '指示を受けた ' + om[1] + ':' + om[2]; }
$ diff 公開物 手元 → 完全一致
```

＊受け口の二つ（`inbox-watch.ps1`・`watch-notify.ps1`）は `~/.claude` にあり、
　公開リポジトリには置いていない（非公開の控えリポジトリの側）。
