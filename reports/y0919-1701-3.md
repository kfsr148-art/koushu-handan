# 【宣言・全文（再掲）】設定画面に「言葉の一覧」の釦を足す（v1452）

**ヨシ待ち** — 2026-09-19 18:23（VAIO）。**印: y0919-1701**（同じ印の三度目＝**全文の再掲**）。

＊同じ枠が **17:45 と 18:21 の二度**届いた。**中身は一字も変えていない**——17:50 に押した
`reports/y0919-1701-2.md` と同じもので、届いていなかった場合に備えて**もう一度立て直した**。
**まだ一行も触っていない。** ヨシを取るまで工事に入らない。

## 何をするか（宣言の中身）

設定画面（`catRoomBuild()`・**L8405〜L8578**）に釦 **「言葉の一覧」** を一つ足す。
押すと重ねが開き、説明欄「たまに出る言葉」の **10語（L1545〜L1554）** と
**締めの一文（L1555）** が出る。閉じると設定画面へ戻る。

---

## ① 触る行（ファイル名と行番号）と足す行数

### `koushu-handan.html`

| 行 | いま | どう直すか | 増減 |
|---|---|---|---|
| **L2** | `<html lang="ja" data-ver="1451">` | `data-ver="1452"` | 書き替え1行 |
| **L1424** | `<div id="verTag">v1451</div>` | `v1452` | 書き替え1行 |
| **L1544** | `<b>たまに出る言葉</b><br>` | 行の**頭**に `<span id="glossaryBlock">` を足す（後ろの字はそのまま） | 書き替え1行 |
| **L1555** | `答え合わせで「七対子の道」…知らせ。` | 行の**末**に `</span>` を足す（前の字はそのまま） | 書き替え1行 |
| **L8537 の前**（`const jibakuBtn` の直前） | — | 釦を作る **7行**を足す | **+7行** |
| **L8559** | `…appendChild(misdiagBtn); buttonRow.appendChild(jibakuBtn);…` | 間へ `buttonRow.appendChild(kotobaBtn);` を差し込む | 書き替え1行 |
| **L8564** | `[switchBtn, edamameBtn, shinshiBtn, usagiBtn, ledgerBtn, misdiagBtn, jibakuBtn, close]` | `misdiagBtn` の次へ `kotobaBtn` を足す（格子の整列と折り返しを同じに掛けるため） | 書き替え1行 |
| **L8586 の後**（`window.catRoomOpen = catRoomOpen;` の次） | — | 重ねを作る／開く **約38行**（`glossaryEl` / `glossaryBuild()` / `glossaryOpen()` / `window.glossaryOpen`） | **+38行** |

**足すのは合わせて約45行、書き替えは6行。説明欄の字は一字も増減しない。**

足す7行（設定の釦）はこの形にする。

```js
const kotobaBtn = document.createElement('button');
kotobaBtn.type = 'button';
kotobaBtn.textContent = '言葉の一覧';
kotobaBtn.style.cssText = 'padding:6px 12px; border-radius:8px; border:1px solid #8a7f5c; background:#2c3644; color:#e8e2d0; font-size:11px; white-space:nowrap;';
kotobaBtn.onclick = function(){ glossaryOpen(); };
```

＊上は5行。残る2行は L8570 の枠色の群（`[ledgerBtn, misdiagBtn]`）へ入れるかどうかの行と、
　重ねの閉じで設定へ戻す行。**枠の色は既定の金茶のまま**にする（記録の二つと同じ青灰にもできる。
　色は選ばないので、違う方がよければ言ってください）。

### `check.js`（本体の溢れの見張り。⑦の画面を 9→10 にする）

| 行 | どう直すか |
|---|---|
| **L303 の次** | `else if(screen === 'glossary'){ window.closeTitleScreen(); window.glossaryOpen(); }` を1行足す |
| **L667** | `SCREENS` の並びへ `'glossary'` を足す（書き替え1行） |

### その他

- **`ver.txt`** … `1451` → `1452`（版の三箇所目）
- **`serifu.txt`** … `node serifu-extract.js` で**再抽出**。台詞は一つも足さないが、
  本体が +45行ぶん伸びて**行番号がずれる**ため（作法17。`pre-push` の `--check` が落ちる）

---

## ② 10語と締めの一文を一箇所から出す形（どこへ置き、どちらがどう読むか）

**新しい定数は作らない。字は説明欄の本文に一つきり置いたままにする。**

- **置き場所** … いまの場所のまま（`engineHelpOverlay` の中、**L1544〜L1555**）。
  ここを `<span id="glossaryBlock">…</span>` で囲むだけ。**字は動かさない・写さない・複製しない。**
- **説明欄の読み方** … 今までどおり。静的な HTML がそのまま画面に出る。
  組み立ても書き換えもしない（**読む仕掛けを一つも足さない**）。
- **設定側の読み方** … 釦を押したとき、重ねの中身を一度だけこう作る。

```js
const src = document.getElementById('glossaryBlock');
body.innerHTML = src ? src.innerHTML : '';
```

　以後は作った重ねを使い回す（開くたびに読み直さない）。

**なぜこの形にするか** … 既に同じ手が本体にある。猫牌の長い説明（`toggleCatHelp`・**L4966〜L4975**）が
`helpSec5` の `innerHTML` を写して使っている。**同じ流儀に揃える**のが、字を二重に持たない
いちばん短い道で、`<span>` 一枚のほかには本体の作りを変えない。

＊**定数（JS の文字列）に持たせる形は採らない。** その形だと、説明欄の側も定数から組み立てる
　ことになり、**静的な HTML を JS 組み立てへ作り替える**という、頼まれていない工事が要る。
　字が二箇所になる危険は、どちらの形でも同じだけ無い。

---

## ③ 説明欄の見た目が一字も変わらないことを、どう突き合わせて示すか

**四つで示す。**

1. **字の一致（sha256）** … 工事の前に `L1544〜L1555` を取り出して sha256 を控える。
   工事の後、同じ範囲から `<span id="glossaryBlock">` と `</span>` を**剥いだ**ものの sha256 を取り、
   **前後で同じ**であることを札に書く。
2. **`diff` が0行** … 同じ二つを `diff` に掛けて**差が出ない**ことを示す。
3. **`git diff` の中身** … 本体側の差分が
   **版の2行（L2・L1424）＋ span の2行（L1544・L1555）＋ 設定側の足し分だけ**であること、
   説明欄の**他の行が一行も出てこない**ことを、差分の行数つきで示す。
4. **画面でも測る** … `<span>` は**行内の札**なので、囲んでも改行も間も増えない。
   これを言葉で済ませず、⑦（画面の溢れ）に**新しい重ねを足して 9→10画面**にし、
   手元は速い版（568x320 のひと視野）、雲はフル版（21視野）で通す。
   説明欄そのものは今までどおり測り続ける。

＊ここは仕組みの分かれ道なので、こちらで「測る側」を選んだ。
　足さないと、**新しい重ねだけ溢れの見張りから外れる**ため。

---

## ④ 判定・条件・光る牌・`analyze()` には触らない

**一字も触らない。** 名指しで挙げる。

`analyze()` ／ `bestShapeOneSuit` ／ `toneBlocksOneSuit` ／ `toneFacts()` ／
`toneAngleLine()` ／ `toolEyeSay()` ／ `tenLeans()` ／ 光る牌の印（`multi-mark`・`naki-mark`・
`dup-mark`・`safe-mark`・`float-mark`・`edge-mark`・`head-mark`・`cat-mark`・`eda-mark`・`om-mark`）／
攻守の敷居と条件の式。

**ほかに触らない物** … `mitate-new.txt`・`stable`・`adv-check.js`・`first-sense-base.json`・
説明欄の**文面そのもの**（`<span>` で囲むだけで、字は書き替えない）。

---

## ③（指示3）釦の位置

**「誤診記録」の次・「▶️ ずんだもんと現場猫」の前**になっている。

画面に出る順は `appendChild` の順（**L8558・L8559**）で決まる。直した後はこうなる。

| 並び | 釦 |
|---|---|
| 1〜4 | 猫牌スイッチ／枝豆スイッチ／AIスイッチ／兎スイッチ |
| 5・6 | BGM／効果音 |
| 7 | 猫牌率 |
| 8 | **誤診記録** |
| **9** | **言葉の一覧**（← ここに入る） |
| 10 | ▶️ ずんだもんと現場猫 |
| 11 | ✕ 閉じる（二列ぶち抜き） |

＊釦を作る7行を `const jibakuBtn`（**L8537**）の**直前**へ置くのは、**作る順も並びと揃える**ため。
　画面の並びを決めているのは L8559 の差し込みの位置。

---

## 段取りと見込み

`<span>` を巻く → 釦と重ねを足す → 版を三箇所（L2・L1424・`ver.txt`） →
`check.js` に画面を足す → `node serifu-extract.js` →
**速い版（`node check-all --fast`）が通ってから一度で commit** → push（雲がフル版を回す）。
**見込み 60分。**

**ヨシはまだ返さなくてよい、との指示なので、このまま待ちます。**
着手の合図は「**y0919-1701 にヨシ**」。
