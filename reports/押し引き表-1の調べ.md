# 押し引き表-1 の調べ — 判定盤が持っている物・持っていない物

**実装はしていない。**`koushu-handan.html` を字面で読んだだけ。

## A. 巡目／相手のフーロ数（最初の問い）

| 見た物 | いま |
|---|---|
| **巡目の入力** | **無い。**盤面の `<input>` は3つだけ … 手牌の字（`qiText`）／写真（`ssFile`）／型紙（`ssTplFile`） |
| **巡目を内部に持つか** | **持っていない。**判定の頭に「点棒・役・**巡目は度外視**」と明記（L2479） |
| 巡目という語 | 9箇所。**すべて出力の文**（「N巡目までに一向聴」等の助言）と注記。入力ではない |
| **相手のフーロ数** | **無い。**「フーロ」「副露」「ポン」は**0件**。「チー」「カン」は語の説明文と台詞の「アカン」だけ |
| 相手の情報一般 | 「他家」「下家」「上家」「河」は**0件**。「相手」12件は注記と探偵編の文。**盤面は自分の13枚しか知らない** |

**判定は `analyze()` ただ一つで、引数を取らない**（`function analyze()`・L2481）。
材料は大域の `hand`（自分の牌）だけ。**外から巡目や相手を渡す口が、そもそも無い。**

## B. 追加するなら触る所（見積り）

| 所 | 何をする | 目安 |
|---|---|---|
| 盤面（HTML） | 巡目の入力（1〜18）と、相手のフーロ数（0〜4×3人）の口 | 20〜40行 |
| 入力の受け（JS） | 値を読んで大域へ持つ・盤面を組み直すときに保つ | 20〜30行 |
| `analyze()` | **変更禁止**（作法5）。**触れない** | — |
| 判定の後段 | `analyze()` の外で、返り値と新しい二つを合わせて攻守を振り直す | 40〜80行 |
| 人柄の見立て行 | 七人の材料（`toneFacts()`）に足すなら、印と行の対応も同時に（作法20） | 30〜60行 |
| 検査（`check.js`） | 新しい入力の作り値と、⑦の画面の溢れ | 30〜50行 |
| 台詞 | 巡目・フーロに触れる言い回しを足すなら `serifu.txt` の再抽出まで（作法17） | 別途 |

**いちばん重いのは `analyze()` を触れないこと。**新しい二つは**判定の外側**で効かせるしかなく、
「配牌の第一感」という今の作り（点棒・役・巡目を度外視して13枚だけで決める）と**二重の物差し**になる。

## C. 物差しを足す前の点検に照らして

`learnings` という名の文書は**無い**。近いのは `CLAUDE.md` と
`.claude/skills/koushu-handan-dev/SKILL.md`（13節・143行）で、そこから当たる所は次のとおり。

| 決め | この件で問題になる点 |
|---|---|
| 作法5「`analyze()` と `bestShapeOneSuit` は変更禁止」 | **判定の中へは入れられない。**外側で足すしかない |
| 作法20「見立て行と光る牌は同じ材料を指す」 | 巡目・フーロを人柄の材料にするなら、**印（金枠）も同じ物を指す**必要がある。巡目は牌ではないので**光らせる先が無い**（兎と同じ扱いになる） |
| 作法20「材料は `toneFacts()` が一度だけ組む」 | 新しい二つも**そこで一度だけ**組む。チップを押すたびに数え直さない |
| SKILL.md 4「頼まれていない改善を足さない」 | 入力を増やすと、**配牌専用**という今の立て付けを変えることになる。枠の外へ広がりやすい |
| 作法16「画面に収まっているか」 | 入力の口を増やすと、判定後の二択の bottom が視野を越えやすい。⑦の実測が要る |
| 作法4／SKILL.md 9「版の三箇所を同時に」 | 盤面を触るので必ず要る |
| 作法24「実機で見てもらう」 | 入力の口は**指で押す物**。作法10（当たり判定44px）も掛かる |

## D. 追加の調べ（対子・第一打・受け入れ）

### ① 対子の数え … **持っている**

```
L2745  let kinds=0, pairs=0;
       Object.values(allCnt).forEach(c=>{ kinds++; if(c>=2) pairs++; });
L2747  const chiitoiSt = 6 - pairs + Math.max(0, 7 - kinds);
```

**七対子の向聴を専用に測っている**（対子の種類数＋足りない種類の補正）。
同じ所で国士（`kokushiSt`）も測り、**標準形と合わせた三つの最小**を向聴として返す。
画面にも「七対子の道」という言い方で出る（L1515）。

### ② 第一打 … **示していない**

「第一打」「打牌」「discard」は**0件**。
ただし**14枚以上を渡された回に限り**、内部で捨て牌を選んでいる。

```
L2818  let bestBase=99, bestLeft=null;
L2823  if(st<bestBase){ bestBase=st; bestLeft=left; }   ← 切ったあとの向聴がいちばん小さい牌
L2827  const ref = bestLeft || baseTiles.slice(0,13);   ← 受け入れはその手を基準に測る
```

**規則は「切ったあとの向聴が最小になる牌」**（同点は先に見つかった方）。
**これは受け入れを測るための内部の都合で、画面には出さない。**
※そもそも 14枚以上は「配牌判定の対象外」として弾く枝もある（L2487）。

### ③ 受け入れ … **数えている**（が、専用の関数名は無い）

- 数える所 … `analyze()` の中の「受け入れの広さ（伸びしろ）を測る」節（L2655〜）
- 使う関数 … **`shanten(tiles, mode)`**（L2670）と `tilesArray(h)`、`normalizeForShanten(...)`
- 数え方 … 34種を1枚ずつ足し、**向聴が減る牌を「有効牌」**として**種類数**で広い／狭いを見る
  （枚数ではなく**種類数**。四枚持ちは `have>=4` で除く）

**複合形（カンチャン＋リャンメンが重なる形）の扱い**

**形ごとに数えていない。**「引いたら向聴が減るか」だけを見るので、
`4568m` のような重なりは**牌の種類として自然に合算**される（4m・5m・7m がそれぞれ有効牌になる）。
つまり**複合形を特別扱いする枝は無く、二度数えも起きない**。

＊ただし**良形／愚形の別は、これとは別の所**で見ている（`bestShapeOneSuit`＝**変更禁止**）。
　受け入れの数え方を変えると**良形カウントと土台がずれる**ので、注記にも
　「良形カウントを総当たりに直したのに合わせ、受け入れの土台もここへ寄せた」（L2741）とある。
