# ヨシの猫-7 — ヨシ待ちの頭を現場猫の顔へ（印 y0912-2015）

**終わり（残り1件）** — 2026-09-12 20:1x（VAIO）。panel v131。押し済み（`46de6f40`）。

`git pull` で `genba-face-40x36.png`（40x36・六色＋透け）を取り込み、二箇所へ入れた。

| どこ | ヨシ待ちのとき | 普段 |
|---|---|---|
| 返事パネルの頭の36pxの枠（`#runcat`） | `genba-face-40x36.png` を **40x36 の等倍**（伸縮なし） | **元の白猫のまま**——作業中はコマ送り（`cat0〜4-w`）、それ以外は寝姿（`cat-sleep-w`） |
| ウィジェット | 同じ絵を **原寸**（40x36） | 今までどおり（作業中 `cat2-white`／手待ち `cat-sleep-white`／異常 `cat4-white`） |

- 箱は 56x36 なので、40x36 を入れると**横に16px余るだけ**。高さは変わらないのでヘッダーは伸びない
- 拡大は `#runcat` の `image-rendering: crisp-edges / pixelated` に任せる（**伸縮はしない**）
- ヨシ待ちが解けた回に寸を 56x36 へ戻す一行を入れてある（入れないと白猫が 40x36 のまま出る）
- 黄色い札の中の全身（`genba-cat-160.png`・160px）は**ヨシの猫-6 のまま**。触っていない

## 検査

- `panel-check` … **全てPASS**（版が三箇所とも v131）
- `widget-check` … **全てPASS**（`genba-face-40x36.png` … 200。押してから24秒で公開側に出た）

## 実機で見るところ

返事パネルをヨシ待ちで開き、題の右の枠に**現場猫の顔**が出ること（ぼやけず、四角いまま）。
ヨシ待ちが解けたら**白猫へ戻る**こと。ウィジェットのヨシ待ちも同じ顔。

## 残り

1. **選択釦-1（v1439）** — 宣言済み・ヨシ待ち（印 `y0912-1900`）。兎の一行を「切るなら〈牌〉」へ替えるだけ。
