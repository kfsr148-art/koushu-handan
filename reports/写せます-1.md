# 写せます-1 — 作業中0件へ転じたら「写せます（N件）」を一発（甲案）

**VAIO 2026-08-29 23:2x ／ `~/.claude/watch-notify.ps1`（公開リポジトリには置いていない）**

`y0830-utsuseru` のヨシ（**甲案＝件数を出す**）を受けて実装した。
**`panel.html` にも `koushu-handan.html` にも触っていない。**

## 入れた所

`watch-notify.ps1` **L1600-1660**（使用量の節の直前）。

| 行 | 中身 |
|---|---|
| **L1613** | `$CopyReadyPath = ~/.claude/copy-ready.txt`（前の回の状態を控える印） |
| **L1615** | `$ReadyRepo`（公開側の置き場）。**`$UsageRepo` はこの下で決まるので当てにできず、自前で持つ** |
| L1617-1622 | `state.json` の `stat` を読む |
| L1622-1628 | 印から前の回の状態を読む |
| L1630-1650 | **前が「作業中」で今が「作業中でない」回だけ**、件数を数えて `Send-Ntfy` |
| L1651-1653 | 今の状態を印へ書き直す |

**一度だけになる仕組み** … 送った後は印が「作業中でない」に変わるので、次の巡回では条件に当たらない。
**作業中へ戻れば印も「作業中」に変わる**ので、次に外れたときまた一度だけ鳴る。
**パネルを開き直しても鳴らない**（鳴らす判断が機械の側にあり、開閉と関係が無い）。

## 文面と件数

```
題   : ✅ 終わりました（返事不要）
本文 : 写せます（N件）
```

N は `notices.json` のうち `seen.json` の `upto` より新しい件の数。
**素で試したところ `upto=0` で `N=20`**、いまの状態は `作業中`（＝まだ鳴らない、正しい）。

＊端末で既に写した分はこちらから見えないので、**パネルの数より多く出ることがある**。
　甲案の裁定（件数が出れば開く前に分かる）どおり、そのまま件数を出す。

## 始末

- 写しを取った（`watch-notify.ps1.bak-20260830b`）
- **構文OK（1941行）**、**BOM 有り**、`Send-Ntfy` の重複なし
- `watch-notify.ps1` は**毎分呼び直される**ので、常駐の起こし直しは要らない（作法29 の但し書き）

## 実機で見るところ

作業中が終わった直後（**最大60秒以内**）に、iPhone へ「**写せます（N件）**」が届く。
続けて何度もパネルを開いても、**二度目は鳴らない**。次に作業中へ入って、また終わったときに一度だけ鳴る。
