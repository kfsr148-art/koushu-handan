# 今日の止まりの直し（起こし直し・速い版の上限・鍵の言葉）

**終わり（残り0件）** — 2026-09-19 20:2x（VAIO）。`koushu-handan.html`・`stable` には触っていない。
宣言 y0919-1701 の書きかけ（本体 +48 −6・check.js +2 −1）も**そのまま残っていて、触っていない**。

**元は一つ。** 19:03 に手元で回した速い版（`check-all --fast`）が止まらずに這い続け、
空きを700MB台まで食った。窓が落ちたあとの起こし直しも、この重さで空振りした。

## 1. 起こし直しが働かなかった訳 … **走ったが、窓を立てる前に予定表の上限（2分）で殺された**

**① 走ったか … 走った。** `ClaudeRevive` は毎分走っていて、**19:38:49 に「対話の claude が0本」を記録**した（`revive.log`）。

**② 何を見て判じたか … 「生きている」とは判じていない。0本を正しく見ていた。**
19:41:25 の回が「0本が2分続いた」と判じて起こしの段へ入り、閉じ手（`ledger-close.ps1`）を
**19:42:31** に済ませた（`ledger-close.log`）。ところが続く `Get-ScheduledTask`／`Start-ScheduledTask`
（読み込みの重い組）の途中で**予定表の上限 PT2M に当たり、19:43:25 に殺された**（事象329＝時間切れで停止）。
19:43:25 の回も閉じ手の途中で同じく殺された。**revive も fail も記録に残らず**、
`ClaudeCodeAtLogon` の最終実行は 09-18 22:32 のまま。

| 刻 | 回 | どうなったか |
|---|---|---|
| 19:38:25 | 0本を見た | 19:38:49 `dead`（立ち上がりだけで24秒） |
| 19:39:25・19:40:25 | 2分に満たない | 何もせず返る（正しい） |
| **19:41:25** | 起こしの段へ | 閉じ手 19:42:31 → **19:43:25 に上限で殺された** |
| **19:43:25** | 起こしの段へ | **19:45:30 に上限で殺された** |
| 19:44:20 | — | claude が別の手で立った（`cmd /k claude.exe --continue …`・予定表の記録なし。手で立てたと見られる） |
| 19:46:25・19:48:25 | 生きている側 | また上限で殺された（重さが続いた） |
| 19:51:45 | 生きている側 | やっと `alive 戻っていた` を記録（手元の検査を止めた直後） |

**③ 予定表側の理由と更新の影響 … 更新は元ではない。**
claude の数え方は名（`claude.exe`）だけで、場所も版も見ていない。更新は **2.1.278 が 19:46:41 に入った**
（`~/.local/share/claude/versions`）が、道は `C:\Users\user\.local\bin\claude.exe` のまま変わらず、
19:44:20 に立った窓もこの道。止まりはどれも**更新より前**に起きている。

**直した所（`~/.claude/revive-claude.ps1`・写し `.bak-20260919`）**
- 閉じ手の**終わりを待たない**（切り離して走らせる。読むのはいまの控えなので、起こした窓が書き替える前に読み終わる）
- 窓を叩くのを **`schtasks.exe /End`→`/Run`** に替えた（重い組を読み込まない）
- 叩く前に **`waking` の一行**を残す。上限で殺されても「起こしにかかった」が記録に残る
- 触らない所 … `DEAD_MIN`（2分）・`GAP_MIN`（10分）・予定表の上限（PT2M）・数え方・入力待ちの見張り

**作り値（四通り合格・一時の所で、偽の schtasks・偽の閉じ手〈8秒かかる〉・偽の知らせ）**

| 場合 | 結果 |
|---|---|
| イ 0本が3分 | ✔ **2.1秒で返った**（閉じ手の8秒を待たない）。`waking`→`revive`、`/End`→`/Run` の順、偽の知らせ1通。閉じ手は10秒後に済 |
| ロ 0本が1分 | ✔ 何もしない |
| ハ `/Run` が失敗 | ✔ `waking`→`fail … 終了コード 1` |
| ニ 起こしてから10分以内 | ✔ 続けて叩かない |

本物の `ClaudeRevive` も直した後の回は **結果 0x0**（20:12:12）。

## 2. 速い版の上限 … **全体327秒・段ごとに中央値の3倍（床30秒）**

**① 直近5回の所要**（雲の `check.yml`「速い版」の段）… **83・81・114・109・109秒、中央値109秒**
（09-18 23:41／23:28／22:31／11:19／09-17 12:11）。手元の記録には段ごとの刻が無いので、雲の記録を物差しにした。

**② 上限**（`check-all.js` の頭に定数で置いた）

| | 中央値 | 上限 |
|---|---|---|
| 全体 | 109秒 | **327秒** |
| ⑦ 狭い画面での溢れ | 63.3秒 | **190秒** |
| ⑯・⑰・⑱ | 5.3・3.8・4.0秒 | 3倍は16・12・12秒 → **床の30秒** |
| adv-check（丸ごと一段） | 6.1秒 | 3倍は19秒 → **床の30秒** |
| ほかの段（①〜⑥・⑧〜⑮・⑲〜㉓） | 1秒未満 | **床の30秒** |

＊**床（30秒）を足したのはこちらの判じ。** 中央値が0.0秒の段は、3倍が0秒になって即座に切れるため。
＊段ごとの中央値は、5回のうちログが読めた3回から取った。
＊超えたら「**✗ 上限で切った：止まった段 ⑦ 狭い画面での溢れ・段の経過 N秒・全体の経過 M秒（段の上限 190秒）**」を出し、
　子を**孫の Edge ごと**落として（Windows は `taskkill /T`）、残りは回さず **FAIL（終了コード1）** で抜ける。
＊**フル版には掛けない**（視野が21で、所要がまるで違う）。

**作り値（四通り合格・偽の check.js／adv-check.js、上限を縮めた写しで）**

| 場合 | 結果 |
|---|---|
| イ ふつうに終わる | ✔ 両方PASS・終了コード0 |
| ロ ⑦で居座る（孫を一本立てて） | ✔ 4秒で切った「止まった段 ⑦ 狭い画面での溢れ・段の経過 4秒（段の上限 3秒）」・終了コード1・adv-check は回さない・**孫も落ちた** |
| ハ 段は短いが全体が長い | ✔ 12.1秒で切った「止まった段 ⑧・全体の上限 12秒」・終了コード1 |
| ニ フル版で⑦が長い | ✔ 切らずに両方PASS |

**③ 今日固まった回 … ⑦（狭い画面での溢れ）。** 19:03:55 に起こした速い版が、19:41:21 に一つ目の視野（568x320）の
10画面を終え（`check-progress.txt`「1/2」）、19:51 に二つ目の視野（900x381）の4画面目 `settings` を headless の Edge で
測っている最中だった。⑦だけで約48分（雲の中央値は63秒）。20:0x にこちらで止めた。

＊**手元での回し直しはしていない。** 手元の速い版は headless の Edge を立てる——それが今日の止まりの元で、
　`pre-push` の決め（VAIO で headless の Edge は二度と立てない）にも当たる。代わりに `check-all.js` を押して
　**雲の速い版を新しい上限つきで回した**（下の実測）。

**雲の実測（上限を入れた後の一本目）** … 走り 35439474191（02710cf4）の「速い版」が **success・91秒**（上限327秒の内）。
フル版は同じ走りの後段で回っている（上限は掛けていない）。

## 3. 鍵の言葉「鍵:ESC」 … **足した**

`~/.claude/inbox-watch.ps1`（写し `.bak-20260919`）。郵便受けの一行が**丸ごと「鍵:ESC」**（前後の空白・改行は無視）と
一致したときだけ、**受信箱へ流さず**（台帳の印にも積まず）、claude の窓へ esc を一回送り、`inbox-watch.log` に一行残す。

- 送る手は新しい **`~/.claude/send-esc.ps1`**。題が「麻雀 攻守判断 (Claude Code)」で始まり、子に `claude.exe` を持つ窓を探し、
  その窓の**入力へ直に esc を一打だけ書く**（`WriteConsoleInput`）。前面の窓は動かさず、ほかの窓には何も届かない。
- 題の合う窓が**0本か2本以上なら送らない**。20秒で返らなければ切る（見張りの輪を止めない）。
- 一致は字どおり。**「鍵：ESC」（全角のコロン）・「鍵:esc」・「鍵:ESC 以上」は鍵とみなさず、今までどおり流す。**
  ＊全角のコロンも受けたい場合は言ってください（字の選び方なので、こちらでは広げていない）。
- 常駐は起こし直した（常駐の起動 20:11:31 ＞ 台本の更新 20:10:43、一本だけ）。

**作り値（本物の窓には当てていない）**

| 場合 | 結果 |
|---|---|
| 送り手：偽の窓（別の題）が一本 | ✔ `sent 2`・偽の窓が受け取ったキーは **Escape 一つだけ** |
| 送り手：題の合う窓が無い | ✔ `notfound`・何も送らない |
| 送り手：題の合う窓が二本 | ✔ `many`・**どちらにも送らない** |
| 送り手の前後で本物の claude | ✔ pid 10320 のまま |
| 受け手：`鍵:ESC`／`   鍵:ESC   ` | ✔ 送り手を呼び、受信箱へは流さない（記録「鍵の言葉「鍵:ESC」を受けた。窓へは流さない。esc を一回送った」） |
| 受け手：`鍵:ESC 以上`・`鍵：ESC`・`鍵:esc`・`ふつうの指示。以上` | ✔ 今までどおり受信箱へ流れた |
| 受け手：窓が無い回 | ✔ 記録「窓が見つからず送らなかった（notfound）」・受信箱へは流さない |

＊受け手の作り値は、本物の台本から `Say`・`Is-KeyEsc`・`Send-KeyEsc` と分岐の一行をそのまま写して回した。

## 4. 写し・構文・作り値 … **全部済**

| 台本 | 写し | 構文検査 | 作り値 |
|---|---|---|---|
| `check-all.js` | `check-all.js.bak-20260919` | `node --check` OK | 四通り合格 |
| `inbox-watch.ps1` | `inbox-watch.ps1.bak-20260919` | Tokenize OK（BOM あり） | 七通り合格 |
| `send-esc.ps1`（新） | — | Tokenize OK | 四通り合格 |
| `revive-claude.ps1` | `revive-claude.ps1.bak-20260919` | Tokenize OK | 四通り合格 |

## 5. 宣言 y0919-1701 の書きかけ … **残っている。触っていない**

`git diff --numstat` で **koushu-handan.html 48/6・check.js 2/1**（ほかに ver.txt・serifu.txt・serifu-adv.txt の再抽出分）。
commit したのは `check-all.js` だけ（02710cf4）。

## 6. koushu-handan.html・stable … **触っていない**

## 前の枠の分

- **止まっていた段** … 上の 2-③（**⑦**・二つ目の視野の `settings` の最中）。
- **19:05〜19:15 に機械は起きていた（ただし重かった）** … `hook.log` の生存が 19:08・19:10・19:12・19:16、
  `watch-step-log.txt` の巡回が 19:06・19:09・19:11・19:14・19:15 に入っている。毎分のはずの巡回が2〜3分おきに間引かれており、
  寝てはいないが、速い版の Edge で詰まっていた。
