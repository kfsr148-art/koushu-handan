# 通知の宛先-1 — 旧話題へ送る・読む箇所は **0件**

**VAIO 2026-08-30 16:52 ／ 本体（`koushu-handan.html`）にも `panel.html` にも触っていない**

**結論 ── 送る者も読む者も一つもありません。置き換えは要りませんでした。**

＊話題名は公開リポジトリに書かない決まり（`notify-record.ps1` L46 が控えからも伏せる）なので、
　この報告では**先頭だけ**を出します。

---

## 数えた範囲

`~/.claude` 直下の**台本と設定 52ファイル**（`*.ps1` / `*.json` / `*.vbs` / `*.txt` / `*.tsv` /
`*.cmd` / `*.bat`。`.bak` を除く）、および**予定表の六つ**と `settings.json`。

## 旧話題（`kh-ece55ad0…`）が現れる所 ── 4件。**どれも送る・読むではない**

| ファイル | 行 | 用途 | 送る／読むか |
|---|---|---|---|
| `orders-open.tsv` | **255** | **この指示そのものの控え**（「…へ送る箇所が残っているか」という項目の字） | **違う** |
| `rc-ntfy-topic.txt.retired-20260823` | **1** | **引退させた話題の写し**（`.retired-` は退避の名） | **違う**（読む者なし） |
| `rc-ntfy-topic.txt.retired-20260826` | **1** | 同上 | **違う**（読む者なし） |
| `watch-notify.log` | **1・3・5 ほか** | **2026-08-21 の送信の記録**。ntfy が返した JSON に話題名が入っている | **違う**（過去の記録） |

**`.retired-` を読む者は居ない。** `watch-notify.ps1` **L348** が
`*.bak-*` と `*.retired-*` を**明示的に飛ばして**いる。

```powershell
if ($f.Name -like '*.bak-*' -or $f.Name -like '*.retired-*') { continue }
```

## 送る側が話題名をどこから取るか ── 全部 `rc-ntfy-topic.txt` の一枚

| 台本 | 行 | 何 |
|---|---|---|
| `watch-notify.ps1` | **L443** | `$TopicPath = Join-Path $Root 'rc-ntfy-topic.txt'` |
| 同上 | **L626** | `('https://ntfy.sh/' + $topic)` へ POST（押し送りの出口） |
| `daily-notice.ps1` | **L33** | `$Topic = Join-Path $Root 'rc-ntfy-topic.txt'` |
| 同上 | **L205** | `('https://ntfy.sh/' + $tp)` へ POST（定時の報せ） |
| `home-backup.ps1` | **L25・L54** | 同じ一枚を読む |
| `inbox-watch.ps1` | **L94** | `$SendPath`（置き場を持つだけ。送信の口は 2026-08-26 に畳んだ） |

**いま `rc-ntfy-topic.txt` に入っているのは `kh-5te46s…`（末尾 `ue6i`）** ＝ 現行の話題。

## 置き換えは行っていない

**0件なので、置き換える対象がありません。** 試験送信も行っていません
（送る先は既に現行の一つだけで、確かめる差が無いため）。

＊現行の話題が生きていることは、**本日の押し送り23件が HTTP 200 で通っている**ことで
　示されています（通知の題-5 の試し送り・通知の急ぎ-2 の実測を含む）。

## 併せて確かめた

| 何 | 結果 |
|---|---|
| `settings.json` に話題名 | **0件** |
| 予定表六つ（`ClaudeBoard` / `ClaudeDailyNotice` / `ClaudeHomeBackup` / `ClaudeHookHeartbeat` / `ClaudeSweepChecks` / `ClaudeWatchNotify`）の引数 | **六つとも話題名なし** |

---

## iPhone 側に残っている購読について

**VAIO の側から止める手はありません。** 話題名は「送る先の名前」でしかなく、
**購読は端末が自分で持っている**ためです。2026-08-26 を最後に止まっているのは、
**こちらが送らなくなった**からで、機械の側は正しく現行の話題だけを使っています。

＊消すなら **iPhone の ntfy アプリで、その購読を解除**していただく形になります。

## 残り

`orders-open.tsv` の未了 ── **残り0件。**
