# 通知の題-4 — 二字を上書きし、押し送りの🙀 も揃えた

**VAIO 2026-08-30 04:05 ／ 本体（`koushu-handan.html`）にも `panel.html` にも触っていない**

---

## 1. 直した所 ── 二つのファイルの二箇所

| ファイル | 行 | 前 | 後 |
|---|---|---|---|
| `~/.claude/daily-notice.ps1` | **L127** | `😺 平気だにゃ` | **`😺 現在も順調にゃ`** |
| 同上 | **L127** | `🙀 なんか変だにゃ、見て` | **`🙀 異変発見だにゃ`** |
| `~/.claude/watch-notify.ps1` | **L511** | `🙀 なんか変だにゃ、見て` | **`🙀 異変発見だにゃ`** |

```powershell
# daily-notice.ps1 L127
$title = $(if ($allOk) { '😺 現在も順調にゃ' } else { '🙀 異変発見だにゃ' })

# watch-notify.ps1 L511
$PUSH_TITLE_OVER = '🙀 異変発見だにゃ'
```

**変えていないもの。**

- **`$PUSH_TITLE_DONE`（L509）＝ `😽 終わったにゃ、写して`** — そのまま
- **`$PUSH_TITLE`（L505）＝ `😼 ヨシ頼むにゃ`** — そのまま
- **本文** — 定時の報せの四行（状態・台帳・見張り・使用量）も、押し送りの本文も一字も触っていない
- **`？` を `○` と数えない扱い** — 通知の題-2 のまま。判定は
  `if ($a -notmatch '○$') { $allOk = $false }` の一行で、**`？` は `○` で終わらないので 🙀 の側**

**🙀 の字が二箇所で揃った。** 定時の報せの異変（`daily-notice.ps1` L127）と、
押し送りの見込み超過（`watch-notify.ps1` L511）が**同じ `🙀 異変発見だにゃ`** になる。

## 2. 出る字の写し ── 三つ

### ①定時の報せ・○のとき（台本を `-DryRun` で本物の値で回した）

```
😺 現在も順調にゃ
状態: 手待ち / 通知の題-2（定時の報せの題を見張り四つの結果で決める）
台帳: 残り 0件
見張り: 生存○ 見張り○ 片付け○ 受信箱○
使用量: セッション 6% / 週全体 78% / 週Fable 56%
```

### ②定時の報せ・×のとき（同じ式へ印を与えて組んだ。実際には止めていない）

| 場合 | 本文の見張りの行 | 題 |
|---|---|---|
| 見張りが落ちた | `見張り: 生存○ 見張り×2 片付け○ 受信箱○` | **`🙀 異変発見だにゃ`** |
| 受信箱が落ちた | `見張り: 生存○ 見張り○ 片付け○ 受信箱×落` | **`🙀 異変発見だにゃ`** |
| **数えられなかった（`？`）** | `見張り: 生存○ 見張り○ 片付け○ 受信箱？` | **`🙀 異変発見だにゃ`** |

### ③押し送りの三種（`Push-Ntfy` L576-578 と同じ式）

```
wait  → 「😼 ヨシ頼むにゃ」          （変えていない）
done  → 「😽 終わったにゃ、写して」  （変えていない）
over  → 「🙀 異変発見だにゃ」        ← 今回そろえた
```

### ntfy へ渡る `Title` ヘッダの実物

```
😺 現在も順調にゃ
  =?UTF-8?B?8J+YuiDnj77lnKjjgoLpoIboqr/jgavjgoM=?=

🙀 異変発見だにゃ
  =?UTF-8?B?8J+ZgCDnlbDlpInnmbropovjgaDjgavjgoM=?=

😽 終わったにゃ、写して
  =?UTF-8?B?8J+YvSDntYLjgo/jgaPjgZ/jgavjgoPjgIHlhpnjgZfjgaY=?=

😼 ヨシ頼むにゃ
  =?UTF-8?B?8J+YvCDjg6jjgrfpoLzjgoDjgavjgoM=?=
```

## 3. 作法29 の手順

| 段 | `daily-notice.ps1` | `watch-notify.ps1` |
|---|---|---|
| ① 写しを取る | `.bak-20260830b`（161行） | `.bak-20260830d`（1944行） |
| ② 直す | L127（Edit の道具） | L511（Edit の道具） |
| ③ 構文検査 | **構文OK 161行／BOM 有り** | **構文OK 1944行／BOM 有り** |
| ④ 通らなければ戻す | 通ったので戻していない | 同左 |
| ⑤ 常駐の起こし直し | **不要**（予定表が 09:00・21:00 に呼び直す） | **不要**（予定表が毎分呼び直す） |

---

## 実機で見るところ

- 次の定時の報せ（**09:00**）の題が **`😺 現在も順調にゃ`**（「平気だにゃ」でも「🕘 定時の報せ」でもない）
- 次の**ヨシ待ち**の押し通知が **`😼 ヨシ頼むにゃ`**（変わっていないこと）
- 次の**終わり**の押し通知が **`😽 終わったにゃ、写して`**（変わっていないこと）
- 見込み超過の押し通知が出たら **`🙀 異変発見だにゃ`**

本文はすべて今までどおり。

## 残り

`orders-open.tsv` の未了 ── **残り0件。**
