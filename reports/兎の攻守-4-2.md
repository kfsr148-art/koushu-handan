# 兎の攻守-4 — 手牌の外の情報がどこまであるか（一覧のみ）

**状態：終わり（残り0件）／VAIO 2026-08-29 20:2x**

**この報告は数字と一覧だけを並べる。判断は書かない。**
**本体（`koushu-handan.html`）は一字も書き換えていない。数えただけ。**

＊`y0829-usagi4` のヨシを受け、再送された 1〜4 を実施した。**5〜8 は前便で済**（下の「残り」に併記）。

---

## 1. 記録に手牌以外の列があるか

### 1-1. 列の全一覧

**`core-battle-log-dora.tsv`（38列）**

| # | 列 | # | 列 | # | 列 |
|---|---|---|---|---|---|
| 1 | 局 | 14 | widthGood | 27 | yakuhaiPair |
| 2 | 席 | 15 | goodShapes | 28 | kokushiSeen |
| 3 | 親 | 16 | weakShapes | 29 | kokushiKinds |
| 4 | 配牌 | 17 | totalBlocks | 30 | kokushiShanten |
| 5 | ドラ表示牌 | 18 | shapeOK | 31 | yakumanReach |
| 6 | ドラ | 19 | shapeNeeded | 32 | isDealer |
| 7 | doraCount | 20 | fillGood | 33 | defHonors |
| 8 | verdict | 21 | fillWeak | 34 | defTerminals |
| 9 | borderline | 22 | fillRemain | 35 | 結果 |
| 10 | baseShanten | 23 | hasValue | 36 | 点棒 |
| 11 | acceptTypes | 24 | doraValue | 37 | 役 |
| 12 | acceptTiles | 25 | doraCount | 38 | 巡目 |
| 13 | widthLabel | 26 | redCount | | |

**`core-battle-log.tsv`（25列）** … 上から「ドラ表示牌・ドラ・doraCount・widthGood・fillGood・fillWeak・fillRemain・kokushiSeen・kokushiKinds・kokushiShanten・yakumanReach・isDealer」を除いたもの。

### 1-2. 指示の七項目

| 項目 | 有／無 | 列名と値の範囲 |
|---|---|---|
| **各家の持ち点（局開始時）** | **無** | 点棒（36列目）は**増減のみ**で、範囲は **−32000 〜 +32000**。開始時の持ち点は無い |
| **場（局番号）と本場** | **無** | 「局」（1列目・**1〜1000**）は**記録の通し番号**で、東一局・南二局といった場ではない。本場の列も無い |
| **親か子か** | **有** | 「親」（3列目・**0／1**）。あわせて「isDealer」（32列目・0／1）と「席」（2列目・**0〜3**）。**親の席は1000局すべて 0**（＝全行が東一局相当） |
| **自分の順位（局開始時）** | **無** | 順位の列は無い。持ち点が無いので記録からは組めない |
| **残り局数** | **無** | 列が無い。1000局すべてが**同じ位置の一局**なので、記録の中に差が存在しない |
| **他家の捨て牌・鳴き** | **無** | 列が一つも無い。あるのは自分の配牌13枚だけ |
| **局の結末以外の途中経過** | **一部あり** | 「巡目」（38列目・**1〜18**）＝打ち切った巡目のみ。「結果」（35列目・**和了／放銃／被ツモ／流局／無関係**）、「役」（37列目・和了した席のみ） |

＊点棒の合計が局内で0にならない局が **525 / 1000** ある（立直棒・供託の分）。

---

## 2. 無い列を、対局を回している側から足せるか

`core-battle.js` は `@kobalab/majiang-core` の牌譜（`paipu`）を受け取り、
**`paipu.log[0]`（第1局）だけ**を読んでいる（L110-111）。牌譜が持っている値を実測で確かめた。

| 項目 | 対局側が持つか | 出どころ | 記録に足せるか・工数の見込み |
|---|---|---|---|
| **各家の持ち点（局開始時）** | **持つ** | `qipai.defen` … 実測 `[25000,25000,25000,25000]` | **足せる。列を4本足すだけで10分**。ただし第1局しか採っていないので**全行25000で一定**になる |
| **場と本場** | **持つ** | `qipai.zhuangfeng`（0＝東場）／`qipai.jushu`（0＝一局目）／`qipai.changbang`（本場）／`qipai.lizhibang`（立直棒） | **足せる。10分**。ただし第1局のみなので**全行が 東一局0本場0本**で一定 |
| 親か子か | 持つ | `qipai.jushu` から算出（既に列にある） | **済** |
| **自分の順位（局開始時）** | **持つ** | `qipai.defen` を並べ替えるだけ | **足せる。15分**。ただし第1局は全員同点で順位が付かない |
| **残り局数** | **持つ** | `paipu.log.length`（**実測15局**）と局の位置 | **足せるが改修が要る。30分**。いまは `log[0]` だけを採っているので、**全局を採るように直す**必要がある。行数は約15倍になる |
| **他家の捨て牌・鳴き** | **持つ** | 局の配列の各手 … 実測で1局46手・内訳 `qipai`1／`zimo`21／`dapai`22／`fulou`1／`hule`1。`dapai` は `{l:席, p:'s9'}` の形 | **足せるが別ファイルが要る。1時間**。一行に収まらないので「1局1行の手順ファイル」を別に書き出す形になる |
| **途中経過（決着の巡目以外）** | **持つ** | 同上の手の並び（何巡目に誰が鳴いた／立直したか） | **足せる。上と同じ経路で1時間**|
| （参考）最終成績 | 持つ | `paipu.defen` ／ `paipu.point` ／ `paipu.rank` | **足せる。10分**。ただし第1局の判定と最終順位の対応になる |

**＊どれも「記録の取り直し」が要る。** この機械の実測で **36秒/局（10局＝360.6秒）＝1000局で約10時間**。
`core-battle-log-dora.tsv` は既存の配牌へドラを置き直したものなので列を足せたが、
**持ち点・場・鳴きは既存の記録から復元できない**（元の対局の牌譜が残っていない）。

---

## 3. `analyze()` が受け取る引数

**引数は0個。** 宣言は L2434 の `function analyze() {` で、括弧の中は空。

外の見晴らし（グローバル）から読む値が**5つ**あり、**そのうち4つが手牌以外**。

| 値 | 行 | 手牌かどうか |
|---|---|---|
| `hand` | L1749 | **手牌** |
| `doraCount` | L1750 | 手牌以外 |
| `doraIndicator` | L1751 | 手牌以外 |
| `roundWind` | L1753 | 手牌以外 |
| `seatWind` | L1754 | 手牌以外 |

**持ち点・順位・局・本場・他家の捨て牌は、一つも受け取っていない。**

---

## 4. 本体の画面で、手牌以外を入力できる箇所

| 入力できるもの | 行 | 中身 |
|---|---|---|
| **ドラ表示牌** | **L1572**（釦 `doraToggle`）／L2320（`indicatorArmed` の切替）／L1775-1777（牌を押して指定） | ドラ表示牌のコードを1枚 |
| **場風・自風（縦持ち）** | **L1594-1610**（`windGrid` の8升 `.seat-cell`。`data-round` × `data-seat`）／L2398-2410（押したときの動き） | 東東〜南北の8通り |
| **場風・自風（横持ち二段式）** | **L1613-1620**（`.w2-r` 二つ・`.w2-s` 四つ）／L2413-2429 | 先に場、次に自 |
| **連チャン／親流れ** | L2341-2350（`nextHand`） | 自風を一つ進めるだけ。局番号や持ち点は動かさない |
| （参考）手入力欄 | L1634（`qiText`） | **手牌**の文字入力 |

**持ち点・順位・局番号・本場・残り局数・他家の捨て牌／鳴きを入れる欄は、一つも無い。**
`持ち点`・`順位`・`本場`・`東一局`・`残り局`・`供託` の字を本体で探しても、
**入力欄としては1件も出てこない**（「掛け持ち点検」「優先順位」など別語の一部として当たるだけ）。

---

## 残り

**残り0件。**

| 番号 | 項目 | 状態 |
|---|---|---|
| **兎の攻守-4 の 1〜4** | 手牌の外の情報の棚卸し | **済**（この報告） |
| 兎の攻守-4（追補）5-8 | 上振れの余地の測定 | **済**（前便） |

`orders-open.tsv` に「済／取消／未了」以外で残っているのは次の四行だけで、**いずれも作業ではない**。

- `凍結-0`（**発効中**）… 見張り一式の凍結。状態であって作業ではない
- `再起動-1` / `再起動-2`（**断線-5 へ統合・消していない**）… 統合先の `断線-5` は済
- `被り-1`（**記録のみ・直しは当てない**）… 調べの結果は報告書に残した

**未着手・未了は0件。**

---

## 本体に触れていないことの確かめ

```
$ git status --short koushu-handan.html ver.txt
（変化なし）
```

版は **v1429** のまま。今回書いたのはこの報告書と台帳だけ。

---

＊この控えは `9bef58a` の時点の `report-latest.md` から起こした（印 `兎の攻守-4-2`）。
　機械が書く「送った知らせ」の節は控えに残していない。
