# 穴の直し-1 ③ の裁定 — 焼き付けの四枚を消した（6.97MB）

**VAIO 2026-08-30 16:34 ／ 本体（`koushu-handan.html`）にも `panel.html` にも触っていない**

**縮んだのは作業木と配布の 6.97MB 分だけです。git の履歴には中身がそのまま残っています。**

---

## ① 作法21 へ追記した（消す前に）

`CLAUDE.md` の「21. 素材の作法」へ、**消してよい四枚と焼き直しの実コマンド**を書いた。

```
**焼き付けの四枚は、リポジトリに置き続けない（2026-08-30 の裁定）。**
`asset-proof-dark.png` ／ `asset-proof-light.png` ／ `head-proof-dark.png` ／
`head-proof-light.png` の四枚は、**道具で何度でも焼き直せる**ので**消してよい**。
点検が要るときは、その場で焼いて見て、見終わったら置いておかなくてよい。

    node asset-proof.js     → asset-proof-dark.png ／ asset-proof-light.png
    node head-proof.js      → head-proof-dark.png  ／ head-proof-light.png

＊**消してよいのはこの四枚だけ。** 同じ「確認の絵」でも
`usagi-proof-dark.png` ／ `usagi-proof-light.png` ／ `sizing-review.png` の三枚は、
**焼いた道具がリポジトリに無い**ので消さない（消すと戻らない）。
＊**元絵（`*_src.png`）は上の「原版として残す」がそのまま効く。** 消さない。
```

## ② 四枚を消して push した

消す前に**焼き直せることを確かめた**。

```
asset-proof.js : 構文OK（9898 byte）
head-proof.js  : 構文OK（9841 byte）
```

**読む者が居ないことも最終確認した** ── 当たったのは `asset-proof.js` / `head-proof.js` の二つだけで、
どちらも **`fs.writeFileSync` で書く側**。開く者は一つも無い。

| ファイル | 大きさ |
|---|---|
| `asset-proof-dark.png` | 1,946,799 byte |
| `asset-proof-light.png` | 1,965,069 byte |
| `head-proof-dark.png` | 1,681,055 byte |
| `head-proof-light.png` | 1,720,220 byte |
| **合計** | **6.97MB** |

commit `726a10c`。**本体・`panel.html`・`ver.txt`・`panel-ver.txt` には一切触れていない**
（`git diff --stat HEAD` で差分ゼロ）。

## ③ 公開側の実読み

### 消した四枚 ── すべて 404

```
asset-proof-dark.png  : HTTP 404
asset-proof-light.png : HTTP 404
head-proof-dark.png   : HTTP 404
head-proof-light.png  : HTTP 404
```

### 残すと決めた三枚 ── すべて 200

```
usagi-proof-dark.png  : HTTP 200
usagi-proof-light.png : HTTP 200
sizing-review.png     : HTTP 200
```

### 元絵（抜き取り）── 残っている

```
kishi_hit_src.png        : HTTP 200
usagi_idle_front_src.png : HTTP 200
```

### 本体と `panel.html` ── 無事

```
ver.txt       = 1431
panel-ver.txt = 102
koushu-handan.html : HTTP 200 ／ 公開 10,687,226 byte ／ 手元 10,687,226 byte ／ 中身が完全一致
panel.html         : HTTP 200 ／ 公開    153,594 byte ／ 手元    155,921 byte ／ 中身が完全一致
```

＊`panel.html` の byte 差 **2,327** は**行数ぶんの改行**（手元が CRLF・公開が LF）。
　行数 2,334 とほぼ一致する。**中身の差ではない**（改行を揃えた比べで完全一致）。

## 縮んだのはどこか

**作業木と配布の 6.97MB 分だけです。**

```
追跡しているファイルの合計 : 56.3 MB（247件）  ← 消す前は 63.3MB（251件）
pack の大きさ              : 165.68 MiB       ← 変わらない
```

**git の履歴には中身がそのまま残っています。**

```
最後に触れた commit : 726a10c（消した回）
その commit の中身  : 1,946,799 byte（履歴から取り出せる）
```

＊履歴まで縮めるには `git filter-repo` などで**過去を書き換える**要があり、
　**取り返しのつかない操作**（作法29 ④）になります。今回はしていません。

## 残したもの

| 群 | 件数 | 大きさ | 残す理由 |
|---|---|---|---|
| 元絵（`*_src.png`） | 18件 | 17.7MB | 作法21 の「原版として残す」 |
| `usagi-proof-*` / `sizing-review.png` | 3件 | 7.32MB | **焼いた道具がリポジトリに無い**（消すと戻らない） |

---

## 実機で見るところ

**本体（`koushu-handan.html`）と返事パネルが今までどおり開く**こと。
消したのは点検用の絵だけで、遊ぶ人にも読む人にも見えていた物ではありません。

## 残り

`orders-open.tsv` の未了 ── **残り3件**（通知の宛先-1 ①②③）。
