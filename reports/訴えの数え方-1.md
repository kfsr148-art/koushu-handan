# 訴えの数え方-1 — 定時の「訴え: N件」を、種類の数にした

**状態** … 作り値まで済み／**次の定時（09:00）で見届ける**／記録・訴えの一発・判定には触っていない

## 直した所（`daily-notice.ps1` の数える二行だけ）

| | 前 | いま |
|---|---|---|
| 数える単位 | **行**（同じ訴えが続くと何十件にもなる） | **種類**（同じ種類が続いている間は一件） |
| `rung` | 訴えとして数えていた | **数えない**（鳴らした控えであって訴えではない） |

```
訴え: 3件（cards-full・ask-note・subj-gap）… pipe-warn.log
```

＊**行の数は失っていない。**`pipe-warn.log` を見れば何行あるかは分かる。
　定時の一行で知りたいのは「**いくつの事が起きているか**」なので、そこを出す。
＊`pipe-check` の記録の付け方・訴えの一発（その回に鳴らす方）・判定には**触っていない**。

## 作り値（本物の数えの枝を字面のまま抜き出して回した）

```
(甲) cards-full 26 ＋ ask-note 7 ＋ subj-gap 5 ＋ rung 2
     → 訴え: 3件（cards-full・ask-note・subj-gap）        ← 枠のとおり
(乙) rung だけ 2行                → 行ごと出さない（訴えではないため）
(丙) cards-full だけ 26行         → 訴え: 1件（cards-full）
(丁) 訴えが無い（ok だけ）        → 行ごと出さない
(戊) 三種類が一行ずつ             → 訴え: 3件（cards-full・ask-note・subj-gap）
```

## 実地

**次の定時（09:00）の報せで見届ける。**いまの `pipe-warn.log` の直近12時間は
`cards-full` ／ `ask-note` ／ `subj-gap` ／ `rung` を含むので、
**「訴え: 3件（…）」**の形で出れば合格（前の数え方なら16件と出ていた）。

## 実地 — 09:00 の定時は**出ていなかった**ので、DryRun で確かめた

```
予定表 ClaudeDailyNotice … 最後 09/06 09:00:00／結果 267014（前の回が止められた）
daily-notice.log の最後 … 2026-09-05 21:00:55
```

**今日の 09:00 の報せは、走り出したが止められて出ていない。**（この枠の直しとは別件。）
そこで、本物の材料に対して `-DryRun`（送らずに本文だけ出す）で確かめた。

```
🙀 異変発見だにゃ
状態: 作業中 / 訴えの数え方-1＝定時の報せの「訴え: N件」を
台帳: 残り 2件（先の穴-2-1・訴えの数え方-1）
見張り: 生存○ 見張り×2147946720 片付け○ 受信箱○
使用量: セッション 65% / 週全体 26% / 週Fable 40%
訴え: 2件（cards-full・subj-gap）… pipe-warn.log      ← **種類の数。rung は入っていない**
```

**前の数え方なら「16件」**（直近12時間の行を数えていた）。いまは**2件**。
＊`ask-note` が入っていないのは、直近12時間に無いため。
＊`rung` は `pipe-warn.log` に有るが、数にも括弧の中にも**出ていない**。

**次の 21:00 の定時で、送られる本物の一行も同じ形になる。**

## この回に見えた別件（報告だけ・直していない）

| 見えた物 | 中身 |
|---|---|
| `ClaudeDailyNotice` の結果 **267014** | 今日の 09:00 の報せが**止められて出ていない** |
| `台帳: 残り 2件（先の穴-2-1・…）` | 台帳の数えは「`済` で始まらない行」を残りとする。**`取下：…` は済とみなされていない** |
| `見張り×2147946720` | 見張りの点検が誤りを出している（8月の「見張りの×-1」と同じ数字） |
