# v1429 — 副産物三件を本体へ当てた（撤退予約線のコメント／totalBlocks の寄せ／hasValue の閾値）

**状態：終わり（残り0件）／VAIO 2026-08-29 15:3x**

**結論** — 被り-1 の調べで出た副産物三件を本体へ当てた。**判定（`verdict`）は4000行で一件も動いていない。**
画面に出る変化は**兎の票が6件（0.15%）攻→黙るへ**変わるのと、**孤立ドラ一枚の手で根拠欄①の印が ✓→× に変わる**の二つだけ。
検査は速い版で **check・adv-check とも PASS**、`serifu.txt` は再抽出して本体と一致、
**公開側（raw）を実読みして手元と完全一致**（`ver.txt`=1429・`data-ver`=1429・`verTag`=v1429）。

＊`fillRemain` の件は**当てていない**（設計は依頼者側で検討中）。

---

## 0. `hasValue` の読み手の一覧（③を当てる前の確認）

指示どおり、当てる前に全部 grep した。**根拠欄①以外に読み手は無し**だったので、そのまま当てた。

| 場所 | 行 | 種別 |
|---|---|---|
| `koushu-handan.html` | L2872（旧） | 定義 `const hasValue = doraValue>0;` |
| `koushu-handan.html` | L3056（旧） | `analyze()` の返しに載せているだけ |
| `koushu-handan.html` | **L8474 / L8475 / L8476（旧）** | **根拠欄①（✓×・題・詳細）＝唯一の読み手** |
| `core-battle.js` L73／`core-probe.js` L119 | — | **列名の並び**（書き出す項目名の配列）。値は読んでいない |
| `board.json`／`notices.json`／`status.md`／`report-latest.md` | — | 控えの文章 |

執事は `a.doraValue >= 1`（L4048）を**直に**読んでおり、`hasValue` は経由していない。

---

## 1. 当てた三件

### ① 撤退予約線 — 実装を正とし、コメントを直した

| 行（新） | 直した中身 |
|---|---|
| **L8400** | `// 撤退予約線：`**`受け入れ枚数から`**`「一段進む…」` → `// 撤退予約線：`**`初期シャンテンから`**`「一段進む…」` |

**実装を正とした理由（三つ）**

1. すぐ下の注記（新 L8407-8408）が「期限は**初期シャンテン別**の実測（400局・**一向聴到達巡目の中央値**から一巡詰めた値）で引く」と書いており、**受け入れ枚数別の実測は存在しない**。実装を `acceptTiles` へ変えると、裏付けの無い数字で期限を引くことになる。
2. 誤診記録側の同じ表（`misdiagRetreatBody`・`TURN_BY_SHANTEN`）も **`r.sh`** を見ており、**二箇所とも向聴で揃っている**。片方だけ変えれば二本立ての食い違いを作る。
3. **初版（`6dee8a2`・2026-07-26）から実装は `baseShanten` だけ**で、字が一度も実装に追いついていない。**古いのはコメントの側**。

判定の中身は変えていないので、**4000行の採点し直しでも変化0件**（下の2節）。

### ② `totalBlocks` — 七対子の上書き**後**へ寄せた

| 行 | 直した中身 |
|---|---|
| **L2514（旧）** | `const totalBlocks = goodShapes + weakShapes;` を**削除** |
| **L3028-3031（新）** | 七対子ルートの上書きの**直後**へ移し、注記三行を添えた |

寄せ先を「上書き後」にした理由 — 七対子ルートへ倒れた手で、**エンジンが採らなかった面子手の分解の数**を「まとまりの総数」と名乗るのは筋が通らない。判定（`shapeOK`）も虎の台詞の `{n}` も、**上書き後の `goodShapes`** を見ている。

### ③ `hasValue` — 執事と同じ `doraValue >= 1` へ揃えた

| 行 | 直した中身 |
|---|---|
| **L2870-2873（新）** | `const hasValue = doraValue>0;` → **`doraValue>=1`**（注記三行つき） |
| **L8483-8488（新）** | 題と詳細の分岐を `a.hasValue` → **`a.valueSources.length`** |

**題と詳細も一緒に差し替えた理由** — `hasValue` だけ直すと、孤立ドラ一枚の手で
「**打点の種なし／ドラ・赤・役牌対子なし**」と**嘘を書く**（実際はドラが一枚ある）。
差し替え後は「**打点 0.5 — ドラ1（孤立）**」の字は残したまま、**印だけ ✓→×** になる。

**閾値を執事側へ寄せた根拠（4000行）** — この記録はドラを渡していないため `doraValue=0.5` が出ない。
**打点の種が赤五一枚だけの手**で代用した（孤立の判定は本体と同じ式：同色±2に相棒がいない）。

| 区分 | n | 和了% | 放銃% | 平均点棒 |
|---|---|---|---|---|
| 打点0（`doraValue`=0） | 2343 | 18.0 | 12.8 | −72 |
| **打点の種一枚・孤立** | **104** | **16.3** | **9.6** | **+62** |
| 打点の種一枚・組に入る | 689 | 20.9 | 11.9 | +622 |
| 打点2以上 | 340 | 31.2 | 12.4 | +1192 |

**孤立した打点牌一枚は和了率で打点0すら下回り（16.3% < 18.0%）、平均点棒 +62 も、組に入る場合（+622）より打点0（−72）にずっと近い。**
向聴別も同じ向き（向聴3：孤立18.2% < 打点0 21.4% < 組入23.8%／向聴4：孤立16.0%・打点0 15.3%・組入19.9%）。

### ＋ 兎の注記に断りを一つ足した

`tenLeans` の兎の注記にある **実測50000回（攻め1.34%・的中83.1%）** は、②を当てる**前**の数え方で取ったもの。
「v1429 で寄せたため4000行では攻め率が 1.40%→1.25% に動く（票が変わるのは 6/4000＝0.15%）」と四行添えた。
**50000回の測り直しは別件**として残してある。

---

## 2. 指示1 — 4000行を回し直したときの変化行数（件ごと）

`core-probe.js` で **v1428 の写しと v1429 の本体を並べて呼び**、同じ4000行に掛けた。

```
旧版 analyze() : v1428.html L2434〜3063（630行）
新版 analyze() : koushu-handan.html L2434〜3068（635行）
```

| 件 | 変化した行数 | 宣言との突き合わせ |
|---|---|---|
| **①撤退予約線のコメント** | **0 / 4000** | コメント一語のみ。`analyze()` の返しにも表示の式にも変化なし |
| **②`totalBlocks` の寄せ** | `totalBlocks` **493 / 4000（12.3%）**<br>**兎の票 6 / 4000（0.15%）** | 宣言は **6件** → **一致** |
| **③`hasValue` の揃え** | **0 / 4000** | この記録は `doraCount` を0で渡しており `doraValue=0.5` が出ないため（3節） |
| **`verdict`（攻守）** | **0 / 4000** | 宣言どおり一件も動かない |

`analyze()` の27項目のうち**値が変わったのは `totalBlocks` だけ**（493件）。ほかは全項目0件。

### ②の中身

```
動き方 : {"a→黙る": 6}
  向聴2  totalBlocks 6→4（良形4＋弱形0）  票 a→黙る
  向聴2  totalBlocks 6→4（良形4＋弱形0）  票 a→黙る
  向聴1  totalBlocks 6→5（良形5＋弱形0）  票 a→黙る
  向聴2  totalBlocks 6→4（良形4＋弱形0）  票 a→黙る
  向聴2  totalBlocks 6→4（良形4＋弱形0）  票 a→黙る
  向聴2  totalBlocks 6→4（良形4＋弱形0）  票 a→黙る
```

票の内訳は **攻 56→50／守 1066（据え置き）／黙る 2878→2884**。
連れて動く見た目は、兎が黙る分だけ**攻守の箱の幅比（`vzRatio`）が同じ6手で変わる**こと。
兎の一言は `fillWeak` 由来なので変わらない。

---

## 3. 指示2 — ③で根拠欄①の題と詳細が変わった件数、`valueSources.length` の分布

| | 件数 |
|---|---|
| **根拠欄①の題／詳細が変わった行** | **0 / 4000** |
| **`hasValue`（✓×）が変わった行** | **0 / 4000** |

| `valueSources.length` | 件数 | 割合 |
|---|---|---|
| **0件**（打点の種なし） | **2343** | 58.6% |
| **1件** | **1431** | 35.8% |
| **2件以上** | **226** | 5.7% |

**0件だった理由を明記しておく。** `core-battle.js` は `analyze()` へ **ドラを 0 で渡している**（L128）ため、
4000行すべて `doraCount`=0 → `doraValue` は整数しか取らず、**`doraValue=0.5` が一度も出ない**。
③が効くのは `0 < doraValue < 1` の手だけなので、**この記録そのままでは当然0件**になる。

**どれくらいの頻度で効くか**を別に数えた。同じ4000手に**ドラ表示牌を34種すべて置いてみる**と：

| 形 | 通り | 割合 |
|---|---|---|
| **`doraValue` がちょうど 0.5（＝印が ✓→× に変わる）** | **10105** | **7.43%** |
| （参考）ドラ0枚 | 90573 | 66.6% |
| （参考）ドラ1枚・孤立 | 16671 | 12.3% |
| （参考）ドラ1枚・組に入る | 22545 | 16.6% |
| （参考）ドラ2枚以上 | 6211 | 4.6% |

＊「ドラ1枚・孤立（12.3%）」のうち、**赤も役牌対子も無い**ものだけが `doraValue`=0.5 になるので 7.43%。

ドラを記録に残した打ち直しは、この機械で **実測36秒/局（10局=360.6秒）＝1000局で約10時間**かかるため見送った。
必要なら背後で回せる（`qipai.baopai` にドラ表示牌が入っていることは確認済み）。

---

## 4. 指示3 — 版を上げて公開まで通し、公開側を実読みした

**版は三箇所を同時に上げた**（作法4）。

| 場所 | 前 | 後 |
|---|---|---|
| `koushu-handan.html` L2 `data-ver` | `1428` | **`1429`** |
| `koushu-handan.html` L1316 `verTag` | `v1428` | **`v1429`** |
| `ver.txt` | `1428` | **`1429`** |

**納品前の検査**

```
node serifu-extract.js         … 合計 7593 行／口調の混入 0 件／探偵編の抜粋 238 行
node serifu-extract.js --check … serifu.txt・serifu-adv.txt とも「本体と一致」
node check-all --fast          … PASS check（①〜㉑）／PASS adv-check（①即死罠・②時間切れ）／両方PASS
```

**公開まで** — commit `0a04752` → push（`origin/main` = `0a04752`）。

**公開側（raw）の実読み**

```
$ curl -s .../main/ver.txt?_chk=...
1429

$ curl -s .../main/koushu-handan.html?_chk=... | grep 'data-ver=\|verTag'
2:<html lang="ja" data-ver="1429">
1316:  <div id="verTag">v1429</div>

$ 三件の実読み
2873:    const hasValue = doraValue>=1;
3031:    const totalBlocks = goodShapes + weakShapes;
8400:    // 撤退予約線：初期シャンテンから「一段進むのに要する巡目」を見積もり、期限を先に決めておく
8485:      title: a.valueSources.length ? `打点 ${a.doraValue} — ${a.valueSources.join('・')}` : '打点の種なし',

$ diff 公開物 手元
公開物と手元は完全一致
```

**実機で見るところ** — 判定を押して、

- **根拠欄①**：ドラ表示牌を指定し、そのドラが**一枚だけ・同色±2に相棒なし**の手にすると、
  題は「**打点 0.5 — ドラ1（孤立）**」のまま、**左の印が ✓ ではなく ×** になっていること
  （赤五も役牌対子も無い手であること）
- **投票欄の兎**：七対子に寄った手（対子が4〜5組で、面子手の分解では6ブロック取れる形）で、
  兎が**攻めの箱から消えて黙る**こと
- **版の字**：左上が **v1429**

---

## 5. 残り

**残り0件。**

| 番号 | 項目 | 状態 |
|---|---|---|
| 副産物-1 | 撤退予約線のコメントと実装の食い違い | 済（v1429・実装を正とした） |
| 副産物-2 | `totalBlocks` の二通りの数え方 | 済（v1429・上書き後へ寄せた。兎の票6件＝宣言と一致） |
| 副産物-3 | 執事と根拠欄①の閾値の食い違い | 済（v1429・執事側 `>=1` へ揃えた） |

`orders-open.tsv` に「済／取消／未了」以外で残っているのは次の三行だけで、**いずれも作業ではない**。

- `凍結-0`（**発効中**）… 見張り一式の凍結。状態であって作業ではない
- `再起動-1` / `再起動-2`（**断線-5 へ統合・消していない**）… 統合先の `断線-5` は済

**未着手・未了は0件。**

---

## 6. 手つかずのまま残していること（依頼どおり）

- **`fillRemain` を軸に使う設計** — 依頼者側で検討中のため**当てていない**。
  実測は前回の報告のまま（向聴2で15.2pt・向聴3で9.5pt、単調）。
- **兎の50000回の測り直し** — ②で攻め率が 1.40%→1.25% に動くため、注記の数字は**寄せる前のもの**。
  本体には断りを四行入れてあるが、測り直しそのものは別件。

---

＊この控えは `43e41c0` の時点の `report-latest.md` から起こした（印 `y0829-fukusan`）。
　機械が書く「送った知らせ」の節は控えに残していない。
