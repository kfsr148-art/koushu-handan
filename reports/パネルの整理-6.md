# パネルの整理-6 — 釦の字に「残N／全M件」を出した（panel v95）

**VAIO 2026-08-30 13:14 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 直した所（`panel.html` L843-855）

```js
var n = buildBundle().count;
/* 全数 M ＝ 控えに載っている札の数（未写し＋写した印の付いた札）。v95。
   ＊N だけだと「あと何件あるのか」は分かっても「全体でどれだけ溜まっているか」が
     見えない。押して写るのは今までどおり N だけで、M は**目安として添えるだけ**。
   ＊M は allRows の数をそのまま使う。数え直しはしない（同じ数を二箇所で作らないため）。 */
var m = (allRows || []).length;
if (n > 0) {
  b.textContent = 'まとめて写す（残' + n + '／全' + m + '件）';
  …
}
```

| | 何を数えるか | どこから |
|---|---|---|
| **残 N** | **未写しの札**（写した印の付いていないもの） | `buildBundle().count` |
| **全 M** | **控えに載っている札の全数**（未写し＋写した印の付いた札） | `allRows.length` |

**変えていないもの。**

- **押して写る中身は未写しの N だけ**（`buildBundle()` の `todo` は v89 のまま）
- **未写し0件のときの動き**（畳む）
- **通知の「写せます（N件）」の N**（`watch-notify.ps1` は触っていない）

## 実測（作法14「写しに probe」）

`panel.html` の写しを一時の場に作り、IIFE の内側へ一片を差し込んで headless の Edge を
`--dump-dom` で回した。**本体には何も入れていない**（写しごと消した）。

| | 状態 | 未写し | 控えの全数 | **釦の字** | 押せるか | 描かれるか |
|---|---|---|---|---|---|---|
| 甲 | 作業中・写した印なし | 20 | 20 | **`まとめて写す（残20／全20件）`** | 押せる | 出ている |
| 丙 | 手待ち・写した印なし | 20 | 20 | **`まとめて写す（残20／全20件）`** | 押せる | 出ている |
| **戊** | **未写し2件・写した印18件** | **2** | **20** | **`まとめて写す（残2／全20件）`** | 押せる | 出ている |
| 丁 | 手待ち・全部に写した印 | 0 | 20 | `まとめて写す（0件）` | 押せない | **畳んでいる** |
| 乙 | 作業中・未写し0件 | 0 | 20 | `まとめて写す（0件）` | 押せない | **畳んでいる** |

**戊が眼目** ── 18件に写した印が付いていても、**残は2件**。全体で20件溜まっていることも同時に見える。
押して写るのは**残の2件だけ**（`count 2`）。

個別の「写す」は **36個中0個が押せない**＝全部押せる。

### 未写し0件のときについて（一点、確かめ）

ご指示は「**作業中なら予定表示、手待ちなら畳む**」でしたが、**いまは作業中でも釦は畳まれます**。
これは **パネルの整理-3（v92）で「予定表示の釦を削除し、下の黄色の一行だけ残す」**と
決めたためで、**予定表示そのものは黄色の行が受け持っています**。

```
乙（作業中・未写し0件）
  釦          : 畳んでいる（釦なし）
  板の跡地    : why stline ｜ 色 rgb(232, 193, 74) ｜ 作業中、HH時MM分終了予定
```

**「今のまま」を、v92 で決めた形のまま据え置いた**という意味で受け取っています。
釦の側に予定表示を戻すご意向なら、そのように直します。

## 版と検査

| 何 | 値 |
|---|---|
| `PANEL_VER` | **`'95'`**（L374） |
| `verTag` | **`panel v95（8月30日）`**（L352） |
| `panel-ver.txt` | **`95`** |
| 構文 | `<script>` を切り出して `node --check` → **OK** |

公開側の実読みは下の「公開の確かめ」に。

---

## 実機で見るところ

1. 釦の字が **`まとめて写す（残N／全M件）`**
2. **写したあとに残が減り、全は変わらない**（例：残20／全20 → 押す → 新しい札が2件来て 残2／全20）
3. 押して写るのは**残の分だけ**
4. **版の字が `panel v95`**

## 残り

`orders-open.tsv` の未了 ── **残り5件**（穴の直し-1 ②③／通知の宛先-1 ①②③）。
