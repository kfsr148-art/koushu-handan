# 錠の名の裁定と、土台の直し-1 の実装（印 y0910-1535）

**終わり（残り2件）** — 三・五（土台の直し-1）は済み。**一と二は、この窓からは手が届かない。**

---

## 0. 先に言うべきこと — この窓は VAIO ではない

**いま走っているのは、クラウドの器（Linux）です。**`~/.claude` はここにありません。

| 探した所 | 結果 |
|---|---|
| `~/.claude`（＝`/root/.claude`） | クラウド側の設定だけ。`git-push.ps1`・`push-mine.ps1`・`heavy-push.ps1` は**無い** |
| `hook.log` ／ `watch-notify.log` ／ `work-note.txt` ／ `yoshi-open.tsv` ／ `orders-open.tsv` | **一つも無い** |
| `koushu-handan` リポジトリの中 | `release-stable.ps1` だけ。`git-push.ps1` は履歴にも**無い**（`git log --all -- '*git-push*'` が0件） |
| `kfsr148-art/claude-home-backup`（`~/.claude` の控え） | **繋ぐのを自動モードの関門に二度断られた**（`add_repo` が read でも push でも拒否） |

**だから一（錠の名の直し）と二（ログの照合）は、この窓では実行できません。**
できないものを「やった」とは書けないので、代わりに**手順と、ここで測れた分**を置きます。

---

## 一、錠の名 — 直せていない。直し方は一行

**裁定＝直す**は承りました。**実施できていません。**

```powershell
# 写しを取る
Copy-Item ~/.claude/git-push.ps1 ~/.claude/git-push.ps1.bak-20260910b

# 150行目の一字（\ が一枚落ちている）
#   いま  : 'LocalClaudeGitPushGate'
#   直す  : 'Local\ClaudeGitPushGate'

# 三つの台本で字面が揃ったことの実測
Select-String -Path ~/.claude/git-push.ps1, ~/.claude/push-mine.ps1, ~/.claude/heavy-push.ps1 `
  -Pattern 'ClaudeGitPushGate' | ForEach-Object { $_.Filename + ':' + $_.LineNumber + '  ' + $_.Line.Trim() }
```

＊`git-push.ps1:150` ／ `push-mine.ps1:24` ／ `heavy-push.ps1:52` の三行が
　**`Local\ClaudeGitPushGate` で揃う**ところまでが実測。
＊この直しは作法34（`\` が食われる）の型なので、**直した後に必ず字面を目で読み返す**こと。

**こちらで直すなら**、`kfsr148-art/claude-home-backup` を繋ぐ許しをください
（`add_repo` が通れば、写し・一字・実測まで一度でやれます）。
ただし**控えのリポジトリを直しても VAIO の実体は変わりません**。実体は VAIO 側で当てる要があります。

---

## 二、20:01:35 の壊れと錠の食い違いの関係 — **一行**

> **`hook.log` と `watch-notify.log` はこの窓から読めないので「二本以上あるか」は答えられない。**
> 公開の記録（commit）で見えるかぎり、**20:01:35 の前後一分に成功した押しは `state` の三本だけで、書き手は一種類**。

以下は、**ログの代わりに測れた分**です（`origin/main` を 3051 commit まで深掘りして数えた）。

| 何を | 実測 |
|---|---|
| 20:01:35 の前後一分に**成功した** commit | **3本**（20:00:40 `46fa59d` ／ 20:01:03 `584e72f` ／ 20:01:35 `1712602`）。**書き手は `state` 一種類だけ** |
| 20:01:35 の次に成功した commit | **09-10 00:57:12** `fd68bc8`（手押し）。空白 **296分** |
| 全履歴で、隣り合う commit が**2秒以内** | **63組** |
| その63組のうち、**書き手が違う**もの | **63組（全部）**。同じ書き手どうしが2秒以内に並んだ例は**0組** |

**読み方**（断定はしない）。
成功した押しだけを見ると、20:01 の一分間に居たのは `state` 一人で、**衝突の証拠は出ていません**。
ただし**ログが記録するのは「叩いた」であって「成功した」ではない**ので、
落ちた側はこの数字に出てきません。**電源の線を消す材料にはなりません。**

一方で、**63組が全部「書き手違い」**という並びは、**別々の書き手が2秒以内に同じ作業樹へ入っている**
ことを示します。錠が効いていれば起きにくい形で、**錠の名の食い違いと矛盾しません**。
`.git` の壊れ方（`index file corrupt` ／ `refs/heads/main cannot be resolved`）も、
**同時に二本の git が同じ index を触った**ときの典型です。

＊**確かめ方**は変わりません。VAIO で `hook.log` と `watch-notify.log` の 20:00:35〜20:02:35 を切り出し、
　押しを叩いた行が二本以上あるか、あるなら**どの台本とどの台本か**を見る。
　`\d` は使わず `[0-9]` で組むこと（作法34）。

---

## 三、「公開の止まり-1 で入れた二つの直し」の名 — 一行ずつ

**お尋ねの二つ（pub-read の UTF-8 固定／訴えに本文を載せない）とは別の物です。**
その二つは **y0910-1305-3 の二**で入れたもので、公開の止まり-1 の分ではありません。

公開の止まり-1（印 y0910-1330）で入ったのは、次の二つです。

| # | ファイル | 一行 |
|---|---|---|
| **①** | `git-push.ps1` | **道の落とし** — 追跡もされず実体も無い道は `git add` に渡す前に落とす（一覧は `ls-files` で一度だけ引く）。併せて、終了コード1でも `origin/main..HEAD` が残っていれば押す |
| **②** | `pipe-check.ps1` | **配列** — `ConvertFrom-Json` の戻りを一度変数へ受けてから配る（③と、同じ穴の371行目の札めぐりも）。読めなかった回は `pub-read` で訴える |

**どちらも残す／取り消すの裁定は、まだこちらに来ていません。**
①は今回の錠の名と同じファイルなので、**錠を直すときに一緒に見るのが早い**です。

---

## 四、閉じる二件 — **こちらでは落とせない**

- 承認の足止め-1 ③（13:05）… ヨシ承りました。**仕事は済んでいます**（`look.js` で入り、`allow` 二項も入っている）
- 公開の止まり-1（13:12）… ヨシ承りました。**裁定＝直す**として受けました

ただし、**印を落とす先の `~/.claude/yoshi-open.tsv` がこの窓にありません。**
VAIO 側で二件を落としてください。こちらの帳面では「閉じた」として扱います。

---

## 五、未検収を順に

### （1）押しの黙り-1 — **VAIO 側で済んでおり、検収待ち**

理由は y0910-1330 の三で確定済み。`$STALE_PUB_MIN = 20` の条件は**元からあり、効いていなかっただけ**で、
`pipe-check.ps1` ③（配列）を直した時点で初めて働くようになりました。
**この窓から足せる新しい手はありません。**実機の巡回で `pub-late` が立つかを見るのが検収です。

### （2）土台の直し-1 — **実装した。v1438**

y0909-1921 のヨシに沿って、**宣言の八項目をそのまま**当てました。

| # | 何を | どうしたか |
|---|---|---|
| 1 | `shanten`（88行） | **`analyze()` の外へ持ち上げた。本文は一字も変えていない** |
| 2 | `analyze()` の中の呼び | **一字も変えていない**（名が同じなので外の一本を呼ぶ） |
| 3 | `pickCutTile` | `cutShantenMin` → `shanten` へ**三箇所**向け直した |
| 4 | `cutShanten`（88行） | **削った** |
| 5 | `cutShantenMin`（13行） | **削った** |
| 6 | `check.js` ㉒ | 「写しが本体と同じか」→「**切り候補の物差しが一本か**」へ置き換え |
| 7 | `check.js` に一節 | **㉓ 第一感が動いていないか（向聴と第一打）**を足した |
| 8 | 作法5 | 「変更禁止」→「**判定の核は、宣言とヨシを取ってから**」へ改めた |

**本体は 11376行 → 11276行（100行減）。**`analyze()` は 635行 → 546行。

#### 第一感が動いていないことの確かめ（三通り）

| 見たもの | 方法 | 実測 |
|---|---|---|
| **字面** | 空白を潰した `shanten` の指紋を、v1437 の `shanten`／`cutShanten` と突き合わせ | **3249字・sha256 `caee8a3d…` の三つが一致**（旧本体・旧写し・新本体） |
| **値（物差し）** | v1437 から `shanten`・`cutShanten`・`cutShantenMin` を切り出し、**219,550手**を流して突き合わせ | **不一致 0件**（`shanten`＝`cutShantenMin`＝`cutShanten`、`std` の呼びも0件） |
| **値（画面）** | 種を固定した**200手**を、v1437 と v1438 の**写しに probe** を差して判定させ、盤面から読み取り | **裁き（攻／守）0件違い ／ 見立て行 0件違い ／ 牌の印（`cut-mark` を含む全種）0件違い** |

＊200手の内訳は 攻39・守161。`cut-mark` は200手すべてに付いた。
＊`verdict-word`（判定の台詞）だけは 151件違ったが、**同じ版を二度回しても 162件違う**ので**振り直しの揺れ**。
　裁き・見立て・印は二度回しても 0件違い。
＊この 200手比べは**一度きりの移行の証し**。`check.js` に残したのは、上二つ（字面の指紋と、24手の向聴・第一打の表）です。
　24手の表は**v1437 の写しの側から取って**あるので、一致することが「動いていない」の証しになります。

#### 検査

| | 結果 |
|---|---|
| **フル版** `node check.js`（9画面×21視野＝**189通り**） | **PASS**。189/189 測れ、はみ出し・重なり・横溢れ **0件** |
| `node adv-check.js` | **PASS**（① 即死罠 ／ ② 時間切れ） |
| `node serifu-extract.js` | 再抽出済み。合計 **7593行** ／ **口調の混入 0件** ／ `--check` 一致 |
| **シム** | **除去済み**。probe は写し（一時ディレクトリ `koushu-cap-*`）にだけ差し、実行後に写しごと消した。**本体には一片も入れていない** |

#### 宣言に無い一件（言っておきます）

`SKILL.md` の作法10 も「変更禁止」と同じ字だったので、**併せて改めました**。
片方だけ直すと作法書が自分と食い違うためです。**要らなければ戻します。**

#### 宣言に無いので**入れなかった**一件

`window._doraCode`（作法15 の口）の始末＝**甲（`analyze()` の戻り値に `doraCode` を足す）は入れていません。**
宣言の「触る所」の八項目に無かったためです。`check.js` ㉒ の「ドラを外へ出す」もそのまま残してあります。
**やるなら次の回で。**

---

## 六、控えと札

**`work-note.txt` も `~/.claude` にあるので、この窓からは書けません。**
代わりに、この報告を `reports/y0910-1535.md` と `report-latest.md` に置き、
`reports/` ごと push しました（作法33）。札（ntfy）も VAIO の見張りが立てるものなので、
**こちらからは鳴らしていません**。

---

## 実機で見るところ

**版の字が `v1438`。**判定を出したあと、**牌の印がこれまでと一つも変わっていない**こと——
とくに**兎（切り候補）の `cut-mark` が、v1437 と同じ一枚に付く**こと。
人柄七人の金枠と、現場猫・枝豆・紳士の色枠も、v1437 と同じ牌に付きます。

---

## 実測（一項目一行）

- 本体 … 11376行 → **11276行**（−100）／`analyze()` 635行 → **546行**
- `shanten` の指紋 … **3249字 sha256 `caee8a3dfbb2c564…`**（旧本体・旧写し・新本体の三つで一致）
- 物差しの突き合わせ … **219,550手・不一致 0件**
- 画面の突き合わせ … **200手・裁き0件違い・見立て0件違い・印0件違い**（台詞のみ151件、これは振り直しの揺れ）
- フル版 … **189/189 測定・溢れ0件・PASS**／`adv-check` **PASS**
- serifu … **7593行・口調の混入0件・`--check` 一致**
- 20:01:35 前後一分の**成功した** commit … **3本・書き手は `state` 一種類**
- 20:01:35 の次の成功 commit … **09-10 00:57:12（空白 296分）**
- 隣り合う commit が2秒以内 … **63組・うち書き手違い 63組（同一書き手は0組）**
- `git-push.ps1` … **この窓に無い。直していない**

---

## 残り

`残り2件`

1. **錠の名の直し**（`git-push.ps1:150` → `Local\ClaudeGitPushGate`）… **未了：この窓から `~/.claude` に手が届かない。**
   VAIO で当てるか、`claude-home-backup` を繋ぐ許しをください
2. **20:01:35 前後のログ照合**（`hook.log` ／ `watch-notify.log` に押しの記録が二本以上あるか）…
   **未了：ログがこの窓に無い。**commit の記録で測れた分だけ二に出しました

＊別に、**公開の止まり-1 で入れた二つの直し（①道の落とし／②配列）を残すかの裁定**が、まだ来ていません。
