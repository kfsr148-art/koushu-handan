# パネルの整理-13 — 一番上の状態の札を消し、印を黄色の行へ移した（panel v102）

**VAIO 2026-08-30 14:36 ／ 本体（`koushu-handan.html`）には触っていない**

---

## 1. 札ごと表示をやめた

`panel.html` **L222-227**（CSS）。

```css
/* 一番上の状態の札は画面から消す（v102）。**器と塗る処理はそのまま**で、見せ方だけ止める。
   ＊中身は下で全部賄えている——状態と時間は黄色の行、終わった題は
     「直近に終わった仕事」の箱。**いま動いている枠の印**は黄色の行の頭へ移した。
   ＊消したので、画面の頭は黄色の行から始まる。 */
#state{display:none !important;}
```

**器（`#state`）と塗る処理（`paintState()`）はそのまま残してある。**
消したのは見せ方だけで、`lastEndAt` を決める道も、まとめて写しの中身も変わらない。

## 2. 印を黄色の行の頭へ

札を畳むと、**いま動いている枠の印が画面のどこにも出なくなる**。黄色の行の頭へ移した。

```
作業中（パネルの整理-13）、HH時MM分終了予定（開始HH:MM・経過N分・指示HH:MM）
```

### 直し

| 何 | 行 | 内容 |
|---|---|---|
| 印を控える | **L663** | `var lastMark = '';`（`pullState` が置き直す） |
| 置く所 | **L2232-2238** | 作業中の件名を割った `sp.mark` を `lastMark` へ |
| 頭に足す | **L697-703** | `var head = '作業中' + (lastMark ? '（' + lastMark + '）' : '');` |
| 頭を受ける | **L616-621** | `endPredText(head)` に引数を足した（渡さなければ今までどおり「作業中」） |

**印が取れない件名のとき**（割れない形）は `lastMark` が空になり、
**今までどおり「作業中、…」**のまま。字が欠けたり `（）` だけ残ったりしない。

## 3. 画面の頭は黄色の行から

**黄色の行の `min-height:79px`（v100）は今のまま。** 触っていない。

## 4. 実測（作法14「写しに probe」）

`panel.html` の写しを一時の場に作り、IIFE の内側へ一片を差し込んで headless の Edge を
`--dump-dom` で回した。**本体には何も入れていない**（写しごと消した）。

### 画面に出ている順（上から）

```
1. header
2. h1
3. stLine      ← **黄色の行から始まる**
4. btnCopyBoard
5. board
6. doneBox
7. shotsrow
8. facts
9. verTag
10. usageTag
11. usageWhen
```

**`state`（一番上の札）がこの並びに居ない。** v101 では 3番目に居た。

### 三通りの状態（`pullState` の中と同じ枝を同じ順で通した）

```
■ 作業中（件名は説明つき）
   札      : **出ていない（畳んだ）**
   黄色   : 作業中（パネルの整理-8）、14時33分終了予定（開始13:40・経過7分・指示13:40）

■ 作業中（件名は印だけ）
   札      : **出ていない（畳んだ）**
   黄色   : 作業中（パネルの整理-9）、14時33分終了予定（開始13:40・経過7分・指示13:40）

■ 手待ち
   札      : **出ていない（畳んだ）**
   黄色   : 次の指示待ち

■ ヨシ待ち
   札      : **出ていない（畳んだ）**
   黄色   : ヨシを返してください
```

- **札は三通りとも出ていない**（中身は持ったまま）
- **作業中の黄色の行の頭に印が入っている**（`作業中（パネルの整理-8）、…`）
- **手待ち・ヨシ待ちの字は v101 のまま**

## 版と検査

| 何 | 値 |
|---|---|
| `PANEL_VER` | **`'102'`**（L426） |
| `verTag` | **`panel v102（8月30日）`**（L404） |
| `panel-ver.txt` | **`102`** |
| 構文 | `<script>` を切り出して `node --check` → **OK** |

公開側の実読みは下の「公開の確かめ」に。

---

## 実機で見るところ

1. **一番上の大きい札が無い**。画面が**黄色の行から始まる**
2. 作業中のとき、黄色の行が **`作業中（いまの件の印）、HH時MM分終了予定（開始…・経過…・指示…）`**
3. 手待ち・ヨシ待ちの字は今までどおり
4. **版の字が `panel v102`**

## 残り

`orders-open.tsv` の未了 ── **残り6件**（穴の直し-1 ②③／通知の宛先-1 ①②③／
通知の急ぎ-2 ②（測り中））。
