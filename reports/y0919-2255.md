# 連携の総点検（壊さず、作り値と実読みだけで）

**終わり（残り0件）** — 2026-09-19 23:1x（VAIO）。`koushu-handan.html`・`stable` には触っていない。
本物の窓・本物の溜め場・本物の ntfy・本物の push には一度も当てていない（作り値はすべて一時の所）。

## 1. 予定表 Claude* の全件（23:00 の実読み）

| 名 | 引き金 | 最終実行 | 結果 | 次回 | 上限 |
|---|---|---|---|---|---|
| ClaudeBoard | 時刻 08-27 00:00 から10分ごと | 09-19 23:00:00 | 0x0 | 23:10:10 | 5分 |
| ClaudeCodeAtLogon | ログオン時 | 09-18 22:32:32 | 0x0 | — | なし |
| ClaudeDailyNotice | 毎日 09:00 と 21:00 | 09-19 21:00:00 | 0x0 | 09-20 09:00 | 5分 |
| **ClaudeEdgeSweep** | 毎日 03:00 | **一度も走っていない** | **0x41303** | 09-20 03:00 | 10分 |
| ClaudeHomeBackup | 毎日 03:30 | 09-19 03:30:30 | 0x0 | 09-20 03:30 | 10分 |
| ClaudeHookHeartbeat | 10分ごと | 09-19 22:58:58 | 0x0 | 23:08:08 | なし |
| ClaudeJamWatch | 1分ごと（:45） | 09-19 23:00:00 | 0x0 | 23:01:01 | 2分 |
| ClaudePipeCheck | 10分ごと | 09-19 23:00:00 | 0x0 | 23:10:10 | 3分 |
| ClaudeRevive | 1分ごと（:25） | 09-19 23:00:00 | 0x0 | 23:01:01 | 2分 |
| ClaudeSweepChecks | 20分ごと | 09-19 22:51:51 | 0x0 | 23:11:11 | 5分 |
| **ClaudeWatchNotify** | 1分ごと（:58） | 09-19 23:00:00 | **0x41301** | 23:01:01 | 10分 |
| **ClaudeWeeklyReboot** | 毎週土曜 01:00・10分ごとに見直し | **一度も走っていない** | **0x41303** | 09-26 01:00 | 10分 |

**0 以外の理由**
- **0x41303（EdgeSweep・WeeklyReboot）** … 「まだ一度も走っていない」。どちらも 09-19 に作った仕事で、最初の回がまだ来ていない（03:00・次の土曜）。不備ではない。
- **0x41301（WatchNotify）** … 「いま走っている」。読んだ瞬間に巡回の最中だった（状態 Running）。不備ではない。

## 2. 起こし直し（ClaudeRevive）の作り値 … **起こしの命令まで進む。19:41 の空振りも作り値で再現した**

偽の数え方（claude 0本）と「0本が3分続いた」印を一時の所に置き、**直す前（`.bak-20260919`）と今の台本を並べて**回した。
予定表の上限（本物は2分）を20秒に縮め、閉じ手を25秒かかる偽物にして 19:41 の重さを作った。本物の窓は立てていない（予定表への命令は偽物が受ける）。

| | 返った刻 | 記録 | 予定表への命令 |
|---|---|---|---|
| **直す前** | 20.6秒で**上限に殺された** | **何も残らない** | **無し（窓は立たない）** |
| **今** | **2.2秒で自分で返った** | `waking 起こしにかかる（0本が3分）` → `revive 起こし直した（今日1回目・0本が3分続いた）` | `/End /TN ClaudeCodeAtLogon` → `/Run /TN ClaudeCodeAtLogon` |

**起こしの命令が叩く字**（`ClaudeCodeAtLogon` の中身・実読み）
```
C:\Windows\System32\cmd.exe /c start "麻雀 攻守判断 (Claude Code)" /MAX /D "C:\Users\user\Desktop\mahjong\koushu-handan" cmd.exe /k C:\Users\user\.local\bin\claude.exe --continue --remote-control koushu-handan
```

**「済」の裏付け** … 直す前の台本は、閉じ手の終わりを待ち、`Get-ScheduledTask` を読み込んでいる間に上限で殺され、**記録も命令も残さない**——
19:41:25 と 19:43:25 の回（事象329・revive.log に何も無い・ClaudeCodeAtLogon の最終実行が 09-18 のまま）と同じ形になった。
今の台本は閉じ手を切り離し、`schtasks.exe` で叩くので、同じ重さでも2秒で命令まで届く。

## 3. 札の道を端から端まで … **通った。stale の型も当てた**

本物の `watch-notify.ps1` の頭（L1〜324）と知らせの道（L686〜1536：溜め場・まとめ出し・控え・公開の確かめ・押し送り）をそのまま写し、
道だけ一時の所へ向けた。`curl.exe` の3箇所は偽物に替え、`notify-record.ps1` は写しの中で commit／push を偽物に替えた（本物の道の残り0箇所を数えて確かめた）。

**ふつうの回**

| 段 | 記録（偽の root の watch-notify.log） |
|---|---|
| 溜め場へ入れる | `知らせを溜めた [🔔 作り値の札（総点検）]（60秒待つ）` |
| 巡回が拾う | `溜めた知らせ 1本をまとめて出す（最後の一本から121秒（待ち60秒））` |
| **notices.json へ書き出す** | 偽の repo の `notices.json` に `{"title":"🔔 作り値の札（総点検）","time":1789826745,"message":"総点検の作り値です。\n本物へは送りません。"}`／偽の commit+push：`status.md,notices.json,notices-1789826746.json,latest-name.txt` |
| 公開側の確かめ | 偽の curl で `notices.json?_chk=…` を読み `公開側に出たのを確かめた（0秒）。押し送る` |
| **ntfy へ送る直前** | 偽の curl へ `https://ntfy.sh/fake-topic-for-test`・題 `=?UTF-8?B?8J+YvCDjg6jjgrfpoLzjgoDjgavjgoM=?=`（＝😼 ヨシ頼むにゃ）・Priority 4・Click パネル → `押し送りOK（HTTP 200）` |
| 控え | `控えへ入れた [🔔 作り値の札（総点検）]` |

**stale の型（公開側が古い）** … 偽の curl が古い `notices.json` だけを返す形で回した。
`公開側にまだ出ない（30秒）。次の回へ預ける` → `ntfy-pending.txt` に「期限・wait・その件の刻」の一行。**ntfy へは一度も呼ばれない**（curl の呼び出しは notices.json の二回だけ）。

## 4. 本物の hook.log で効いていること（今日の記録から）

- **命令の見張り** … hook.log に 21:56:45 から `PreToolUse`／`PostToolUse` が入り、見張りの足跡に毎分 `命令の見張り +1.7秒`（23:05:03）・`+2.7秒`（23:06:04）が出ている。今日の Bash 31組の所要は中央値2秒・最長29秒で、上限は下限の10分。札はまだ一度も立っていない（`cmd-watch.log` 無し）。
- **検査の上限** … 手元の速い版の実走で三度効いた：20:31 `⑦ 狭い画面での溢れ・段の経過 190.8秒（段の上限 190秒）`／20:56 `⑯・30.6秒（上限30秒）`／21:03 `adv-check・30.5秒（上限30秒）`。Edge の段を飛ばした後の 21:10 は `所要 4.6秒（上限 327秒）`。

## 5. 見つかった不備

**命令の見張りに二つ（土台ではない。直して作り値を添えた）**
- **終わりの行の欠け** … 今日 49本の始まりのうち **16本に終わりの行が無かった**（落ちた命令は PostToolUse が出ない／重い日に async の hook が15秒の上限で切られる）。
  このままだと、長い一区切りの途中で**終わった命令に「長く回っています」の札が立つ**。
  → `cmd-watch.ps1`：**始まりから30秒より後に別の命令が始まっていれば終わった扱い**（claude は答えが返ってから次を出すため）。
  → `settings.json`：PreToolUse／PostToolUse の上限を **15→60秒**。直した後（23:08〜）は始まり3・終わり2で、欠けは走っている一本だけ。
- **命令の日本語が化ける** … `tool-mark.ps1` が受け取りを CP932 で読んでいた（`grep -n "譛ｬ菴…"`）。→ **UTF-8 で読む**一行。実読みで `grep 本体v1451 state.json` と正しく出た。

**作り値（cmd-watch・五通り合格）** … イ 超えた→1枚／ロ 超えない→0枚／ハ 同じ命令三回→1枚のまま／**ニ 終わりの行が欠け、1分後に次が始まった（15分）→0枚**／**ホ 同じ束（5秒後）だけ→1枚**。

**土台に触るので直さない（案）**
- **stale の回に iPhone が鳴らない** … 🪟 は `$T_BAD_B` の道で**名乗りなし**のまま出るので「押し送りはしない（送る状態でない: 名乗りなし）」で断られる（20:38:11 の実記録）。
  案：stale の回は、先に出る 🪟 の呼び出しへ名乗り `bad` を渡す（本編は空なので、いまの入口の断りでそのまま落ちる）。

## 6. 三つの表

### 通った道

| 道 | 確かめ方 | 結果 |
|---|---|---|
| 予定表 Claude* 12件 | 実読み | 0 以外の3件は「未実行」「走行中」で不備なし |
| 起こし直し（0本→起こしの命令） | 作り値（新旧並べ・上限を縮めて） | 今は2.2秒で `/Run /TN ClaudeCodeAtLogon`。直す前は殺されて何も残らない＝19:41 の再現 |
| 札の道（溜め場→巡回→notices.json→公開の確かめ→ntfy の直前） | 作り値（本物の台本の写し・偽の curl と git） | 通った（HTTP 200 は偽） |
| stale の型（公開側が古い） | 作り値 | 30秒粘って預け、ntfy は呼ばない |
| 命令の見張り | 本物の hook.log と足跡 | 毎分回っている。Pre/Post が入っている |
| 検査の上限 | 今日の実走4回 | ⑦・⑯・adv-check で切れ、Edge を外した後は4.6秒 |

### 直した所

| 所 | 何を | 写し | 確かめ |
|---|---|---|---|
| `~/.claude/cmd-watch.ps1` | 30秒より後に次の命令が始まっていれば終わった扱い | `.bak-20260919` | 構文 OK・作り値五通り |
| `~/.claude/tool-mark.ps1` | 受け取りを UTF-8 で読む | `.bak-20260919` | 構文 OK・実読みで日本語が正しく出た |
| `~/.claude/settings.json` | PreToolUse／PostToolUse の上限 15→60秒 | `.bak-20260919b` | 直した後の欠けは走っている一本だけ |

### 残った穴

| 穴 | 訳 | 案 |
|---|---|---|
| stale の回に iPhone が鳴らない | 🪟 が名乗りなしで出るので押し送りの関門で断られる（土台） | 🪟 の呼び出しへ名乗り `bad` を渡す |
| 題だけ空の札は、溜めても出す側で「読めない」と居座る | 入口は片方あれば入れ、出す側は両方要る（土台） | 出す側の判じを入口と揃える。いまこの札を作る道は無い |
| 落ちた命令の終わりの行 | PostToolUse が出ない（PostToolUseFailure は今回足していない。名がこの版で効くか確かめていないため） | 足すなら settings の hook を一つ。いまは cmd-watch の「次が始まったら終わり」で空振りを止めている |
| EdgeSweep・WeeklyReboot は一度も走っていない | 最初の回が 09-20 03:00・09-26 01:00 | 最初の回の後に結果を読む |

## 作法36 の突き合わせ（見張りの台本を変えたので）

見張りの段の並びは前の札（y0919-2149）と同じ（生存の合図 → **命令の見張り** → 譲りの判じ → …）。`cmd-watch.ps1` の中身だけを変えた。雲で回る見張りは無い。
検査の段（手元の速い版 17段・雲の速い版と フル版は全部）はこの枠で変えていない。
