# タスクスケジューラの動作記録が有効になりました

**状態：終わり** — 2026-08-25 07:55 時点

## 三通りとも一致しています

| 読み方 | 値 |
|---|---|
| `wevtutil gl` | `enabled: true` |
| レジストリ `WINEVT\Channels\…\Enabled` | `1` |
| `Get-WinEvent -ListLog` | `IsEnabled: True` |

前回のように「片方だけ true」という食い違いはありません。

## 中身が使えることまで確認しました

直近12分で **8件**。`ClaudeWatchNotify` の始まりと終わりが対で残っています。

```
07:50:58  タスクを始めた    \ClaudeWatchNotify
07:50:58  処理を起こした    \ClaudeWatchNotify
07:50:58  動作を始めた      \ClaudeWatchNotify
07:51:07  動作が終わった    \ClaudeWatchNotify
07:51:07  タスクが終わった  \ClaudeWatchNotify
```

**201（動作が終わった）の中身**

```
タスク スケジューラは、タスク "\ClaudeWatchNotify"、インスタンス "{c4a8fbbd-…}"、
操作 "powershell.exe" を正常に完了しました。リターン コード: 0
```

**終了コードが読めます。** 次に穴が空いたら、

- **上限で殺された** — 終わりの記録が無い／リターン コードが 0 以外
- **そもそも起動しなかった** — 始まりの記録が無い

が、記録だけで分かれます。

## 次の穴での読み方（二本立て）

| | 何が分かるか |
|---|---|
| **① 足跡の道筋** | どの段に何秒居たか（`watch-notify.log` の「前の回は途中で終わっていた」の二行をそのまま） |
| **② 動作記録** | そもそも起動したか・どう終わったか（id 100 始まり／102 終わり／200 動作の始まり／**201 動作の終わり＝リターン コード**） |

片方だけでは足りないので、次に穴が空いたら**両方を並べて**読みます。

## この一連で入れたもの

| | 中身 |
|---|---|
| 実行の上限 | `ClaudeWatchNotify` `ClaudeHookHeartbeat` とも 3分/2分 → **10分** |
| `git push` の見切り | `Take-Usage` の push を **20秒**で切る（終了コードの読み方も直済み） |
| 足跡 | 一回ぶんの道筋（時刻＋段の名前＋前の段からの秒数） |
| 4100 警告 | `Get-Content \| Select-Object -First 1` → `-TotalCount 1`（**3分で0件**、直す前は約11件） |
| `usage.json` | `watch`（at／claude／windows／state）を追加 |
| 動作記録 | **有効化済み**（起動と終了コードが残る） |

## いまの版

本体 **v1426** ／ stable **`f3837e8`** ／ panel **v29**。本体もパネルも触っていません。
