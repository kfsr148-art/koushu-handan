# 今日入れた仕掛けの噛み合わせ — 先回りで洗った

**状態：終わり** — 2026-08-26 06:1x 時点

**結論** — **四つのうち三つで、まだ起きていない不具合が出ました。直しました。**
①開いている印が**黙って捨てられる**／②**見ていない札を巻き込む窓**／④**押し直しが一度も成功していない**。
③は不具合ではなく、**線引きに迷う例が四つ**あります（挙げるだけにしてあります）。

---

# 分かったこと

## ① `yoshi-open.tsv` — 黙って捨てていた。年限と記録を入れた

**落ちるのは「ヨシが合ったとき」だけ**でした。答えの出ない印は**永久に残り**、
50件を超えると**古い方から黙って捨てられて**いました。

**いちばん危ういのはここです。** 黙って捨てた印に後から宛名を付けると、
**「その印の待ちは既に済んでいます」と嘘をつきます**（済んでいないのに）。

**直したこと**

- **年限3日**（`$OPEN_KEEP_DAYS`）。過ぎた行は落とし、**落とした印の名を記録に残す**
- **数の上限50**（`$OPEN_KEEP_MAX`）も、**切るときに名を挙げる**

**実測（三通り）**

```
① 年限   残り: yOLD-1・yNEW    記録: 年限切れを落とした（3日）：yOLD-5
② 上限   残り: b・c・d・e・yNEW2  記録: 5件を超えたので古い方を落とした：a
③ 重複   残り: yDUP            （同じ印は二度積まない）
```

**裸の「ヨシ」がいつまでも古い件を指す**心配は**ありません**——裸のヨシは
**いちばん新しい一件**を落とす作りです。古い行が残っても指し先はずれません。

## ② `seen.json` — 時計はずれない。取り置きは安全な向き。危ういのは「窓」

**時計のずれはありません。** `notices.json` の `time` を書くのも、`seen-set.js` が読む時計も、
**同じ VAIO の時計**です。GitHub Pages は時刻を打ちません。

**取り置き（キャッシュ）は安全な向きにずれます。** 公開へ回るまで**約5分**（04:36→04:41 で実測）。
その間パネルは**古い `seen`** を読むので、**札が消えるのが遅れるだけ**で、消えすぎることはありません。

**残る危うさは「見ていない札を巻き込む窓」です。** 時刻で切るので、
**直前に届いたばかりの札**まで読んだことにしてしまいます。
実測では**札の間隔は3分49秒〜28分**なので、窓は数分ぶん開きます。

**直したこと** — `seen-set.js` が、**この操作で新たに片付く札を名前を挙げて出す**ようにしました。
黙って消しません。取り違えたら `--clear` で戻せます。

＊**パネル側でも札は消えません。**「写した札」の箱へ、青い**「チャットで読んだ」**の印つきで入ります。
　押して写した札（緑の「届いた」）と**印を書き分けて**あるので、後から見分けられます。

## ④ 押し出し — 衝突は増えていない。だが**押し直しが一度も成功していなかった**

**衝突（remote rejected）は今日ゼロ件です。** 今日の commit は **85件**
（`state.json` 38／`usage` 20／`status.md` 18／作業 9）で、押し出しの失敗の記録は**1件だけ**でした。

**その1件が問題でした。** 記録に残っていた失敗は、いずれも**押し直しが空振り**しています。

```
2026-08-25 22:09:54  notices  時間切れで切った（20秒）: git push
2026-08-25 22:09:56  notices  終了コード 128：error: cannot pull with rebase: You have unstaged changes.
2026-08-25 22:16:45  usage    （同じ形）
```

**原因** — `git pull --rebase` は、**手元に commit していない変更が一つでもあると 128 で止まります**。
`report-latest.md` はほぼ常に触っているので、**押し直しは実質いつも失敗**していました。

**実地で確かめました**（いま `report-latest.md` と `usage.json` が未 commit の状態で）

```
--autostash なし  終了コード 128 : cannot pull with rebase: You have unstaged changes.
--autostash あり  終了コード 0  : 通った
そのあと手元の変更 : M report-latest.md / M usage.json  ← 退避が戻っている
```

**直したこと** — `pull --rebase` に **`--autostash`** を付けました。退避と戻しは git がやるので、
**作業中の変更には触れません**。

**失われた中身はありません。** 押す口は三つとも**同じ手元のリポジトリ**を使うので、
落ちた回の commit は次の押し出しが一緒に運びます。**手元 HEAD と `origin/main` の差は0件**で、
22:09／22:16 の前後の commit もリモートに揃っています。

---

# ③ 作法29 の線引き — 実例と、**迷う例四つ**

**止まると判じた例（今日の実際）**

| 例 | どれに当たるか |
|---|---|
| 本体に遷移口を掘る工事 | **②本体の工事** — 宣言してヨシを待った |
| 本体に掘るか、写しに掘るか | **①設計の分かれ道** — 実際にここで分かれた |
| `stable` を進める | ③外へ出す物 |
| `shots/` を消す | ④取り返しのつかない削除 |

**止まらないと判じた例（今日の実際）**

| 例 | 理由 |
|---|---|
| パネル v34（`seen.json`） | 指示済みの実施 |
| 作法27 の書き直し | 指示済みの実施 |
| `git-push.ps1` に `--autostash` | 内部の直し。外へ出る物ではない |
| `yoshi-open.tsv` の年限 | 同上 |

## 迷う例（判じきれていないもの）

1. **返事パネルは「外へ出す物」か。** `panel.html` は公開ページですが、**読むのはご本人だけ**です。
   今日は「遊ぶ人向けではない道具」と見て止まりませんでした。
   **作法29 の文面は `koushu-handan.html` とパネルを分けていません。**
2. **`CLAUDE.md` の作法を自分から変えるのは①か。** 今日はいずれもご指示があったので進めました。
   **自発的に作法を変える場面**では、①に当たると判じるべきだと思っています。
3. **`~/.claude` の見張り台本**は「外へ出す物」ではありませんが、壊すと**知らせが止まります**。
   不可逆ではないので止まらない、と判じています。ただし**今日 `watch-notify.ps1` を実際に壊した**ので、
   ここは危ういままです。
4. **git の管理外のファイルを消すこと**（一時ファイルなど）は④に当たるか。
   戻せないので④と判じていますが、毎回止まると写し取りの写しすら消せません。
   **いまは「その回で自分が作ったものは④に当たらない」**として運用しています。

**線引きを直す案は出していません**（挙げるところまでが今回のご依頼と受け取りました）。
1 と 3 は、作法29 に一行足せば消える迷いです。

---

# 分からなかったこと

- **年限3日が妥当かは分かりません。** 今日までの待ちは**その日のうちに答えが返っています**が、
  三日置く場面がまだ無いので、**実際に切れる場面を見ていません**。切れたときは記録に名が残ります。
- **`--autostash` の直りは、見張りが実際に衝突する場面ではまだ試せていません。**
  確かめたのは「未 commit があっても `pull --rebase` が通る」ところまでです
  （そこが空振りの原因だったので、直りとしては足りていると見ています）。
- **`seen.json` の「窓」に何件入るかは、その時々です。** 札の間隔は3分49秒〜28分と幅があり、
  一定の安全域は引けません。**名前を挙げて出す**ことで気づける形にしました。
- **押し出しの回数が増えた影響は、まだ測れていません。** `seen.json` とヨシの一覧を足したのは
  今日で、`ntfy-daily.tsv` も数え始めたばかりです。**数日ぶん溜まってから報告します。**

# 触ったもの

| ファイル | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1` | 開いている印の**年限3日**と**上限の記録**（`$OPEN_KEEP_DAYS` / `$OPEN_KEEP_MAX`） |
| `~/.claude/git-push.ps1` | `pull --rebase` に **`--autostash`** |
| `seen-set.js` | **この操作で新たに片付く札を名前を挙げて出す** |
| `koushu-handan.html` | **触っていません** |
