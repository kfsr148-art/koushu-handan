# 報告 — 公開ページの更新（2026-08-24 VAIO時計）

**状態：終わり（返事不要）。**

## いま何をしているか

**3件目（公開ページの更新）が終わりました。**次は4件目（縦中央揃え）に入ります。

結論を先に三行で。

- **`report-latest.md` の未commitは、もう残っていませんでした。**前の回に `7b68695` で commit・push 済みで、作業フォルダは `origin/main` と一致しています。
- **「github.io へ公開を上げる」という別の手順はありません。**GitHub Pages は `source=main:/` の設定で、`main` へ push した時点でそれが公開になります。**v1423 は `b14e513` の push で既に配られていました。**
- **検査は両方 PASS。**公開ページを実際に開いて、版の表示が本体と揃っていることを確かめました。

## 八項目

```
[12:14] 公開ページの更新（v1423）。既に配られていたので、確かめて閉じた
状態: 終わり
したこと: 検査を通し、公開ページを実際に開いて版が揃っていることを確かめた。変更は無し。
版: 公開ページ v1423 ／ 手元の本体 v1423 ／ stable v1420
ファイル:
- 変更なし（確認のみ）
- 公開の設定: source=main:/ ／ html_url=https://kfsr148-art.github.io/koushu-handan/
実測:
- node check.js --fast：21項目すべて PASS（12:09:05〜12:12:53、3分48秒）
- node adv-check.js：2項目とも PASS（12:13:03〜12:13:32、29秒）
- 公開ページを headless Edge（900x381）で開き、描画後の DOM から読んだ
    data-ver=1423 ／ 画面の verTag=v1423 ／ ver.txt=1423
- 入口・本体とも HTTP 200
食い違い: ご指示にあった「report-latest.md の未commit」は、既に閉じてありました。
          また「公開を上げる」手順は存在せず、push がそのまま公開になります。
答え: 公開ページは既に v1423 を配っていた。表示も本体と一致している。3件目は確認だけで閉じてよい。
未決: なし
そのまま: 【この回に新しく書いた本文】

■ 前提の訂正が二つ
- report-latest.md の未commitは、もう残っていなかった。前の回に 7b68695 で commit・push 済み
- 「github.io へ公開を上げる」という別の手順は無い。Pages は source=main:/ なので、
  main へ push した時点でそれが公開になる。v1423 は b14e513 の push で既に配られていた

■ 公開ページの状態
- status=built ／ source=main:/ ／ https_enforced=true ／ public=true
- 入口 https://kfsr148-art.github.io/koushu-handan/ は HTTP 200
- 本体 https://kfsr148-art.github.io/koushu-handan/koushu-handan.html も HTTP 200

■ 版の突き合わせ（⑥）
- 公開ページ : data-ver=1423 ／ 画面の verTag=v1423 ／ ver.txt=1423
- 手元の本体 : data-ver=1423 ／ verTag=v1423 ／ ver.txt=1423
- 三箇所とも公開側と手元で一致

■ 実際に開いて確かめた
- curl で属性だけ読むのではなく、headless Edge で描画させ、画面に出る verTag の字を読んだ
- 題は「配牌 攻守判定」、DOM は 9920KB

■ 検査の所要が戻った
- 前回 8分21秒かかった check.js が 3分48秒に戻った。機械の重さが原因だったと見てよい

■ シム
- 検査の仕掛けは一時ディレクトリの写しだけ。実行後に消した。本体にシムは無い
```

## 版の突き合わせ（⑥）

| | `data-ver` | 画面の `verTag` | `ver.txt` |
|---|---|---|---|
| 公開ページ（github.io） | **1423** | **v1423** | **1423** |
| 手元の本体 | **1423** | **v1423** | **1423** |

`curl` で属性だけ読むのではなく、headless Edge（900x381）で実際に描画させ、画面に出る `verTag` の字を読んでいます。

## 公開の仕組み（覚え書き）

```
$ gh api repos/kfsr148-art/koushu-handan/pages
{"status":"built","html_url":"https://kfsr148-art.github.io/koushu-handan/",
 "source":{"branch":"main","path":"/"},"public":true,"https_enforced":true}
```

`main` の直下をそのまま配る設定です。**別に「公開を上げる」操作はありません。**`main` へ push すれば、それが公開ページになります。逆に言うと、**検査を通していないものを push すると、そのまま利用者に届きます。**


# これまでの記録 — 見張りを止めました（2026-08-24 VAIO時計）

**状態：ヨシ待ち。**見張りを止めたままにするか、戻すかの裁定をお願いします。

## いま何をしているか

**見張り（`ClaudeWatchNotify`）を止めました。手は止めています。**

結論を先に三行で。

- **止めた時刻は 2026-08-24 09:26:10**（VAIOの時計）。予定表の `ClaudeWatchNotify` を `Disabled` にし、実体のプロセスが0本であることを確かめました。
- **止めた後、新しい行は増えていません。**毎分無条件に書かれる `watch-status.log` が 09:26:00 で止まり、09:27／09:28 の二回分が欠けています。知らせの行も push もゼロです。
- **繰り返しの原因は、控え（`work-note.txt`）を書き直さなかったこと**です。`watch-notify` は Stop のたびに新しい鍵を立てますが、本文はその控えから組み直すため、控えが古いままだと同じ本文が何度も書き出されます。**書き直さなかったのは私です。**

## 止めたことの確かめ（09:28:05 時点）

| 見るもの | 値 | 意味 |
|---|---|---|
| 予定表 `ClaudeWatchNotify` | `Disabled` | 止まっている |
| 実体のプロセス | 0本（09:26:39） | 走っていない |
| `watch-status.log` の最終行 | 09:26:00 | 毎分無条件に書く記録が止まった。09:27／09:28 が欠けている |
| `notify-sent.tsv` | 17行のまま | 知らせの行が増えていない |
| `notices.json` | 17行のまま／この題 1行（09:20:04） | 同上 |
| `status.md`・`notices.json` の最終更新 | 09:25:03 のまま | 書き直されていない |
| 知らせの push | 09:25:03 が最後 | 09:26 以降ゼロ |

途中で「PID 2388 が残っている」と出ましたが、**これは誤検出**でした。問い合わせに使った powershell 自身の命令文に `watch-notify` の字が入っており、自分自身を数えていました。自分を除いて数え直すと0本です。

## 六度の内訳

| 時刻 | 出どころ |
|---|---|
| 08:26:45 | 見張り（Stop 08:25:13） |
| 08:40:06 | 見張り（Stop 08:40:01） |
| 08:46:10 | 見張り（Stop 08:45:34） |
| 09:03:03 | 見張り（Stop 09:02台） |
| 09:14:04 | 見張り（Stop 09:13:40） |
| **09:20:06** | **私の手動実行。**前回のご指示（3）「この題で一度書き出して行が増えないことを示せ」に応じて `notify-record.ps1` を直接呼びました |

09:25:03 の push は**別件**です。新しい件名で1行増えており、⑱の行は 09:20:04 のまま動いていません。

## なぜ同じ件が繰り返し書き出されたのか（確定）

1. `watch-notify` は Stop が鳴るたびに `done:<Stopの時刻>` という**新しい鍵**を立てます。
2. 本文は `work-note.txt` から組み直します。控えが書き換わっていなければ、**本文は前回と同じ**になります。
3. 鍵は毎回違うので「新しい知らせ」として扱われ、`status.md` と `notices.json` が書き直され、**毎回 commit＋push** されます。
4. `notify-record` は同じ題の行を置き換えるので、**一覧は1行のまま**。積まれていたのは行ではなく書き出しの回数でした。

**控えを書き直さなかったのは私です。**止まるたびに `work-note.txt` を書き直す決まりでしたが、08:25 以降の五回、それをしていませんでした。

## 直しの効き

09:22 に入れた指紋（本文の MD5）は動いています。`watch-state.txt` に `bodyHash=9b3033159c3db0c5903165e7760d4845` が出ており、09:25 の書き出しでは新しい件名が1行足され、⑱の行は動きませんでした。

## 戻すとき

```
Enable-ScheduledTask -TaskName 'ClaudeWatchNotify'
```

止めたままだと、**異常（落ちた・生存が途切れた・働き手が落ちた）の検知も止まります。**戻すかどうかの裁定をお願いします。


# これまでの記録 — 同じ知らせが五回出た件（2026-08-24 VAIO時計）

**状態：終わり（返事不要）。**

## いま何をしているか

**手は止めています。**次の作業には入っていません。

結論を先に三行で。

- **`status.md` の行は増えていません。**五回の push すべてで「この題の行数=1」でした。同じ題の上書き（`Rank-Body`）は効いています。題に時刻も記号も混ざっていません。
- **増えていたのは書き出し＋push の回数**です。控え（`work-note.txt`）が前の回のまま Stop が五回鳴り、そのたび同じ本文が組み直されて `status.md` が書き直され、毎回 commit＋push されました。受け取る側にはそれが五件に見えます。
- **5分おきの出し直しは無関係でした。**あれは `wait:`（ヨシ待ち）の枝の中だけにあり、`done:`（終わり）には掛かりません。実測でも、その時間帯に出し直しの行は一本もありません。

## 八項目

```
[09:20] 同じ知らせが五回出た件——上書きは効いていた。原因は書き出しの回数
状態: 終わり
したこと: 五回の正体を特定し、watch-notify.ps1 に本文の指紋を入れて、終わりの知らせは前と同じ本文なら出さないようにした。
版: 本体 v1421（触っていない）／ panel v23 ／ stable v1420
ファイル:
- ~/.claude/watch-notify.ps1:392-410 指紋の関数（Body-Hash）と控えの変数（$prevBodyHash）
- ~/.claude/watch-notify.ps1:420 控えから bodyHash= を読む
- ~/.claude/watch-notify.ps1:560-569 送る前に指紋を比べ、同じ本文なら出さない
- ~/.claude/watch-notify.ps1:598-601 控えへ bodyHash= を書く（done: の回だけ書き換える）
- 控え: ~/.claude/watch-notify.ps1.bak-20260824
実測:
- 五回の push（08:26:45／08:40:06／08:46:10／09:03:03／09:14:04）それぞれの notices.json を開いて数えた。全部「この題の行数=1」
- 同じ題・同じ本文で notify-record.ps1 を一度呼んだ。直す前 notices.json 16行／この題 1行／notify-sent.tsv 16行 → 直した後 16行／1行／16行。行は増えず、時刻だけ 09:14:02→09:20:04 に置き換わった
- watch-notify.log にその時間帯の「ヨシ待ちを出し直した」は0件。直近の出し直しは 07:39 と 07:44 で、この件より前
- watch-state.txt に bodyHash= の行が出ている（新しい仕掛けが回っている証拠）
- 構文検査：ParseFile で「構文 OK」。BOM 付き UTF-8 のまま（efbbbf）
食い違い: ご指摘の「上書きが効いていない」は、実測では起きていませんでした。行は毎回1行のまま置き換わっています。積まれて見えたのは書き出しの回数です。
答え: 直す先は上書きの判定ではなく、書き出しの入口。終わりの知らせは、控えが書き換わっていなければ出さない。
未決: 指紋で止まることの実測（二度目が来ないこと）は、Stop が二回鳴らないと示せません。この回の終わりが一回目で、次に止まるときが二回目になります。そこで確かめて報告します。
そのまま: 【この回に Code タブへ書いた本文】

■ (1) status.md にこの題は何行あるか
- 知らせの一覧の中では 1行（17行目）。3行目は一覧ではなく状態の一行で、work-note.txt の件名を写したもの
- 五回の push それぞれの notices.json を開いて数えても、全部「この題の行数=1」だった

■ (2) なぜ上書きが効いていないように見えたか
- 上書き（Rank-Body）は効いていた。題に時刻も記号も混ざっておらず、別件とは見なされていない
- 積まれていたのは status.md の行ではなく、書き出し＋push の回数
- Stop が五回鳴り、そのたび watch-notify が新しい鍵 done:<時刻> を立てた
- 本文は work-note.txt から組み直すが、控えが 08:25 のまま書き換わっていなかったので、五回とも同一本文になった
- notify-record は行を置き換えるので一覧は1行のまま。しかし status.md と notices.json は毎回書き直され、毎回 commit＋push された

■ 直した中身（watch-notify.ps1 のみ。本体は触っていない）
- 本文の指紋（MD5）を控え（watch-state.txt）へ bodyHash= として残す
- done:（終わり／調べた）を送る直前に指紋を比べ、前と同じ本文なら出さない
- ヨシ待ちの出し直しには掛けない。あちらは同じ本文を出すのが目的
- 指紋は done: の回だけ書き換える。ほかの鍵の回に空で潰すと次の done: が素通りするため

■ (3) 行が増えないことの実測
- 直す前: notices.json 16行／この題 1行／notify-sent.tsv 16行
- 同じ題・同じ本文で notify-record.ps1 を一度呼ぶ
- 直した後: notices.json 16行／この題 1行／notify-sent.tsv 16行。時刻だけ 09:14:02→09:20:04

■ ⑧の持ち回りについて
- 持ち回っていた。work-note.txt の「そのまま:」は 08:25 の回に書いたきりで、以降の Stop 五回がすべてそれを再び出していた
- この回から、⑧はその回に新しく書いた本文だけにする
```

## 五回の正体

| 時刻 | commit | この題の行数 |
|---|---|---|
| 08:26:45 | 67e7519 | 1 |
| 08:40:06 | 302b626 | 1 |
| 08:46:10 | 638a5fb | 1 |
| 09:03:03 | 46e9a8a | 1 |
| 09:14:04 | f816bc3 | 1 |

行はどの回も1行です。増えていたのは、この表そのもの——書き出しの回数でした。

## 5分おきの出し直しが掛かっていないことの根拠

- **字面** — 出し直しは `watch-notify.ps1` の `elseif ($key -like 'wait:*')` の枝の中だけにあります。`done:` の鍵はその手前の `if ($key -ne $prev)` で処理され、出し直しの枝には入りません。
- **ログ** — 08:26／08:40／08:46 の三行はいずれも「鍵 … -> done:…」、つまり鍵が変わった行です。「ヨシ待ちを出し直した」の行はこの時間帯に一本もありません。
- **控え** — `watch-state.txt` は `remind=0` のままでした。


# これまでの記録 — ⑱の揺れを止めた（種で固定）と、ヨシ待ちにも⑧（2026-08-24 VAIO時計）

**状態：終わり（返事不要）。**

## いま何をしているか

**手は止めています。**次の作業には入っていません。

結論を先に三行で。

- **⑱は直しました。参考には落としていません。**揺れの元は三つとも `check.js` の測り方の側で、本体は関係ありませんでした。
- **`node check.js --fast` を5回続けて回して、⑱の節は5回とも一字一句同じ**です（md5 まで一致）。
- **ヨシ待ちの知らせにも⑧を付けました。**前回の宣言が手元に届かなかった件です。

## 八項目

```
[08:18] ⑱の揺れを止めた（種で固定）と、ヨシ待ちにも⑧
状態: 終わり
したこと: ⑱の揺れの元を三つ潰して測り方を固定した。5回続けて回して出力は5回とも同一。ヨシ待ちの知らせにも⑧を付けた。
版: 本体 v1421（触っていない）／ panel v23 ／ stable v1420
ファイル:
- check.js:1447-1454 種（SEED）と、射程・入力止めの読み取りを足した
- check.js:1470-1523 乱数の差し替え・叩く場所・打数で切る・開き直し
- check.js:1546-1548 「叩いた数」と「開き直し」の書き出し
- check.js:1557 仮想時計の予算 240秒→360秒
- ~/.claude/watch-notify.ps1:440-444 ヨシ待ちの本文に⑧を足す
- ~/.claude/watch-notify.ps1:558-565 5分おきの出し直しの一行は⑧より前へ
実測:
- node check.js --fast を5回。⑱の節は5回とも同一（md5 40bcaa192f7c37d01313f29de06538cc）
- 叩いた数は毎回 78 / 45 / 81、連戦の開き直し1回、喜び3回、規定の2倍を過ぎた喜び0
- 5回とも終了コード0（21項目すべて PASS）
- ヨシ待ちの本文を実物の控えで組んで18行・⑧あり。出し直しの本文でも⑧が最後で、案内の一行はその前
食い違い: なし
答え: 揺れていたのは本体ではなく測り方。⑱は直したので、合否の対象に残す（参考には落としていない）。
未決: CLAUDE.md の表は「速い版＝568x320 のひと視野」と書いてあるが、いまの check.js は 568x320 と 900x381（judged だけ 844x390）を回す。どちらに合わせるかは決めていない。
そのまま: 【この回に Code タブへ書いた本文】

■ ヨシ待ちの知らせにも⑧を付けた
- watch-notify.ps1 の ask の枝で、⑧より前の分を $askHead に控え、$body の末尾へ「そのまま: 」を足す
- 5分おきの出し直しの一行は⑧より前へ入れる。⑧は末尾まで全部を飲み込むため
- 実物の控えで確かめた：本文18行・⑧あり／出し直しの本文でも⑧が最後で、案内の一行はその前

■ ⑱が揺れていた元（三つとも probe の側。本体ではない）
- 連打が捨てられていた。hit() は Date.now() < edaCoolAt の間は何もせず返る（coolTime 360ms）。probe は28msごとに叩いていたので、13回に12回は捨てられていた
- 射程の外を叩いて豆投げになっていた。鳥の中心を叩いていたので、短辺×0.35 より遠い鳥は nageThrow に落ちていた（豆は26pxまで寄らないと捕れない）
- 鳥の湧きが乱数のままだった。45秒の窓に10羽ぶん寄ってくるかは運

■ 直した中身（check.js の probe だけ。本体は一字も触らない）
- 乱数を種つきの数列に差し替えた（SEED 20260824）。差し替えるのは写しの中だけ
- 叩く場所を、剣士から鳥へ向かう線上の射程の内側（reach×0.9）にした。剣士の立ち位置は床の影の中心から読む
- 叩く間隔を coolTime に合わせた（40msを10回、その間に喜びを見張る）
- 窓を秒（45秒）ではなく打数（150打）で切った
- 連戦が終わって札が出たら開き直す（ライフ切れで終わるため）。開き直した回数も控える
- 仮想時計の予算を240秒→360秒
- 書き出しに「叩いた数」と「開き直し」を足した

■ 確かめ
- node check.js --fast を5回続けて回し、⑱の節は5回とも一字一句同じ（md5 40bcaa192f7c37d01313f29de06538cc）
- 叩いた数は毎回 78 / 45 / 81、開き直し1回、喜び3回、規定の2倍を過ぎた喜び0
- 5回とも終了コード0

■ 参考には落としていない
- 揺れが消えたので、⑱は合否の対象に残す
```

## 何が揺れていたのか

⑱は**実際に遊んで10羽捕らせ**、喜んだ瞬間を掴まえる作りです。その「10羽捕る」が運任せでした。

| # | 揺れの元 | 直した中身 |
|---|---|---|
| 1 | **連打が捨てられていた**（`coolTime` 360ms のあいだ `hit()` は何もせず返る。probe は28msごとに叩いていた） | 叩く間隔を `coolTime` に合わせた |
| 2 | **射程の外を叩いて豆投げになっていた**（鳥の中心を叩いていた。短辺×0.35 より遠いと `nageThrow`） | 剣士から鳥へ向かう線の、**射程の内側**（`reach×0.9`）を叩く |
| 3 | **鳥の湧きが乱数のままだった** | 写しの中の `Math.random` を**種つきの数列**に差し替えた |

あわせて、窓を秒ではなく**打数**で切り、ライフ切れで連戦が終わったら**開き直す**ようにしました。

## 揺れていないことの確かめ

`node check.js --fast` を**5回**続けて回し、⑱の節を突き合わせました。

```
r1 40bcaa192f7c37d01313f29de06538cc
r2 40bcaa192f7c37d01313f29de06538cc
r3 40bcaa192f7c37d01313f29de06538cc
r4 40bcaa192f7c37d01313f29de06538cc
r5 40bcaa192f7c37d01313f29de06538cc
```

**叩いた数まで毎回同じ**（78 / 45 / 81）です。ここが同じということは、鳥の湧きも動きも同じ道を辿っているということです。

**種（`SEED = 20260824`）は動かさないこと。**動かすと別の遊びになり、揺れていないことの確かめが最初からやり直しになります。コードの脇にも同じ注意を置きました。

## ヨシ待ちの知らせに⑧を付けた

前回の宣言が手元に届かなかったのは、**ヨシ待ちの枝にだけ⑧を足していなかった**ためです（前回の報告では「未決」として残していました）。

出し直し（5分おき）の一行は、**⑧より前**に入れます。⑧は「そのまま:」から末尾までを全部飲み込む決まりなので、うしろに付けると案内の一行まで⑧の中身になります。

---

# これまでの記録 — ⑧そのままを知らせの経路に乗せ、同じ題の上書きを厚み比べにした（2026-08-24 VAIO時計）

**状態：終わり（返事不要）。**

## いま何をしているか

**手は止めています。**次の作業には入っていません。

結論を先に三行で。

- **⑧そのままが、控えから知らせの本文まで通るようになりました。**これまでは panel.html だけが⑧を読めて、書く側に欄が無く、経路の途中で落ちていました。
- **同じ題の上書きは「中身が増えるときだけ」になりました。**⑧を持つ件を、見張りが書く復帰や異常の一行では消せません。断ったときは新しい行として足します。
- **⑧を実際に流して、もう一つ穴が出たので塞ぎました。**⑧の中の見出しの無い行が③したことへ流れ込んでいました（panel v23）。

## 八項目

```
[02:17] ⑧そのままを経路に乗せ、同じ題の上書きを厚み比べにした
状態: 終わり
したこと: ⑧を控えから知らせの本文まで通し、同じ件名の上書きを「項目数も字数も減らないときだけ」に変えた。⑧が③へ流れ込むのも止めた。
版: panel v23（8月24日）／本体 v1421 ／ stable v1420
ファイル:
- ~/.claude/watch-notify.ps1:126-157 Get-Note の型に raw を足し、「そのまま:」から下を行のまま拾う
- ~/.claude/watch-notify.ps1:465-468 stop の本文の末尾へ「そのまま: 」を付けて足す
- ~/.claude/notify-record.ps1:46-56 Rank-Body を新設（見出しの数と字数で厚みを見る）
- ~/.claude/notify-record.ps1:85-102 同じ件名の行は、項目数も字数も減らないときだけ置き換える
- panel.html:488-494 ⑧から下を切り、head だけを③⑥と pick に渡す
- panel.html:168 verTag を v23（8月24日）へ
実測:
- 三本とも構文OK（PSParser 二本・node --check 一本）
- 控えの写しに⑧を八行（空行・字下げ・「件名:」を含む）与えて、八行がそのまま raw に入った
- 実物の work-note.txt でも raw 22行・802字が入った
- notify-record.ps1 の写しで三通り：⑧持ちを入れる→1行／同じ件名の薄い復帰→足されて2行／同じ件名の厚い件→薄い方だけ置き換わって2行
- panel.html の sixLines は九行で、したこと=「二点を直した」だけ。⑧は空行も字下げも残った
食い違い: ⑧を実際に流したところ、⑧の中の見出しの無い行が③したことへ出た（「■ 一段目 - 字下げを残す行」）。⑧を経路に乗せた副作用なので、panel v23 として同じ回で塞いだ。頼まれた二点の外だが、塞がないと⑧が届くたびに③が壊れる。
答え: なし
未決: ヨシ待ちの知らせ（🙋）の本文には⑧を足していない。あちらは件名・待っているのは・答え方の三行だけの形で、ファイルも実測も入れていない。揃えるなら別の回に。
そのまま: 【この回に Code タブへ書いた本文】

■ ⑧そのままを知らせの経路に乗せた
- watch-notify.ps1 の Get-Note の型に raw を足した
- 控えの「そのまま:」の行から下は、見出しの振り分けをせず行をそのまま拾う。Trim も一行化もしない
- 拾った分は $r.raw に入れ、stop の本文の末尾へ「そのまま: 」を付けて足す
- 控えが古い（stale）ときは入れない。完了・ファイル・実測と同じ扱い

■ 同じ題の上書きを「中身が増えるときだけ」にした
- notify-record.ps1 に Rank-Body を新設。見出しの数（ファイル／実測／食い違い／答え／未決／そのまま）と字数で厚みを見る
- 同じ件名の行を消す（＝置き換える）のは、項目数も字数も減らないときだけ
- 減るときは上書きを断り、新しい行として足す。見張りが書く復帰や異常の一行もこれで消せない

■ ⑧が③へ流れ込むのを止めた（panel v23）
- ⑧を実際に流したら、⑧の中の見出しの無い行が「したこと」に出た
- sixLines の頭で body を「そのまま:」の位置で切り、head だけを③⑥と pick に渡す
- ⑧の取り出しは今までどおり body から。改行も字下げもそのまま

■ 確かめたこと
- Get-Note の写しに⑧を八行（空行・字下げ・「件名:」を含む）与えて、八行がそのまま raw に入った
- notify-record.ps1 の写しで三通り：⑧持ちを入れる→1行／同じ件名の薄い復帰→足されて2行／同じ件名の厚い件→薄い方だけ置き換わって2行
- panel.html の sixLines に同じ本文を通して、九行が 状態・したこと・版・ファイル・実測・食い違い・答え・未決・そのまま の順で出た。したことは「二点を直した」だけ
```

## なぜ⑧が落ちていたか

panel.html は v22 から⑧を読める作りでした。落ちていたのは**書く側**です。

```
work-note.txt（控え）→ watch-notify.ps1（本文を組む）→ notify-record.ps1（控えと写し）→ notices.json → panel.html（八項目に開く）
                            ↑ ここに⑧の欄が無かった
```

`Get-Note` が拾う欄は 件名／待ち／完了／ファイル／実測／種類 の六つで、`そのまま` はどこにも無く、stop の本文も 件名＋完了＋ファイル＋実測 の四つだけを組んでいました。**⑧は控えに書いても、そこで捨てられていました。**

⑧は一行では終わらないので、拾い方も他の欄と別にしてあります。「そのまま:」の行に当たったら、そこから下は**見出しの振り分けをせず、行をそのまま**足します。字下げも空行も、⑧の中に出てくる「件名:」のような行も、そのまま残ります。

## なぜ⑧を持つ件が消えたか

v22 で入れた「同じ題は上書き」は、**件名だけ**を見ていました。

見張りは、Claude Code が戻ったときに「件名＋Claude Code が戻りました。」の一行を出します。件名は同じなので、**⑧を持つ厚い件が、この一行で置き換わりました**。実際、8月23日 23:24 の⑧持ちの件は、23:42 の復帰通知に消されています。

そこで `Rank-Body` を入れ、**厚み**（見出しの数と字数）で比べるようにしました。

| 新しい件 | 古い件 | どうなるか |
|---|---|---|
| 項目も字数も減らない | | 置き換える（これまでどおり） |
| どちらかが減る | | **上書きを断り、新しい行として足す** |

復帰や異常の一行は見出しを持たないので、⑧持ちの件を消せません。二本並びますが、**消えるより並ぶほうがましだ**という決めです。

## 検査

`node check-all --fast` を回しました。**一度目は⑱が FAIL、二度目は PASS** です。

```
一度目  FAIL ⑱ 喜ぶ動きが必ず終わるか  ✗ 喜びを一度も起こせなかった（測り方の問題かもしれない）
二度目  PASS ⑱ ほか二十項目すべて PASS（終了コード0）
```

**この回の変更とは関わりがありません。**`koushu-handan.html` は一字も触っておらず、HEAD と同じ中身のままです（触ったのは `panel.html` と `~/.claude` の二本）。⑱は喜びの動きを実際に起こしてから測る項目で、**起こせないと FAIL になる**作りです。二度目で起きて PASS しているので、測り方の揺れとみて先へ進めました。探偵編の回帰（`adv-check.js`）は一度目から PASS です。

## 残していること

**ヨシ待ちの知らせ（🙋）の本文には⑧を足していません。**あちらは「件名／待っているのは／答え方」の三行の形で、ファイルも実測も入れていません。⑧まで載せるなら、あの三行の形ごと決め直す話になるので、この回では触っていません。

---

# これまでの記録 — 押し送りを異常だけに絞り、読み物と状態をリポジトリ経由へ（2026-08-23 19:40 VAIO時計）

**状態：終わり（返事不要）。**

## いま何をしているか

**手は止めています。**次の作業には入っていません。

結論を先に三行で。

- **案Bを入れ、パネルから送る側の道具を撤去しました。**ntfy へ出るのは異常だけ、読み物と状態は全部リポジトリ経由です。
- **作業時間の表示のご懸念は当たっていました。**v7まで ntfy 依存で、今日の429で実際に止まっていました。**v8で state.json へ移してあり、いまは ntfy と無関係**です。
- **途中で、黙って止まっていた不具合を一つ見つけて直しました。**`state.json` の書き出しが59分間止まっていました。実測しなければ気づけないものでした。

## 変えたファイル

| ファイル | 変えた中身 |
|---|---|
| `~/.claude/watch-notify.ps1` | `Record-Notice` を新設し、**送る送らないに関わらず控えを残す**。`Is-Alarm` で**押し送りは異常だけ**に絞る。控えを `Send-Raw` の成功時から切り離した |
| `~/.claude/notify-record.ps1` | **`notices.json` を書く**（status.md と同じ二十件を機械向けに）。`-RewriteOnly` を新設。押し出しは status.md と両方 |
| `~/.claude/inbox-watch.ps1` | **`Publish-Status` 冒頭の関門を撤去**（下の「見つけた不具合」）。使わなくなった話題名の宣言も外した |
| `panel.html` | **v10** 一覧を `notices.json` から読む／**v11** 送る側の道具を撤去／**v12** 読み先を公開ページへ |

## 見つけた不具合 — `state.json` が59分間、黙って止まっていた

`Publish-Status` の冒頭に、**状態の話題名が無ければ何もせず帰る**という関門が残っていました。

```powershell
function Publish-Status() {
  $t = Read-Topic $StatPath      # rc-ntfy-status-topic.txt
  if (-not $t) { return }        # ← ここで毎回帰っていた
```

状態を `state.json` へ移したとき（今日18時台）に外し忘れ、**話題名のファイルを畳んだ時点で書き出しが丸ごと止まりました。**記録には何も出ないので、外から見て分かりません。**四つの実測をしなければ、そのまま気づけませんでした。**

直したあと、書き出しが再開したことを確認しています。

```
19:31:03  常駐の見張りを始めます
19:31:08  状態を書き出した：作業中 / 押し送りを異常だけに絞る（案B）
```

## 読み先を公開ページへ移した理由（実測）

`raw.githubusercontent.com` は**5分の取り置き**があり、しかも `?_t=` では破れません。

```
raw   : Cache-Control: max-age=300   → 20秒おきに3回叩いても、押し出し済みの新しい中身が返らない
Pages : Cache-Control: max-age=600   → ?_t= ごとに新しい鍵。実測 Age: 0 で最新が返る
```

**押し出しは届いていたのに、読む側が古い写しを掴んでいました。**`state.json`・`notices.json`・`ver.txt` の三つを公開ページ側へ移しました。**`stable` の版だけは raw のまま**です（公開ページは main しか出さないため。版はめったに動かないので5分の遅れで足ります）。

## パネルから撤去したもの（v11）

緑の「ヨシ」釦／「直しの指示を書く」の入力欄／「送る」釦／「確認も見る」の切り替え、および言づて欄と送信の処理。

**残したもの**：上部の状態表示、知らせの一覧、各件の「写す」、「話題名を入れ直す」。

### 撤去前に確かめた受信箱の使われ方

| | |
|---|---|
| **書く** | `inbox-watch.ps1` の3か所のみ。材料は**すべてパネルからの ntfy 送信** |
| **読む** | `inbox-feed.ps1`（この窓へ流す）／`notify-record.ps1`（`status.md` の未処理数）／`inbox-watch.ps1`（`state.json` の `todo`） |

**書き手がいなくなるので受信箱は常に0件になりますが、読み手3つはいずれも「0件」を返すだけで壊れません。**`watch-notify.ps1` にも `inbox.txt` の名は出ますが、除外一覧に名前があるだけで中身は読んでいません。

## 四つの実測（公開ページ側から）

```
① 一覧    : 20 件。先頭 17:33:02【✅ 終わりました（返事不要）】
② 写す    : 状態: 終わり
            したこと: 件1＝inbox-feed.ps1 に文字コードの固定を1行（932で起こしても
                      UTF-8 で出ることを実測）。件2＝panel v6 が tags で送り元を印し、
                      inbox-watch.ps1 がその印で判じる（卓上5通りとも正）。
            版: 本体v1421 ／ stable v1420
            ファイル: 未確認
            実測: 未確認
            未決: 未決なし
③ 作業時間: 【作業中】押し送りを異常だけに絞る（案B）
            見込み50分・開始から8分 ／ 書き出しから463秒（新しい）
④ status.md: **作業中** — 未処理 0 件 ／ 19:17:17 書き出し
```

**②は本体から実物の関数を切り出して動かしたもの**で、画面の飾りではなく実際に写る中身です。

## 残っている一点

**「ファイル」と「実測」は、いまも「未確認」になります。**知らせの本文がその情報を運んでいないためです。**写す側は既に受け取れる形**で、本文に `ファイル: …` `実測: …` の行があればそのまま埋めます。**私が止まるときにその二行を書けば、次から実物が入ります。**

## 以前の記録










## 済んだこと（全件）

| | 中身 | 確かめ方 | 結果 |
|---|---|---|---|
| **1** | フックから `-WindowStyle Hidden` を全廃し、窓を持たない起動へ | 対照実験 T4・T5 | **済**（親可視・新窓なし） |
| **2** | 入れたあと、フックを何度も発火させて確認 | 実発火6回＋その後の実運転3回 | **済**（9回とも `可視`） |
| **3** | 隠れた窓を戻す手を用意 | フック側＋見張り側の二重。人工的に隠して復帰を確認 | **済**（`HID` を検出して復帰） |
| **4** | 報告書を止まるたびに書き直して push／見張りが1分ごとに状態を書き足す | この報告＋`watch-status.log` | **済** |
| **E** | `rc-check` を畳む | 予定表の削除・台本の改名・見張りの再確認 | **済** |
| **F** | 鳴きの材料は採らない | 裁定を記録 | **済**（実装なし） |
| **G** | `stable` を進めたらクロードへ知らせる仕組み | DryRun・空振り・試し投げ・二重送信の四通りで確認 | **済**（下書き確定・二重送信も止めた） |

## 外から読める記録の在り処

**Claude Code が黙って止まっても、この三つは外から読めます。**

| ファイル | 何が書いてあるか | 間隔 |
|---|---|---|
| `~/.claude/watch-status.log` | 鍵・プロセス数・窓の数・迷子・隠れ戻し・生存の古さ・最後の状態 | **1分ごと（必ず1行）** |
| `~/.claude/watch-notify.log` | 送った知らせと、鍵が変わった瞬間 | 変化のときだけ |
| `~/.claude/hook.log` | フックの発火と、窓の取っ手・可視／不可視 | 発火のたび＋生存は10分ごと |

`watch-status.log` は**変化が無くても毎分書きます**。沈黙と停止を見分けるための行です。
2日分（2880行）を残して古いほうから捨てます。

---
## 【1】窓が消えた前後、見張りは何をしていたか

**動いていました。黙っていたのは作りのとおりです。**

`watch-notify.ps1` は**鍵が変わったときだけ**一行書きます。同じ鍵なら無記録です。
したがって「行が無い＝止まっていた」ではありません。

```
2026-08-21 23:36:04  送信 [Claude Code: back up]
2026-08-21 23:36:05  送信 [Claude Code: finished]      ← 中身が 20:34:17（【3】）
2026-08-21 23:36:05  鍵 dead -> done:2026-08-21 20:34:17 （プロセス1本）
2026-08-21 23:38:04  鍵 done:2026-08-21 20:34:17 -> run:2026-08-21 23:37:08 （プロセス1本）
2026-08-21 23:48:13  鍵 run:2026-08-21 23:37:08 -> run:2026-08-21 23:48:09 （プロセス1本）
2026-08-21 23:55:05  送信 [Claude Code: finished]
2026-08-21 23:55:05  鍵 run:2026-08-21 23:48:09 -> done:2026-08-21 23:54:39 （プロセス2本）
```

**裏取りが三つあります。**

- **予定表の記録** — `ClaudeWatchNotify` は `last=2026/08/21 23:57:57`、`next` が1分後。回り続けています。
- **生存の記録** — `hook.log` の `heartbeat` が 20:38:04〜23:48:06 まで **20本すべて 10.0 分間隔で欠けなし**。
  うち3時間は Claude Code が落ちていた間です。見張りは Claude Code と独立に生きています。
- **20:35 の実績** — 本数がゼロになった回は、ちゃんと `process gone` を投げています。仕組み自体は働きます。

**なぜ知らせが飛ばなかったか。**`ntfy` を投げる条件が

```
if ($alive -eq 0) { ... 'Claude Code: process gone' ... }
```

**`claude.exe` の本数がゼロのときだけ**だからです。今回は**本数が 1 のまま**でした。→【2】

---

---

## 【2】窓消えの原因と、根から断った直し

### 事実——落ちていません。窓だけが「隠されて」いました

現場の実測です。

| PID の連なり | 何の窓か | 起動 | 取っ手 |
|---|---|---|---|
| `cmd` 15320 → `claude` 5844 ＋ `conhost` 3232 | 「プロセス落としの耐久試験」の窓 | 23:35:13 | **0（窓なし）** |
| `cmd` 680 → `claude` 5936 ＋ `conhost` 10492 | いまのこの窓 | 23:53:23 | 263662（窓あり） |

`claude.exe` 5844 は**23:57:20 に commit まで済ませて**走り続けていました（`e1232a7`）。
イベントログに `Application Error` は**一件もありません**。落ちたのでも外から閉じられたのでもなく、
**窓だけが画面から消えた**——これが出発点でした。

消えた時刻は **23:48:09〜23:53 の間**です（23:48:09 の `hook.log` が `GetWindowTextW` で
「◐ プロセス落としの耐久試験」を読めている＝その時点では窓が在った）。

### 対照実験——犯人は `-WindowStyle Hidden` 単独

試し窓を立て、その子として `hook-notice.ps1` を走らせ、親の窓を追いました。

| 試験 | `-WindowStyle Hidden` | `AttachConsole(親)` | 親の窓 |
|---|---|---|---|
| **T1**（当時と同じ形） | あり | あり | **可視 → 不可視**（取っ手は残る） |
| **T2** | **無し** | あり | **可視のまま**／題も `繋ぎ=親` で正常に書けた |
| **T3** | あり | **無し** | **可視 → 不可視** |

**T3 が決め手です。**繋ぎ替えを殺しても窓は隠れました。子のコンソールアプリは既定で
**親のコンソールを引き継ぐ**ので、`-WindowStyle Hidden` がその**親の窓＝Claude Code の窓**に当たります。
`AttachConsole` は無実でした。

**そして「壊れた」のではなく「隠された」のだと分かりました。**取っ手は残ったままで、
`IsWindowVisible` が偽になるだけです。`.NET` の `MainWindowHandle` は**可視の窓しか返さない**ので、
外からは 0＝窓なしに見えます。現場の `cmd` 15320 が 0 だったのは、これでした。

### 直し① — 三つのフックから旗を全廃

`settings.json` の `Notification` / `Stop` / `UserPromptSubmit` から、`-WindowStyle Hidden` の
2要素を**3件とも**外しました。JSON として読めることを確認済み。

```
外す前: -NoProfile -ExecutionPolicy Bypass -WindowStyle Hidden -File …\hook-notice.ps1 -Kind notification
外した後: -NoProfile -ExecutionPolicy Bypass -File …\hook-notice.ps1 -Kind notification
```

**入れる前に実測しました（T4）。**旗なしで走らせ、0.3秒刻みで55回、親の窓と画面上の窓の総数を数えています。

```
  0.6s 親の窓=[855596|可視|cmd.exe]        画面の窓の総数=6
  1.2s 親の窓=[855596|可視|KHTEST_T4]       画面の窓の総数=6
  6.0s 親の窓=[855596|可視|★待ち KHTEST_T4] 画面の窓の総数=6
--- 試験中に現れた窓 ---
376|★待ち KHTEST_T4        ← 試験窓それ自体だけ
```

**「親の窓が可視のまま」「新しい窓も出ない」の両方を満たします。**

### 直し② — 旗の代わりに、自前の窓だけを隠す

旗を外すと、コンソールを新しく与えられた場合に一瞬だけ窓が出る余地が残ります。
そこで `hook-notice.ps1` の冒頭へ、**共有かどうかを見分けてから隠す**仕掛けを入れました。

```
GetConsoleProcessList() で、いまのコンソールに繋がっているプロセスの数を数える
  1 なら … 自前のコンソール → 隠してよい（SW_HIDE）
  2 以上なら … 親と共有している → 触らない   ← ここが旗との決定的な違い
```

旗は数を見ずに無条件で隠します。この仕掛けは**共有しているときは指一本触れません。**

### 直し③ — 隠れていたら戻す（フック側）

繋ぎ替えた直後と、処理の終わり際の**二度**、親の窓が不可視なら `SW_SHOWNA`（活性化せずに出す）で戻します。
**可視のときは何もしません**（最小化しているだけの窓を勝手に起こさないため）。

### 確かめ——新しい形で二通り

| 試験 | 親の窓 | 記録 |
|---|---|---|
| **T5**（旗なし・本番の形） | **最後まで `可視`** | `自窓=触らず` `戻し=無` `取っ手=11666162／可視` |
| **T6**（わざと旗つき・最悪の形） | 4.4秒で `不可視` → **4.8秒で `可視` に復帰** | `戻し=有` ＝ 戻す手が効いた |

**T6 が保険の証明です。**万一どこかで旗が復活しても、0.4秒で戻ります。

### 確かめ——本物のフックを6回

この窓（`cmd` 680）の取っ手を、発火のたびに外から測りました。

```
発火前              取っ手=263662  可視  題[◑ Koushu-handan directory]
1回目 notification  取っ手=263662  可視  題[★待ち ◐ Koushu-handan directory]
2回目 stop          取っ手=263662  可視  題[●終わり ◐ Koushu-handan directory]
3回目 resume        取っ手=263662  可視  題[◑ Koushu-handan directory]
4回目 notification  取っ手=263662  可視  題[★待ち ◑ Koushu-handan directory]
5回目 stop          取っ手=263662  可視  題[●終わり ◑ Koushu-handan directory]
6回目 resume        取っ手=263662  可視  題[◐ Koushu-handan directory]
```

`hook.log` の側にも同じことが残ります。

```
2026-08-22 00:31:08  notification  … 繋ぎ=親  自窓=触らず  戻し=無  題[◐ Koushu-handan directory]
2026-08-22 00:31:11  notification  画面=出  取っ手=263662／可視  戻し2=無  画面の題[★待ち ◐ …]
```

**6回とも `可視`、`戻し` は全て `無`**——つまり隠されること自体が起きていません。

---

## 【3】見張りの直し（窓の増減・隠れ窓・古い状態・1分ごとの記録）

### ① 画面が一つ見えなくなったら投げる

**本数ゼロだけでは今回を取り逃します。**`watch-notify.ps1` は窓の数を数えるようになりました。

**数え方。**`claude.exe` は自分では窓の取っ手を持たないので、親を上へ辿ります。
**`explorer` などに突き当たったらそこで止めます**——登り切ると机の窓を数えてしまい、
最初の実装では `win=2 / orphan=0` という誤った値が出ました（実測で見つけて直した）。

**誤報を防ぐ関門。**見張り自身に窓が見えない状況では全部が「窓なし」に見えます。
`explorer.exe` の取っ手を対照に取り、そちらも 0 なら**窓の判定ごと飛ばします**（`-1`＝不明）。

**投げる知らせを三つ足しました。**

| 鍵 | 条件 | 題 |
|---|---|---|
| 画面の減り | 画面のある `claude` が前回より減った | **異常です** |
| 迷子 | 生きているのに画面が無い `claude` が出た瞬間 | **異常です** |
| 隠れ戻し | 取っ手はあるのに見えない画面を見つけて戻した | **異常です** |

**実地で確かめました。**控えを細工して「迷子が出た瞬間」「見えていた画面が 2 枚から 1 枚になった瞬間」を作り、
**iPhone へ2通とも受理**されています（`exit=0`＋受理ID）。

### ② 「取っ手はあるのに不可視」を見つけて戻す

**ここが新しい目です。**`MainWindowHandle` は可視の窓しか返さないので、
**隠された窓と、窓が無いことを区別できません。**そこで最上位の窓を自前で走査し、
**可視／隠れ／無し**の三通りに分けました。隠れを見つけたら `SW_SHOWNA` で画面へ戻し、知らせます。

**人工的に隠して確かめました。**

```
1) 起動直後      取っ手=19271276  可視=True   WinScan=VIS
2) わざと隠す    MainWindowHandle=0（0 に見える）  実際の可視=False  WinScan=HID
3) 見張りが戻す  戻した=True  MainWindowHandle=19271276  可視=True  WinScan=VIS
```

**2 の行が要点です。**古い見張りはここを「窓なし」と読み違えます。新しい見張りは `HID` と読み、戻します。

### ③ 落ちる前の状態を送り直さない

状態行が `$FRESH_MIN = 10` 分より古ければ、`wait`／`done` を**送りません**。
鍵だけ静かに書き換え、理由を記録に残します。写しで対照つきに確かめました。

| | `stop` の古さ | 送信の試み | 鍵 |
|---|---|---|---|
| **B1** | 3時間前 | **1回だけ**（`back up` のみ）＝ `finished` は**投げず** | `run:` へ静かに移行＋理由を記録 |
| **B2**（対照） | 1分前 | 2回（`back up`＋`finished`） | `done:` へ |

```
鍵 dead -> run:2026-08-21 21:25:15 （プロセス2本・窓1・迷子1）（状態行が古いので送らず: 180分前・stop）
```

**`process gone` と `heartbeat stale` は鮮度の関門を通しません。**あの二つは「いま起きていること」だからです。

### ④ 1分ごとに状態を書き足す

`~/.claude/watch-status.log` へ、**変化が無くても毎分1行**書きます。

```
2026-08-22 00:33:04  鍵=run:2026-08-22 00:31:25  プロセス=1 窓=1 迷子=0 隠れ戻し=0 生存=5分前 最後の状態=resume 2026-08-22 00:31:25
```

`watch-notify.log` は変化のときだけなので、**沈黙と停止が見分けられません。**この記録がその穴を塞ぎます。
2日分（2880行）を残し、古いほうから捨てます。

### ⑤ 迷子の窓は片付けました

`cmd` 15320 / `claude` 5844 / `conhost` 3232 を木ごと落としました。`claude.exe` はこの窓の1本だけです。

---

## 【4】鳴きの測定（三翻以上）——済んでいます

**条件** — 赤5入り・子・ドラ表示なし・無作為 50000 回。作法§14「写しに probe」方式で、
本体には何も入れていません。向聴は測定台の側で自前に計算し、`analyze()` には触れていません。

**二種で回しました。**前回の窓が 20:15 に種 20260821 で一度回し終えており（落とされる19分前で、
報告に出ていませんでした）、今回 23:42–23:48 に種 20260822 で追試しました。
**二回の数字はほぼ完全に重なります**ので、以下は追試（20260822）の値で書きます。

| | 種 20260822（今回） | 種 20260821（前回） |
|---|---|---|
| 所要 | probe内 316.4秒／実時間 359.8秒 | probe内 311.7秒／実時間 336.2秒 |
| 全体の攻め率 | 43.89%（21943/50000） | 43.91%（21955/50000） |
| 三翻以上の見込み | 5.01% | 4.97% |

※ ご指定の「全体の攻め率 44.14%」とは 0.25pt ずれます。種違いの揺れの幅で、
以下の「全体との差（pt）」は**その回の実測 43.89% を基準**に取っています。

### 1. 縮む段数の分布

**【定義どおり】門前の向聴（面子手・七対子・国士の最小）− 鳴きの向聴（面子手のみ）**

| 段数 | 件数 | 割合 |
|---|---|---|
| -4段 | 5 | 0.01% |
| -3段 | 34 | 0.07% |
| -2段 | 1499 | 3.00% |
| -1段 | 7070 | 14.14% |
| 0段 | 41392 | 82.78% |

**この定義では、段数は 0 か負にしかなりません。**門前側だけが七対子と国士を持てるので、
差は「門前のほうが進んでいる」側にしか出ません。これでは材料になりません。

**【実質の読み】鳴きの芽（同じ牌を2枚以上持つ組）を貪欲に最大4回まで刻子へ変えたとき、面子手の向聴が下がる段数**

| 段数 | 件数 | 割合 |
|---|---|---|
| 0段 | 11660 | 23.32% |
| 1段 | 20597 | 41.19% |
| 2段 | 13712 | 27.42% |
| 3段 | 3712 | 7.42% |
| 4段 | 319 | 0.64% |
| **2段以上** | **17743** | **35.49%** |

以下の帯はすべて**実質の読み**で取っています。4枚持ちは5枚目が出ないので芽から外しました。
鳴きを一回しか許さないと「2段以上」が構造的に 0 件になるため、貪欲に重ねています。

### 2〜4. 帯ごとの出現率と攻め率

| 帯 | 件数 | 出現率 | 攻め率（的中） | 全体との差 |
|---|---|---|---|---|
| **2段以上 かつ 三翻以上** | 1670 | **3.34%** | **95.45%** | **+51.56pt** |
| 1段以上 かつ 三翻以上 | 2389 | 4.78% | 94.81% | +50.92pt |
| **三翻以上のみ（対照）** | 2506 | **5.01%** | **94.33%** | **+50.45pt** |
| 2段以上（打点の条件なし） | 17743 | 35.49% | 50.87% | +6.98pt |
| 1段以上（打点の条件なし） | 38340 | 76.68% | 46.70% | +2.81pt |
| 3段以上（打点の条件なし） | 4031 | 8.06% | 37.29% | **-6.60pt** |
| **0段（一段も縮まない）** | 11660 | **23.32%** | 34.65% | -9.24pt（**守り率 65.35%**） |

**読み取れることが三つあります。**

- **目安は満たします。**「2段以上 かつ 三翻以上」は出現 3.34%（1%以上）・的中 95.45%（83%以上）。
  ご指定の二つ目の条件に当たります。
- **ただし、働いているのは打点であって縮む段数ではありません。**
  打点だけの対照が 5.01%・94.33%。段数の条件を重ねると、**出現率を 5.01%→3.34% に削って、
  的中は +1.12pt しか上がりません。**段数の寄与はほぼ無いと見るべきです。
- **段数だけでは向きすら安定しません。**2段以上で +6.98pt なのに、**3段以上では -6.60pt と裏返ります。**
  よく縮む手ほど攻める、にはなっていません。攻めの物差しとしては使えません。

**3. の答え** — 「一段も縮まない」は出現 23.32%・**守り率 65.35%**。守り側の目安（的中70%）には届きません。

### 5. 独立性の総当たり

**「2段以上 かつ 三翻以上」（鳥の票 1670件）**

| 相手 | 母数 | 同じ側 | 逆を向く | 相手の沈黙 |
|---|---|---|---|---|
| 判定エンジン | 1670 | **100.00%** | 0 | 0 |
| **執事（打点）** | 1670 | **94.85%** | 86 | 0 |
| 軍師 | 1670 | 70.48% | 493 | 0 |
| ずんだ | 673 | 99.26% | 5 | 997 |
| お嬢様 | 1670 | 28.32% | 1197 | 0 |
| マダム | 485 | 44.12% | 271 | 1185 |
| 一姫 | 1464 | 98.16% | 27 | 206 |
| 先生 | 286 | 98.25% | 5 | 1384 |
| 現場猫 | 359 | 32.59% | 242 | 1311 |
| **枝豆（対子）** | 1670 | **95.45%** | 76 | 0 |
| 紳士 | 276 | 22.10% | 215 | 1394 |
| 兎 | 251 | **7.57%** | 232 | 1419 |

**「三翻以上のみ（対照）」（鳥の票 2506件）**

| 相手 | 母数 | 同じ側 | 逆を向く | 相手の沈黙 |
|---|---|---|---|---|
| 判定エンジン | 2506 | 100.00% | 0 | 0 |
| 執事 | 2506 | 93.70% | 158 | 0 |
| 軍師 | 2506 | 67.80% | 807 | 0 |
| ずんだ | 1002 | 98.50% | 15 | 1504 |
| お嬢様 | 2506 | 31.80% | 1709 | 0 |
| マダム | 778 | 46.66% | 415 | 1728 |
| 一姫 | 2143 | 96.31% | 79 | 363 |
| 先生 | 616 | 86.69% | 82 | 1890 |
| 現場猫 | 582 | 43.13% | 331 | 1924 |
| 枝豆 | 2506 | 84.16% | 397 | 0 |
| 紳士 | 452 | 22.35% | 351 | 2054 |
| 兎 | 587 | 21.29% | 462 | 1919 |

**「0段（一段も縮まない）」（鳥の票 11660件）**

| 相手 | 母数 | 同じ側 | 逆を向く | 相手の沈黙 |
|---|---|---|---|---|
| 判定エンジン | 11660 | 100.00% | 0 | 0 |
| 執事 | 11660 | 87.94% | 1406 | 0 |
| 軍師 | 11660 | 58.78% | 4806 | 0 |
| ずんだ | 4033 | 85.42% | 588 | 7627 |
| お嬢様 | 11660 | 52.74% | 5511 | 0 |
| マダム | 4495 | 75.35% | 1108 | 7165 |
| 一姫 | 480 | 68.13% | 153 | 11180 |
| 先生 | 4336 | 64.07% | 1558 | 7324 |
| 現場猫 | 3345 | 59.70% | 1348 | 8315 |
| 枝豆 | 11660 | 65.30% | 4046 | 0 |
| 紳士 | 4740 | 72.19% | 1318 | 6920 |
| 兎 | 5039 | 74.58% | 1281 | 6621 |

**100% と 0% について（ご指定の明記）**

- **100% の相手は「判定エンジン」ただ一人。**ただしこれは**構造上の当たり前**です。
  エンジンの票は判定そのもので、的中の分母と同じものを見ています。**材料の手柄ではありません。**
- **0% の相手はいません。**最も低いのは**兎で 7.57%**（母数251のうち232件が逆）。
  0% ではありませんが、ほぼ裏返しの札です。兎は残り枠の埋まり方を見ているので、
  「鳴いて縮む＝枠がまだ埋まっていない」と読むのが筋で、向きが逆になるのは理屈に合います。

**ご指定の二人を目立たせます。**

> **枝豆（対子）との一致 95.45%**（逆はわずか 76件・沈黙 0）
>
> **執事（打点）との一致 94.85%**（逆は 86件・沈黙 0）

**この帯で票を出す面々は、ほぼ全員が同じ側を向いています**（ずんだ 99.26%・一姫 98.16%・先生 98.25%）。
つまり、**この材料は五人目として新しいことを何も言いません。**特に枝豆は沈黙が 0 で、
1670件すべてに票を出したうえで 95.45% 一致しています。**鳥を足しても、枝豆の票が一票増えるのとほぼ同じです。**

### 参考：鳴いた場合に見込める翻の分布

| 翻 | 件数 | 割合 |
|---|---|---|
| 0翻 | 30837 | 61.67% |
| 1翻 | 10200 | 20.40% |
| 2翻 | 6457 | 12.91% |
| 3翻 | 1806 | 3.61% |
| 4翻 | 514 | 1.03% |
| 5翻 | 148 | 0.30% |
| 6翻 | 23 | 0.05% |
| 7翻 | 10 | 0.02% |
| 8翻 | 5 | 0.01% |

翻の見込みは、役牌（三元牌の対子以上）＋対々和＋タンヤオ＋染め（混一2・清一5）＋ドラで数えています。
場風・自風は中立化してあるので数えていません。七対子は鳴けないので数えていません。

### 鳴きの測定の結び

**「引ける／引けない」の字で言えば、目安の二つ目を満たすので「引けます」。**
ただし**その手柄は打点にあって、縮む段数にはありません**。そして**打点の側は執事と枝豆が既に言っています**。

**裁定が出ました——この材料は採りません。**（2026-08-22）本体には元から入れていないので、
外す作業は発生しません。測定台と生の集計は残してあります。数字は上のとおりで、
**目安は満たすが、手柄は打点にあり、その打点は執事と枝豆が既に言っている**——という形での不採用です。

---

## 【5】測定の間の記録の点検

**生存の記録は無傷、接続の記録は1回だけ落ちました。**

| | 本数 | 期間 | 間隔 | 判定 |
|---|---|---|---|---|
| `heartbeat`（生存） | **20本** | 20:38:04 〜 23:48:06 | **全区間ちょうど 10.0 分** | **途切れなし** |
| `rc-check`（接続） | **18本** | 20:43:13 〜 23:33:14 | 全区間ちょうど 10.0 分 | **23:43 の1回が欠落** |

**生存の記録は、Claude Code が落ちていた 3 時間（20:35〜23:36）を挟んで一度も欠けていません。**
見張りが Claude Code と独立に動いていることの実証になっています。

**欠けた 23:43 の正体。**

```
ClaudeRemoteControlCheck  last=2026/08/21 23:43:43  result=267014  next=2026/08/21 23:53:53
```

`267014` は 16 進で `0x00041306`＝**`SCHED_S_TASK_TERMINATED`（実行が打ち切られた）**。
この予定表の実行時間の上限は **`PT5M`（5分）**です。**23:43:43 に始まり、5分を超えたので 23:48:43 に殺されました。**
`rc-check.ps1` は `Add-Content` を `try/catch` で包んでいるので、**落ちたことは記録に残りません。黙って一行消えます。**

**なぜ5分も掛かったか。**測定が **23:42:02〜23:48:06** に走っていて、その真ん中に当たります。
headless の Edge が2つしかない CPU を占め、`Get-NetTCPConnection` が返らなくなったためです。
**機械が空いている状態で同じものを計ると 4.9 秒**でした。約60倍です。

**裏取り。**測定が終わったあとの **23:53:22 の回は、いつもどおり書けています。**
落ちたのは測定と重なった1回だけで、**「重い処理を回している間だけ、接続の記録が落ちる」作り**だと確定します。
生存のほう（上限 `PT2M`）は中身が一行の追記だけなので生き延びました。

**もう一つ、見張りの穴。**`watch-notify.ps1` は**生存の記録が15分途切れたら知らせます**が、
**接続の記録が落ちたことは見ていません。**今回も知らせは飛んでいません。

---

## 【6】`rc-check` は畳みました（裁定 E）

**接続の見分けの印は、TLS の繋ぎ先では引けませんでした。**繋がっている側のほうが繋ぎ先が少なく、
しかも全部が切断中にもあったもので、差分が取れません。**28行すべてが「接続=不明」**——
記録の間隔は途切れていませんでしたが、**中身は最初から空**でした。

**畳んだ中身。**

| | したこと |
|---|---|
| 予定表 | **`ClaudeRemoteControlCheck` を削除**。消す前に定義を `rc-check-task.xml` へ書き出し済み（戻せます） |
| 台本 | `rc-check.ps1` → **`rc-check.ps1.retired-20260822`** へ改名（消していません） |
| 控え | `rc-endpoints.txt` → **`rc-endpoints.txt.retired-20260822`** へ改名 |
| 記録 | `hook.log` の既往 **28行はそのまま残しました**（追記のみの記録なので消しません） |

**触れていないもの。**`rc-ntfy-topic.txt` は名前が似ていますが**見張りが使う話題名**です。別物なので無傷。
`ClaudeHookHeartbeat`（10分）と `ClaudeWatchNotify`（1分）も残っています。

```
ClaudeHookHeartbeat  state=Ready  last=2026/08/22 0:48:48  result=0
ClaudeWatchNotify    state=Ready  last=2026/08/22 0:51:51  result=0
```

**畳んだ後どうなるか。**

- 接続の見分けは、`hook-notice.ps1` が**押し通知の結果の字**（`inactive`／`不送信`）から読む経路**だけ**に
  なりました。題に付く「**切断**」の印は今までどおり働きます。
- **見張りの知らせには影響しません。**見張りは `rc-check` を見ていません。畳んだ後に回して確認済み。
- **10分ごとの `Get-NetTCPConnection` が消えました。**重い処理と重なったときの巻き添え（23:43 に
  5分の上限で打ち切られた件、【5】）も、これで起きません。

---

## 【7】30分の耐久について

**中止のご指示に従い、待つのはやめました。**そのうえで、**待たずとも実績は既に揃っています。**

- **生存の記録は 20:38〜23:48 の 3時間10分、20本すべてが 10.0 分間隔で欠けなし。**
  うち 3 時間は Claude Code が落ちていた間です。30分の耐久より厳しい条件を通っています。
- **接続の記録は 20:43〜23:33 の 2時間50分、18本が 10.0 分間隔。**
  そのあと **23:43 の1回だけ**、測定の負荷で落ちました（【5】）。23:53 で復帰しています。

**押し通知の修理そのものは、これで一通り通っています。**【3】の送り直しと【2】の窓消えは**この回で直しました**。【6】の「不明」は**畳みました**。未決はもう残っていません。

---

---

## 【8】`stable` を進めたら、クロードへの連絡文の下書きが出る仕組み

**`release-stable.ps1`**（リポジトリ直下）を新しく置きました。`stable` を進める一連の**最後**に、
連絡文の下書きを組んで `ntfy` へ投げ、報告書の末尾にも残します。

### 見張りとは繋がっていません

**この台本は `watch-notify.ps1` にも `hook-notice.ps1` にも一本も線が通っていません。**
`stable` を進めるときに手で呼ぶ台本で、ヨシ待ちや完了の知らせに混ざる経路がそもそもありません。

### する順

| | 中身 |
|---|---|
| 1 | 前回 `stable` の位置を控える。`stable` が無ければ止まる |
| 2 | 進める先が `origin/main` に載っているか確かめる（載っていなければ止まる） |
| 3 | `git tag -f stable <commit>` → `git push -f origin stable` |
| 4 | 前回→今回の題を拾い、**`v<番号> ` で始まる回だけ**残す |
| 5 | 版番号を落とし、**最初の `（` で切る**。言い換え表を当てる |
| 6 | 新しい順に**最大3行**。0行なら**投げない** |
| 7 | `ntfy` へ「クロード連絡どき」で投げる → 報告書の末尾へ残す → commit＋push |

### 内部作業の落とし方

**言葉で除外していません。**作法§4・§6により、**本体を触った回は必ず題が `v<番号> ` で始まります**。
検査・報告書・見張りの回は版が上がらないので付きません。**この一本で切れます。**

実測——`stable`（v1416）から現在まで**39件のうち、本体を触った回は3件**。残る36件は自動的に落ちました。

### 出た下書き（【5】の試し運転）

```
https://kfsr148-art.github.io/koushu-handan/

・一索の鳥にライフ制が付いた
・遊び方の説明を実際の動きに合わせた
・枝豆の印の絵が変わった

触ってみて
```

**元になった題はこの3つです。**

| 題 | 下書きの行 |
|---|---|
| `v1419 一索の鳥にライフ制を入れた（ライフ3・全モード共通／…）` | 一索の鳥にライフ制が付いた |
| `v1418 説明文の一文を実行時の文言に合わせた（…）` | 遊び方の説明を実際の動きに合わせた |
| `v1417 枝豆の印を🫛から🌱へ替えた（…）` | 枝豆の印の絵が変わった |

**URL に `?_v=` は付けていません。**版番号は書かない決まりなので、素のURLです。

### 題は日本語で通りました

`ntfy` のヘッダに生の日本語は載せられないので、**RFC 2047 で符号化**して渡しています。
返りで確かめました。

```
"title":"クロード連絡どき"
```

**iPhone の通知にも日本語のまま出るはずです。**ここはご確認をお願いします。

### 試し運転で動かしていないもの

**`stable` は動かしていません。**`-NoTag` で連絡だけ出しました。実機検収がまだで、
進めてよい版はこちらでは決められないためです（作法§19）。
`stable` は手元・origin とも **d5d5e9f（v1416）のまま**です。

### 一回の生成で一通だけ（同じ下書きは二度投げない）

**下書きは検収OKをいただき、この形で確定しました。**あわせて、**同じ知らせが二通飛ばない**ようにしました。

**元の作りは「走らせれば投げる」だけでした。**だから台本を二度走らせれば二通出ます
（試し運転で実際に二通お届けしてしまいました）。

**直し方——本文の指紋で見分けます。**

```
投げる前に、本文（下書きそのもの）の SHA1 を取り、
~/.claude/release-notice-sent.txt に同じものが控えてあれば、投げない。報告書へも重ねて書かない。
投げられたときだけ控えに足す（送信に失敗した回は控えないので、走らせ直せば投げ直せる）。
```

**鍵に区間（commit の範囲）は混ぜていません。**最初は混ぜていましたが、それだと
**`HEAD` が一つ進んだだけで鍵が変わり、本文が同じでも投げてしまいます**。
読み手が受け取るのは本文なので、本文で見分けるのが正しい形でした。

**確かめました。**

| | 試験 | 結果 |
|---|---|---|
| ① | 同じ本文をもう一度走らせる | **投げない**（`投げ済みか : 済み` で停止。報告書への追記も無し） |
| ② | `HEAD` が進んでも本文が同じなら止まるか | **止まる**。区間 `..0a8bceb` / `..e73276c` / `..HEAD` の三通りで**材料は同じ3件**、よって本文も鍵も同じ |
| ③ | 押し通しの口 | `-Force` を付けたときだけ投げ直せる |

**試験のあいだ、新しい通知は一通も出していません。**控えに鍵を先に書いておき、
「止まること」を確かめる形にしたためです。

**副産物**——②の試験で一時 commit を作ったところ、**「進める先が `origin/main` に載っていない」の関門**が
先に働いて止まりました。こちらも効いています。

### 正直に申し上げる限界と、その手当て

**言い換えは機械です。**上の3行は今の題からきれいに出ますが、将来これが不格好になる回はあります。
`-Lines "行1","行2","行3"` で**文言だけ手で差し替えられる口**を付けてあります。既定は全自動です。
言い換え表は台本の冒頭（`$SAYING`）にあり、行を足すだけで増やせます。

**数え落としの関門を付けました。**試験の途中で、題の数が実数（4件）より少なく（2件）出る回が
**一度だけ**ありました。再現せず、原因は掴めていません。**推測で片付けるのは危ないので**、
題の数と commit の数を突き合わせ、**合わなければ連絡を組まずに止まる**ようにしてあります。
材料が欠けたまま連絡を出すより、止まって回し直すほうが安全だからです。

### 使い方

```
.\release-stable.ps1 -Commit <sha>              進めて、投げて、報告書へ残して push
.\release-stable.ps1 -Commit HEAD -DryRun       何も動かさず、文面だけ見る
.\release-stable.ps1 -Commit HEAD -NoTag        タグは動かさず、連絡だけ出す
.\release-stable.ps1 -Commit <sha> -Lines "…"   文言だけ手で差し替える
```

---

## 【9】報告書が古く見えた件（三つ重なっていました）

### ① 私の落ち度 — 「書き足す」だけで「書き直す」をしていなかった

**題と状態行が 2026-08-22 01:20 から 09:59 まで一度も変わっていません。**
中身は今日だけで **19 回** commit しており、本文は伸びていましたが、
先頭の一行と状態行が古いままでした。**開いた人には止まって見えます。**

```
b32f7d2 09:59  # 報告 — クロードへの連絡の…（01:20）
ca423a4 09:51  # 報告 — クロードへの連絡の…（01:20）
099ac60 07:41  # 報告 — クロードへの連絡の…（01:20）
   …（8版すべて同じ先頭）
```

決まりは「**止まるたびに書き直す**」であって「書き足す」ではありません。守れていませんでした。
**この版から、止まるたびに先頭（題・状態行・結論）を書き直します。**

### ② `raw` は push から約5分遅れる（実測）

`raw.githubusercontent.com` は `Cache-Control: max-age=300` で写しを持ちます。
`?t=` を付けて迂回を試みても `X-Cache: HIT` のままでした。

| | 時刻 |
|---|---|
| push | **09:59:51** |
| 10:02:32 の取得 | まだ古い（`X-Cache: HIT`） |
| 10:03:18 の取得 | まだ古い（`X-Cache: HIT`） |
| 10:04:03 の取得 | まだ古い（`X-Cache: HIT`） |
| **10:04:48 の取得** | **入れ替わり**（`X-Cache: MISS`） |

**約 4分57秒。**持ち時間の 300 秒とほぼ一致します。**push 直後に見ると、必ず一つ前が出ます。**

### ③ `stable` の版に、まさにその報告が入っている

お書きになった「2026-08-20 の投票欄の折り返しの報告」は、`stable`（`d5d5e9f`）の中身と一致します。

```
git show d5d5e9f:report-latest.md | head -1
→ # 報告：投票欄の折り返しの条件を案Bへ（v1416）
```

`stable` は v1416 のまま止めてあり（実機検収がまだのため）、そこに紐づく報告書も当時のものです。
**`main` の側は、いま確かに新しくなっています。**

```
git ls-remote origin refs/heads/main → ca423a4…（当時）
raw/main の「送った知らせ」→ 5 件あり
```

**どちらを開かれたか、確認させてください。**`/main/` を開いて古かったのなら②の5分、
`/stable/` や古いタブ・ブックマークなら③です。②なら5分待てば必ず入れ替わります。

### 付記 — 知らせを送っても push はされません

「送った知らせ」の節は、ntfy へ送るたびに**作業ファイルへ**書き足されますが、
**commit も push もしません**（見張りは1分ごとに走るので、勝手に commit させるのは危険と判断しました）。
したがって**最後の push 以降に飛んだ知らせは、次の push まで GitHub には出ません。**
いまも一件そうなっていたので、この回で載せました。

---

### 打ち切りと、以後の決まり（2026-08-22 11:30）

**切り分けは打ち切りになりました。**読む側の仕組みが古い写しを掴んでいるのが原因で、
**push には落ち度がありません。**新しい名前のファイルを置いて照合しても、
掴んでいるのがそちら側である以上、結果は変わらないためです。

**確かめた事実だけ残しておきます。**

| | |
|---|---|
| `main` の先頭 | `46d1805` / 2026-08-22 10:06:19 +0900 |
| その commit の `report-latest.md` | 入っている（70行追加・6行削除）。先頭行は今日の 10:06 |
| push | 成功。打ち直して `Everything up-to-date`（終了コード 0） |
| commit 固定URL をこちらから取得 | 846行・今日の 10:06・手元の commit と**完全一致**（`X-Cache: MISS`） |
| そちらに出ていた中身 | `c48286a`（2026-08-20 11:59）＝**47時間前** |

**中身が変わり得ない不変URLで両端が食い違ったので、GitHub 側の遅れでは説明が付きませんでした。**

**以後の決まり。**

- **答えは報告書ではなく、知らせの本文に直接書く。**長いときは (1/3) のように分ける。
- **報告書は引き続き書く**が、**答えの置き場としては当てにしない。**
- 「中身は報告書にあります」と書かない。届かない先を指すことになる。

切り分け用に置いた `freshcheck-112636.txt` は消しました。

---

## 【10】v1420 — 折り返しの条件の危うさを塞いだ

**案Bのまま確定しました。**実測で収まっていて検査も通っている形を、将来の保守の危うさだけを
理由に戻すのは割に合わない、という裁定です。**危うさは次の二点で塞ぎました。**

### ① 条件の脇に、連動の注釈を一行

`koushu-handan.html` の折り返しの条件（案B・二本立て）の直前へ入れました。

```
/* ＊この 350/380/384 は、上の切り詰め @media (orientation: landscape) and (max-height: 380px)（1106行）と連動。片方を変えるときは必ず両方を見る。 */
@media (orientation: landscape) and (min-width: 844px) and (min-height: 350px) and (max-height: 380px),
       (orientation: landscape) and (min-width: 844px) and (min-height: 384px) {
```

**連動先は実物を当たって 1106行**と確かめました。案Bは切り詰めの `max-height: 380px` に
寄りかかった条件なので、あちらの数字が変われば黙って壊れます。**その一点を字で残しました。**

### ② `check.js` ⑦ のフル版の視野に `900x375`

```
速い版 : 568x320, 900x381
フル版 : 568x320, 667x375, 844x390, 932x430, 900x340, 900x375, 900x381
```

**350〜380 の帯の中ほどです。**`900x340` と `900x381` では帯の中が見えません。
切り詰め側の数字が将来変わったとき、**ここが真っ先に崩れて検査に出ます。**

速い版へは入れていません。速い版は 568x320（いちばん狭い）と 900x381（段差そのもの）の
二点構成で、`900x340` も同じ理由でフル版だけです。これは見張りなので、
レイアウトを触った回に回るフル版が置き場として合います。

### stable を v1420 へ進めた（2026-08-22 12:59）

`git tag -f stable 5c02f20` → `+ d5d5e9f...5c02f20 stable -> stable (forced update)`。origin も `5c02f20` を指しています。

**途中で二つ直しました。**

- **数え落としの真因は文字コードでした。** `[Console]::OutputEncoding` が Shift-JIS のとき、git の UTF-8 出力を誤って復号し、**日本語の直後の改行が食われて題どうしが繋がり**ます（60件が16件、1行が1279文字に膨張）。**起動元の窓によって変わる**ため前回は再現しませんでした。台本の冒頭で UTF-8 に固定しています。**関門が止めてくれたので、欠けたまま連絡が出る事故は起きていません。**
- **`-Lines` が一行に潰れました。** `-File` で起動すると `'a','b','c'` が一個の文字列 `a,b,c` として届きます。潰れた本文が一度飛んだので、正しい三行で出し直しました（受理 `bHYUWcVGvHfX`）。台本には、一個きりで読点混じりなら開き直す受け皿を入れました。

**連絡文は検収済みの三行を手渡しています。** 自動の言い換えでは v1420 が「折り返しの条件の脇に、切り詰め max-height:380px」という遊ぶ人に通じない行になり、枝豆の行を押し出していました。**版を上げるだけで中身が遊ぶ人に見えない回**は、この形で手渡すのが要ります。
### 検査（フル版・両方PASS）

**12:23:16 → 12:37:15（13分59秒）。落ちた視野 0。測った組み合わせ 63 / 63。**

| 視野 | judged の二択 bottom |
|---|---|
| 568x320 | 317.8 / 320 |
| 667x375 | 345.3 / 375 |
| 844x390 | 387.5 / 390 |
| 932x430 | 382 / 430 |
| 900x340 | 327.8 / 340 |
| **900x375** | **361.8 / 375**（今回足した視野。一段のまま収まった） |
| 900x381 | 380.5 / 381 |

- `check`（本体）— 21項目すべて PASS
- `adv-check`（探偵編）— 即死罠・時間切れとも PASS
- `serifu.txt` — 7593行・口調の混入 0件・本体と一致（版を上げたので組み直し。台詞の中身は無変更）

### 行き違いの経緯

案Aのご指示は、**47時間前の報告書**（`c48286a`・v1415 の追検証）を前提にしたものでした。
その **1時間16分後**の v1416 で案Bが実装・push されており、経緯が古い報告書に載っていません。
**ご指示の「900x340・900x381 を足す」も v1416 で既に入っていました。**
一度止めてお伝えしたことで、実装済みのものを戻す事故を避けられています。

---

## 【11】返事の受け口（2026-08-22 13:40）

**返事の経路を二本立てにしました。**チャットからの返事に加えて、**ntfy の返信用話題**からも受けます。

| | 控えの置き場 |
|---|---|
| 送る側（知らせを出す。従来） | `~/.claude/rc-ntfy-topic.txt` |
| 返す側（返事を受ける。新設） | `~/.claude/rc-ntfy-reply-topic.txt` |

返す側の名前は `khr-` ＋ 48桁の乱数で、送る側と同じ長さ（52字）です。
**名前そのものが合鍵**なので、人目に触れる場所へ貼らない前提です。

### 見張り方

```
powershell -File ~/.claude/wait-reply.ps1 [-Max 240]     # 30秒おき・既定2時間
```

**ヨシ待ちで止まる前に、背後で走らせます。**止まっている間はこちらから動けないので、
**見張りが返事を拾って終了したときの通知で呼び戻してもらう**形にしてあります。
`since` は見はじめた時刻なので、それ以前の投稿は拾いません（古い返事の取り違えを防ぐため）。

**受け取った文の扱い** — 「ヨシ」なら進む。**それ以外の文は直しの指示**として扱います。

### 作る途中で二つ躓きました

**① 字面の切り出しでは取り出せませんでした。**最初は `"message":"…","priority"` の形を当てにして
いましたが、**素の投稿には `priority` が付かない**ので外れます。JSON を解いて取る形に直しました。

**② 本文が添付として届くことがあります。**`curl --data-binary` に日本語を直に渡すと、
**シェルの文字コードで壊れて不正な UTF-8 になり、ntfy が添付ファイル扱い**にします
（`message` が「You received a file: attachment.txt」になる）。
ファイル経由で渡せば本文として通りますが、**見張りの側でも添付なら中身を取りに行く**ようにしました。
iPhone のアプリから投げるぶんには、そのまま本文として届きます。

---

## 【12】返事パネルの通し試験（2026-08-22 18:18）

**通りました。**返事が受け口に届き、見張りが拾って作業が進みました。

```
17:05      件名「パネル試験」でヨシ待ちを立てる（返事してください が飛ぶ）
17:05      受け口の見張りを起こす
18:18:16   返信話題に「ヨシ」が到着
18:18:17   見張りが147巡目で拾い、正常終了（呼び戻し）
```

**拾ったのが本物であることの裏取り。**囮（14:51:45 の「ヨシ【古い返事・読み捨てられるべきもの】」）
でも、私の自動試験（16:56:07 の「パネルと同じ形の自動試験」）でもありません。

**古い返事の読み捨ても確かめました。**囮は見張りを起こす **5秒前**に仕込み、
**2時間まるごと無視され続けました**。`since` を起こした時刻にしているので構造的に拾いません。

### ヨシ待ちが三回立たなかった件（原因と直し）

**控えの書き忘れではありませんでした。**14:51:48 に `待ち` を書いてあります。
**真因は、題を決める材料が控えではなく「どのフックが鳴ったか」だったこと**です。
応答の終わりに鳴るのは `stop` で、見張りは `stop` を見た時点で「終わりました」に決めていました。
控えの `待ち` は `notification` の枝でしか読まれず、素通りしていました。

```
14:52:36 stop → 鍵 done:14:52:39   終わりました
14:53:59 stop → 鍵 done:14:54:03   終わりました
16:56:45 stop → 鍵 done:16:56:48   終わりました
```

**16:57 の一件だけは書き忘れです。**16:55:16 の `resume` が `待ち` の行を消したあと、
あの回は書き直していませんでした（`notification` は鳴きましたが、待ちが空で `wait-silent`）。

**直し①——控えを題の決め手にした。**`待ち` があれば、最後のフックが `stop` でも
「返事してください」にします。`dead` と生存記録の途切れの次、`notification`／`stop` より**前**に置きました。

**直し②——書けていなければ止まれない形にした。**止まる処理の冒頭で控えを確かめ、
`待ち` も `完了` も無ければ**その場で一行書いてから**知らせへ進みます。
順を **確かめる → 無ければ書く → 知らせを出す** に固定しました。書き忘れて詫びる形はやめています。

**実地の確認** — `待ち` を書いた状態で本物の `stop` を鳴らすと、
`最後の状態=stop` のまま鍵が `wait:` になり「返事してください」で飛びました（受理 `QWoIIEhruMjA`）。

---

## 【13】自動起動の試験（2026-08-22 21:36）

受信箱に投げられた一件——`~/.claude/wake-test.txt` に「自動で起きました」と一行書く——を、
起動後に受信箱を先に読む手順どおりに拾って片付けました。

| | 中身 | 確かめ方 | 結果 |
|---|---|---|---|
| 1 | 起きたあと `~/.claude/inbox.txt` を先に読む | 未処理 `[ ]` が1件（21:36:19） | **済** |
| 2 | `~/.claude/wake-test.txt` に一行書く | 書いたあと読み直し（BOM 付き UTF-8） | **済**（21:36:48） |
| 3 | 受信箱の該当行に `[x]` を付ける | 末尾2行を読み直し | **済** |
| 4 | 控え（`work-note.txt`）を件名・待ち・完了の形で書き替える | 読み直し | **済**（待ちは空＝返事不要） |

新しい仕掛けは足していません。本体（`koushu-handan.html`）も検査も触っていないため、
`check-all` は回していません。

## 触れていないこと

- **リポジトリの本体は無変更。**`koushu-handan.html` も `ver.txt` も触っていません。この報告だけです。
- **試験の仕掛けは本体に何も残していません。**写しに probe の作法どおり、`hook-notice.ps1` の写しを
  一時ディレクトリに作り、記録先だけ逃がして駆動しました。試し窓（`KHTEST_*`）は毎回片付け済みで、
  残っていないことを確認しています。
- **消したものはありません。**`rc-check` は削除ではなく**改名**で畳んであり、予定表の定義も
  `rc-check-task.xml` に控えてあります。`hook.log` の既往28行もそのままです。
- **控えを取ってあります** — `~/.claude/` に `watch-notify.ps1.bak-20260822` /
  `hook-notice.ps1.bak-20260822` / `settings.json.bak-20260822` / `rc-check-task.xml`。

## 直したファイルの一覧

| ファイル | 直したところ |
|---|---|
| `~/.claude/settings.json` | 三つのフックから `-WindowStyle Hidden` を全廃（3件） |
| `~/.claude/hook-notice.ps1` | 自前の窓だけ隠す仕掛け／隠れていたら戻す（二度）／取っ手の可視を記録 |
| `~/.claude/watch-notify.ps1` | 窓の数・隠れ窓の検出と復帰・状態行の鮮度・1分ごとの記録 |
| 予定表 | `ClaudeRemoteControlCheck` を削除（`ClaudeHookHeartbeat` と `ClaudeWatchNotify` は残す） |
| `release-stable.ps1` | **新規。**`stable` を進めて、クロードへの連絡文の下書きを作って投げる |
| `~/.claude/rc-check.ps1` ほか | `.retired-20260822` へ改名（消していません） |

三本の `.ps1` はいずれも **BOM 付き UTF-8 のまま**保存し、
`System.Management.Automation.Language.Parser` で構文検査を通しています。

## 検収の状況

**下書きの本文は検収OK。この形で確定しました。**

- 三行の言い回し・順番 … **確定**
- ひとこと添え「触ってみて」 … **確定**（毎回同じで可）
- 題「クロード連絡どき」 … `ntfy` の返りで日本語が通ることを確認済み

**二通届いた件は直しました。**本文の指紋で見分け、同じ下書きは二度投げません（【8】）。

**ヨシ待ちの知らせに「待っている中身」の一行を入れました。**（2026-08-22 01:28）見張りは会話を知らないので、止まる側が `~/.claude/yoshi-question.txt` へ要点を書き、`watch-notify.ps1` がヨシ待ちの本文にだけ足します。控えが無い／状態行から10分以上離れていれば「（何を待っているかの控えがありません）」と正直に出します。**この本文が次の一本で検収対象です。**

**答え終わった問いの控えは、返事が来た時点で空にします。**（2026-08-22 01:33）`hook-notice.ps1` の `resume` で `yoshi-question.txt` を空にします。実際に発火させて、控えが 0 バイトになることを確認しました。これで答え済みの問いが次の知らせに載る穴は塞がりました。

**控えが空のときは、ヨシ待ちの知らせを出しません。**（2026-08-22 01:36）控えは三通りに分けて扱います——**中身あり＋新しい**なら一行を載せて知らせ、**中身あり＋古い**（状態行から10分以上）なら「控えがありません」と断って知らせ、**空／ファイル無し**なら待っている裁定が無いとみなして`wait-silent:` の鍵だけ静かに進め、知らせは出しません。握りつぶしではなく、`watch-notify.log` へ「控えが空なので、ヨシ待ちの知らせは出さない」と一行残します。

**知らせの題を、こちらが何をすればいいかで三つに書き分けました。**（2026-08-22 01:50）`返事してください`（裁定を待っている）／`終わりました（返事不要）`（作業が終わった・プロセスが戻った）／`異常です`（プロセスが消えた・画面が見えなくなった／隠された・生存記録が途切れた）。**本文の一行目は用件そのもの**にし、通知を開かずとも返事できる形にしました。題は RFC 2047 で符号化して日本語を通しています。三つとも実際に投げ、`ntfy` から本文を取り出して確認済みです。

**本文を三つの型に直しました。**（2026-08-22 02:00）どの題でも**一行目は件名**（何の作業の話か）で、**日時は入れません**（通知の一覧に出るため）。`返事してください`＝件名／待っているのは：〜／答え方。`終わりました`＝件名／何をしたか。`異常です`＝件名／何が起きたか／こちらがすること。内輪の「窓」はやめて「Claude Code の画面」と書き、数の増減だけで終わらせず次にすることを一行足しました。件名・待ち・完了は `~/.claude/work-note.txt` に止まる直前に書き、件名が無いときは「不明」と書かず直前に触ったファイル名を出します。**同じ題が60秒以内に続くときは最後の一件にまとめ、末尾に「他◯件」を足します。**三種＋まとめの四本を実際に送り、ntfy から本文を取り出して確認済みです。

**「閉じた」という言い方をやめ、「見えなくなった」に直しました。**（2026-08-22 02:05）切り分けで出た結論は「窓は閉じておらず、隠されていて取っ手もプロセスも残っていた」でしたので、`減りました`→`見えなくなりました`、`閉じた画面`→`見えなくなった画面`、`画面だけが消えました`→`画面だけが見えなくなっています` に揃えました。見張りの記録（`窓が減った`→`画面が見えなくなった`）と、この報告書の言い回しも合わせています。

**その直しが文を壊していたので、直し直しました。**（2026-08-22 02:12）語ごとの置き換えで「画面が 2 枚から 1 枚に見えなくなりました」という壊れた文になっていました。→ `見えていた画面が 2 枚から 1 枚になりました。1 枚が見えなくなっています。` **「こちらがすること」も直しました。**隠れているだけでプロセスは生きているので、開き直すと同じものが二重に立ちます。→ `そのままお待ちください。自動で戻します。戻らないときだけ開き直してください。`

**「異常です」の三行目を二通りに分けました。**（2026-08-22 02:20）**A＝見張りが隠れた画面を見つけられた場合**は「そのままお待ちください。自動で戻します。戻らないときだけ開き直してください。」、**B＝迷子（プロセスは生きているが画面が一つも見つからない）**は「開き直してください。古いプロセスが残るので、開き直したあとに古いほうを終わらせてください。」。**判定は見張りが機械的に付きます**——隠れた画面は見つけ次第その場で戻して枚数に数え直すので、「画面が減った」まで来た時点で見張りは戻せていません。よって隠れ戻し＝A、画面の減り・迷子＝B。判定がつかない側は B（待たせて何も起きないより害が小さいため）。本文はどちらも三行のままです。

**題も二つに分けました。**（2026-08-22 02:25）`異常です（待てば戻ります）`／`異常です（手が要ります）`。iPhone の一覧に出るのは題の一行だけなので、開かずにどちらか分かる形にするのが目的です。割り振りは A・B のまま——**待てば戻ります**は隠れ戻しの一件、**手が要ります**はプロセスが消えた・生存記録が途切れた・画面の減り・迷子の四件。判定がつかない側は「手が要ります」に寄せています。題と三行目は `$T_BAD_A`／`$ACT_A`、`$T_BAD_B`／`$ACT_B` の対で並べて定義し、片方だけ変わらないようにしました。**まとめ送り（60秒）は題ごとに掛かる**ので、待ちの知らせと手の要る知らせが互いに潰し合うことはありません。

**二つの題を実物で確かめました。**（2026-08-22 07:37）どちらも本物の経路を通しています。`異常です（手が要ります）`は控えを細工して画面の減りを起こし、`異常です（待てば戻ります）`は**この画面を一瞬だけ隠して見張りに戻させました**。取っ手 263662 が `可視=True → 隠した直後 False → 見張りが走ったあと True` と動き、記録にも「隠れ窓を戻した 1 枚」が残っています。**知らせの文面どおり、待っているだけで戻りました。**

**「待てば戻ります」の二行目を進行形に直しました。**（2026-08-22 07:41）`画面が隠されていたので、1 枚を出し直しました` は戻し終えた後の報告になっていて、三行目の「そのままお待ちください」と噛み合っていませんでした。→ `隠された画面を 1 枚見つけました。いま戻しています。中身は無事です。` 二行目と三行目が同じ時制を向くようになりました。

**報告書の末尾に「送った知らせ」の節を足しました。**（2026-08-22 07:55）ntfy へ送るたびに、題と本文を**要約せずそのまま**書き足します。新しい順に五件まで、超えたら古いものから落ちます。書くのは `~/.claude/notify-record.ps1` で、**見張りと `release-stable.ps1` の両方から呼ばれます**（ntfy へ出る道は二本あるため）。節は `<!-- 送った知らせ ここから／ここまで -->` の印で囲ってあり、その間だけ差し替わります。**この節は手で書き換えないこと**——次の送信で上書きされます。いまは ntfy に残っていた実物の五件で埋めてあります。

**`stable` は動かしていません。**進めてよい版を言っていただければ、
`.\release-stable.ps1 -Commit <sha>` 一本で「進める→push→連絡（一通だけ）」まで通ります。

## 申し送り（`handover-next.md` は上書き済み）

- `koushu-handan` は **v1419（`62cc775`）まで push 済み**。一索の鳥のライフ制12項目と
  遊び方本文5箇所を実装、フル版 `check-all` は全PASS。**実機検収はまだ。**
- `~/.claude/` の見張り：`watch-notify.ps1` / `hook-notice.ps1` / `RESTART.md` / `rc-ntfy-topic.txt`。
  予定表は **`ClaudeHookHeartbeat`（10分）と `ClaudeWatchNotify`（1分）の二つ**
  （`ClaudeRemoteControlCheck` は 2026-08-22 に畳んだ）。
- **`ntfy` 経路は開通済み。**試し送り・完了・ヨシ待ち・プロセス落とし・復帰の五種に加え、
  **窓の減り・迷子**の2種も着信を確認。**隠れ戻し**の一種を新設（未着信確認）。
- **止まるときは理由が何であれ必ず知らせを出してから止まる**（恒久の決まり）。
- **止まるたびに報告書を書き直して push する**（恒久の決まり）。
- **窓消えの原因は `-WindowStyle Hidden`。**フックでこの旗を使わない。使うと親の窓ごと消える。
- **鳴きの材料は採らない**（2026-08-22 裁定）。
- **`stable` を進めるときは `release-stable.ps1` を使う。**タグ移動・push・クロードへの連絡文まで一本で通る。
- `pre-push` の検査は `check.js:872` に起動の旗と120秒の蓋を入れて 415秒→303秒 に短縮済み。
  報告だけの回は `--no-verify` で通してよい。

---

## クロード連絡どき（2026-08-22 01:07 の下書き）

そのまま写して送れます。

```
https://kfsr148-art.github.io/koushu-handan/

・一索の鳥にライフ制が付いた
・遊び方の説明を実際の動きに合わせた
・枝豆の印の絵が変わった

触ってみて
```

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
見張りを止めた（09:26:10）。繰り返しの原因は控えを書き直さなかったこと
予定表の ClaudeWatchNotify を Disabled にし、実プロセス0本を確認した。止めた後、毎分無条件に書かれる watch-status.log が 09:26:00 で止まり、知らせの行も push も増えていない。繰り返しの原因は、Stop のたびに新しい鍵 done:<時刻> が立つ一方、本文の元になる work-note.txt を私が書き直していなかったこと
ファイル: 予定表 ClaudeWatchNotify（Disabled）／~/.claude/watch-notify.ps1（指紋の仕掛けは 09:22 に入れ済み）
実測: 止めた時刻 09:26:10。実プロセス0本（09:26:39）。09:28:05 時点で watch-status.log の最終行は 09:26:00 で、09:27／09:28 の二回分が無い。notify-sent.tsv 17行のまま、notices.json 17行のまま、この題 1行のまま（09:20:04）。status.md と notices.json の最終更新は 09:25:03 のまま。知らせの push は 09:25:03 が最後
そのまま: 【この回に Code タブへ書いた本文】

■ 止めた
- 予定表の ClaudeWatchNotify を Disabled にした。止めた時刻 2026-08-24 09:26:10（VAIOの時計）
- 実体のプロセスは0本（09:26:39 確認）。途中「PID 2388 が残っている」と出たのは、問い合わせに使った powershell 自身の命令文に watch-notify の字が入っていたための誤検出だった

■ 飛ばないことの確かめ（09:28:05 時点）
- watch-status.log は毎分無条件に書かれる。その最終行が 09:26:00。09:27 と 09:28 の二回分が無い＝確かに止まっている
- notify-sent.tsv 17行のまま／notices.json 17行のまま／この題 1行のまま（09:20:04）
- status.md と notices.json の最終更新は 09:25:03 のまま
- 知らせを書き出した push は 09:25:03 が最後。09:26 以降はゼロ

■ 六度の内訳
- 08:26:45 見張り（Stop 08:25:13）
- 08:40:06 見張り（Stop 08:40:01）
- 08:46:10 見張り（Stop 08:45:34）
- 09:03:03 見張り（Stop 09:02台）
- 09:14:04 見張り（Stop 09:13:40）
- 09:20:06 これは私の手動実行。前回のご指示（3）「この題で一度書き出して行が増えないことを示せ」に応じて notify-record.ps1 を直接呼んだもの
- 09:25:03 は別件。新しい件名で1行増えており、⑱の行は 09:20:04 のまま動いていない

■ なぜ繰り返し書き出されたのか（確定）
- watch-notify は Stop が鳴るたびに done:<Stopの時刻> という新しい鍵を立てる
- 本文は work-note.txt から組み直す。控えが書き換わっていなければ本文は前回と同じになる
- 鍵は毎回違うので「新しい知らせ」として扱われ、status.md と notices.json が書き直され、毎回 commit＋push される
- notify-record は同じ題の行を置き換えるので、一覧は1行のまま。積まれていたのは行ではなく書き出しの回数
- 控えを書き直さなかったのは私です。止まるたびに work-note.txt を書き直す決まりでしたが、08:25 以降の五回それをしていませんでした

■ 直しの効き
- 09:22 に入れた指紋（本文の MD5）は動いている。watch-state.txt に bodyHash=9b3033159c3db0c5903165e7760d4845 が出ている
- 09:25 の書き出しでは新しい件名が1行足され、⑱の行は動かなかった
```

### 2. ✅ 終わりました（返事不要）

```
同じ知らせが五回出た件——上書きは効いていた。原因は書き出しの回数
五回の正体を特定した。status.md の行は増えていない（毎回1行のまま置き換わっていた）。増えていたのは書き出し＋push の回数で、原因は控えが前の回のまま Stop が五回鳴ったこと。watch-notify.ps1 に本文の指紋を入れ、終わりの知らせは前と同じ本文なら出さないようにした
ファイル: ~/.claude/watch-notify.ps1:392-410（指紋の関数と控えの変数）／:420（控えから指紋を読む）／:560-569（送る前に指紋を比べる）／:598-601（控えへ指紋を書く）／控え: ~/.claude/watch-notify.ps1.bak-20260824
実測: 同じ題・同じ本文で notify-record.ps1 を一度呼んだ。notices.json 16行→16行、この題 1行→1行、notify-sent.tsv 16行→16行。行は増えず、時刻だけ 09:14:02→09:20:04 に置き換わった。五回の push（08:26／08:40／08:46／09:03／09:14）はすべて「この題の行数=1」で、積まれていない
そのまま: 【この回に Code タブへ書いた本文】

■ (1) status.md にこの題は何行あるか
- 知らせの一覧の中では 1行（17行目）。3行目は一覧ではなく状態の一行で、work-note.txt の件名を写したもの
- 五回の push それぞれの notices.json を開いて数えても、全部「この題の行数=1」だった

■ (2) なぜ上書きが効いていないように見えたか
- 上書き（Rank-Body）は効いていた。題に時刻も記号も混ざっておらず、別件とは見なされていない
- 積まれていたのは status.md の行ではなく、書き出し＋push の回数
- Stop が五回鳴り（08:25:13／08:40:01／08:45:34／09:02:xx／09:13:40）、そのたび watch-notify が新しい鍵 done:<時刻> を立てた
- 本文は work-note.txt から組み直すが、控えが 08:25 のまま書き換わっていなかったので、五回とも同一本文になった
- notify-record は行を置き換えるので一覧は1行のまま。しかし status.md と notices.json は毎回書き直され、毎回 commit＋push された。受け取る側にはそれが五件に見える

■ 直した中身（watch-notify.ps1 のみ。本体は触っていない）
- 本文の指紋（MD5）を控え（watch-state.txt）へ bodyHash= として残す
- done:（終わり／調べた）を送る直前に指紋を比べ、前と同じ本文なら出さない。ログに「前と同じ本文なので出さない」を残す
- ヨシ待ちの出し直しには掛けない。あちらは同じ本文を出すのが目的
- 指紋は done: の回だけ書き換える。ほかの鍵の回に空で潰すと次の done: が素通りするため

■ (3) 行が増えないことの実測
- 直す前: notices.json 16行／この題 1行／notify-sent.tsv 16行
- 同じ題・同じ本文で notify-record.ps1 を一度呼ぶ
- 直した後: notices.json 16行／この題 1行／notify-sent.tsv 16行。時刻だけ 09:14:02→09:20:04

■ ⑧の持ち回りについて
- 持ち回っていた。work-note.txt の「そのまま:」は 08:25 の回に書いたきりで、以降の Stop 五回がすべてそれを再び出していた
- この控えから、⑧はその回に新しく書いた本文だけにする
```

### 3. ✅ 終わりました（返事不要）

```
⑱の揺れを止めた（種で固定）と、ヨシ待ちにも⑧
⑱の揺れの元を三つ潰して測り方を固定した。5回続けて回して出力は5回とも同一。参考には落としていない。ヨシ待ちの知らせにも⑧を付けた
ファイル: check.js:1447-1454（種と射程・入力止めの読み取り）／check.js:1470-1523（乱数の差し替え・叩く場所・打数で切る・開き直し）／check.js:1546-1548（打数と開き直しの書き出し）／check.js:1557（仮想時計 240秒→360秒）／~/.claude/watch-notify.ps1:440-444（ヨシ待ちに⑧）／~/.claude/watch-notify.ps1:558-565（出し直しの一行は⑧より前）
実測: node check.js --fast を5回。⑱の出力は5回とも同一（md5 40bcaa192f7c37d01313f29de06538cc）。叩いた数は毎回 78 / 45 / 81、開き直し1回、喜び3回、規定の2倍を過ぎた喜び0。5回とも終了コード0（21項目すべて PASS）
そのまま: 【この回に Code タブへ書いた本文】

■ ヨシ待ちの知らせにも⑧を付けた
- watch-notify.ps1 の ask の枝で、⑧より前の分を $askHead に控え、$body の末尾へ「そのまま: 」を足す
- 5分おきの出し直しの一行は⑧より前へ入れる。⑧は末尾まで全部を飲み込むため
- 実物の控えで確かめた：本文18行・⑧あり／出し直しの本文でも⑧が最後で、案内の一行はその前

■ ⑱が揺れていた元（三つとも probe の側。本体ではない）
- 連打が捨てられていた。hit() は Date.now() < edaCoolAt の間は何もせず返る（coolTime 360ms）。probe は28msごとに叩いていたので、13回に12回は捨てられていた
- 射程の外を叩いて豆投げになっていた。鳥の中心を叩いていたので、短辺×0.35 より遠い鳥は nageThrow に落ちていた（豆は26pxまで寄らないと捕れない）
- 鳥の湧きが乱数のままだった。45秒の窓に10羽ぶん寄ってくるかは運

■ 直した中身（check.js の probe だけ。本体は一字も触らない）
- 乱数を種つきの数列に差し替えた（SEED 20260824）。差し替えるのは写しの中だけ
- 叩く場所を、剣士から鳥へ向かう線上の射程の内側（reach×0.9）にした。剣士の立ち位置は床の影の中心から読む
- 叩く間隔を coolTime に合わせた（40msを10回、その間に喜びを見張る）
- 窓を秒（45秒）ではなく打数（150打）で切った
- 連戦が終わって札が出たら開き直す（ライフ切れで終わるため）。開き直した回数も控える
- 仮想時計の予算を240秒→360秒
- 書き出しに「叩いた数」と「開き直し」を足した

■ 確かめ
- node check.js --fast を5回続けて回し、⑱の節は5回とも一字一句同じ（md5 40bcaa192f7c37d01313f29de06538cc）
- 叩いた数は毎回 78 / 45 / 81、開き直し1回、喜び3回、規定の2倍を過ぎた喜び0
- 5回とも終了コード0

■ 参考には落としていない
- 揺れが消えたので、⑱は合否の対象に残す
```

### 4. 🙋 ヨシしてください

```
check.js ⑱ の揺れの特定と、揺れない測り方への直し
待っているのは：下の直し方（probe の三点を直して乱数を固定する／それでも揺れたら⑱を参考に落とす）で進めてよいかのヨシ
答え方：「ヨシ」で進めます。直すところがあれば、その旨をお書きください。
（2回目のお知らせ。まだ待っています）
```

### 5. ✅ 終わりました（返事不要）

```
⑧そのままを経路に乗せ、同じ題の上書きを厚み比べにした
⑧を控えから知らせの本文まで通し、同じ件名の上書きを「項目数も字数も減らないときだけ」に変え、⑧が③へ流れ込むのを止めた（panel v23）
ファイル: ~/.claude/watch-notify.ps1:129-155（Get-Note に raw）／~/.claude/watch-notify.ps1:465-468（stop の本文に そのまま:）／~/.claude/notify-record.ps1:46-56（Rank-Body 新設）／~/.claude/notify-record.ps1:85-102（上書きの判定）／panel.html:488-494（⑧から下を切って head を渡す）／panel.html:168（v23）
実測: 三本とも構文OK ／ 実物の work-note.txt を Get-Note に通して raw 22行・802字（空行・字下げ・「件名:」を含む）／ 控えの写しで 厚い件→1行、同じ件名の薄い復帰→足されて2行、同じ件名の厚い件→薄い方だけ置き換わって2行 ／ panel の sixLines は九行で したこと=「二点を直した」だけ ／ check-all --fast は一度目⑱FAIL（喜びを起こせず・測り方の揺れ）二度目PASS、pre-push でも両方PASS ／ push 4015a9e
そのまま: 【この回に Code タブへ書いた本文】

■ ⑧そのままを知らせの経路に乗せた
- watch-notify.ps1 の Get-Note の型に raw を足した
- 控えの「そのまま:」の行から下は、見出しの振り分けをせず行をそのまま拾う。Trim も一行化もしない
- 拾った分は $r.raw に入れ、stop の本文の末尾へ「そのまま: 」を付けて足す
- 控えが古い（stale）ときは入れない。完了・ファイル・実測と同じ扱い

■ 同じ題の上書きを「中身が増えるときだけ」にした
- notify-record.ps1 に Rank-Body を新設。見出しの数（ファイル／実測／食い違い／答え／未決／そのまま）と字数で厚みを見る
- 同じ件名の行を消す（＝置き換える）のは、項目数も字数も減らないときだけ
- 減るときは上書きを断り、新しい行として足す。見張りが書く復帰や異常の一行もこれで消せない

■ ⑧が③へ流れ込むのを止めた（panel v23）
- ⑧を実際に流したら、⑧の中の見出しの無い行が「したこと」に出た
- sixLines の頭で body を「そのまま:」の位置で切り、head だけを③⑥と pick に渡す
- ⑧の取り出しは今までどおり body から。改行も字下げもそのまま

■ 確かめたこと
- Get-Note の写しに⑧を八行（空行・字下げ・「件名:」を含む）与えて、八行がそのまま raw に入った
- notify-record.ps1 の写しで三通り：⑧持ちを入れる→1行／同じ件名の薄い復帰→足されて2行／同じ件名の厚い件→薄い方だけ置き換わって2行
- panel.html の sixLines に同じ本文を通して、九行が 状態・したこと・版・ファイル・実測・食い違い・答え・未決・そのまま の順で出た。したことは「二点を直した」だけ
```

<!-- 送った知らせ ここまで -->



















































































































































---

## クロード連絡どき（2026-08-22 13:28 送信・確定）

宛先はチャット側のクロード。ユーザが受け取って渡す。そのまま写して送れます。

```
https://kfsr148-art.github.io/koushu-handan/
v1420

・一索の鳥にライフ制が付いた
・遊び方の説明を実際の動きに合わせた
・枝豆の印の絵が変わった
・横向きで画面が低いときに、次の局へ進む釦（連チャン／親流れ）が画面の下に隠れることがあったのを直した

判断待ちなし

触ってみて
```
