# 😽 が届かない件と、「版 —／最終確認 —」の件

**状態：終わり（この回そのものが 😽 の試しになります）** — 2026-08-25 21:4x 時点

**件1 の結論** — **断られていません。終わりの状態が一度も起きていませんでした。**
仕掛けは入っていますが、**通る機会が無かった**のが実際のところです。
**件2 は先の回で直して push 済み**です（panel v33）。原因は v32 ではなく **panel v18 からの持ち越し**でした。

---

# 件1. 😽 が届かない

## 記録で確かめたこと

**① ntfy へ出ていない。断られてもいない。**

変更を入れたのは **14:30 ごろ**です。それ以降の押し送りの断りは、次の一種類だけでした。

```
5件  押し送りはしない（この待ちでは既に送った）      ← ふつうのヨシ待ちの重複よけ
0件  押し送りはしない（送る状態でない: …）
0件  終わりの押し送りを落とした：… 上限（60件）
0件  押し送り失敗（HTTP …）
```

**終わりが理由の断りは一件もありません。**

**② そもそも「終わり」の状態になっていない。**

14:30 以降、鍵が移った先は次の二つだけでした。

```
5件  -> run:      （動作中）
9件  -> wait:     （ヨシ待ち）
0件  -> done:     ← 一度も出ていない
```

控えへ入れた題も、15:00 以降は **`🙋 ヨシしてください` が6件だけ**。`✅ 終わりました` は0件です。

**③ 今日いちばん最後に `done:` が出たのは 12:42:10** —— **変更を入れる約2時間前**でした。

```
2026-08-25 12:42:10  鍵=done:2026-08-25 12:41:33  …  最後の状態=stop
```

## なぜ終わりにならなかったか

判定は**ヨシ待ちが終わりより先**に置かれています。

```
} elseif ($note.ask -and -not $note.stale) {
  # 控えに「待ち」がある＝ヨシ待ち。どのフックが最後に鳴ったかに関わらず、これを優先する。
  $key = 'wait:' + $lastKindTs
```

**控えに「待ち」が書いてあると、stop が鳴っても「ヨシ待ち」が勝ちます。**
これは意図した順で、そうしないと「応答の終わりに鳴る stop が勝って、待っていても『終わりました』で出る」ためです。

そして作法25で **`待ち:` の行は必ず書く**ことにしているので、
**待つものがある限り、終わりの状態には入りません**。私は毎回ヨシ待ちで止まっていたので、
😽 の道は**一度も通らなかった**——それだけの話でした。

## 直したこと

**① 潜んでいた取り違えを一つ塞ぎました。**
押し送りが通ったときに立てる「この待ちでは送った」の印を、**ヨシ待ちのときだけ**立てるようにしました。

```powershell
# 直す前
$script:pushedWait = 1
# 直したあと
if ($state -eq 'wait') { $script:pushedWait = 1 }
```

終わりの押し送りでこの印を立てると、**同じ回に続くヨシ待ちが「既に送った」で断られます**。
いまは控え（`$keepPushed`）が `done:` の回に 0 へ戻すので表には出ませんが、
**その戻しに寄りかかった作り**でした。寄りかからないようにしました。

**② この回を、実際の試しにします。**
控えの `待ち:` を **`なし`** にして止まります。そうすれば鍵は `done:` になり、
**😽 が実際に飛ぶはず**です。実機に届いたかどうかを見てください。
届かなければ、記録に断りの理由が残ります（次の回でそれを読みます）。

＊これは**ごまかしではありません**。件1・件2とも手は済んでおり、残っているのは実機での見届けだけで、
　それは `未検収:` に並べてあります。**こちらから答えを待っている問いはありません**。

## 変更した行

| ファイル | 行 | 中身 |
|---|---|---|
| `~/.claude/watch-notify.ps1` | **556–560** | 押し送り済みの印を、ヨシ待ちのときだけ立てる |

＊見張りの台本に版の数字はありません。パネルのように三箇所そろえる仕掛けを持っていないためです。

---

# 件2. 「版 —／最終確認 —」（**先の回で直して push 済み・panel v33**）

**v32 の巻き添えではありませんでした。** `pullState()` の中で
**「連絡なし」の早戻りが `paintFacts()` より前**にあり、連絡なしの間だけ塗られずに
HTML の初期値が残っていました。`git show a4025ee:panel.html`（v30）で、
**v30 の時点で既に同じ順**（`paintFacts`=959行／早戻り=941行）だったことを確かめています。
入ったのは **panel v18**（`eefa76b`）です。

| 行（`panel.html`） | 中身 |
|---|---|
| **341–348** | `reFacts()`（版が届いたら、その場で塗り直す） |
| **354・355・359・360** | `pullVer()` の四つの落ちから `reFacts()` を呼ぶ |
| **1086–1097** | **`paintFacts` を「連絡なし」の早戻りより前へ移した**（本題） |
| **1112** | 元の呼び出しを削除（上へ移したので二重になる） |
| **230 / 251 / `panel-ver.txt`** | 版を **v33** へ |

**再現と確認**（`state.json` を50分前にして）

| | 直す前 | 直したあと |
|---|---|---|
| `#fVer` | **`版 —`** | `本体 v1426 ／ 公開 v1426` |
| `#fSeen` | **`最終確認 —`** | `最終確認 20:23` |

ついでに、開いた直後に「公開 v未確認」が最大15秒居座る取りこぼしも塞ぎました。

---

# 実機で見るところ

1. **😽 が届くか** — この回の終わりで飛ぶはずです。**「😽 返事パネル」**の通知が来たか
2. **返事パネルの版の字が `panel v33`**（v32 のままなら `?_v=33` を付けて一度だけ開く）
3. **機械の一行の下が「本体 v1426 ／ 公開 v1426」「最終確認 hh:mm」**で、横棒でないこと
4. **開いた直後から**「公開 v」に数字が出ていること
