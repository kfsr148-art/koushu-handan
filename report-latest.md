# パネルの札が出ない件 ／ ntfy を題の一行だけに

**状態：ヨシ待ち（実機で見てください）** — 2026-08-24 23:10 時点

## 結論：どちらも空になっていません

パネルが札を組み立てる元は **`notices.json`（リポジトリ側）** です。
**ntfy の本文は元から使っていません**（`panel.html:361-364` の注記のとおり、v10 で
読み物はリポジトリ経由へ一本化してあります）。ntfy の本文を全廃しても、パネルの
中身は減りません。

| 見たもの | 結果 |
|---|---|
| `notices.json` | **20件・最新 22:42** — 空ではない |
| 公開パネルを headless で描画 | **写す釦 21個**・札の時刻 22:42 / 22:19 / 22:16 — 出ている |

まっさらな端末（headless）では札が正しく並びます。**本体もリポジトリも正常でした。**

## 空に見えた本当の理由

v24 で「写した札は一覧から落とす」を入れ、その印（`localStorage`）は開き直しても
残る作りにしました。**全件を写すと一覧は空になり**、そのとき画面には
「まだ知らせがありません」と出ていました。控えには20件届いているのに、です。

壊れたようにしか見えないので、そこを直しました。

**panel v28** — `panel.html:376-406`

写した札を落とした結果0件になったが、元の件数が0でないとき：

- 「**◯件ぜんぶ写し済みです。新しい知らせが出れば、ここに並びます。**」
- ＋「**写した印を消して出す**」釦

釦を押すと印だけを消して出し直します。**控え（`notices.json`）には触りません。**

- 「押した直後に消える」動きはそのまま（ご指示3）
- 札の中身（件名・時刻・写す釦）も変えていません（ご指示3）

## 通知の本文とパネルの中身は、もともと切り離してあります（ご指示2）

`~/.claude/watch-notify.ps1` の `Send-Ntfy` は二つを別々に呼びます。

```
Record-Notice $asciiTitle $body   ← リポジトリ側。中身を全部渡す（変えていない）
Push-Ntfy     $state     $body    ← ntfy 側。$body は使わない（題の一行だけ）
```

この回の変更は `Push-Ntfy` の中だけで、`Record-Notice` には一切触れていません。

---

## ntfy を「題の一行だけ・記録を残さない」形に

| 項目 | 中身 |
|---|---|
| title | **😼 返事パネル** ← これだけ |
| message | U+200B（幅ゼロ空白。見えない） |
| priority | 4（高） |
| click | 返事パネルのURL（叩けば開く） |
| tags | 付けない |
| Cache | **no**（ntfy 側に記録を残さない） |

**届いた通知の見た目（実測）** — 題の行に「😼 返事パネル」だけ。本文の行は空。

**ntfy の癖（実測）**

- 本文なし・空白ひとつ → ntfy が `triggered` を勝手に入れる
- U+200B → 残る（Unicode の空白類ではないので削られない）
- `Tags` を付けると絵文字が題の頭へ足され、一行が二つの絵文字で始まる

**記録を残さない件（消える動きの実測）**

**`ntfy.sh` に「消す」口はありません**（`DELETE` は 404。実測）。溜めてから消すのでは
なく、`Cache: no` で**最初から溜めない**形にしました。

- 実測 … 本番の話題名で1件送り、前後の溜まり数を数えた。**21件 → 21件（増えず）**
- いま溜まっている21件は消せませんが、**12時間で自動的に消えます**
  （`expires − time` = 43200 秒。実測）。以後は増えません。
- 引き換えに、購読していない間に出た分は後から拾えません。気づかせるだけの道具なので
  構わない、という整理です。

## 送り口の判定を題から状態へ（ご指示7）

これまでは題が「🙋 ヨシしてください」でなければ断る形で、題を一行に統一すると効かなく
なるところでした。**送る側が状態を名乗り、`'wait'` だけが通る**形へ書き換えました。
名乗らなければ送りません（既定を「送らない」側へ倒してあります）。

| 場所 | 中身 |
|---|---|
| `watch-notify.ps1:372-383` | `Push-Ntfy([string]$state, [string]$body)` |
| `watch-notify.ps1:431-436` | `Send-Ntfy` の第5引数で状態を通す（既定は空＝送らない） |
| `watch-notify.ps1:765-768` | 鍵の頭から状態を取る（`wait:` だけが `'wait'`） |
| `watch-notify.ps1:790` | 出し直しにも `'wait'` を渡す |

**実測**（捨て話題名。本番の端末は鳴らしていません）

| 状態 | 送った |
|---|---|
| ヨシ待ち（1回目）`wait` | **1 件** |
| ヨシ待ち（2回目）`wait` | 0 件 ← 同じ待ちで二度送らない |
| 終わり `done` | 0 件 |
| 異常・手が要る `dead` | 0 件 |
| 異常・待てば戻る `stale` | 0 件 |
| 生存 `heartbeat` | 0 件 |
| 定期配信 `usage` | 0 件 |
| 名乗りなし（空） | 0 件 |

`REMIND_MAX = 0`（出し直さない）／ntfy 側に溜まった件数 **0 件**。

## 変更した行番号

| ファイル | 行 |
|---|---|
| `panel.html` | 376-406（全部写し済みの受け）／193, 213（版） |
| `panel-ver.txt` | 28 |
| `~/.claude/watch-notify.ps1` | 357-362（題と行き先）／385-413（一行だけ・Click・Cache: no）／372-383, 431-436（判定を状態へ）／765-768, 790（鍵から状態） |

## 実機で見ていただきたいところ

1. 返事パネルの画面下の版の字が **panel v28**
2. 一覧に札が並ぶ（件名・時刻・写す釦）
3. 全部写し済みなら「**◯件ぜんぶ写し済みです**」と「**写した印を消して出す**」釦が出る
   （「まだ知らせがありません」とは出ない）

## いまの版

本体 **v1426** ／ stable **`0250108`** ／ panel **v28**。本体は触っていません。

> 本日の押し送りは既に上限20件へ達していたため、確認のために枠を空けて2件送りました。
> 本日の実送信は22件（上限20＋確認の2件）。件数の記録はいま20件に戻してあります。
