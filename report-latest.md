# 生存記録の件は一区切り — 丙を検収いただきました

**状態：終わり（一区切り）** — 2026-08-25 05:25 時点

丙（甲・乙）と足跡の時刻入れを検収いただきました。heartbeat 側を10分へ上げた判断も
そのままでよいとのご裁定です。

> **次に穴が開いたときは、足跡の道筋をそのまま報告します。** 要約せず、`watch-notify.log` の
> 「前の回は途中で終わっていた」の二行をそのまま貼ります。段と滞在秒が判断の材料なので、
> こちらで噛み砕くと材料が減るためです。

## 入れたもの（検収済み）

| | 中身 | 実測 |
|---|---|---|
| **甲** | 実行の上限 3分 → **10分** | `ClaudeWatchNotify` PT3M→PT10M ／ `ClaudeHookHeartbeat` PT2M→PT10M |
| **乙** | `git push` を **20秒で見切る** | 届かない宛先で **20.1秒で戻った** |
| **足跡** | 一行上書き → **一回ぶんの道筋** | どの段に何秒居たかが並ぶ |

### 甲：実行の上限を10分へ

`ClaudeHookHeartbeat` も一緒に上げてあります。**生存記録を書いているのはこのタスク**で、
2分のままでは穴が残るためです（01:08 と 01:18 を落としていました）。

### 乙：`git push` を20秒で見切る（`watch-notify.ps1:963-984`）

`Start-Process` で切り離して起こし、20秒で終わらなければ切ります。切ったときは控え
（`usage-push` / `usage-key`）を更新しないので、**次の回がそのまま出し直します**。
終了コードが0でないときも同じ扱いです。

段の中で時間の上限が無いのはここだけでした（`curl` には `--max-time 25` がありました）。

### 足跡：一回ぶんの道筋（`watch-notify.ps1:129-175`）

各行は **時刻＋段の名前＋前の段からの秒数**。「開始」で書き起こし、以降は足していきます。

平常時の道筋（実測）

```
05:07:16  開始                 +0秒
05:07:16  窓とプロセスを数えた  +0.5秒
05:07:16  hook.log を読んだ    +0.1秒
05:07:17  控えを読んだ          +0.1秒
05:07:17  知らせの判定を終えた  +0.1秒
05:07:17  使用量を終えた        +0.1秒
05:07:17  完了                 +0.1秒
```

殺された回を模したときに、次の回が書くもの（実測）

```
前の回は途中で終わっていた → 2026-08-25 04:52:41 / 知らせの判定を終えた / +0.4秒（その段のまま 16分）
  前の回の道筋: 開始+0秒 → 窓とプロセスを数えた+0.6秒 → hook.log を読んだ+0.1秒
                → 控えを読んだ+159秒 → 知らせの判定を終えた+0.4秒
```

「控えを読んだ」に159秒かかったことが一目で読めます。正常に終わった回では出ません。

## 分かっていること・分かっていないこと

**分かったこと** — 重い作業が走ると、毎分の見張りが実行時間の上限に届きます。
実測で `node check-all --fast`（中で headless Edge を何度も起こす）の最中、1回ぶんの所要が
**2.6秒 → 68秒（26倍）** になり、状態行は最大1分49秒ずれ、一分が丸ごと欠けました。
二度の穴（23:38〜23:56／01:01〜01:25）は、どちらもこちらが検査・headless Edge・git を
回していた時間帯にすっぽり入っていました。

**分かっていないこと** — **27分の穴そのものは再現できていません。** 仕組みの向きは掴めましたが、
そこに至る組み合わせまでは詰め切れていません。次に開いたときは、足跡の道筋がそのまま材料に
なります。

## 残していること

- **タスクスケジューラの動作記録の有効化**（管理者の窓で一行。下記）
- `inbox-watch.ps1` の 4100 警告（16秒おき）— 別件として後で
- 穴の引き金そのものの特定

```powershell
Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-Command','wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true; wevtutil gl Microsoft-Windows-TaskScheduler/Operational | Select-String enabled; Read-Host "Enterで閉じます"'
```

窓に「**enabled: true**」と出れば成功です。有効にすると起動と終了コードが全て残り、
**足跡の道筋と併せて二本立て**で辿れます。

## いまの版

本体 **v1426** ／ stable **`f3837e8`** ／ panel **v29**。

**stable は据え置きです。** この回に触ったのは `~/.claude` 配下でリポジトリの外なので、
進める対象がありません。本体とパネルは触っていません。
