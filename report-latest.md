# 動作記録の確認 ／ watch.state が変わったときも押し出す

**状態：終わり** — 2026-08-25 08:05 時点

## 1. 動作記録 — 読み直しました

| 読み方 | 値 |
|---|---|
| `wevtutil gl` | `enabled: true` |
| レジストリ `WINEVT\Channels\…\Enabled` | `1` |
| `Get-WinEvent -ListLog` | `IsEnabled: True` |

**溜まり始めています。** 直近30分で **55件**。

| id | 中身 | 件数 |
|---|---|---|
| 100 | タスクを始めた | 8 |
| 102 | タスクが終わった | 8 |
| 200 | 動作を始めた | 8 |
| **201** | **動作が終わった（リターン コードつき）** | **8** |
| 129 | 処理を起こした | 8 |
| 107 | 時刻の引き金 | 6 |
| 119 | 起動の要求 | 2 |
| 322 | 既に走っているので飛ばした | 7 |

> 322 が7件あるのは、前の回がまだ走っている間に次の引き金が来た分です。多重起動は
> `IgnoreNew` なので捨てられます。**これは正常な姿**で、穴のときはここが連続します。

## 2. 押し出しの条件に「状態が変わったとき」を足しました

| | 条件 |
|---|---|
| これまで | 値が動いたとき ／ 30分経ったとき |
| これから | それに加えて **`watch.state` が変わったとき** |

| 場所 | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1:890-894` | `usage-pub-state.txt`（最後に押し出した状態） |
| `~/.claude/watch-notify.ps1:1005-1023` | 判定に `$stateMoved` を足した |
| `~/.claude/watch-notify.ps1:1040-1044` | 押し出したら状態も控える |

**三つの枝を実測で確かめました**（記録に出た文そのまま）

```
値が動いた   使用量：HTTP 200 push した（値が動いた：…／手待ち）
状態が動いた 使用量：HTTP 200 push した（状態が 作業中 → 手待ち へ動いた：…／手待ち）
どちらも無し 使用量：HTTP 200。値も状態も動いていないので push しない（…／手待ち）
```

## 3. 増える回数の見込み（実測から）

直近 **24.0時間**の状態行 **1142本**を、使用量の取得と同じ **10分刻み（117点）** で見ました。

| | 一日ぶん |
|---|---|
| いまの押し出し | **37 回** |
| 状態が変わった回数 | **47 回** |
| **足したあとの見込み** | **約 84 回** |

- 重なり（同じ回で値も状態も動いた分）を差し引いていないので、**上振れの見積もり**です。
- **取得そのものは10分おきのままなので、押し出しは一日 144 回を超えません。**

直近の移り変わり（10分刻みで見たとき）

```
05:24:55  ヨシ待ち → 手待ち
06:38:48  手待ち → 作業中
07:27:28  作業中 → ヨシ待ち
08:01:01  ヨシ待ち → 手待ち
```

## 4. 07:28 の「画面が2枚から1枚」

**解消しています。** 07:28:19 を最後に同じ記録は出ておらず、以後の状態行は
`プロセス=1 窓=1` のまま安定しています。

## いまの版

本体 **v1426** ／ stable **`f3837e8`** ／ panel **v29**。本体もパネルも触っていません。
