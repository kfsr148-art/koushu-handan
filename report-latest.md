# 郵便受けの五件 — 待ち札の修理と、観察三件の読み直し、較正-1の続き

**状態：終わり（VAIO）／残り0件**

**結論** — 五件すべて済みました。**いちばん大きい発見は二．です。「2.1%→1.9%」は改善ではありません**でした。分母のせいでもありません。**前回と今回で欠けを数える関門が違っていた**（750秒 → 15分）だけで、**関門を揃えると 2.14% → 2.68% と、むしろ上がっています**。ただしこの差は誤差の範囲（z=0.57）で、**下がったとも上がったとも言えない**、というのが正確なところです。

`y0829-start` は閉じました。

---

# 一．待ち札の件名と待ち本文の食い違い（郵便-1）

## 何が起きていたか

10:08 の札は、こう出ていました。

```
起こす経路の回帰試し（wake-test.txt への時刻書き込み）   ← 件名は新しい
印: y0829-start
待っているのは：本日期日の観察三件…から着手してよいか   ← 待ち本文は 10:00 のまま
```

原因は**二つあり、両方を塞ぎました**。

## (1) 一覧の行が一度も書き換わらなかった — `watch-notify.ps1`

印を積む処理は、**同じ印が既に一覧にあれば行を一切触らない**作りでした。

```
以前： $seen が真なら、件名も待ち先も書き換えずに素通り
```

控えに `印:` を書いて件を切り替えると、`y0829-start` の行は最初に立てた 10:00 の件名と待ち本文のまま残ります。**裸のヨシは一覧のいちばん新しい行へ落ちる**ので、そこが古い待ちのままだと、ヨシが古い件へ落ちます。

**直した箇所 … `watch-notify.ps1` L1160〜L1183**（同じ印を立て直した回は、件名と待ち先をいまの姿へ差し替える。立てた時刻＝二つ目の欄は動かさない。積んだ順が入れ替わってしまうため）

写しで**6通り**確かめました。

| 場合 | 結果 |
|---|---|
| 新しい印 | 積む（今までどおり） |
| 同じ印・件名も待ちも同じ | 触らない |
| **同じ印・件名だけ変**（事故の形） | **差し替える** |
| 同じ印・待ちだけ変 | 差し替える |
| 四つ目の欄が無い古い行 | 四欄へ直す |
| 別の印の行 | 触らない |

## (2) 控えの側で止めていなかった — `stop-guard.ps1`

そもそも待ち本文が古いまま**控えに書かれていた**のが元です。関門は「この回に書き直したか」を控え全体の指紋で見ており、**件名だけ変えれば指紋は変わる**ので通っていました。

**直した箇所 … `stop-guard.ps1` L64〜L105**（控えの `印:` が指す行を一覧から引き、**件名だけが変わって待ち本文が一字も動いていなければ、終了コード2で差し戻す**）

写しで**7通り**確かめました。

| 場合 | 期待 | 実測 |
|---|---|---|
| **件名変・待ち同じ**（事故の形） | 差し戻す | **2** ✓ |
| 件名変・待ちも変 | 通す | 0 ✓ |
| 件名同じ・待ち同じ（続きの回） | 通す | 0 ✓ |
| `待ち: なし` | 通す | 0 ✓ |
| `印:` の行が無い | 通す | 0 ✓ |
| 一覧に無い印 | 通す | 0 ✓ |
| 差し戻し済み（堂々巡り防止） | 通す | 0 ✓ |

**行数と構文** … `watch-notify.ps1` 1743→1762行、`stop-guard.ps1` 93→137行。**両方とも構文エラー0・BOMあり**、写しは `.bak-20260829`。作法28 のとおり Edit の道具で置換し、膨らみは起きていません。

---

# 二．欠け率を区間で切り直す（郵便-2）

## お尋ねへの答え — **読みは正しいが、理由が違います**

「改善ではない」という**結論は正しい**。ただし**分母が増えた見かけ**ではありません。真因は**関門の取り違え**です。

控えの測り方（`heartbeat-gap-followup` の記録）は **`g>750`（750秒＝12.5分）**でした。前回の報告はこれを **15分**で数えていました。**同じ材料でも関門が違えば別の数**になります。

## 関門を起点と同じ750秒に揃える

| 区間 | 区間数 | 欠け | 率 |
|---|---|---|---|
| **起点まで**（〜08-26 18:58・生存795本目） | 794 | 17 | **2.14%** |
| **起点より後** | 373 | 10 | **2.68%** |
| 全域（08-21〜08-29） | 1167 | 27 | 2.31% |

**起点の「795本中17回＝2.1%」は、この関門で実測と一字一句合いました**（17/794＝2.14%）。数え方の再現が取れたので、比較の土台は確かです。

## 関門ごとの見え方（同じ材料）

| 関門 | 全域 | 起点まで | 起点より後 |
|---|---|---|---|
| **>750秒（起点の測り方）** | 2.31% | 2.14% | **2.68%** |
| >15分（前回の報告） | 1.89% | 1.86% | 1.94% |

**前回の「1.9%」は、起点の「2.1%」と並べてはいけない数**でした。

## 日ごと（関門750秒）

| 日 | 区間 | 欠け | 率 |
|---|---|---|---|
| 08-21 | 120 | 4 | 3.33% |
| 08-22 | 144 | 0 | 0.00% |
| 08-23 | 138 | 4 | 2.90% |
| 08-24 | 140 | 3 | 2.14% |
| 08-25 | 140 | 4 | 2.86% |
| 08-26 | 141 | 4 | 2.84% |
| 08-27 | 142 | 5 | 3.52% |
| 08-28 | 140 | 3 | 2.14% |
| 08-29 | 62 | 0 | 0.00% |

**傾きはありません。**直した 08-26 の前後で線を引いても、日ごとに並べても、2〜3%のあたりを行き来しているだけです。

## 統計として

起点まで 17/794 = 2.14% ／ 起点より後 10/373 = 2.68% を二標本で比べると **z = 0.57**。**|z| < 1.96 なので差は誤差の範囲**です。**下がっても上がってもいない**、が正確な言い方になります。

## 最長の穴の並び（関門750秒・全27件のうち上位10）

| | 穴 | 長さ | 落ちた本数 | 区間 |
|---|---|---|---|---|
| 1 | 08-21 19:28 → 20:08 | **40分** | 3 | 起点まで |
| 2 | 08-27 22:08 → 22:41 | 34分 | 2 | 起点より後 |
| 3 | 08-28 11:54 → 12:26 | 32分 | 2 | 起点より後 |
| 4 | 08-26 21:48 → 22:18 | 30分 | 2 | 起点より後 |
| 5 | 08-23 13:08 → 13:38 | 30分 | 2 | 起点まで |
| 6 | 08-25 00:58 → 01:28 | 30分 | 2 | 起点まで |
| 7 | 08-25 05:28 → 05:58 | 30分 | 2 | 起点まで |
| 8 | 08-24 10:49 → 11:18 | 29分 | 2 | 起点まで |
| 9 | 08-28 02:29 → 02:58 | 29分 | 2 | 起点より後 |
| 10 | 08-23 23:30 → 23:58 | 28分 | 2 | 起点まで |

**最長の穴も改善していません。**起点より後にも 34分・32分・30分・29分が出ています。

## 退避先（`hook-fallback.log`）の3本

3本のうち**実際に穴を塞いだのは1本だけ**でした。

| 退避した生存 | 挟まる穴 | 効いたか |
|---|---|---|
| 08-26 19:54:43 | 19:48 → 19:58（10分） | 穴ではなかった |
| **08-26 20:38:08** | 20:28 → 20:48（20分） | **欠けを1件消した** |
| 08-27 05:08:04 | 05:04 → 05:18（14分） | 穴ではなかった |

退避の仕掛けは**効いていますが、効いた回数が少なすぎて率を動かしていません**。

---

# 三．逆向きの疑い1件の中身（郵便-3）

記録は全期間で**この1行だけ**です（`watch-notify.log` L2667）。

```
2026-08-28 19:19:51  ★逆向きの疑い：鍵は wait:y0828-holdtest（止まっている扱い）なのに CPU 10.7%。最後のフックは resume 2026-08-28 19:18:36
```

## 何がどう「逆」だったか

**鍵は「止まっている」と言い、CPU は「働いている」と言った。**その食い違いです。前後の記録を並べると、どちらも正しかったことが分かります。

`hook.log`（L4053〜L4058）

```
2026-08-28 19:18:36  resume        押し通知=なし  タイトル=OK  繋ぎ=親  自窓=触らず  戻し=無  題[◐ Koushu-handan mahjong]
2026-08-28 19:18:36  resume        画面=出ず  取っ手=328156／可視  戻し2=無  画面の題[◑ Koushu-handan mahjong]
2026-08-28 19:20:53  stop-guard    控えあり（この回に書いた・待ちの行あり）。通す
2026-08-28 19:20:53  inbox-feed    stop 未処理なし
2026-08-28 19:20:54  stop          押し通知=なし  タイトル=OK  繋ぎ=親  自窓=触らず  戻し=無  題[◑ Koushu-handan mahjong]
2026-08-28 19:20:57  stop          画面=出  取っ手=328156／可視  戻し2=無  画面の題[●終わり ✳ Koushu-handan mahjong]
```

`watch-notify.log`（L2656〜L2668）

```
2026-08-28 19:18:48  開いている印へ積んだ：y0828-holdtest（一覧は 4 件）
2026-08-28 19:18:48  待たせずに出す [🙋 ヨシしてください]
2026-08-28 19:19:04  待たせずに出す [🙋 ヨシしてください]
2026-08-28 19:19:26  鍵 run:2026-08-28 19:14:18 -> wait:y0828-holdtest （プロセス1本・窓1・迷子0）
2026-08-28 19:19:50  鍵 run:2026-08-28 19:14:18 -> wait:y0828-holdtest （プロセス1本・窓1・迷子0）
2026-08-28 19:19:51  ★逆向きの疑い：鍵は wait:y0828-holdtest（止まっている扱い）なのに CPU 10.7%。最後のフックは resume 2026-08-28 19:18:36
2026-08-28 19:21:33  鍵 wait:y0828-holdtest -> done:2026-08-28 19:20:57 （プロセス1本・窓1・迷子0）
```

## 読み

| 時刻 | 何が起きたか |
|---|---|
| 19:18:36 | `resume` — **応答が始まった**（題は ◐ ＝働いている印） |
| 19:18:48 | 控えに `待ち:` と `印: y0828-holdtest` が書かれ、見張りが札を立てた |
| 19:19:26 | 見張りが鍵を `run:` → **`wait:`** へ倒した |
| **19:19:51** | **逆向きの疑いが記録された（CPU 10.7%）** |
| **19:20:57** | **`stop` がやっと鳴った** — 応答が実際に終わったのはここ |

**控えは応答の途中で書かれます。**待ちの行を書いてから `stop` が鳴るまで、この回は **2分ちょうど**ありました。見張りは「控えに待ちがあれば、どのフックが最後かに関わらずヨシ待ちとする」（`watch-notify.ps1` L1087）という決まりで動くので、その2分間は**鍵が `wait:`、窓は現に働いている**という状態になります。

**故障ではありません。**CPU の読み（10.7%）も正しく、鍵の決め方も設計どおりです。二つが**別々の瞬間を指している**だけで、`stop` が鳴った時点で自動的に揃います。

**残る影響**は、その2分間だけ状態行とパネルが「ヨシ待ち」と出ること、札が少し早く出ることの二つです。どちらも自然に直ります。

---

# 四．送らなかった回の中身（郵便-4）

## まず数の訂正 — **24回ではなく31回**でした

前回の「24回」は取りきれていませんでした。`watch-notify.log` 全体で **31回**、観察を立てた 08-26 20:50 より後に限ると **29回**です。

「名乗りなし」は、押し送りの関数が**状態を渡されずに呼ばれた**ことを指します。送ってよい状態は `wait`／`done`／`over` の三つだけで、それ以外は名乗りなしも含めて全部断る作りです（`watch-notify.ps1` L563-566）。

## 件名で並べた内訳

| 題 | 件数 | 中身 |
|---|---|---|
| （空） | **14** | 溜めた知らせをまとめて出すときに題が空になっていた。**08-29 08:39 の「溜めの空出し」の直しで解消済み**（以降は題が入っている） |
| 🪟 異常です（手が要ります） | **6** | 下に一件ずつ |
| Claude Code watch (test) | 5 | 試験用の知らせ。送らなくてよい |
| 🔎 調べました | 3 | 元から押し送りの対象外 |
| ✅ 終わりました（返事不要） | 3 | 落ちから立ち直った直後の一本。別経路で `done` として送られている |

日ごとでは 08-26=6／08-27=1／08-28=16／08-29=8 でした。

## 人を呼ぶべきだった件が混ざっていないか — **0件です**

判断が要るのは「異常です（**手が要ります**）」の6件だけなので、全件を追いました。

| # | 時刻 | 何が起きたか | 立ち直り | 人の手は要ったか |
|---|---|---|---|---|
| 1 | 08-26 13:25 | 生存が古い（stale） | **3分**で復帰（13:28） | 不要 |
| 2 | 08-26 18:53 | 生存が古い（stale） | **5分**で復帰（18:58） | 不要 |
| 3 | 08-26 22:25 | 落ち（dead・プロセス0本） | **9分**で復帰（22:34） | **再起動の稽古の最中**。人は目の前にいた |
| 4 | 08-26 22:36 | 落ち（dead・プロセス0本） | **1分**で復帰（22:37） | 同上 |
| 5 | 08-27 09:23 | 画面が見えなくなった（2→1） | **24秒**後に `done`、窓1本で継続 | 不要 |
| 6 | **08-28 03:01** | 落ち（dead・最後のフックは39分前） | **25分**で復帰（03:27） | 不要（常駐が起こし直した） |

**6件すべて自力で立ち直っており、人を呼ぶべきだった件はありません。**3と4は再起動の稽古そのもので、むしろ鳴らないほうが正解でした。いちばん際どい6番（深夜3時・25分の落ち）も、手待ちの常駐が自分で起こし直しています。

## ただし、残る危うさは名指ししておきます

**「異常です」は設計として押し送りしません**（`watch-notify.ps1` L562 の注記）。今回は6件とも自力で直りましたが、**本当に自力で戻れない落ち方をしたときも、同じように鳴りません**。パネルの一覧には載るので、見に行けば分かります。

控えの決まり（「止まるときは必ず押し通知を出す — 完了・ヨシ待ち・エラー・判断待ち、すべて」）とは、ここが噛み合っていません。**いまは凍結中なので直していません。**鳴らす側へ倒すかは、決めていただく件として置きます。

---

# 五．較正-1の続き — borderline が 956対2 になる出どころ（郵便-5）

## 数の確認

| borderline | verdict | 件数 |
|---|---|---|
| 0 | defend | 2059 |
| 0 | attack | 983 |
| **1** | **attack** | **956** |
| **1** | **defend** | **2** |

## borderline を立てる場所は二箇所しかない

`koushu-handan.html` L3034〜L3047。**この二行が全てです。**

```js
3034	let verdict, edgeFlag = false;
3035	if(shapeOK){
3036	  const narrowDefend = acceptTiles < (isDealer ? 6 : 8) && doraValue <= 1;
3037	  verdict = narrowDefend ? 'defend' : 'attack';
3038	  if(narrowDefend) edgeFlag = true;     // ← borderline=1 かつ defend は、ここだけ
3039	} else {
3040	  const wideAttack = acceptTiles >= (isDealer ? 20 : 24) && doraValue >= 1;
3041	  verdict = wideAttack ? 'attack' : 'defend';
3042	  if(wideAttack) edgeFlag = true;       // ← borderline=1 かつ attack は、ここだけ
3043	}
3045	if(kokushiSeen){ verdict = 'attack'; edgeFlag = false; }
3047	if(yakumanReach){ verdict = 'attack'; edgeFlag = false; }
```

**二つは `if`／`else` で分かれているので、混ざりません。**`shapeOK` が真なら L3038 しか通らず、偽なら L3042 しか通りません。したがって **956対2 は、この二つの条件それぞれの発火率そのもの**です。

## 発火率を実測する

| 枝 | 母数 | 幅の関門を通った数 | ドラの条件も満たした数（＝borderline） |
|---|---|---|---|
| L3038 `narrowDefend`（shapeOK=1） | 978 | 2（**0.20%**） | **2（0.20%）** |
| L3042 `wideAttack`（shapeOK=0） | 3022 | 2727（**90.24%**） | **956（31.63%）** |

**実測が 2 と 956 にぴたり合います。**分岐の読みは確定です。

## なぜこうなるか — **関門が配牌の受け入れ枚数と桁が合っていない**

配牌13枚での `acceptTiles` の分布を測りました。

```
最小 6 / 5%点 18 / 25%点 28 / 中央 57 / 75%点 73 / 95%点 91 / 最大 123
```

この分布に、二つの関門を重ねます。

| 関門 | 当たる割合 |
|---|---|
| `narrowDefend`：受け入れ **< 8枚**（子） | 全体の **0.05%** |
| `wideAttack`：受け入れ **>= 24枚**（子） | 全体の **85.38%** |

**片方は下限の外、もう片方は下限のすぐ上にあります。**受け入れが8枚を切る配牌はほぼ存在せず（4000件中2件）、24枚を超える配牌はほぼ全部です。

理由は**向聴です。**

| 向聴 | 件数 | 受け入れ平均 |
|---|---|---|
| 1向聴 | 17 | 14.6枚 |
| 2向聴 | 410 | 26.4枚 |
| 3向聴 | 1421 | 42.4枚 |
| 4向聴 | 1586 | 62.0枚 |
| 5向聴 | 539 | 77.9枚 |

配牌は**3〜4向聴が大半**（4000件中3007件）で、受け入れは40〜60枚が普通です。**8枚や24枚という関門は、1向聴前後の手（受け入れ10〜20枚台）を想定した目盛り**で、配牌に当てると桁がずれます。

## 結論

**攻めへ寄っているのは判定の偏りではなく、関門の置き場所です。**

- `narrowDefend`（L3038）は**事実上の死に枝**。4000件中2件しか通らない
- `wideAttack`（L3042）の**幅の条件は効いていない**。90%が素通りする
- 結果、borderline は**実質「ドラを1枚以上持っているか」だけで割れている**（幅の関門を通った2727件のうち、ドラありは956件＝35.1%）

**紙一重の印が、幅ではなくドラの印になっていました。**

## 参考までに、実際に効いた2件

```
行1032  局258 席1 子  13456m12377p123s   受け入れ2種6枚   ドラ0  良形5/必要4
行1808  局452 席1 子  234m12355p1s3355z  受け入れ3種6枚   ドラ1  良形5/必要4
```

どちらも**受け入れ6枚**という極端に狭い1向聴で、関門（8枚）を割った唯一の形です。

## 手は付けていません

目盛りの見直しは**本体（`koushu-handan.html`）の工事**なので、作法29 の②に当たります。**この報告を読んでいただいてから**、別の件として立てます。台帳の `積み残し-1` にも同じ趣旨で書いてあります。

なお、いまの目盛りでの全体は **攻 1939（48.5%）／守 2061（51.5%）** で、攻守の割合そのものは偏っていません。

---

# 六．残り

**残り0件。**

| 番号 | 状態 |
|---|---|
| 凍結-0 | 発効中（見張り・通知・台帳の凍結。今回の二本は「修理」として実施） |
| 再起動-1／-2 | 断線-5 へ統合済み（消していない） |

未了は**0件**です。

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
中断の後始末と、観察三件の報告
(1)後始末＝壊れている物は無し。台本九本すべて構文OK・BOMあり・関数の重複0、常駐は1本（PID 9040・起動 09:06:33＞台本 09:00:52）で直しは効いている。未着だったのは記録だけで、台帳へ 即時性-1 を済で追加し報告書を追いつかせた。回帰の実測を一本取った（受信箱→着手 30秒）。(2)観察-1＝欠け率 2.1%→1.9%。(3)観察-2＝08-26=40・08-27=19・08-28=54・08-29=6。(4)観察-3＝0件→1件。観察三件を済で閉じた。
ファイル: ~/.claude/orders-open.tsv／report-latest.md
実測: 回帰 10:06:26→10:06:56＝30秒（見張りが 10:06:33 に増分を見つけ 10:06:35 に起こした）。三種の即時性は 09:02:53／09:02:58／09:03:03 に5秒間隔で別々の札として控えへ載り、まとめられていない。生存の欠け＝全域1167本中22回（1.9%）・観察を立ててから360本中7回（1.9%）・本日61本中0回。最長の穴は 08-27 22:08→22:41 の34分。押し送り＝08-25:9／08-26:40／08-27:19／08-28:54／08-29:6（10:08時点）、送らなかった24回はすべて「名乗りなし」。逆向きの疑い＝1件（08-28 19:19:51、resume の75秒後）。
```

### 2. Claude Code watch (test)

```
後始末と観察三件が終わりました。壊れている物は無く、中断した回の直しは全て入っていました（未着は記録だけ）。回帰の実測は受信箱→着手30秒。観察-1 生存の欠け率 2.1%→1.9%（本日0回）。観察-2 押し送り 08-26:40／08-27:19／08-28:54／08-29:6。観察-3 逆向きの疑い 0件→1件（08-28 19:19、resume の75秒後）。未了は0件になり、残りは凍結-0（発効中）だけです。
```

### 3. 🙋 ヨシしてください

```
起こす経路の回帰試し（wake-test.txt への時刻書き込み）
印: y0829-start
待っているのは：本日期日の観察三件（観察-1・観察-2・観察-3）から着手してよいか、別の件を先にするか
答え方：「y0829-start にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
```

### 4. 🙋 ヨシしてください

```
窓の立ち上げ。残り4件の申告と、報告書の積み残しの片付け
印: y0829-start
待っているのは：本日期日の観察三件（観察-1・観察-2・観察-3）から着手してよいか、別の件を先にするか
答え方：「y0829-start にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
```

### 5. 🕒 延びています

```
三種の即時性の実測と、ヨシ待ち中に取り込みが止まる穴の修理
終了予定09:50を過ぎています（経過51分）
```

<!-- 送った知らせ ここまで -->

