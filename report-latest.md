# 7:16〜7:17 の状態の食い違い — 原因を特定して直した

**状態：終わり** — 2026-08-26 07:2x 時点

**結論** — **黒い窓が正しく、見張りの側が嘘をついていました。**
何もしていないまま**10分**が過ぎると、見張りが**勝手に「作業中」へ変わる**作りでした。
07:15:12 に実際にそれが起きています。**直しました。**
**キューに残っているものはありません**（受信箱の未処理は0件）。

---

## ① その時刻に実際どうだったか — **止まっていました**

| 見る先 | 07:16〜07:17 の中身 |
|---|---|
| `hook.log`（フックが鳴った記録） | 最後は **notification 07:04:44**。次は **resume 07:18:43** まで無い |
| `usage.json` の `watch`（07:14:08 に書かれた） | `state: 手待ち` ／ `lastStop: 07:03:42` ／ `lastResume: 07:02:45` ／ **`cpu: 0.7%`** |
| `watch-status.log` | 07:14 まで `idle:` ／ **07:15 から `run:`** |
| `state.json`（`inbox-watch.log` の書き出し） | 07:04:34 手待ち → **07:15:12 作業中** → 07:19:30 手待ち |

**機械が測った値は「止まっている」と言っています。**
`lastStop`（07:03:42）が `lastResume`（07:02:45）より新しく、**CPU は 0.7%**。
何も走っていません。

**三つの見え方の答え合わせ**

| 見え方 | 判定 |
|---|---|
| **黒い窓＝「終わり」で手待ち** | **正しい** |
| **ウィジェット＝7:14:08 の「待機中・claude 1本」の緑** | **正しい**（ただし2〜3分古い値） |
| **Code タブ＝処理中に見える** | **こちら側には、その時刻に走っていた形跡がありません** |

## 原因 — 10分でひとりでに「作業中」へ変わる

見張りの分岐は、上から順に「フックが**新しいこと**」を条件に持ちます
（`$kindFresh = $kindAge <= $FRESH_MIN`、**`$FRESH_MIN = 10` 分**）。

**ただ古くなっただけの `stop` / `notification` は、どの条件にも当たらず落ちどころへ流れます。**
その落ちどころが**一律 `run:`（＝作業中）**でした。

```
07:04:44  notification が鳴る
07:14:44  10分が過ぎて $kindFresh が false になる
07:15:03  どの分岐にも当たらず run: へ落ちる → 「作業中」
07:15:12  state.json にも「作業中」が書かれる     ← ウィジェットとパネルはこれを読む
07:19:30  次のフックで「手待ち」に戻る
```

**約4分半のあいだ、機械だけが「作業中」と言っていました。**

## 直したこと — 位相は「最後に鳴ったフック」が決める

古くなったことは**位相を変えません**。resume で始まり、stop / notification で止まる。それだけです。

```powershell
if ($lastKind -eq 'resume') { $key = 'run:'  + $lastKindTs }
else                        { $key = 'idle:' + $lastKindTs }
```

**実測（四通り）**

```
resume        → run:      （動いている）
stop          → idle:     （止まっている）  ← 直す前は run: だった
notification  → idle:     （止まっている）  ← 直す前は run: だった
（フック無し） → idle:                      ← 直す前は run: だった
```

古さは「**知らせを出すかどうか**」の側（`$kindFresh`）で既に効いているので、二重には効かせません。

＊見張りの要に手を入れたので、作法29 の四段を踏みました——
　**写しを取り**（`watch-notify.ps1.bak-20260826`・1272行）→ 直す → **構文検査 PASS** → そのまま。

## ② Code タブが「処理中」で、こちらが「終わり」を書く場面はあるか

**あります。** 二つは**別のものを見ています**。

- **Code タブ** … アプリ側の**キューと配信**の様子（雲の側）
- **こちらのフック** … **この端末のこの回**の始まりと終わり

**キューに積まれた指示が、まだこの端末へ届いていない**あいだ、
アプリは「処理中」に見え、こちらは既に止まっています。**07:04:44 に止まり、07:18:43 に resume**
——**約14分**、指示は届いていませんでした。この帯がそのまま「食い違い」に見えます。

**どちらが正しいかを判じる材料**（`usage.json` の `watch`）

| 材料 | 意味 |
|---|---|
| **`cpu`** | **いちばん強い材料。** 0〜1%なら、こちらでは何も走っていない |
| `lastStop` と `lastResume` | 新しい方が位相。`lastStop` が新しければ止まっている |
| `noteAt` | 控えを書いた時刻。`lastResume` より古ければ、控えは前の回のもの |

**07:14:08 の値は `cpu 0.7%` ／ `lastStop` が新しい。** 止まっていた、が答えです。
＊**返事パネルの「機械が見た様子」の一行**が、まさにこれを出します（v32 で入れたもの）。
　控え由来の状態と食い違えば、**両方を並べて**出します。

## ③ キューに残っているもの — **ありません**

```
inbox.txt   未処理 [ ] 0 件 ／ 済み [x] 24 件
```

**35分前の指示も残っていません。** 07:18:43 の resume で届き、その回で処理しています
（07:19:04 に stop）。**溜まっているものはゼロです。**

＊`inbox.txt` は「待っていない間に届いた指示」の置き場です。ヨシ待ち中に届いた分もここへ入りますが、
　いま未処理は0件でした。

---

# 分からなかったこと

- **Code タブが何を根拠に「処理中」と出していたかは、こちらからは分かりません。**
  アプリ側の見え方で、この端末に記録が残りません。**こちら側の記録には、その帯に動きがありません。**
- **配信が14分かかった理由も分かりません。** 07:04:44 から 07:18:43 まで、
  こちらのフックには何も鳴っていません。回線か、アプリ側の待ち行列かの区別が付きません。
  ＊これは 08-25 に調べた**糸切れ3件**と同じ形（プロセスは生きているのに届かない）です。
- **今回の直しで「作業中」の誤りは消えますが、逆向き（本当に動いているのに idle: と出る）**が
  起き得るかは、まだ確かめていません。**resume が鳴らないまま長く走る**回があれば起こります。
  いまの作りでは resume は必ず先に鳴るので、起きないと見ていますが、実地では見ていません。

# 触ったもの

| ファイル | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1` | 落ちどころを「最後のフックで決める」形へ（**934-946行**） |
| `~/.claude/watch-notify.ps1.bak-20260826` | 触る前の写し（作法29 の四段） |
| `koushu-handan.html` `panel.html` | **触っていません** |
