状態：ヨシ待ち ／ 2026-08-21 00:43 ／ v1418 納品済み。走り中の抜け道の塞ぎ方（案A〜C）の裁定と、倒れる音の三案の選択

【保留】
- **走り中の抜け道の塞ぎ方**（この報告の§2。案A推奨）← 実装前に出す約束のもの
- **倒れる音 三案（A/B/C）の選択**（聞いてから決める、と伺っています）
- **ライフ0の倒れる絵の素材待ち**（704x1524・**白背景**・剣は手から離さず杖のように突く）
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（目盛り画像は `head-proof-*.png`）
- `sizing-review.png` を残すか消すか

# 納品：v1418 説明文の一文を実行時に合わせた／走り中の抜け道の塞ぎ方の案

日付 2026-08-21 ／ 版 **v1418** ／ 前版 v1417（`85addf0`）

---

## 1. 納品：v1418

**1322行の、その一文だけを直した。** 範囲は**狭いほう＝(a)** と判断した。

| | |
|---|---|
| 直す前 | 一致すれば「**🌱枝豆も本判定と一致、ダブルヨシ**」。 |
| 直す後 | 一致すれば「**🌱4組。揃い**」。 |

**実行時の文言はコードから取り出して当てた。** 字面を手で打たず、
`window._edamameNote = pairTripletN + '組。揃い'` の `'組。揃い'` を読み出して
「🌱」＋例の組数「4」＋その文字列、という形で組んでいる。**実行時の文言は一文字も変えていない。**

**周辺には手を入れていない。** ご指示どおり、次はすべて**そのまま残した**。

- 地の文「ここまでは常に**ダブルヨシ**が出る計算になりますが」
- 「🌱枝豆は攻めと言ったが、本判定は守り──珍しい食い違い」
- 「🌱枝豆も薄い、守りヨシ」
- 「🌱枝豆は守りと言ったが、本判定は攻め──珍しい食い違い」
- 現場猫・紳士のくだり、用語の言い換え

### 検査

**`node check-all`（フル版）**

| | 結果 |
|---|---|
| `check.js` 本体 ①〜㉑ | **全PASS**（✗ 0件） |
| ⑦ 狭い画面での溢れ（6視野 × 9画面） | **PASS**。二択の bottom は6視野とも**前版と同値** |
| ⑬ 非日本語文字の混入 | **PASS** |
| `adv-check.js` 探偵編の回帰 | **全PASS** |

- **`node --check`**：`<script>` 4ブロックとも構文OK（一時ファイルは切り出して検証後に削除）
- **`serifu.txt` 再抽出**：7593行・**口調の混入0件**、`--check` で一致を確認
- **版表記3箇所**を **1418** に同時更新（`data-ver` / `verTag` / `ver.txt`）
- **テスト用のシムなし。** `analyze()` / `bestShapeOneSuit` にも触れていない

---

## 2.【2】走り中の抜け道の塞ぎ方 — 三案（**実装前の提示。着手していません**）

### いま何が起きているか（実測した仕組み）

**接触の条件**（10388行）に `!moving` が入っている。

```
if(!frozen && tnow >= joyUntil && !moving && tnow >= recoilUntil && tnow >= invulnUntil)
```

**移動の刻み**（定数から）

| | 値 | その間の安全 |
|---|---|---|
| 押しっぱなしで走り出すまで（`holdMove`） | **300ms** | **無防備** |
| 隣のゾーンまで走る（`zoneMoveMs`） | **300ms**（端から端は600ms） | **安全**（`!moving`） |
| 到着後の息切れ（`breathMs`） | **250ms** | **無防備** |

**「指を置き続けるだけで永久に安全」にはならない。** 走り出しの300msと到着後の250msは無防備で、
`holdStart` も `!moving` を条件にしているので、走っている最中に次の走りは仕込めない。

**しかし半分は安全になる。** 端から端へ往復すれば **600ms 安全 / 550ms 無防備**で、
**被弾の機会が半分に落ちる**。しかも**走り斬り**（10399行）は移動中に帯の鳥を捕るので、
**安全な時間帯にだけ稼げる**。ライフを入れると、この往復が最適解になってしまう。

**もう一つ、被弾直後に走ると無敵が消える**（10653行 `recoilUntil = 0; invulnUntil = 0;`）。
いまは無敵が見た目の都合だけなので害が無いが、**ライフが付くと逃げた側が損をする**。
**どの案を採っても、ここは直す必要がある。**

### 案A（推奨）— 移動中も当たる。ただし絵は替えない

- 10388行の **`!moving` を外す**。
- `recoil()` に「**絵を出すか**」の別を持たせ、**移動中は走り絵のまま**にする。
  **ライフは減らし、悲鳴と無敵1.0秒は地上と同じに掛ける。**
- 10653行の **`invulnUntil = 0` をやめる**（無敵は走っても持ち越す）。

**利点。【3】で決まった空中の扱いと同じ形になる。** 規則が「**地上＝絵あり／空中と移動中＝絵なし**」の
一本にまとまり、説明も一行で済む。走り斬りも途中で切れない。

**決めておくこと。** 同じフレームで**走り斬りの捕獲と接触が両方起こりうる**（10395行の走り斬りは
10388行の接触判定の直後にある）。**捕獲を先に処理してから接触を見る**順にすれば、
「捕った瞬間に当たった」が「捕れずに当たった」にならない。

### 案B — 移動中も当たり、仰け反りも出す

- `!moving` を外し、**移動を中断して仰け反る**。

**利点。**手触りがいちばん分かりやすい（当たったら止まる）。
**難点。走り斬りが途中で切れる。**【3】で空中の中断を避けた方針とも食い違う。
移動が中断されたとき**ゾーンの位置をどう決めるか**（元へ戻すか、途中で止めるか）の裁定も要る。

### 案C — 接触の規則は触らず、移動の連発だけ鈍らせる

- 到着後 N ミリ秒は `holdStart` を受けない（`edaCoolAt` に相乗り）。

**利点。**接触まわりを一切触らないので、既存の手触りが変わらない。
**難点。抜け道は狭まるだけで塞がらない**（走っている間はやはり無敵）。
**操作の気持ちよさが落ちる**——連続で走れなくなるのは、いまの遊びの芯に触る。

### 所見

**案Aを推します。** ご指定の「指を置き続けるだけで永久に安全という形は成立しない」に正面から応え、
**【3】の空中の裁定と同じ規則になる**のが大きい。案Cは抜け道が残るので、ご指定を満たしません。

---

## 3. 裁定の記録（実装はしていません）

- **【3】空中の被弾** — **ライフだけ減らす**。仰け反りの絵と落下の中断は入れない。
  **悲鳴と無敵1.0秒は地上と同じに掛ける。**
- **【4】倒れる絵の元絵** — **白背景**（作法§21 のとおり。黒背景は取り下げ）。
  **704x1524**、人物の大きさは他と揃える。
  **姿勢は「膝をついて前へ崩れ、剣を杖のように突いて体を支える」。**
  **剣は手から離さず、体に触れたままにする**——離すと**刀身の銀色が縁から繋がった白として
  背景に飲まれる恐れ**があるため。仰け反り（体が後ろへ反り剣が右上へ跳ねる）と見分けが付く形。
- **倒れる音** — 三案を聞いてから選ぶ。
- **最高記録** — 作らない。連戦の保存・表示は現状のまま（無し）で据え置き。

**この姿勢の条件は理にかなっています。** `char_export.js` は縁から繋がっている白だけを背景と
みなす作りなので、**体から離れた銀の刀身は「外の白」と地続きになって落ちます**。
v1399 の刀身の欠けと同じ型の事故で、体に触れたままなら起きません。

---

## 4. 触れていないこと

- ライフ制の**実装には一切着手していない**。今回入れたのは説明文の一文だけ。
- 倒れる音は**試聴版（本体の外の単独ファイル）**のみ。本体には何も入れていない。
- `sizing-review.png` は消していない。

---

## 5. 次

- **走り中の抜け道の塞ぎ方**（案A / B / C）。案Aなら、走り斬りと接触の順序もあわせて決めたい。
- **倒れる音の三案**。
- 素材二件（倒れる絵・船長の絵）は届き次第。
- v1418 の実機検収もお待ちしています。
