# 生存記録の書き損ねを直す — 粘って書き、駄目なら退避する

**状態：終わり** — 2026-08-26 19:5x 時点／生存の一本が欠けない形にした／待ちなし

**結論** — `hook.log` への追記が黙って捨てられていた件を直した。
**250ms 待って最大3回まで書き直し、それでも駄目なら `hook-fallback.log` へ退避**する。
見張りの側も退避先を読むようにしたので、**退避した回が「欠け」と数えられることはない**。
実測で両方の半分を確かめた。あわせて `stable` を検収済みの版へ進めた。

---

## 一．stable を進めた（作法19）

panel v36 と4件の検収を受けたので、`stable` を `5e3d324` → **`02efb97`** へ進めた。
本体 `ver.txt=1427` ／ `panel-ver.txt=36`。

## 二．直したこと

### ① 書く側 — 粘って、駄目なら退避する（`hook-notice.ps1`）

以前は一行だった。

```powershell
try { Add-Content -LiteralPath $LogPath -Value $line -Encoding UTF8 } catch { }
```

`hook.log` は見張りが毎分 `Get-Content -Tail 400` で握るため、**ぶつかると IOException で落ち、
`catch { }` が黙って飲む**。タスクは戻り値0のまま生存だけが消えていた。

- **落ちたら 250ms 待って書き直す。最大3回**（初回＋2回）
- **3回とも駄目なら `hook-fallback.log` へ退避**する。生存の一本を捨てない
- 退避した行には `＊hook.log へ3回とも書けず退避` を付ける。
  **数えられないと、直しが効いているかを後から確かめられない**ため

### ② 読む側 — 退避先も生存とみなす（`watch-notify.ps1`）

**ここが無いと直しにならない。** 退避した回をそのまま「欠け」と数え、
15分の関門に当たって偽の警報が鳴り続ける。`hook.log` の末尾400行に加えて
`hook-fallback.log` の末尾40行も読み、**新しいほうを生存の時刻に採る**。

## 三．実測

**書く側** — `hook.log` を `FileShare.Read` で握ったまま生存を打たせた。

```
退避先の行数（前）: 0
退避先の行数（後）: 1   戻り値=0
→ 2026-08-26 19:54:43  heartbeat     生存  ＊hook.log へ3回とも書けず退避
```

**読む側** — 見張りの拾い方だけを同じ手順で再現した。

```
hook.log だけで見た生存  : 2026-08-26 19:48:07
退避先も見た生存         : 2026-08-26 19:54:43
生存の古さ               : 0 分（関門は15分）→ 警報は鳴らない
```

`hook.log` だけを見ていた頃なら、この時点の古さは6分。あと9分で偽の警報が鳴っていた。

**構文検査** — `hook-notice.ps1` PASS（271行）／`watch-notify.ps1` PASS（1405行）。
触る前の写しは `hook-notice.ps1.bak-20260826` ／ `watch-notify.ps1.bak-20260826d`。

## 四．後日報告の宿題

**数日ぶんが溜まったら、欠け率を測り直して報告する。**
直す前は **795本中17回＝2.1%**。どこまで下がったかと、退避が何回起きたかを出す。

測り方と「退避先も足して数える」注意は、記憶 `heartbeat-gap-followup` に残した。
＊`hook.log` だけ見ると、**退避した回をそのまま欠けと数えてしまう**。

## 五．残っている手当て（未実施）

`hook.log` は **284KB まで肥って畳まれない**。毎分の読みが長引くぶん、ぶつかる窓も広い。
今回の直しは「ぶつかっても落とさない」ようにしただけで、**ぶつかる回数そのものは減っていない**。
畳む仕掛けを入れれば根の側が細くなるが、**今回は指示の範囲外なので手を付けていない**。
後日の欠け率を見て、必要なら改めて相談したい。

## 六．前の回までの成果（検収済み）

`~/.claude` の非公開控え（`kfsr148-art/claude-home-backup`・130件・鍵/話題名ゼロ）、
18:53の異常の真因特定、押し送りの1日の上限の撤廃、作業時間の見える化（panel v36）。
**4件とも検収を受けた**ので `stable` を進めた。詳細は一つ前の版の報告書にある。

---

## 実機で見るところ

**今回は画面に出るものを触っていない**ので、実機で見てもらう点はなし。
効き目は数日後の欠け率で出る。

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
4件納品（~/.claude の git 化・生存記録の調査・押し送り上限の撤廃・作業時間の見える化 panel v36）
①~/.claude を git 管理化し非公開リポジトリ claude-home-backup へ130件を push（鍵・話題名は0件）②18:53「生存記録15分途切れ」の真因を特定——hook.log への追記が IOException で黙って捨てられる。/clear とは無関係で、18:58:06 に自力復帰 ③押し送りの1日の上限（DAILY_MAX）と打ち止めの一文を撤廃 ④作業時間の見える化：状態行とパネルへ「開始HH:MM・見込みN分・経過N分」／見込み超過を一発だけ知らせる（mikomi-over.txt が担保）／CLAUDE.md 作法25へ「見込み:」を追加 ⑤inbox-watch.ps1 の書き直し判定に見込みが入っていない不具合を直し、常駐を起こし直した（PID 5188→2844）⑥node check-all --fast を切り離して回し直し、check・adv-check とも PASS（FAIL 0件・⑦は9画面すべて溢れなし）
ファイル: ~/.claude/.gitignore／~/.claude/watch-notify.ps1（1392行）／~/.claude/inbox-watch.ps1（376行）／panel.html（v36）／panel-ver.txt／state-line.js／CLAUDE.md／report-latest.md
実測: 遠隔130件・記憶44件・鍵/話題名0件／生存の欠けは795本中17回（2.1%）で /clear と無関係／構文検査は watch-notify・inbox-watch とも PASS／状態行が「開始19:10・見込み25分・経過12分」で出た／check-all --fast 両方PASS・FAIL 0件
実機: 返事パネルの状態の二段目が「開始HH:MM・見込み25分・経過N分」の形（旧「見込み25分・開始からN分」）／版の字が panel v36
未検収: 2026-08-26 panel v36（作業時間の見える化）
```

### 2. 🕒 延びています

```
作業時間の見える化（panel v36）— 納品前の検査を回し直し中
延びています（見込み5分・経過6分）
```

### 3. ✅ 終わりました（返事不要）

```
作業時間の見える化（panel v36）— 納品前の検査を回し直し中
①~/.claude を git 管理化し非公開リポジトリ claude-home-backup へ130件を push（鍵・話題名は0件）②18:53「生存記録15分途切れ」の真因を特定——hook.log への追記が IOException で黙って捨てられる。/clear とは無関係で、18:58:06 に自力復帰 ③押し送りの1日の上限（DAILY_MAX）と打ち止めの一文を撤廃 ④作業時間の見える化：状態行とパネルへ「開始HH:MM・見込みN分・経過N分」／見込み超過を一発だけ知らせる（mikomi-over.txt が担保）／CLAUDE.md 作法25へ「見込み:」を追加 ⑤inbox-watch.ps1 の書き直し判定に見込みが入っていない不具合を直し、常駐を起こし直した（PID 5188→2844）⑥node check-all --fast を Start-Process で切り離して回し直し中（前回は⑦の実測で外から止められた）
ファイル: ~/.claude/.gitignore／~/.claude/watch-notify.ps1（1392行）／~/.claude/inbox-watch.ps1（376行）／panel.html（v36）／panel-ver.txt／state-line.js／CLAUDE.md／report-latest.md
実測: 遠隔130件・記憶44件・鍵/話題名0件／生存の欠けは795本中17回（2.1%）で /clear と無関係／構文検査は watch-notify・inbox-watch とも PASS／状態行が「開始19:10・見込み25分・経過12分」で出た／check-all --fast は PID 12004 で実行中
実機: 返事パネルの状態の二段目が「開始HH:MM・見込み25分・経過N分」の形（旧「見込み25分・開始からN分」）／版の字が panel v36
未検収: 2026-08-26 panel v36（作業時間の見える化）
```

### 4. 🪟 異常です（手が要ります）

```
点検3件を検収。この窓は畳めます
見張りの生存記録が 15 分途切れています。仕組みのどこかが止まっています。
こちらがすること：端末の様子を見て、動いていなければ開き直してください。
```

### 5. ✅ 終わりました（返事不要）

```
点検3件を検収。この窓は畳めます
panel v35・点検7件・仕上げ2件の検収を受けた。畳む前の最終確認を済ませた——報告書に締めの節（未検収ゼロ・次の窓へ渡す四つ）を書いて push、控えの記憶 handover-next を v1427／panel v35／作法30まで へ更新、開いている印の一覧は全部検収済みなので空にした、未 commit は0件。/clear はこちらからは打てないので、そちらでお願いする
実測: 未 commit 0件／本体 ver.txt=1427・panel-ver.txt=35・stable の ver.txt=1427 で揃っている／開いていた印8件を空にした／締めの節を書くとき、node へ渡した文のバッククォートが bash に食われて字が抜けたので、ヒアドキュメントで書き直して push した（作法28 と同じ形の穴）
```

<!-- 送った知らせ ここまで -->
