# 全画面の写し取り — 第一段（外から呼べる画面だけ）

**状態：ヨシ待ち（実機で見てください）** — 2026-08-25 13:2x 時点

**結論** — **45枚を撮り、失敗0・指紋のかぶり0**。台本は `shot-all.js` の一本で、
`Start-Process` で切り離して回す形。本体 `koushu-handan.html` には**一切触れていない**。

---

## 0. 途中で分かった大きな一件 — 本体は縦向きでは遊べない

指示は「iPhone の論理寸法 390×844」でしたが、**その縦のままでは一枚しか撮れません**。
本体は横向き専用で、縦で開くと全面に催促が出ます。

> お客様、画面を横にお回しくださいませ／縦のままでは、お給仕いたしかねます

そこで **同じ論理寸法を横に倒した 844×390 を主寸法**にし、
**縦の催促画面も「辿り着ける画面」の一枚として残しました**（`00-portrait-rotate.png`）。

| | 値 |
|---|---|
| 引き | Playwright **WebKit**（`~/.claude/tools/pw`。リポジトリの外に置いた） |
| 寸法 | **844×390**（横・主）／ **390×844**（縦・催促の一枚のみ） |
| 画素の倍率 | **2倍**（1688×780 の PNG） |
| 開く先 | `https://kfsr148-art.github.io/koushu-handan/koushu-handan.html`（公開ページそのまま） |
| かかった時間 | **413.7秒**（45枚。一枚あたり約9秒） |
| 置き先 | `shots/`（**12MB**） |

## 1. 撮れたもの — 45枚

| 群 | 枚数 | 中身 |
|---|---|---|
| 入口 | 4 | 縦の催促／タイトル／案内の一覧／案内の詳細（エンジン） |
| 盤面 | 2 | 牌を入れる前／14枚を並べた姿 |
| 判定後 | 4 | 既定／**守りの判定（羊・鼠）**／**国士の見える手**／判定獣が述べる |
| 判定後（人柄七人） | 7 | 執事・軍師・ずんだ・お嬢様・マダム・一姫・先生 |
| 判定後（スイッチ四者） | 4 | 現場猫・枝豆・紳士・兎 |
| 判定後（お国訛り八種） | 8 | 宮崎・博多・大阪・京都・青森・沖縄・武士・幼児 |
| 見張り台 | 5 | 答え合わせ・猫牌・枝豆・AI の四つ＋手引き |
| 探偵編 | 2 | 乗船の幕／猫室（部屋に入った姿） |
| ミニゲーム | 5 | 品書き／遊んでいる最中 初級・上級・連戦／結果の絵 |
| その他の間 | 4 | 猫部屋／誤診記録／配牌帳／読み取りページ（別ページ `reader.html`） |

**一覧シート** … `shots/index.html`（群ごとに並べ、押すと原寸で開く）
**控え** … `shots/shots.json`（一枚ごとの名・群・札・所要・寸法）

## 2. 「撮れた」を鵜呑みにしないための検査を足しました

一度目の45枚は**すべて「撮れた」と出ましたが、1件は中身が違っていました**。
`23-photo-reader` が `04-board-empty` と**同じ絵**だったのです。
`ssShow()` は `window.ssImg`（実際に読み込んだ写真）が無いと**何もせずに戻る**ため、
段取りが空振りしても PNG は出来てしまいます。

そこで台本に **指紋（md5）のかぶり検査**を入れました。別の画面のはずが同じ絵なら、
その段取りは効いていない疑いとして最後に名指しします。
**撮り直しでは かぶり0件**。ページの中で出た例外も0件です。

## 3. 回し方

```
node shot-all.js                 全部撮る（約7分）
node shot-all.js --only judged   名前や群に judged を含むものだけ
node shot-all.js --scale 3       画素の倍率（既定 2）
node shot-all.js --local         公開ページでなく手元のファイルを開く
node shot-all.js --index-only    撮らずに一覧シートだけ組み直す（札や見た目を直した回）
```

フル版の検査と同じく、**`Start-Process` で切り離して**回します。

```powershell
Start-Process -FilePath (Get-Command node).Source -ArgumentList shot-all.js `
  -WorkingDirectory C:\Users\user\Desktop\mahjong\koushu-handan `
  -RedirectStandardOutput shots\run.log -RedirectStandardError shots\run.err.log -PassThru
```

## 4. 第一段の限界 — 撮れなかった画面と、第二段で掘るもの

### ① 探偵編 — 撮れたのは入口と猫室の二枚だけ

撮れないもの … **各号室**（16号室の即死罠・14号室ほか）／**聞き込み・捜索・買収**の場面／
**推理・ロン・勝ち・敗け・エンディング**／**猫たち**（ワトソン猫・茶色猫・警察猫・電話猫・船長猫）の登場場面。

**掘るもの** — **場を名指しで開く口**。いま `advStart()` は**新規開始しかしません**。
猫室の一枚も、`check.js` ⑦ から写した「幕を5回送り、初歩を選び、廊下から猫室へ入る」という
**手順の再現**であって、部屋を選ぶ口ではありません。進行度・所持・部屋・場面を与えて
任意の幕へ跳ぶ手が要ります。

### ② ミニゲームの勝ち絵 — 出るのは負け絵だけ

撮れたのは **0/3羽「逃げられた」**（負け絵）。外から剣を振っても鳥は捕れず、
45枚のうち**この一枚だけ40秒**かかりました（450回振って取りこぼし）。

**掘るもの** — **結果を指定して結果画面を出す口**。`toriStart(key)` で難易度は選べますが、
**結果は運任せ**で狙って出せません。剣士の各コマ（振り八方向・跳躍・息切れ）も同じで、
遊びの最中の偶然でしか写りません。**コマを指定して止める口**も要ります。

### ③ 写真読み取りの盤面

受付ページ（`reader.html`）までは撮れましたが、**読み取り後の盤面・目盛り合わせ・切り出し**は
撮れません。`ssShow()` が `window.ssImg` を要求するためです。

**掘るもの** — **写真を与える口**（試し用の画像を読ませて `ssImg` を立てる）。

### ④ 中身のある状態

**誤診記録も配牌帳も、いまは空の姿**しか撮れません。記録が溜まった姿は写せません。

**掘るもの** — **状態を仕込む口**（記録を与えてから開く）。

### ⑤ 掘らなくても足せるもの（牌姿を選ぶだけ）

- **龍（国士が見えている）** … 13種そろいの手を入れたところ、出たのは龍ではなく
  **役満手前の鳥 🐦‍🔥** でした。龍を出す牌姿は別に選ぶ必要があります。
- 猫部屋の中の各切り替えを押した姿も、押す段取りを足すだけで撮れます。

### ⑥ 写しの再現性について

判定後の画面は**人柄と訛りがランダムに振られる**ため、同じ牌姿でも**回ごとに台詞が変わります**。
人柄・訛りを名指しした28枚は固定されますが、それ以外の判定後の枚は毎回違う台詞になります。

## 5. 触ったもの

| ファイル | 中身 |
|---|---|
| `shot-all.js` | **新規**。撮る台本 |
| `shots/` | **新規**。PNG 45枚＋`index.html`＋`shots.json` |
| `panel.html` | 一覧の上に「🖼 画面の写し取り一覧」の口を足した。版を **v30** へ |
| `panel-ver.txt` | `30` |
| `.gitignore` | `shots/run.log` と `shots/run.err.log` を除いた |
| `koushu-handan.html` | **触っていません** |

## 6. 実機で見るところ

**返事パネル**を開いて、次の二つを見てください。

1. 一覧のいちばん上、「全部写す」の**すぐ下**に **「🖼 画面の写し取り一覧」** の釦が出ていること。
   押すと写しの一覧（群ごとに並んだ45枚）が開くこと
2. 画面のいちばん下の版の字が **`panel v30`** になっていること

＊v29 のままなら端末が古い版を掴んでいます。その場合は URL に `?_v=30` を付けて一度だけ開いてください。
