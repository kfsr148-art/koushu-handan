# 較正-1 の線引きを確定し、虎の写しも層別へ揃えた（本体 v1428）

**状態：終わり（残り0件）／VAIO 2026-08-29 13:22**

**結論** — 較正-1 の層別の関門（向聴2=25／3=60／4以上は関門なし）を当て、4000行の再採点で
**悪化した層は一つも無い**。あわせて **y0829-tora の案A** で虎の親補正の写し（旧 L4096）を
同じ層別へ揃えた。**文言（`MASCOT_WORDS`）は一字も変えていない**——直したのは
一言を出すかどうかの**条件だけ**。`check.js` の ⑩ も新しい書き方を読めるよう追随させた。
幅の関門の数値を名指しする箇所を総当たりで走査したが、**写し以外には無かった**。

---

## 1. 当てた関門（指示1・3）

`koushu-handan.html` の一箇所だけ。画面の文言・七人の見立て・表示の側には一行も触っていない。

| 行 | 変更前 | 変更後 |
|---|---|---|
| L3041（旧） | `const wideAttack = acceptTiles >= (isDealer ? 20 : 24) && doraValue >= 1;` | — |
| L3040-3043（新・注記） | — | 層別の根拠と、向聴0・1が向聴2の値の流用であることを書いたコメント4行 |
| L3044（新） | — | `const widthGate = baseShanten >= 4 ? Infinity : (baseShanten >= 3 ? 60 : 25);` |
| L3045（新） | — | `const wideAttack = acceptTiles >= (isDealer ? widthGate - 4 : widthGate) && doraValue >= 1;` |

| 層 | 旧・親/子 | 新・親/子 | 備考 |
|---|---|---|---|
| 向聴0 | 20 / 24 | 21 / 25 | 件数1件。線を引けず向聴2を流用（コメントに明記） |
| 向聴1 | 20 / 24 | 21 / 25 | 件数17件。同上 |
| 向聴2 | 20 / 24 | 21 / 25 | 指定どおり25枚 |
| 向聴3 | 20 / 24 | 56 / 60 | 指定どおり60枚 |
| 向聴4以上 | 20 / 24 | 関門なし | `wideAttack` を発火させない |

`narrowDefend`（狭い側・L3036 の 6/8）は据え置き。**親子差も据え置き**とのことなので、
従来の「親は子より4枚甘い」という差をそのまま各層へ持ち込んだ（子の値 − 4 が親の値）。

版は `data-ver` / `verTag` / `ver.txt` の三箇所を 1427 → **1428** に揃えた。

---

## 2. 当てる前の再採点（指示2）

`core-battle-log.tsv` の4000行。うち **7行は国士・役満手前の格上げ**で列の値から verdict を再現できないため、
残り **3993行**で数えた（7行はいずれも defend→attack の上書き）。

### verdict が変わる件数

**890件 / 3993件（22.29%）。向きはすべて attack → defend。** 逆向き（defend→attack）は0件。

| 層 | 変わった件数 | 変わった行の和了率 | 放銃率 | 平均点棒 |
|---|---|---|---|---|
| 向聴0 | 0 | — | — | — |
| 向聴1 | 0 | — | — | — |
| 向聴2 | **0** | — | — | — |
| 向聴3 | 201 | 23.4% | 14.4% | 385 |
| 向聴4以上 | 689 | 18.4% | 11.8% | 18 |

**向聴2で0件**なのは、24→25 の1枚分に該当する行（子で受け24枚ちょうど・親で20枚ちょうど・ドラ1以上・形不足）が
この4000行に一つも無かったため。指定どおり当てたが、この母集団では何も動かない。

### 層ごとの分離（攻 − 守）。悪化した層は無い

| 層 | 件数 | 旧 和了差 | 新 和了差 | 旧 点棒差 | 新 点棒差 | 判定 |
|---|---|---|---|---|---|---|
| 向聴1 | 17 | −11.5 | −11.5 | −360 | −360 | 変化なし |
| 向聴2 | 410 | −3.9 | −3.9 | +830 | +830 | 変化なし |
| 向聴3 | 1419 | +9.1 | **+10.6** | +494 | **+571** | 改善 |
| 向聴4以上 | 2146 | +7.0 | **+14.4** | +657 | **+1159** | 改善 |
| 全体 | 3993 | +8.8 | **+13.4** | +771 | **+1082** | 改善 |

（向聴0は1件で片側しか無く、差が出ない）

### 攻守それぞれの成績（層別）

| 層 | 旧attack | 旧defend | 新attack | 新defend |
|---|---|---|---|---|
| 向聴3 | n759 和了29.5% 放銃12.8% 点710 | n660 20.5% 12.6% 215 | **n558 31.7% 12.2% 827** | n861 21.1% 13.0% 255 |
| 向聴4以上 | n854 20.6% 12.5% 179 | n1292 13.6% 13.1% −478 | **n165 29.7% 15.8% 853** | n1981 15.3% 12.6% −306 |

新 attack の放銃率は 向聴4以上で 12.5%→15.8% と上がるが、和了率が 20.6%→29.7%、
平均点棒が 179→853 と伸びており、**攻めの群として割の良い交換になっている**。

### 移した890行そのものの位置

| 層 | 移動 | 移した行 | 新attack | 新defend |
|---|---|---|---|---|
| 向聴3 | 201 | 和了23.4% 点385 | 31.7% / 827 | 21.1% / 255 |
| 向聴4 | 530 | 和了20.0% 点202 | 30.1% / 882 | 16.7% / −148 |
| 向聴5 | 153 | 和了13.1% 点−673 | （2件のみ） | 11.4% / −754 |
| 向聴6 | 6 | 和了16.7% 点1400 | なし | 19.2% / 350 |

移した行はいずれも**新 attack より新 defend に近い**。降格は成績と合っている。

### 一つだけ添える — 向聴を4/5/6に割ると、向聴5だけ数字が反転する

指示の層（0/1/2/3/4以上）では悪化ゼロだが、**4以上をさらに割ると向聴5で和了差が +2.2 → −11.4 に反転する**。
中身を見ると、**新 attack が2件しか残らない**ためで（向聴5の539件のうち shapeOK は2件だけ）、
その2件が被ツモと流局だったことによる見かけの反転。旧 attack（n155）も和了差 +2.2 と分離はほぼ無く、
**幅の関門は元々この層で何も分けていなかった**。実体のある悪化ではないと判じ、当てた。

### borderline の件数

| | 合計 | narrow（狭くて守） | wide（広くて攻） |
|---|---|---|---|
| 旧 | 958 | 2 | 956 |
| 新 | **68** | 2 | 66 |

956 対 2 の偏りは解消した。

---

## 3. 被り-1（指示4・記録のみ、直しは当てていない）

**申告どおりだった。4000行で食い違い0件。**

| 調べたこと | 食い違い |
|---|---|
| `hasValue` ≠ `doraValue > 0` | **0件** |
| `shapeNeeded` ≠ doraValue だけから決まる値 | **0件** |
| `shapeOK` ≠ `goodShapes >= shapeNeeded` | 0件 |
| `totalBlocks` ≠ `goodShapes + weakShapes` | **493件**（別件・下記） |

### どこで何が同じ数字を見ているか

| 行 | 何が書いてある | 実体 |
|---|---|---|
| **L2867** | `const doraValue = doraEff + redCount + yakuhaiValue;` | **元の一つの数字** |
| **L2872** | `const hasValue = doraValue>0;` | doraValue の言い換え。独立した材料ではない |
| **L2880-2884** | `shapeNeeded` の4分岐 | doraValue だけの関数。しかも `<=0` と `===1` がどちらも4なので**実質3段**（0-1→4／2→3／3以上→2） |
| **L3027** | `const shapeOK = goodShapes >= shapeNeeded;` | 分岐を選ぶのは goodShapes と、**shapeNeeded 経由の doraValue** |
| **L3036** | `narrowDefend` | 見ているのは acceptTiles・isDealer・**doraValue** の三つだけ |
| **L3044-3045** | `wideAttack` | 見ているのは acceptTiles・isDealer・**doraValue**・baseShanten の四つだけ |
| **L8468** | `ok: a.hasValue,` | 画面の「打点の種」の欄。**doraValue と別の材料の顔で並ぶ** |
| **L8469** | `打点 ${a.doraValue} — ...` | 同じ欄の中で、言い換え前と言い換え後が並んで出る |
| **L7113-7114** | ずんだの説明「まとまりが◯つ、必要なのは◯つ」 | 後者（shapeNeeded）は doraValue の関数 |
| **L8647 / L8654** | 「打点 ◯ に対して、良形が ◯ に届かない」 | **同じ数字を二度言っている**（打点＝doraValue、良形の必要数＝doraValue の関数） |

**要するに、borderline を分けているのは `acceptTiles` と `doraValue` の二つだけ。**
画面で「打点の種」「必要な良形」「まとまりの数」と三つの顔で出ているもののうち、
前二つは同じ doraValue、三つ目は判定に関与していない。

### 別件で見つかった — `totalBlocks` が古い値のまま

`totalBlocks` は **L2514** で `goodShapes + weakShapes` として作られるが、
**L3016-3025 の七対子の上書き**（`goodShapes = p2; weakShapes = 0;`）はその**後**に走る。
そのため七対子に寄る手では上書き前の値が残り、**4000行中493行で `goodShapes + weakShapes` と一致しない**
（差1が488件、差2が5件。493件すべて weakShapes=0＝七対子側の行）。

判定には一切効かない（唯一の使い道は **L4017-4018** の `tenLeans` の語り口の分岐）が、
**この一箇所では古い数字を見ている**。台帳の 被り-1 に併記した。直しは当てていない。

---

## 4. 虎の親補正の写しを層別へ揃えた（案A・指示の本体）

### 4-1. `check.js` ⑩ は何がどう落ちていたか（指示1）

⑩ の親補正の節は、`analyze()` 側と虎の写し側を**正規表現で読み取って突き合わせる**作りだった。
落ちていたのは**照合の結果ではなく、読み取りそのもの**。

| 場所 | 中身 |
|---|---|
| `check.js` **L912**（旧） | `const wa = (src.match(/acceptTiles\s*>=\s*\(isDealer\s*\?\s*(\d+)\s*:\s*(\d+)\)/) \|\| []);` |
| `koushu-handan.html` **L3045**（較正-1 で当てた新しい行） | `const wideAttack = acceptTiles >= (isDealer ? widthGate - 4 : widthGate) && doraValue >= 1;` |

**旧 L912 の正規表現は `(isDealer ? 数字 : 数字)` しか読めない。**
較正-1 で三項の中身が数字リテラルから `widthGate - 4 : widthGate` という**式**に変わったため、
一致が空になった。空になると `check.js` **L914** の入口へ落ちる。

```
  if (!nd.length || !wa.length || !cp.length) {          ← L914（旧）
    ng('境目の数値を読み取れない（analyze 側または写し側の書き方が変わった）');   ← L915（旧）
```

FAIL の一行はこれ。**「写しがずれている」ではなく「読み取れない」のほうが出ていた**ので、
⑩ はこの回、写しのずれ（20〜24 と 25／60 の食い違い）を**見てすらいなかった**。
`nd`（狭い側 6/8）と `cp`（写し側）は両方とも読めていた——**落ちていたのは `wa` の一本だけ**。

### 4-2. 当てた直し

**`koushu-handan.html` L4089-4102** — 写しの条件を層別の関門へ揃えた。

| | 変更前 | 変更後 |
|---|---|---|
| コメント（旧 L4088） | `6/8/20/24 は analyze() が使う境目の写し` | `6/8 と幅の関門は analyze() が使う境目の写し` ＋ 層別の内訳4行（向聴2=25／3=60／4以上は関門なし／0と1は2を流用） |
| 関門（新 L4100） | — | `const gate = a.baseShanten >= 4 ? Infinity : (a.baseShanten >= 3 ? 60 : 25);` |
| 判定（旧 L4096） | `return a.shapeOK ? (d <= 1 && r >= 6 && r < 8) : (d >= 1 && r >= 20 && r < 24);` | `return a.shapeOK ? (d <= 1 && r >= 6 && r < 8)`<br>`                 : (d >= 1 && gate !== Infinity && r >= gate - 4 && r < gate);` |

**`MASCOT_WORDS`（L4080-4087）は一文字も触っていない。** 変えたのは
`MASCOT_WORDS.dealer`（「親番なので、基準を少し攻め寄りにしています。」）を**出すかどうかの条件**だけ。

| 層 | 親補正の一言が出る帯（旧） | 帯（新） |
|---|---|---|
| 向聴0・1 | 受け 20〜23枚 | 受け **21〜24枚**（向聴2の流用） |
| 向聴2 | 受け 20〜23枚 | 受け **21〜24枚** |
| 向聴3 | 受け 20〜23枚 | 受け **56〜59枚** |
| 向聴4以上 | 受け 20〜23枚 | **出さない**（`gate !== Infinity` で外す） |

狭い側（`shapeOK` の枝・6〜7枚）は据え置き。`analyze()` の `narrowDefend` を触っていないため。

### 4-3. `check.js` ⑩ の追随

**L907-941**。数字リテラル前提の読み取りをやめ、層別の書き方を両側から読んで突き合わせる形にした。

- `GATE_RE` … `baseShanten >= 4 ? Infinity : (baseShanten >= 3 ? 60 : 25)` を両側から読む
  （写し側は `a.baseShanten` と書くので `(?:a\.)?` で吸収）
- 同じ字面が二箇所にあるので、**`function mascotDealerNudged` の位置で本体を切り分けて**、
  前半＝`analyze()` 側／後半＝写し側として別々に読む
- 親の甘さも字で読む（`analyze()` は `widthGate - 4`、写しは `gate - 4`）
- **向聴4以上を外しているか**も見る（`gate !== Infinity` が写しに無ければ FAIL）

突き合わせる値は5つになった — 狭い側 親6/子8・幅の関門 向聴3=60/向聴2以下=25・親の甘さ4枚。

### 4-4. 総当たりの走査（指示2）— 写し以外に幅の関門を名指しする箇所は**無い**

`acceptTiles` を数値と比べている箇所を全部当たった。**結果は下の一覧がすべて。**

| 行 | 中身 | 幅の関門の写しか |
|---|---|---|
| **L3036** | `acceptTiles < (isDealer ? 6 : 8)`（`narrowDefend`） | `analyze()` 本体（狭い側・据え置き） |
| **L3044-3045** | `widthGate` と `wideAttack` | `analyze()` 本体（今回の層別の関門） |
| **L4100-4102** | 虎の写し | **これが唯一の写し。今回揃えた** |
| L2820-2822 | `acceptTiles >= 7 / >= 4`（聴牌の `widthLabel`） | **違う**。表示の札の物差しで、既に向聴で層分けされている |
| L2824-2827 | `acceptTiles >= 40 / >= 24 / >= 12`（向聴1の `widthLabel`） | **違う**。`24` は数が同じだけの別物 |
| L2830-2833 | `acceptTiles >= 50 / >= 36 / >= 20`（向聴2以深の `widthLabel`） | **違う**。`20` も同上 |
| L4045 | `gunLine = (sh <= 2) ? -1 : (sh === 3 ? 30 : (sh === 4 ? 70 : 9999))` | **違う**。投票欄の傾き（`tenLeans`）で、実測50000回の別の物差し。作法20 で触らないと定めてある。既に層別 |

**虎以外の六人**（現場猫・枝豆・紳士・兎・羊・鼠）—— `catLean`／`edaLean`／`omLean`／
`usagiLean`（L4015-4022）はどれも `acceptTiles` を見ていない。材料は猫牌の枚数・対子と暗刻の組数・
一枚きりの字牌・`totalBlocks`／`fillGood`／`fillWeak` で、**幅の関門とは無関係**。

**判定カード・根拠欄**（L8484-8492「伸びしろ」）—— `a.widthLabel` と `a.acceptTiles` を
そのまま差し込むだけで、数値の関門を持たない。

**集計行**（L7127-7128）—— 「手が進む牌が◯枚残っています」「この向聴数なら『◯◯』の部類です」。
どちらも `analyze()` が付けた札をそのまま出しており、関門の数値を語らない
（L7126 に「広い・狭いの境目は向聴数で変わるので、判定エンジンが付けた札をそのまま使う」と注記あり）。

**撤退予約線**（L8386-8398・仲裁役の言い回し L7212-7245）—— 期限は `TURN_BY_SHANTEN`
（向聴2=3巡／3=6／4=7／5=8）と猫牌4枚で引いており、**受け入れ枚数の関門は使っていない**。

なお L1374・L1428 の解説に出る「**8枚**」は、良形の塊4つ×2枚＝8枚（`shapeNeeded` の話）で、
幅の関門の 8 とは**別の 8**。触っていない。

---

## 実機で見るところ

1. 判定後の画面で、**4向聴の広い手（受け60枚以上・ドラ1以上・良形が足りない手）の判定が「守り」になる**こと。
   いままでは受け24枚で攻めに格上げされていた（較正-1 の関門の効き）。
2. **親番・向聴4以上・良形不足・ドラ1以上**の手で、虎が
   「**親番なので、基準を少し攻め寄りにしています。**」を**言わなくなる**こと。
   向聴2・3では従来どおり出る（帯は 21〜24枚／56〜59枚）。
3. 版の字が **v1428**。

---

## 残り

**残り0件。**

- **較正-1-線引き** … 済（v1428・L3040-3045）
- **較正-1-虎** … 済（v1428・L4089-4102。案Aで写しを層別へ揃えた）
- **虎-1**（⑩ の FAIL の中身を出してから直す） … 済（上の 4-1／4-3）
- **虎-2**（幅の関門を名指しする箇所の総当たり） … 済（上の 4-4。写し以外に無し）
- **虎-3**（v1428 の commit・push と公開側の実読み） … 済（下記）
- **被り-1** … 記録のみ。台帳に積んである。直しは当てない（指示どおり）
- **凍結-0** … 発効中（見張り・通知・台帳の一式。本体と較正は対象外）

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. 🙋 ヨシしてください

```
panel v74 の実機確認（受信箱は0件・台帳も残り0件）
印: y0829-panel74
待っているのは：返事パネルを実機で開いて、版の字が panel v74 になっていること／「まとめて写す（N件）」を押したあと釦が「N件 写した」と出て、その N が押す前の N と同じであること
答え方：「y0829-panel74 にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
実機: 返事パネル ── ①版の字が「panel v74（8月29日）」／②「まとめて写す」を押した後の「N件 写した」の N が押す前の「まとめて写す（N件）」と一致／③詰まっていない間は状態の帯に「郵便受け 未取り込み」の行が出ない
```

### 2. 🙋 ヨシしてください

```
panel v74 の実機確認と、郵便詰まり-3 の実測
印: y0829-panel74
待っているのは：返事パネルを実機で開いて、版の字が panel v74 になっていること／「まとめて写す（N件）」を押したあと釦が「N件 写した」と出て、その N が押す前の N と同じであること
答え方：「y0829-panel74 にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
実機: 返事パネル ── ①版の字が「panel v74（8月29日）」／②「まとめて写す」を押した後の「N件 写した」の N が、押す前の「まとめて写す（N件）」と一致（v73 は板の二枠のぶん必ず少なく出ていた）／③郵便受けが詰まっていない間は、状態の帯に「郵便受け 未取り込み」の行が出ない
```

### 3. 🪟 異常です（手が要ります）

```
窓が二つ走っている件（郵便掃除の直しに入る前の裁定）
見えていた画面が 2 枚から 1 枚になりました。1 枚が見えなくなっています。
こちらがすること：開き直してください。古いプロセスが残るので、開き直したあとに古いほうを終わらせてください。
```

### 4. 🙋 ヨシしてください

```
窓が二つ走っている件（郵便掃除の直しに入る前の裁定）
印: y0829-futatsu
待っているのは：inbox-watch.ps1 の直し（L644 の早戻りを L668 の Sweep-Mailbox の後ろへ回す）を、この窓（pid 5616）が引き取ってよいか。古い窓（pid 1740）に任せるなら、そちらへ回す。古い窓を止めてよいかも併せて
答え方：「y0829-futatsu にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
実機: 判定後の画面で、虎の一言に「親番なので、基準を少し攻め寄りにしています。」が付くのは向聴3以下のときだけ。向聴4以上の手では付かない。文言そのものは v1427 と一字も変わらない
```

### 5. ✅ 終わりました（返事不要）

```
郵便受けの掃除（取り込み済みの行を控えへ移す）
虎-1〜3 を締めた。v1428 を 86d5d87 で push し、公開側の実読みで版の字まで一致を確認
ファイル: koushu-handan.html（L3037-3048・L4086-4102）／check.js（⑩ L907-942）
実測: check-all --fast 両方PASS・FAIL 0件。公開側 ver.txt=1428／data-ver="1428"／verTag=v1428
実機: 判定後の画面で、虎の一言に「親番なので、基準を少し攻め寄りにしています。」が付くのは向聴3以下のときだけ（向聴4以上の手では付かない）。文言そのものは v1427 と一字も変わらない
```

<!-- 送った知らせ ここまで -->







---

## 追記（12:44）　窓が二つ走っている

11:50:05 の `cd … && claude` の誤送で、**新しい窓が一つ立った**。それがこの窓（`claude.exe` pid 5616）で、
`cmd.exe` 9992 → `claude.exe` 5616 → いまの道具の親、と辿れる。**古い窓（pid 1740・08-28 03:25 起動）も
走ったまま**で、11:53:12 に常駐が「仕事 11」で起こしている（`worker.pid` が `job=11`）。
以後、二つの手が同時に動いていた。

古い窓が書いたもの（この窓は一度も触っていない）:

| 刻 | 物 | 中身 |
|---|---|---|
| 12:32:04 | `~/.claude/inbox-watch.ps1` | 郵便掃除（`Sweep-Mailbox`）と郵便の健康（`Write-MailHealth`）を実装 |
| 12:32:09 | `watch-notify.ps1.bak-20260829b` | 作法29 の写し |
| 12:34:42 | `~/.claude/watch-notify.ps1` | 「📮 郵便受けが詰まっています」を180秒で一発 |
| 12:36:00 | 常駐 | 起こし直し（古い pid 9040 を止めた） |
| 〜12:37 | `panel.html` / `panel-ver.txt` / `panel-probe.js` | panel v74（まとめて写すの数え方を `buildBundle()` へ寄せる） |

四つの見張り台本は**いずれも構文OK**（inbox-watch 749行／watch-notify 1832行／stop-guard 137行／
hook-notice 271行）。panel v74 は v1428 の commit には混ぜず、待避して作業ツリーへ戻してある。

### 郵便掃除は実装されたが、一度も走っていない

`Pull-Mailbox` の **L644 の早戻り（`if ($now -eq $fp) { return }`）が、L668 の `Sweep-Mailbox` より前**に在る。
新しい行が足されない限り指紋は変わらないので、掃除の枝には**到達しない**。実際の姿——
`mailbox.md` は15行のまま／`mailbox-archive.md` は頭書き4行のみ／`mailbox-pos.txt` は
`lines=15 fp=ae0196d3…` で **11:45:23 から動いていない**。

直すなら、早戻りを掃除の後ろへ回すか、掃除だけを早戻りより前で試みる形にする。
ただし**同じファイルを二つの窓で直すのは、作法28 が二度踏んだ事故と同じ形**なので、
どちらの窓で続けるかを決めてもらうまで手を付けていない。

---

## 追記（13:13）　二本並んだ原因と、その始末

### 前の追記を一つ訂正する

12:44 の追記で「古い窓（pid 1740）が panel v74 と郵便受けの直しを書いた」と書いたが、**これは誤り**。
書いたのは**見張りが背後へ投げた働き手（仕事11・セッション `0e51f2f4`）**だった。記録で当たる。

| 調べ方 | 0e51f2f4（働き手） | 1dfb453b（pid 1740 の窓） |
|---|---|---|
| `buildBundle` の出現 | 8 | **0** |
| `Sweep-Mailbox` | 10 | **0** |
| `panel-probe` | 18 | **0** |
| `Write-MailHealth` | 9 | **0** |
| 記録の刻 | 11:54:45 〜 **12:36:13** | 〜12:27:20 |

働き手は 11:53:12 に起こされ、**12:36:13 に記録が切れている**。これは常駐の起こし直し（12:36:08）と同じ刻で、
`Start-Job` の親ごと落ちたことを示す。`worker.log` / `worker.err` は**どちらも0バイト**——
**panel v74 を書き上げたのに、報告を一行も残せずに死んだ**。作業ツリーに未コミットで残っていたのはそのため。

### どの見分けが外れたのか

**08-26 の「同一台本の既存常駐を先に止める備え」（`inbox-watch.ps1` 26-47行）は正しく働いていた。**
今日の記録でも 12:36:08 に pid 9040 を一本止めており、常駐が二本並んだ回は一度も無い。

外れていたのは**別の見分け**——`Wake-Claude` の二重防止（旧 182-190行）。

```
$jobs = @(Get-Job -Name $JobName -ErrorAction SilentlyContinue)
foreach ($j in $jobs) { if ($j.State -eq 'Running') { … return $false } }
```

見ているのは「**この見張り自身が投げた仕事**が走っているか」だけ。注記にも
「手待ちでも対話の窓は開いていることがあるので、`claude.exe` の有無では判定しない」と明記してあり、
**開いている窓が働いていることを知る術を、そもそも持っていなかった**。

11:50 に窓が立ち、受信箱の四件を進め始めた。11:53:12、見張りは仕事9が終わったのを見て
「働き手は走っていない」と判じ、**仕事11を投げた**。ここで手が二つになった。

### 塞いだ形

`Wake-Claude` の入口（182-196行）に、**窓が仕事の最中なら起こさない**関門を足した。

```
$skey = ((Get-Content -LiteralPath $StatePath -TotalCount 1) …).Trim()
if ($skey -like 'run:*') { Say '窓が仕事の最中（run:）。二つの手にしないため起こさない'; return $false }
```

見るのは `watch-state.txt` の一行目で、`Publish-Status`（291-294行）が「作業中」と読むのと同じ字。
`wait:`（ヨシ待ち）では今までどおり起こす（2026-08-29 の決まりを変えない）。
起こさなかった回は `$false` を返すので**旗は立ったまま**で、窓が手を離せば次の巡回で起きる。

＊控え `inbox-watch.ps1.bak-20260829`（13:05:11）／構文OK 774行／BOM あり（`EF BB BF`）。
　常駐は 13:06:58 に起こし直し、`pid=13240` の一本だけ（台本の更新 13:05:56 より後）。

### 郵便掃除も、同じ「到達しない」形だった

働き手が置いた `Sweep-Mailbox`（522-571行）は**中身が正しいのに一度も走っていなかった**。
呼び出し（668行）が早戻り（旧 644行）の**後ろ**にあり、指紋が変わらない限り届かなかったため。
早戻りを直し（645-656行）、**読み残しが0のときは指紋が同じ回でも掃除を試みる**形にした。

| 刻 | 出来事 | 行数 |
|---|---|---|
| 13:06:58 | 常駐を起こし直し | mailbox.md 15行 |
| 13:08:10 | 溜まっていた11行を控えへ | **4行**（頭書きだけ） |
| 13:08:48 | 通しの試し：一行足す | 5行 |
| 13:09:11 | 受信箱へ（**23秒**） | 5行 |
| 13:09:14 | 控えへ（**書き足しから26秒**） | **4行 235バイト** |

`mailbox-archive.md` は 16行 12,033バイト。控え（`mailbox-pos.txt`）は `lines=4 / fp=`。
書き戻しは `sha` つきなので、読んだ後に足された行は GitHub が断って消えない。

### 古い窓を止めた

pid 1740（08-28 03:25:43 起動）は、止めた時点で**子プロセス0・12:27:20 から38分無音・自分の未コミットの差分は無し**。
作業ツリーの panel v74 は働き手の遺したもので、`947b648` として先に commit・push してから止めた。
記録（`1dfb453b`・7.9MB）は残っているので、要れば `claude --resume` で読み返せる。
いま生きている `claude.exe` は **pid 5616 の一本だけ**。

---

## 追記（13:15）　郵便詰まり-3 の実測（ヨシ待ちの最中の着手）

| 刻 | 出来事 | 書き足しからの秒数 |
|---|---|---|
| 13:12:44 | 郵便受けへ一行足す | — |
| 13:13:34 | 受信箱へ取り込み | **50秒** |
| 13:14:09 | 見張りが働き手を起こす | 85秒 |
| 13:14:51 | 働き手が着手（`wake-test.txt` へ書いた刻） | **127秒** |
| 13:15:07 | 働き手 終了・受信箱の未処理0件 | 143秒 |

同じ一本で、**新しい `run:` の関門も裏づけが取れた**。

```
13:11:50  窓が仕事の最中（run:）。二つの手にしないため起こさない
13:12:09  （同）
13:12:25  （同）
13:12:40  （同）
13:13:38  （同）        ← 取り込みの直後。旗は立てたまま落とさない
13:14:09  Claude Code を起こした（仕事 1）  ← wait: に変わった回に起きた
```

**塞いだ側は効き、開けておいた側（ヨシ待ち）はそのまま通った。**

### ただし、二つの手になる道はもう一本ある

この一本でも実は手が二つになった。**見張りが働き手を起こしたのと同じ刻に、
`inbox-feed.ps1`（Stop のフック）が同じ一行をこの窓へも渡していた**ためで、
`wake-test.txt` には二行が並んでいる（13:14:45＝この窓／13:14:51＝働き手）。

`run:` の関門はこの道を塞げない。見張りが見た刻には、この窓は確かに `wait:` だったからだ。
今回は一行を書くだけの仕事なので実害は無かったが、**関門は片側しか塞いでいない**。
塞ぐなら「フックが窓へ渡した回は、見張りの側を見送る」という取り決めが要る。
指示を待つ。

---

## 追記（13:22）　異常の文面の書き分けと、13:13 の枠が着かなかった理由

### 1. 窓が減った回を二つに書き分けた

直したのは `watch-notify.ps1`。控え（`watch-state.txt`）へ **`proc=`（claude の本数）**を足し、
窓の減りとプロセスの減りを比べる形にした。

| どちらか | 見分け | 題 | 本文 |
|---|---|---|---|
| **意図して止めた／閉じた** | プロセスも同じだけ減っている | ✅ 終わりました（返事不要） | 「閉じられたか、こちらで止めたものです。残っている物は無いので、**手は要りません**」 |
| **不意に消えた** | 窓だけ消えてプロセスが残る | 🪟 異常です（手が要ります） | 従来どおり「開き直してください。古いプロセスが残るので…」 |

前の本数が控えに無い古い形のときは、**手が要る側**で出す（数えられないものを「手は要らない」と言い切らないため）。
13:08:23 に出た知らせは、こちらの指示で pid 1740 を止めた回だった——プロセスも一緒に終わっており、
片付ける物は無かったのに「開き直してください」と出ていた。

＊写し `watch-notify.ps1.bak-20260829c`（13:17:37）／構文OK 1861行／BOM あり。
　13:19:03 の巡回で例外なく回り、控えに `proc=1` が入ったことを確認。

### 2. 13:13 の枠は、リポジトリに一度も着いていなかった

`mailbox.md` の commit を並べると、**人の手の `Update mailbox.md` は 11:44:39（JST）が最後**。
13:13 前後には一件も無い。受信箱まで来なかったのではなく、**GitHub に書き込めていなかった**。

| 刻（JST） | commit | 書いた者 |
|---|---|---|
| 13:13:39 | 郵便受け：取り込み済み 1行を控えへ移した | 見張り |
| 13:12:45 | 通しの試し：一行足す | 見張り（こちらの試し） |
| 13:09:16 | 郵便受け：取り込み済み 1行を控えへ移した | 見張り |
| 13:08:50 | 通しの試し：一行足す | 見張り（こちらの試し） |
| **13:08:11** | **郵便受け：取り込み済み 11行を控えへ移した** | 見張り |
| **11:44:39** | **Update mailbox.md** | **人の手（これが最後）** |

**帯は「古い窓を止めた前後」ではなく、掃除そのものだった。**
編集の画面は開いたときの姿（blob）を握っている。**13:08:11 に 15行→4行へ短くした**ので、
それ以前に開かれていた画面からの commit は、GitHub に食い違いとして断られる。

### 3. 塞いだ形 — 掃除は「取り込んだ直後」だけに戻した

`inbox-watch.ps1` **658-685行**。

- 掃除の本来の置き場所は**取り込んだ直後**（710行）。書いた人が commit を終えた直後なので、
  その人の画面を巻き込まない。
- 中身が動いていない回に掃除するのは、**前の回の掃除が失敗していたときだけ**
  （`$script:sweepRetry`。557／564／577／584／589行で立て降ろしする）。
  やり直しの道を残さないと、一度失敗した行が次の取り込みまで残る。

13:20:07 に常駐を起こし直し済み（`pid=5276`・台本の更新 13:19:50 より後）。一本だけ。

＊残る取り決め — **溜まった行は「次に一行書いたとき」にまとめて片付く**。
　書かないあいだは溜まったままだが、そのぶん編集の画面を横から短くすることが無くなる。
