# 点検3件（/clear の備え・断片待ちの知らせ・公開vの読み元）

**状態：作業中** — 2026-08-26 16:53 時点／stable を v1427 へ進めた（5e3d324）

**まとめ** — **済み2件／該当済み1件（すでにその作りでした）**。未了なし。
**畳む備えは整いました。** `/clear` はこちらからは打てないので、そちらでお願いします。

---

## 1. `/clear` の備え — **済み**

### 報告書と積み残しは最新です

| | いまの姿 |
|---|---|
| `report-latest.md` | この文書。**先頭に状態行**あり。push 済み |
| 控えの記憶 `handover-next` | **本体 v1427／`stable` も v1427（`5e3d324`）／`panel v35`／作法は30まで**へ更新済み |
| `work-note.txt` の `未検収:` | 更新済み |
| 未 commit の変更 | **無し**（`git status` は空） |

### 作法30 を足しました

> **`/clear` は区切りでのみ。畳む前に報告書と積み残しを最新化する。**
> 畳むと窓の中のやり取りは戻らず、**残るのは書いた物だけ**。古いまま畳むと、
> 次の窓は何が済んで何が残っているか分からない状態で始まる。

＊`/clear` を打つのは人の側なので、「**最新化を済ませたうえで『畳めます』と伝える**」も明記しました。

### いま残っている積み残し（次の窓へ引き継ぐもの）

- **目盛りの実戦較正** — 着手の合図待ち
- **押し出しの回数が増えた影響** — `ntfy-daily.tsv` が数日ぶん溜まってから報告
- **未検収** — 点検7件と仕上げ2件／`panel v35`（この回）

## 2. 断片待ちの知らせ — **該当済み。すでにヨシ待ちと同じ扱いです**

**確認の結果、すでに飛んでいました。** 断片で止まった回の控えは
`待ち: 指示の「以上」の到着` と書いてあり、**「待ち」がある＝ヨシ待ち**として扱われます。

```
2026-08-26 10:29:13  押し送りOK（HTTP 200）本日 20件目
2026-08-26 10:29:13  控えへ入れた [🙋 ヨシしてください]
   ← 控えの件名「点検6件の指示 — 断片を受領、続き待ち」／印 y0826-1028
```

**二度目からは出ません。**

```
2026-08-26 10:30:21  押し送りはしない（この待ちでは既に送った）
```

ひと続きの待ちで押し送るのは**初回1件だけ**という決めが、そのまま効いています。

**時間切れは入れていません。** ご指示のとおり、**届いた断片を勝手に解釈して実行することはしません**。
「以上」が来るまで待ち、来た時点で断片を順に繋いで一つの指示として実行します（作法23）。

## 3. パネルの「公開」欄の読み元 — **済み（付け替えは不可。注記を入れました）**

### 付け替えは**できません**

読み元は **`stable` の枝**で、`raw.githubusercontent` から取っています。

**GitHub Pages は `stable` の中身を配っていません。** Pages が配るのは `main` の内容だけで、
タグや別の枝は対象外です。したがって**遅れの小さい先へ直に付け替えることはできません**。

＊唯一の逃げ道は「`stable` を動かすとき、同じ版を `main` 側の別ファイル（例 `stable-ver.txt`）へも書く」
　という**写しを置く**形です。Pages の遅れ（約5分）で読めるようになりますが、
　**書き忘れると、実際のタグと食い違った数字を出し続けます**。
　**遅れた正しい数字より、速い間違った数字の方が危うい**と判じて、採っていません。

### 遅れの実測（今日）

```
13:5x  stable を 5e3d324（v1427）へ押した
13:5x  raw = 1426   ← 直後は古いまま（三回引いても同じ）
16:53  raw = 1427   ← 追いついた
```

**追いつくまでに最大で3時間近くかかりました。** これまで控えていた「約5分」は、
`raw` については**当てはまりません**（あれは Pages の遅れです）。

### 注記を入れました（**panel v35**）

版の欄のすぐ下に、一行で断ります。

> ＊「公開v」は stable の枝から読んでいます。**最大で数時間遅れる**ことがあります。

事実の行より一段小さく、控えめな色で置いてあります。

---

# 実機で見るところ

**返事パネル**を開いて二つ。

1. 画面のいちばん下の版の字が **`panel v35`**（v34 のままなら URL に `?_v=35` を付けて一度だけ開く）
2. 版の欄（「本体 v… ／ 公開 v…」）の**すぐ下に一行**、
   「＊「公開v」は stable の枝から読んでいます。**最大で数時間遅れる**ことがあります。」が出ていること

＊いま `raw` は追いついているので、**公開v は 1427** と出るはずです。
