# 報告：投票欄の折り返しの条件を案Bへ（v1416）

日付 2026-08-20 ／ 対象 v1416 ／ 前版 v1415（`c3edeb3`）
測定は写しに probe（headless Edge ＋ `--dump-dom`、作法§14）

---

## 1. 裁定

**案B（実測どおりの二本立て）を採用。** 条件が二行になって読みにくいのは承知のうえで、
正しさを取る。案Aは 350〜380px の帯を捨てることになり、収まるのに折り返さない画面が出るため。

あわせて **`check.js` ⑦ の測る視野に 900x340 と 900x381 を追加**。
**900x381 は速い版（`--fast`）にも入れる**——切り詰めが外れてカード高が跳ねる段差そのもので、
レイアウトを触るたびに壊れやすい場所だから、常に見張る。900x340 は素直な高さ不足なので
フル版だけでよい。

---

## 2. 入れた条件

`koushu-handan.html`（投票欄の折り返し）

**前（v1415）**
```css
@media (orientation: landscape) and (min-width: 844px) {
```

**後（v1416）**
```css
@media (orientation: landscape) and (min-width: 844px) and (min-height: 350px) and (max-height: 380px),
       (orientation: landscape) and (min-width: 844px) and (min-height: 384px) {
```

中身（余白の切り詰め12行）は**一つのまま**。カンマ区切りの一括指定にして、同じ本体を
二度書かない——二本に割ると片方だけ直す事故になる。

`orientation: landscape` は v1415 のまま残した。縦向きは幅にかかわらず `.wrap` ごと畳んで
全画面の催促案内（「画面を横にしてお使いください」）だけを出す作りなので、縦向き用の閾値は要らない。

---

## 3. 実測 ── 帯の境目（幅900px・判定後）

判定まで進めて、末尾の二択（連チャン／親流れ）の bottom を測った。

| 高さ | v1415 の bottom | v1416 の bottom | 差（v1416） | 段数 | 判定 |
|---|---|---|---|---|---|
| 330 | — | 322.8 | −7.2 | 1段 | 収まる |
| **340** | **344.3（+4.3 画面外）** | **327.8** | **−12.2** | 1段 | **直った** |
| 349 | — | 332.3 | −16.7 | 1段 | 収まる |
| 350 | 349.3 | 349.3 | −0.7 | 2段 | 変化なし |
| 360 | 354.3 | 354.3 | −5.7 | 2段 | 変化なし |
| 380 | 364.3 | 364.3 | −15.7 | 2段 | 変化なし |
| **381** | **382.5（+1.5 画面外）** | **380.5** | **−0.5** | 1段 | **直った** |
| 383 | — | 381.5 | −1.5 | 1段 | 収まる |
| 384 | 384.0 | 384.0 | ±0.0 | 2段 | 変化なし |
| 390 | 387.0 | 387.0 | −3.0 | 2段 | 変化なし |

**外れる帯は二つとも塞がった。** 350〜380px と 384px 以上（＝もともと収まっていた範囲）の
数値は前版と完全に一致していて、折り返しの挙動を変えていない。

### 余白の薄い二点（今後の注意）

- **384px ちょうどで ±0.0px**。前版から変わっていないが、二択の下端が視野の下端と一致する。
  ここから 1px でもカードが伸びると外へ出る。
- **381px で −0.5px**。一段に落ちたぶんで収まってはいるが、余白は 0.5px しかない。

どちらも ⑦ が見張る位置に入った（384 は 844x390・932x430 と同じ性質、381 は速い版で毎回）。

---

## 4. `check.js` ⑦ の視野

**前**
```js
const VIEWS = FAST ? [[568, 320]] : [[568, 320], [667, 375], [844, 390], [932, 430]];
```

**後**
```js
const VIEWS = FAST ? [[568, 320], [900, 381]]
                   : [[568, 320], [667, 375], [844, 390], [932, 430], [900, 340], [900, 381]];
```

- 速い版：**1視野 → 2視野**（＋ judged だけ 844x390 は従来どおり）
- フル版：**4視野 → 6視野**

速い版の所要は 2〜3分 → **約5分**（視野が倍になったぶん）。push 前の `.githooks/pre-push` も
この時間になる。フル版は約9分 → **約13分**。

視野の意図が後から分かるよう、`VIEWS` の直前に理由をコメントで残した。
速い版の案内文（「速い版：視野は 568x320 のみ…」）も実態に合わせて直した。

---

## 5. 検査

`node check-all`（**フル版**。レイアウトを触った回のため）

| | 結果 |
|---|---|
| `check.js` 本体 ①〜㉑ | **全PASS** |
| ⑦ 狭い画面での溢れ（6視野 × 9画面） | **PASS** |
| ⑬ 非日本語文字の混入（ゼロ基準） | **PASS** |
| `adv-check.js` 探偵編の回帰（即死罠・時間切れ） | **全PASS** |

その他

- `node --check`：`koushu-handan.html` の `<script>` 4ブロック／`check.js` ともに OK
- `serifu.txt`：行番号がずれたため `node serifu-extract.js` で再抽出（7593行・**口調の混入 0件**）。
  `--check` で本体と一致を確認
- 版表記3箇所（`data-ver` / `verTag` / `ver.txt`）を **1416** に同時更新
- **テスト用のシムなし**（帯の測定は写しに probe で行い、写しごと消した。本体には何も入れていない）
- `analyze()` / `bestShapeOneSuit` には触れていない

---

## 6. 次

- **実機での検収**。OK が出れば `stable` を v1416 へ進める（いまは v1377 `3aa4b5d` を指したまま）。
- 未着手のまま残っている件：**振りの八枚の焼き直し**（指示待ち）。
