# 知らせの溜め（hold）が空で吐き出される件を直した

**状態：終わり 08:42（VAIO）／残り4件**

**結論** — 穴は**二つ**あった。両方を直し、**二本が一本にまとまって出ること・60秒の決まりが効くこと・題と本文が入ること**を実測で確かめた。**08-28 17:59 以降に空で出たのは14回**、溜めへ入って消えたのも**14本**。

---

## 穴 その一 — 一本のとき配列がほどける

`Flush-Held` が `Read-Held` の戻りを **`@()` で包んでいなかった**。溜めが**一本のとき**、PowerShell は配列をほどいて**連想配列そのもの**を返す。すると `.Count` が要素数ではなく**鍵の数（at/title/body/prio/tags/state ＝ 6）**を返し、`$held[0]` も `$held[5]` も `$null` になる。

- 記録に必ず「**6本**」と出ていたのは、これが鍵の数だったため
- 待ち始めが `0` と読まれ、経過が**生の通算秒（1787958362秒）**になり、毎回すぐ吐き出した
- 題も本文も `$null` になり「**控えへ入れた []**」で出た

**作法26 が既に注意している穴**（「PowerShell で一覧を受けるときは必ず `@()` で包む」）と同じ形です。**同じ穴を二度踏みました。**

## 穴 その二 — TrimEnd がタブを刈る

`Read-Held` が `.TrimEnd()` で行末を刈っていた。末尾の欄（`state`）は空のことが多く、そのとき行は「…`\t`」で終わる。**`TrimEnd()` はタブも空白として刈る**ので欄が 6→5 に減り、下の「6欄あるか」の検めで**行ごと捨てられていた**。

**こちらのほうが重い。**捨てられた行は読まれないまま、吐き出しの回にファイルごと消える。**溜めた知らせが黙って失われる筋**はこれです。

## 直し

- `$held = @(Read-Held)` … 包む（804行）
- 行末は**改行だけ**落とす。欄は**頭の三つ（刻・題・本文）さえ在れば読み**、残りは既定で埋める
- **題も本文も無い物は出さない。**溜め場も消さずに残し、次の回でもう一度読ませる（空で出す事故の止め）

作法29 の順を踏んだ（写し `.bak-20260829` → 直し → 構文OK・1743行）。`watch-notify.ps1` は**毎分の起動**なので、常駐の起こし直しは要らない。

---

## 実測

**二本を20秒あけて積んだ場面**

| | |
|---|---|
| 一本目を積んだ | 08:37:05 |
| 二本目を積んだ | 08:37:26 |
| 08:38:00 の回 | 「2本を**待たせている**（最後の一本から35秒・待ち始めから56秒）」 |
| 08:39:00 の回 | 「2本を**まとめて出す**（最後の一本から95秒（**待ち60秒**））」 |
| 控えに載った | 08:39:02 |

- **一本にまとまった。**題「🔎 調べました」（最後に立った一本の題）
- **本文に二本ぶんが両方入った**（「この間にまとまった知らせ 2本」＋札二枚）
- **出るまで** … 一本目から**115秒**／二本目から**94秒**
- **60秒の決まりで出た**（上限5分ではない）。見張りが毎分なので、刻みは分単位になる

**空で出た本数** — 08-28 17:59 以降で **14回**（`控えへ入れた []`）。溜めへ入って消えたのも **14本**で、内訳は ✅終わりました **10本**・🔎調べました **4本**。**待たせない三種（ヨシしてください・異常です・延びています）と手の試しは、一本も落ちていない。**

**拾い直し** — **本文は復元できません。**溜め場は吐き出しのたびに消える作りで、読めないまま消されたためです。残せる**時刻と題を並べた控えを一枚**にして戻しました（08:41:02 の札）。最後の一本（08-29 08:05）の中身だけは `worker.log` に残っており、「二度目も届いた／書き足し 08:03:24 → 着手 08:03:38（14秒）」でした。

---

## 訂正

前の回に「`notices.json` が 08-28 17:59 から更新されていない」と申し上げましたが、**誤りです。**控えは新しい順に並んでおり、末尾を読んでいました。控えも公開側も動いており、落ちていたのは**溜めを通した知らせだけ**です。

## 残り

1. **観察-1** 生存の欠け率の報告 — 本日 2026-08-29
2. **観察-2** 押し送り件数の報告 — 本日 2026-08-29
3. **観察-3** 逆向きの疑いの件数の報告 — 本日 2026-08-29
4. **凍結-0** 発効中
