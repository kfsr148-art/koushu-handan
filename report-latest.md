# 指示の郵便受け（GitHub の私有リポジトリ）

**状態：終わり 22:26（VAIO）／残り4件**

**結論** — 私有リポジトリ `kfsr148-art/claude-mailbox` の `mailbox.md` へ一行書き足すと、**最大60秒で受信箱（inbox.txt）へ入り、窓が着手する**。通し試しの実測は **50秒**。ntfy の購読は戻していない。今の「枠を渡して貼る」道はそのまま残り、これは**並行の道**として増えた。

---

## iPhone から一行書き足す手順

1. Safari で `https://github.com/kfsr148-art/claude-mailbox/edit/main/mailbox.md` を開く（初回だけ GitHub へログイン）
2. 開いたら共有ボタン →「**ホーム画面に追加**」。次からはこの一つを押すだけで編集画面が開く
3. 文章欄のいちばん下をタップし、最終行の末尾で改行して、指示を一行書く
4. 右上の緑の「**Commit changes...**」を押す
5. 出てくる画面でもう一度「**Commit changes**」を押す（説明は空のままでよい）
6. 最大60秒で受信箱へ入る。**空行と行頭が `#` の行は読み飛ばす**ので、覚え書きも書ける

---

## 作り

| | |
|---|---|
| 宿 | 私有リポジトリ `kfsr148-art/claude-mailbox`（新設） |
| ファイル | 根に `mailbox.md` 一枚。手元へ clone はしない |
| 取り方 | `gh api repos/…/contents/mailbox.md` で中身だけを取る |
| 間隔 | **60秒**（常駐の巡回15秒の四巡に一度）。認証済みの gh は5000回/時なので軽い |
| 読んだ位置 | `~/.claude/mailbox-pos.txt`（読んだ行数＋中身の指紋） |
| 書く形 | `[ ] YYYY-MM-DD HH:MM:SS  （郵便受け）本文` |

**控えのリポジトリへ置かなかった理由** — `home-backup.ps1` が47行で `git add -A`、85行で `push` を回すので、**遠くにだけ在って手元に無いファイルは「消える側」で staged される**。翌朝 03:30 の控えで郵便受けが消えていた。宿を分けて避けた。

**短くなったときの守り** — 書き換え・切り詰めで行数が減ったら、**その回は何も読まずに数を合わせ直す**。過去の分をもう一度流し込まないため。

---

## 実測（通し）

| | |
|---|---|
| ① 書き足した | 22:22:11 |
| ② 受信箱へ入った | 22:23:00 |
| ③ 掛かった | **50秒** |
| ④ 入った形 | `[ ] 2026-08-28 22:23:00  （郵便受け）郵便受けの通し試し（二度目）。…` |
| ⑤ 窓の読み口 | `inbox-feed.ps1 -Kind read` が「未処理1件」として拾った |
| ⑥ 着手 | 指示どおり `mailbox-test.txt` へ書き、行頭を `[x]` に閉じた |
| ⑦ 短縮の守り | 6行→4行で「この回は読まず、数を合わせ直す」。再流入0件 |

**途中で一度取りこぼした。** 末尾の改行が「空の一行」として数に入り、次に足した本物の一行がその位置に重なって飛ばされた（pos だけ 5→6 に進んで中身は0件）。**末尾の改行を落としてから数える**形へ直し、直後の通し試しで50秒が出た。

もう一つ、**古い常駐が直す前の版のまま最後の一巡を回して pos を書き直していた**ため、直しが効いていないように見えた回があった。作法29 の「常駐を起こし直す」を踏んだうえで、**起こし直しの前後で pos に触らない**のが正しい順。

---

## 触ったファイル

- `~/.claude/inbox-watch.ps1` … 433 → 527行（写し `.bak-20260828c`）。`Pull-Mailbox` と巡回の四巡に一度の呼び出し。構文検査を通した
- `~/.claude/mailbox-pos.txt` … 新設
- `~/.claude/orders-open.tsv` … 郵便受け-1 を済で閉じた

## 残り

1. **観察-1** 生存の欠け率の報告（起点 795本中17回＝2.1%）— 2026-08-29
2. **観察-2** 押し送り件数の報告（起点 08-25=9件・08-26=33件）— 2026-08-29
3. **観察-3** 逆向きの疑いの件数の報告（起点 0件）— 2026-08-29
4. **凍結-0** 発効中（見張り一式は修理と観察報告だけ）
