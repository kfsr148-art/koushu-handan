# 写し取り 第二段 — 遷移口を写しの側に掘った（45枚 → **60枚**）

**状態：終わり** — 2026-08-25 23:4x 時点

**結論** — **60枚**（＋15枚）。**失敗0・指紋のかぶり0**。
**本体 `koushu-handan.html` には一行も入れていません**（`data-ver` 1426 のまま、`PROBE` の残骸0件）。

---

## 0. どこに掘ったか

遷移口は **`shot-all.js` の中**にあります。回すたびに**本体から写しを作って差し込み**、
撮り終えたら**写しごと消します**（作法14）。作り置きも使い回しもしません。

**差し込みは四箇所。それぞれ「その字面がちょうど1件あること」を数えてから当てます。**
本体が変わって当たらなくなったら、黙って撮り続けずに**その場で止めます**。

```
遷移口を差し込めない：①URL の読み取り の当たりが 0 件（1件であるべき）。
本体が変わっています。台本の find を取り直してください。
```

＊本体は改行が CRLF なので、**写しの側だけ LF に均して**から当てています（本体は触りません）。

## 1. 掘った三つ

| # | 口 | 中身 |
|---|---|---|
| ① | `?probe=adv&scene=<intro\|hall\|room\|note\|diff\|end>&room=<部屋>&kind=<終局の種類>` | `advStart()` で一式を組んだあと、見せる場と居る部屋を**直に差し替えて描き直す**。終局は `advEnd(kind)` を呼ぶ |
| ② | `?probe=tori&result=<win\|lose>&level=<easy\|normal\|hard\|endless>&caught=<数>` | 難易度を選んで始めたあと、**捕った数を入れて結果の絵を直に出す** |
| ③ | `?probe=ss&img=<画の在りか>&calib=1` | 写真を `window.ssImg` へ入れてから `ssShow()`。`img` を省くと**その場で描いた試し画**を使う |

**`?probe=` が無ければ、どの口も一切走りません。**

### ②について — **この遊びに「引き分け」はありません**

`finish(win)` の真偽二択で、**勝ちと負けの二つだけ**です。三つ目は本体に無いので作っていません。
（ノルマに足りない数で勝ちを指定することはできます＝絵は勝ち、数は途中。）

### ①について — 場は指定できても、中身は毎回変わります

犯人・目撃者・アリバイは `advNew()` が毎回振り直すためです。
種を固定する口は影響が大きいので、この段では作っていません。

## 2. 増えた15枚

| 群 | 第一段 | 第二段 | 足したもの |
|---|---|---|---|
| **探偵編** | 2枚 | **13枚** | 廊下／猫室／執事の部屋／**16号室（罠を踏まずに）**／探偵ノート／難易度／**終局6種**（勝ち・行動力ぎれ・ナイフ・皆殺し・買収・座礁） |
| **ミニゲーム** | 5枚 | **7枚** | **勝ちの絵**／負けの絵／**上級を勝ち**（遊んでいる最中の3枚はそのまま） |
| **その他の間** | 4枚 | **6枚** | **写真読みの盤面**（試し画を13マスに切り出した姿）／**目盛り合わせ** |
| その他の群 | 34枚 | 34枚 | 変わらず |
| **合計** | **45枚** | **60枚** | **＋15枚** |

一覧シートは `shots/index.html`、控えは `shots/shots.json`。撮影は **411.9秒**（一枚あたり約7秒）、`shots/` は 15MB。

## 3. 途中で見つけて直した四つ

**① 写しの中で `<script>` が二重になっていた**
差し込む字面が `<script>` から始まるのに、差し込む側でも `<script>` を書いていました。
内側の `<script>` を JS として読んで**丸ごと落ち**、`PROBE` ごと未定義に（`Unexpected token '<'`）。
**指紋のかぶり検査が「8枚とも同じ絵」として捕まえてくれました。**

**② 終局が勝ちも敗けも同じ絵だった**
`advBuildEnding()` は頁を**返すだけ**で `advEndPages` には入れません。
呼んで捨てていたので、乗船の幕のまま撮れていました。`advEnd(kind)` を呼ぶ形へ直し、
**6種類が別々の絵**になりました。

**③ 開くのに失敗する回があった**
一度目の通しで、訛りの2枚（京都・青森）が `page.goto` の**30秒切れ**で落ちました。
本体は8MB近くあり、続けて開くと間に合わないことがあります。
**一度だけ開き直す手**を足しました（間を置いて45秒で再挑戦）。段取りの誤りと、ただの取りこぼしを混ぜないための受けです。

**④ 部分撮りが一覧を潰していた**
`--only` で一部だけ撮ると控えを丸ごと書き換えてしまい、**撮っていない分が一覧から消えて**いました。
いまある控えへ**混ぜる**形に直しました。

＊一度目に出た指紋のかぶり（`07-7-tone-sensei` ＝ `08-3-tool-om`）は、判定後の人柄が
　毎回ランダムに振られるための**たまたま**でした。撮り直しでは**かぶり0組**です。

## 4. 回し方

```
node shot-all.js                 全部撮る（約7分・写しを作って消すところまで込み）
node shot-all.js --only 17-      名前や群で絞る（控えへ混ぜるので一覧は潰れない）
node shot-all.js --published     公開ページをそのまま撮る（遷移口は無い＝第二段の場は撮れない）
node shot-all.js --index-only    撮らずに一覧シートだけ組み直す
```

## 5. 触ったもの

| ファイル | 中身 |
|---|---|
| `shot-all.js` | 遷移口・写しを作る手・第二段の撮る対象・開き直し・部分撮りの混ぜ込み |
| `shots/` | PNG **60枚**＋`index.html`＋`shots.json` |
| **`koushu-handan.html`** | **無変更**（`data-ver` 1426／`ver.txt` 1426／`PROBE` の残骸0件） |
