# usage.json に watch を足しました ／ 動作記録の一行（Git Bash 用）

**状態：ヨシ待ち（動作記録の有効化）** — 2026-08-25 07:35 時点

## 1. 動作記録の再有効化 — Git Bash からそのまま貼れる一行

```
! powershell.exe -NoProfile -Command "Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','C:\Users\user\.claude\enable-tasklog.ps1'"
```

`enable-tasklog.ps1` は書き直してあり、**wevtutil ／ レジストリ ／ `Get-WinEvent` の三通りで
読み返し**、`wevtutil` で入らなければ**レジストリへ直に書きます**。最後に三つの値を並べて
出すので、「入ったのに戻った」のか「そもそも入っていない」のかが切り分けられます。

済みましたら一声ください。こちらでも三通りで読み直します。

---

## 2. usage.json の watch — 実際に書かれた一件（そのまま）

```json
"watch":{"at":"2026-08-25T07:29:22+09:00","claude":1,"windows":1,"state":"ヨシ待ち"}
```

**突き合わせ**

| 項目 | 実際の値 | watch |
|---|---|---|
| 鍵 | `wait:2026-08-25 06:54:01` | `state: ヨシ待ち` ✓ |
| claude のプロセス数 | 1 | `claude: 1` ✓ |
| 窓の数 | 1 | `windows: 1` ✓ |

**中身**

| 名前 | 中身 |
|---|---|
| `at` | この回に見張りが刻んだ時刻（状態行と同じ回） |
| `claude` | `claude.exe` のプロセス数 |
| `windows` | 題つきの窓の数 |
| `state` | 作業中／ヨシ待ち／手待ち／異常 |

- **新しい取得は増やしていません**（ご指示2）。使用量を取る回に、既に数えてある値を並べるだけです。
- 遠隔（`origin/main`）にも同じ中身で載っています。公開ページへの反映は数分遅れます。

> **お含みおきください** — `usage.json` が **push されるのは「使用量の値が動いたとき」か
> 「30分経ったとき」だけ**です。`watch` は毎回書き直されますが押し出されるのはその折なので、
> **ウィジェットから見える `watch` は最大30分ぶん古いことがあります。**
>
> 「いま動いているか」を見たいご意図からすると足りないかもしれません。`state` が変わったときにも
> push する形にできます（押し出しは増えます）。ご入用なら仰ってください。今回は「取得を
> 増やさない」というご指示の範囲に留めています。

## 3. 昨夜の直しの取りこぼし（直しました）

`push` を20秒で見切る形にしたとき、時間つきの `WaitForExit` のあとに `ExitCode` を読んで
いましたが、**ここが空になることがありました**（実測）。空を「失敗」と判じてしまい、成功しても
控えを更新せず毎回出し直す形になっていました。

`~/.claude/watch-notify.ps1:1024-1032` — 引数なしの `WaitForExit` をもう一度呼んで出口の処理を
確定させ、空のときは「失敗」とみなさないようにしました。直したあと
「使用量：HTTP 200 push した」が正しく出ています。

## 変更した行

| ファイル | 行 |
|---|---|
| `~/.claude/watch-notify.ps1` | 988-1002（`watch` を組む）／1024-1032（終了コードの読み方） |
| `~/.claude/enable-tasklog.ps1` | 全面（三通りで読み返す形へ） |

## いまの版

本体 **v1426** ／ stable **`f3837e8`** ／ panel **v29**。本体もパネルも触っていません。
