# 記録をドラ入りで取り直し、①②③の変化件数を出し直した（本体は無変更・v1429 のまま）

**状態：終わり（残り0件）／VAIO 2026-08-29 16:0x**

**結論** — ドラ表示牌を局ごとに置いた4000行を作り直した（`core-battle-log-dora.tsv`）。
**③は `doraValue=0.5` が 333行（8.32%）出て、そのすべてで根拠欄①の印が ✓→× に変わった。**
②は前回と同じ **`totalBlocks` 493件・兎の票6件**、①は **0件**、**`verdict` は 0件**。
`totalBlocks` の読み手は **兎ただ一人**（`usagiLean` L4022-4023）で、他に読み手はない。
**本体（`koushu-handan.html`）は一字も書き換えていない。**

---

## 1. 記録の作り直し

### 1-1. 作り方

前の記録（`core-battle-log.tsv`）は `core-battle.js` L128 が `analyze()` へ **ドラを 0 で渡していた**ため、
`doraValue` が整数しか取らず ③ の効き目を一度も測れていなかった。**同じ1000局の配牌に、ドラ表示牌を局ごとに置き直した。**

| | |
|---|---|
| 書き出し先 | **`core-battle-log-dora.tsv`**（4004行＝頭書き3行＋見出し1行＋4000行） |
| 配牌・結末 | `core-battle-log.tsv` と**同じもの**（局・席・親・配牌・結果・点棒・役・巡目） |
| ドラ表示牌 | 局ごとに、**四人の配牌に無い84枚**から一枚。種は `mulberry32(1429)` 固定で、**回すたび同じ並び**になる |
| `doraCount` | 席ごとに `hand.filter(t => t.code === indicatorToDora(表示牌)).length`（本体 L2305 と同じ式） |
| 呼び方 | 本体から `analyze()` と `indicatorToDora()` の**字面を読み出して**組んだ（写しを持たない）。`analyze()` が外から読む値は `hand`／`seatWind`／`roundWind`／`doraCount`／**`doraIndicator`** の五つで、`core-probe.js` は四つしか渡さないため、そこだけ一つ増やした |

**断り** — 結末（結果・点棒・役・巡目）は元の記録のままで、**この表示牌の下で打った結果ではない**
（元の対局が使った表示牌は記録に残っていない）。**①②③の変化件数を数えるための記録**であって、
成績（和了率・放銃率・点棒）の比較には使えない。頭書きの三行にも同じ断りを書いた。

### 1-2. 置いたドラの出方

| 席ごとの `doraCount` | 0枚 | 1枚 | 2枚 | 3枚 |
|---|---|---|---|---|
| 行数 | 2641 | 1181 | 168 | 10 |

### 1-3. ①②③の変化件数（旧版 v1428 の写しと新版 v1429 を並べて呼ぶ）

```
旧版 analyze() : v1428.html L2434〜3063
新版 analyze() : koushu-handan.html L2434〜3068
```

| 件 | 変化した行数 |
|---|---|
| **①撤退予約線のコメント** | **0 / 4000** — コメント一語のみ。`analyze()` の返しにも表示の式にも触れていない |
| **②`totalBlocks` の寄せ** | `totalBlocks` **493 / 4000（12.3%）**／**兎の票 6 / 4000（0.15%）**・動きは `{"a→黙る": 6}` |
| **③`hasValue` の揃え** | 下の表 |
| **`verdict`（攻守）** | **0 / 4000** |

`analyze()` の27項目で値が変わったのは **`totalBlocks`（493件）と `hasValue`（333件）の二つだけ**。
②の数字はドラを置いても前回と**寸分違わない**（`totalBlocks` は打点に依らないため）。

### 1-4. ③の内訳（指示どおり両方書く）

| | 行数 | 割合 |
|---|---|---|
| **`doraValue` が 0.5 になった行** | **333 / 4000** | **8.32%** |
| うち旧版（v1428）で印が ✓ だった行 | **333** | 100% |
| **印が ✓→× へ変わった行** | **333 / 4000** | **8.32%** |
| `hasValue` が変わった行（全体） | 333 / 4000 | — |
| **根拠欄①の題／詳細が変わった行** | **0 / 4000** | — |

**題と詳細が0件なのは、差し替えが効かなかったのではなく「字を変えないために差し替えた」から。**
題の分岐を `a.hasValue` から `a.valueSources.length` へ移してあるので、333行はすべて
「**打点 0.5 — ドラ1（孤立）**」の字を保ったまま、**印だけが ✓→×** になる。
**差し替えていなければ、この333行すべてが「打点の種なし／ドラ・赤・役牌対子なし」という誤りの字になっていた**
（実際にはドラが一枚ある手なので）。

出た例（先頭6件）：

```
3478m36p1247s357z  表示s6→ドラs7  doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
233468m26p33s367z  表示F →ドラC   doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
126m34489p236s47z  表示W →ドラN   doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
17m12678p12279s4z  表示W →ドラN   doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
1568m235p34s3457z  表示S →ドラW   doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
1456m29p779s2467z  表示p8→ドラp9  doraValue 0.5  印 ✓→×  題「打点 0.5 — ドラ1（孤立）」
```

＊前回の見積り（同じ4000手にドラ表示牌を**34種すべて等確率で**置いた計算）は **7.43%** だった。
　今回の **8.32%** はそれより高い。表示牌を**山の残り84枚から引く**と、四人の手に多く入っている牌は
　表示牌になりにくく、その分「手に一枚だけ」の当たり方が増えるため。**実際の卓に近いのは今回の値。**

＊`verdict` が0件なので、**③は攻守を一切動かさない**ことが、`doraValue=0.5` が実際に333回出た記録でも確かめられた。

---

## 2. `totalBlocks` の読み手の一覧

**本体の読み手は兎ただ一人。** 493件動いて票が6件しか動かなかったのは、読み手が一人しかいないうえ、
その一人の使い方が **`totalBlocks >= 6` という一本の境目だけ**だからで、493件のうち境目をまたいだのが6件しかない。

| 場所 | 行 | 種別 |
|---|---|---|
| `koushu-handan.html` | **L3031** | **定義** `const totalBlocks = goodShapes + weakShapes;` |
| `koushu-handan.html` | **L3059** | `analyze()` の**返しに載せているだけ** |
| `koushu-handan.html` | **L4022** | **兎**（`usagiLean`）`if(!a \|\| typeof a.totalBlocks !== 'number') return '';` |
| `koushu-handan.html` | **L4023** | **兎**（`usagiLean`）`if(a.totalBlocks >= 6) return 'a';` ← **唯一、値を判定に使っている箇所** |
| `koushu-handan.html` | L4075-4076 | v1429 で足した**注記の字**（コメント） |
| `core-battle-log.tsv` | 2行目 | **列の見出し**（データ） |
| `core-battle.js` | L72 | **列名の並び**（書き出す項目名の配列）。値は読んでいない |
| `core-probe.js` | L117 | **列名の並び**（同上） |

**結論：値の読み手は「兎のみ」。** 七人の見立て・判定カード・根拠欄・集計行・撤退予約線・虎・
判定獣・誤診記録・見張り台のいずれも `totalBlocks` を読んでいない。

---

## 3. 残り

**残り0件。**

| 番号 | 項目 | 状態 |
|---|---|---|
| 記録直し-1 | ドラ入りで4000行を取り直し、①②③の変化件数を出し直す | 済 |
| 記録直し-2 | `totalBlocks` の読み手の一覧 | 済 |

`orders-open.tsv` に「済／取消／未了」以外で残っているのは次の四行だけで、**いずれも作業ではない**。

- `凍結-0`（**発効中**）… 見張り一式の凍結。状態であって作業ではない
- `再起動-1` / `再起動-2`（**断線-5 へ統合・消していない**）… 統合先の `断線-5` は済
- `被り-1`（**記録のみ・直しは当てない**）… 調べの結果は `report-latest.md` に残した

**未着手・未了は0件。**

---

## 4. 本体に触れていないことの確かめ

```
$ git status --short koushu-handan.html ver.txt
（変化なし）
```

版は **v1429** のまま。今回足したのは記録 `core-battle-log-dora.tsv` と、この報告書だけ。

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
副産物三件を本体へ当てた（v1429）— 撤退予約線のコメント／totalBlocks の寄せ／hasValue の閾値
①撤退予約線 L8400 のコメントを「受け入れ枚数から」→「初期シャンテンから」へ（実装を正とした）②totalBlocks を七対子の上書き直後（新 L3031）へ寄せた③hasValue を doraValue>=1 へ揃え、根拠欄①の題と詳細の分岐を valueSources.length へ差し替えた。当てる前に hasValue の読み手を全部 grep し、根拠欄①以外に無いことを確かめた。兎の注記に「50000回の数字は寄せる前のもの」と四行添えた。版は三箇所同時に v1429、serifu.txt/serifu-adv.txt は再抽出して一致、check-all --fast は両方PASS、commit 0a04752 を push して公開側を実読みし手元と完全一致
ファイル: koushu-handan.html（L2870-2873・L2514削除→L3028-3031・L4074-4077・L8400・L8483-8488・L2 data-ver・L1316 verTag）／ver.txt／serifu.txt／serifu-adv.txt／report-latest.md（全面書き直し）／~/.claude/orders-open.tsv（副産物-1〜3 を済へ）
実測: 【4000行の回し直し（v1428 の写しと v1429 を並べて呼ぶ）】①0件／②totalBlocks 493件（12.3%）・兎の票6件（0.15%・すべて a→黙る）＝宣言の6件と一致・票の内訳 攻56→50／守1066据置／黙る2878→2884／③0件（この記録は doraCount を0で渡しており doraValue=0.5 が出ないため）／verdict 0件／変化のあった項目は totalBlocks だけ。【③の題と詳細】変化0件・valueSources.length は 0件=2343／1件=1431／2件以上=226。ドラ表示牌を34種置くと136000通り中10105通り（7.43%）で印が✓→×になる。【検査】serifu-extract 合計7593行・口調の混入0件・探偵編238行／--check は本体と一致／check-all --fast 両方PASS。【公開側の実読み】ver.txt=1429・data-ver="1429"・verTag=v1429・L2873 hasValue>=1・L3031 totalBlocks・L8400 コメント・L8485 valueSources.length。公開物と手元は diff 完全一致
実機: 判定を押して ── 根拠欄①でドラ表示牌を指定し、そのドラが一枚だけ・同色±2に相棒なし・赤も役牌対子も無い手にすると、題は「打点 0.5 — ドラ1（孤立）」のまま左の印が✓ではなく×／投票欄の兎が、七対子に寄った手（対子4〜5組で面子手の分解では6ブロック取れる形）で攻めの箱から消えて黙る／左上の版の字が v1429
```

### 2. 🙋 ヨシしてください

```
副産物三件の直し（撤退予約線のコメント／totalBlocks の寄せ／執事と根拠欄①の閾値）— 宣言してヨシ待ち
印: y0829-fukusan
待っているのは：本体（koushu-handan.html）へ当てる三件の宣言にヨシ。①撤退予約線 L8392 のコメントを実装（baseShanten）に合わせる②totalBlocks を L2514 から七対子の上書きの直後へ移す（兎の票が6/4000だけ攻→棄権）③hasValue を doraValue>=1 へ揃え、根拠欄①の題と詳細の分岐を valueSources.length へ差し替える
答え方：「y0829-fukusan にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
```

### 3. ✅ 終わりました（返事不要）

```
被り-1の調べ（審査者の材料の総当たり表・被りの洗い出し・空き軸の候補・4000行での検定）
四件すべて済。①画面に出る審査者（七人の見立て・判定カード・根拠欄・集計行・撤退予約線）が読む analyze() の33項目を行番号つきで総当たりの表にした。②同じ材料を見ている組を全員の総当たりで洗い、関数従属5組（新規3組）と相関|r|>=0.60 の14組を出した。③厳密な未使用は0件。攻守の傾きに一切効いていない＝空き軸の候補は14件。④core-probe.js で4000行に analyze() を掛け直し、五数要約と向聴別の和了率・放銃率・平均点棒を出した。本体には一字も触れていない
ファイル: report-latest.md（全面書き直し）／~/.claude/orders-open.tsv（被り調べ-1〜4 を済へ）
実測: 【突き合わせ】再計算 vs 記録＝baseShanten 0/4000・acceptTiles 0/4000・goodShapes 0/4000・defHonors 0/4000・verdict 890/4000（記録は較正-1 前）。【新規の被り】acceptTypes⇔acceptTiles r=0.996／kokushiShanten⇔defHonors+defTerminals r=-0.967／weakShapes⇔fillWeak r=0.824／関数従属 widthLabel→widthGood・kokushiKinds→kokushiSeen・valueSources→hasValue。【空き軸】fillRemain が最強＝向聴2で2枠38.2% vs 4枠23.0%（15.2pt・点棒+1815 vs +841）、向聴3で32.8% vs 23.3%（9.5pt）。borderline は向聴3で24.6% vs 41.5%（+16.9pt・n=53）。acceptTypes は acceptTiles を固定すると単調でなくなり空き軸ではない。doraCount は記録が全0で判じられない。【副産物】totalBlocks は七対子ルート（L3022-3025）で goodShapes+weakShapes と食い違う 493/4000＝12.3%／撤退予約線 L8392 のコメントは「受け入れ枚数から」だが実装 L8397-8405 は baseShanten だけを読む／執事の票（doraValue>=1）と根拠欄①の✓×（doraValue>0）は孤立ドラ1枚（0.5・L2864）でだけ食い違う
```

### 4. ✅ 終わりました（返事不要）

```
答えが届かなかった原因＝畳んだ札に件名が出ないこと（panel v75 で修理）
数字は四度とも本文に入って公開もされていた（13:56:01 の札は本文1138字・押し送りHTTP200）。届かなかったのは panel.html 1027行で「終わりました」の札が畳まれ、表紙が題と時刻だけで件名も本文も出なかったため。panel v75 で畳んだ札の表紙へ本文の一行目（件名）を出すようにした（3add97b）。写しに probe で実測しPASS
実測: 【a】郵便詰まり-3＝書き足し13:12:45→受信箱13:13:34（49秒）→起こす13:14:09（84秒）→着手13:14:51（126秒）。【b】郵便掃除の通し試し＝書き足し13:08:48（4行→5行）→受信箱13:09:11（23秒）→控えへ13:09:14（26秒）。mailbox.md 4行235バイト・archive 16行12033バイト。【c】未達1件＝ヨシしてください・11:31:52。【d】二本並んだ原因＝08-26の備え（inbox-watch.ps1 20-47行）は正常。外れたのは Wake-Claude の二重防止 旧182-190行が Get-Job -Name claude-worker しか見ていなかったこと。塞ぎは182-196行。【e】13:51の詰まりはこちらの実測で作ったもので13:51:44に解消済み、残っている行は無い
実機: 返事パネル ── 版の字が「panel v75（8月29日）」／「✅ 終わりました N件」の箱を開くと、各札の題の下に**件名の一行**が出る（畳んだままでもどの件か分かる）／札を押して開くと件名の行が消えて本文が出る
```

### 5. ✅ 終わりました（返事不要）

```
数字の再掲（郵便詰まり-3／通し試し／未達1件）と、取りこぼしの修理
【a】郵便詰まり-3＝書き足し13:12:45→受信箱13:13:34（49秒）→起こす13:14:09（84秒）→着手13:14:51（126秒）→終了13:15:07。【b】郵便掃除の通し試し＝書き足し13:08:48（4行→5行）→受信箱13:09:11（23秒）→控えへ13:09:14（26秒）。mailbox.md は4行235バイト（頭書き以外0行）、mailbox-archive.md は16行12033バイト。先立つ一本で溜まっていた11行も13:08:10に移した（15行→4行）。【c】まとめて写すの未達1件＝種別 ヨシしてください・時刻 11:31:52（較正-1の線引き確定／y0829-tora の札）。届いたの印は panel.html 1668行の res>t*1000 で、11:38時点の lastResume は 11:36:00。【d】窓が二本並んだ原因＝08-26の備え（inbox-watch.ps1 20-47行）は正しく働いており今日も12:36:08に一本止めている。外れたのは Wake-Claude の二重防止（旧182-190行）で Get-Job -Name claude-worker しか見ていなかった。塞ぎは182-196行に run: の関門。【e】着手されなかった3件＝07:41:40「郵便受けの試しです。届いたら…」起こす経路が消えていた／08:11:02「y0829-notice にヨシ。知らせの控え…」ヨシ待ちで合図を握り潰し／08:19:12「y0829-notice の直しは、知らせの溜め…」同じ穴。三件ともこちらが送った枠。【f】取りこぼし-1 を直した（inbox-watch.ps1 695-727行）
ファイル: ~/.claude/inbox-watch.ps1（695-727行）・panel-mail-probe.js・~/.claude/orders-open.tsv
実測: 取りこぼしの実測＝inbox.txt を読み取り専用にして一行足すと、13:47:36/13:48:41/13:49:42/13:50:43 の四巡とも位置は lines=4 のまま・郵便受けも5行のまま・pending=1。外した20秒後の13:51:44に拾い直し13:51:47に掃除まで通った。押し送りは両方の枝で実測（silent 13:33:04／pending 13:51:04）。パネルの行は 0件=hide/0px・1件=21px・3件=21px でPASS。常駐は pid=8380 の一本。台帳は残り0件
```

<!-- 送った知らせ ここまで -->


