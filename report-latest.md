# A〜D の直しと、ntfy の題を状態で二種類に分けた

**状態：ヨシ待ち（実機で見てください＋上限の裁定）** — 2026-08-25 14:5x 時点

**結論** — A〜D すべて入れ、ntfy の題を状態で分けました。**直しの途中で自分の不具合を一つ見つけて直しています**
（`Start-Process -PassThru` では終了コードが読めない）。
**一日の送信数は上限20件に収まりません**——実測から **約29〜49件/日**。ここだけ裁定をお願いします。

---

## A. 見張りの commit が、私の staged を呑まないようにした

三つの `git commit` に **`-- <自分のファイル>`** を付けました。実証：

```
git add mine.txt                        ← 人が add しただけ
Git-CommitPush -Paths @('usage.json') -Message 'usage: 更新'
→ この commit に入った: usage.json          ★ mine.txt は入らない
→ mine.txt はまだ index に残っているか: mine.txt   ★ 触られていない
```

直す前は `mine.txt` が「usage: 更新」に呑まれていました（前回の実証）。

## B. 弾かれたら pull --rebase して一度だけ押し直す

押し出しの手を **`~/.claude/git-push.ps1`** へ一本化し、三つの台本が読み込む形にしました。
中でやること：**道の名指し**（A）／**20秒の上限**／**押し直し一度きり**／**結果を記録**。

**時間の上限は三つ全部に付きました。** これまで `state.json` と `notices.json` の push は**無制限**で、
終了コードも見ていませんでした。いまはどちらも20秒で切り、結果が残ります。
記録は **`~/.claude/git-push.log`**。

**実際の回で確かめました** — 14:46 の `state.json` と `notices.json` の押し出しは、
記録に何も残さずに通りました（0 以外だけ書く作りなので、無言＝成功）。

### ＊直しの途中で見つけた自分の不具合

入れた直後、押し出しが**毎回 `-3`（終了コードが読めない）**になりました。

```
14:43:22  usage  終了コードが読めない: git … add usage.json
14:43:26  usage  push が通らなかった（-3）。pull --rebase して一度だけ押し直す
14:43:27  usage  pull --rebase が通らなかった（-3）。押し直さない
```

原因は **`Start-Process -PassThru` では `ExitCode` が読めない**こと。
**元の実装が「終了コード （空）」と記録していたのも同じ理由**でした——前回の報告で
「なぜ失敗したかが残らない」と書いた、あの空欄の正体です。
`System.Diagnostics.Process` を直に起こす形へ変えました。

```
init = 0        commit = 0        わざと失敗（無い枝） = 1
14:45:12  test  終了コード 1：error: pathspec 'no-such-branch' did not match any file(s) known to git
14:45:13  test  終了コード 128：error: cannot pull with rebase: Your index contains uncommitted changes.
```

**終了コードも、git が言った理由も、そのまま残るようになりました。**

## C. パネルが「形違いの JSON」で嘘をつく二件を塞いだ（**panel v31**）

| 読む先 | 直す前 | 直したあと |
|---|---|---|
| `state.json` が配列 | **`NaN時間前`**（平常の色） | `連絡なし ／ 状態を読めません（形が違います（at が数ではありません））` |
| `notices.json` が配列でない | **「まだ知らせがありません」** | `一覧を読めません（形が違います（一覧が配列ではありません））` |

`panel-broken-probe.js` を回し直して確かめました。**空の一覧と壊れた一覧が別物として出ます。**

## D. 見張りの足跡を、積み上げの記録にした

`watch-step.txt` は回ごとに消えるので、**遅かった回が後から読めません**でした
（13:22:58〜13:31:18 の8分20秒が、どの段で詰まったのか特定できなかった）。
**`~/.claude/watch-step-log.txt`** へ消さずに足す形にしました（約1000回＝およそ17時間ぶん）。

```
2026-08-25 14:43:02  開始              +0秒
2026-08-25 14:43:03  窓とプロセスを数えた  +0.6秒
2026-08-25 14:43:04  控えを読んだ        +0.8秒
2026-08-25 14:43:05  知らせの判定を終えた  +1.8秒
```

次に穴が空いたら、**どの段に何秒居たか**が並びます。

---

## ntfy の題を状態で二種類に分けた

| 状態 | 題 | いつ |
|---|---|---|
| `wait:` | **😼 返事パネル** | ヨシ待ち（今までどおり） |
| `done:` | **😽 返事パネル** | **終わり（作業が済んで待ちが無い）＝新しく送るようにした** |
| `dead:` `stale:` | 送らない | 異常 |
| `run:` `idle:` `wait-unwritten:` | 送らない | 動作中・用の無い戻り・控えが前の回のまま |

**見分けは状態だけ**で付けます（控えの中身は読みません）。決め所は `Push-Ntfy` の一箇所に閉じました。
**同じ待ちで二度送らない決まりはそのまま**です（`pushedWait` は `wait` のときだけ立て、
終わりの側は既にある「同じ本文は出さない」指紋の関門が上流で効きます）。

**実際の回で確かめました** — 14:46:14、ヨシ待ちの回が **😼** で出ました（本日13件目）。

### 一日の送信数の見込み — **上限20件に収まりません**

`watch-notify.log` から実測しました。

| 日 | ヨシ待ちの控え | 実際に押し送った | 終わりの控え | **足したあと** |
|---|---|---|---|---|
| 08-24（丸一日） | 69件 | **22件** | **27件** | **約49件** |
| 08-25（14:37まで） | 23件 | **10件** | **8件** | 18件 → **一日換算 約29件** |

**08-24 は、終わりを足す前から既に上限へ当たっています。**

```
2026-08-24 23:09:11  押し送りを止めた：本日 20件で上限（20件）に達している
```

**危ういのは、上限に当たると「あとから来たもの」が落ちること**です。
終わりを足すと**ヨシ待ちが押し出される**恐れがあります。いちばん要る通知が落ちる形です。

**こちらの案** — `$DAILY_MAX` を **60** へ上げる。実測の最大（約49件）に余裕を足した数です。
あるいは「**ヨシ待ちに枠を取り置く**」形（例：終わりは12件まで、ヨシ待ちは別枠）も組めます。
**どちらにするか裁定をお願いします。** それまでは **20 のまま**にしてあります。

### 実際に送りました — 2026-08-25 14:42:55 前後、3秒差で二通

| 題 | Title ヘッダ（実際に送った字） |
|---|---|
| 😼 返事パネル | `=?UTF-8?B?8J+YvCDov5Tkuovjg5Hjg43jg6s=?=` |
| 😽 返事パネル | `=?UTF-8?B?8J+YvSDov5Tkuovjg5Hjg43jg6s=?=` |

本文・優先度・`Click`・`Cache: no` は押し送りと同じ形です。本日の件数も 10 → 12 へ足してあります。

**iPhone にどう出たかは、こちらからは見えません。** 次の三点を見て教えてください。

1. **二通とも届いたか**（3秒差で並んでいるはず）
2. **題が一行だけか** — 絵文字が二つ並んでいないか（`Tags` を付けると頭にもう一つ付く）
3. **😼 と 😽 が見分けられるか** — 通知一覧の小さな字で、口の開き方の違いが読み取れるか

## 触ったもの

| ファイル | 中身 |
|---|---|
| `~/.claude/git-push.ps1` | **新規**。押し出しの共通の手（道の名指し・上限・押し直し・記録） |
| `~/.claude/watch-notify.ps1` | A・B・D＋題の分け＋`done` を押し送りへ通す |
| `~/.claude/inbox-watch.ps1` | A・B（`state.json`） |
| `~/.claude/notify-record.ps1` | A・B（`status.md` / `notices.json`） |
| `panel.html` `panel-ver.txt` | C（**panel v31**） |
| `koushu-handan.html` | **触っていません** |

## 実機で見るところ

1. **ntfy** — 14:42:55 前後の二通と、14:46:14 の 😼（上の三点）
2. **返事パネル** — 画面のいちばん下の版の字が **`panel v31`**
   ＊v30 のままなら URL に `?_v=31` を付けて一度だけ開いてください
