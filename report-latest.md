状態：作業中 ／ 2026-08-21 00:26 ／ v1418 の納品（1322行の一文を直した）。フル版check-all を実行中（見込み00:42終了）

【保留】
- **倒れる音 三案（A/B/C）の選択** ← 試聴版をお渡し済み
- **1322行の説明文の修正範囲**（(a) 1番だけ／(b) 枝豆の4つ＋地の文）
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- **ライフ0の倒れる絵の素材待ち**（704x1524・黒背景。§2-6 に注意点あり）
- 振りの八枚の倍率の裁定（目盛り画像は `head-proof-*.png` に焼いた）
- `sizing-review.png` を残すか消すか

# 報告：一索の鳥のライフ制の下調べと、倒れる音の試聴版

日付 2026-08-20 ／ 本体の版 **v1417**（**実装には着手していない**）

---

## 1.【1】倒れる音の三案 — 試聴版をお渡ししました

**`se-audition.html`** を別途お送りしました。ボタンを押すと鳴ります。
**すべて Web Audio の合成**で、既存の wav は一つも使っていません。**本体には何も入れていません。**

| 案 | 方向 | 中身 | 長さ |
|---|---|---|---|
| **A 声が落ちる** | 悲鳴の続きが力尽きる | のこぎり波 520→110Hz、ふるえが 7Hz→2.5Hz と遅くなり幅も細る。共鳴 900→260Hz | 0.90秒 |
| **B 崩れ落ちる** | 物として倒れる | 布の擦れ（雑音 2400→420Hz）→ **剣を落とす金気の一音**（三角波2050Hz・0.42秒の位置）→ **着地**（正弦 96→44Hz＋60Hz以下の雑音） | 0.80秒 |
| **C 息が抜ける** | 静かに終わる | 音程を持たない息（高域 3200→380Hz へ落とす）＋薄い唸り（正弦 74→52Hz） | 0.62秒 |

**方向の違い。A は声、B は物音、C は気配。**
**B が絵（剣を落として崩れ落ちる）といちばん噛み合います**——剣の音と着地音が絵の順序と一致するため。
**A は既存の悲鳴と地続き**なので、被弾音とつながって聞こえます（利点にも欠点にもなります）。
**C はいちばん小さく短い**ので、賑やかな場面の直後でも邪魔になりません。

試聴版には**既存の悲鳴（`TORI_HURT`）を同じ値で組み直したもの**も入れてあります。
被弾のたびに鳴るあの音と、倒れる音がぶつからないかを聞き比べてください。

**入れるときの決まり**（ご指定どおり・未実装）

- 設定の**効果音 ON/OFF に従う**（`seAllowed()` を通す。OFF なら音を作る処理に入らない）
- **追い込みの高さ上げ（`rushPitch`）の対象に含めない**。倒れる音は常に同じ高さ
  （既存の悲鳴も同じ扱いで、`toriHurt` には倍率を掛けていない）
- 鳴らすのは**ライフ0の瞬間だけ**。被弾の悲鳴とは別に呼ぶ

---

## 2.【2】洗い出し

### (1) 体力・負け条件に触れている箇所 — **本文1箇所＋関連3箇所＋コメント2箇所**

**■ 嘘になる本文（`INFO_DETAIL` の「一索の鳥」）**

| 行 | いまの字面 | なぜ嘘になるか |
|---|---|---|
| **4654** | **「体力は無い。当たっても負けにはならない。」失うのは時間だけ。** | ライフ3・ライフ0で負けになるので、**丸ごと逆になる** |

**■ 同じ本文の中で、併せて直る／直す必要がある箇所**

| 行 | いまの字面 | 影響 |
|---|---|---|
| 4653 | 「明けてから**0.6秒**は続けて当たらない」 | 無敵が**1.0秒**になり、数え始めも変わる（下記(3)） |
| 4653 | 「0.2秒のけぞって悲鳴を上げ、その間は動けない」 | 仰け反りの絵と悲鳴は据え置きなので**そのままでよい** |
| 4651 | 「金は高い鳥から選ばれ、捕らえると**ノルマ2羽ぶん**に数える」 | **ライフ1回復**が加わるので**一言足りない** |
| 4657 | 「連戦は…**終わりは✕を押したときだけ**」 | ライフ0でも終わるので**嘘になる** |
| 4655〜4656 | 「30秒でノルマぶん捕まえれば勝ち、届かなければ鳥の勝ち」 | **負け筋が二つ**になる（時間切れ／ライフ0） |

**■ コードのコメント（表に出ないが、読むと嘘になる）**

| 行 | 字面 |
|---|---|
| 9439 | 「ここから仰け反り。被弾の見た目だけで、**体力も負け条件も持たない**。」 |
| 10705 | 「鳥に触れられたときの仰け反り。見た目だけで、**体力も負け条件も無い**。」 |

**■ 本文を通しで拾った結果、上記以外に体力・負け条件へ触れている箇所は無い。**
`体力` `負けにはならない` `負け条件` `ゲームオーバー` `失うのは時間` を全文で当たり、
人柄の台詞に出る「先が思いやられ」を除くと、残るのは上の6箇所だけだった。

### (2) ライフ3を HUD のどこに出せるか — **既存の並びに三つ目の枠を足せる**

**いまの HUD**（`.tori-hud`・579行、組み立ては 9589行）

```
.tori-hud { position:absolute; top:6px; left:10px; right:10px; z-index:5;
            display:flex; align-items:center; gap:12px; pointer-events:none;
            color:var(--bone); font-size:13px; }
.tori-hud .tori-close { margin-left:auto; }   ← ✕だけ右端へ寄る
```

```
<div class="tori-hud">
  <span id="toriCount"></span>     ← 「3 / 5 羽」（連戦は「12 羽」）
  <span id="toriTime"></span>      ← 「12.4」（連戦は空）
  <button class="tori-close">✕</button>
</div>
```

**`#toriTime` の後ろに `<span id="toriLife">` を足せば、そのまま左詰めの三つ目として並ぶ。**
✕ は `margin-left:auto` で右端に居るので、**押しどころは動かない**。
**連戦では `#toriTime` が空**になるため、連戦のときライフは時計の位置へ繰り上がる。

**粒の寸法の候補**（`hoshi_tama` は原寸 **32x32**。`EDA_NAGE.tama` から取れる）

| 案 | 粒の寸法 | 粒の間隔 | 三つの幅 | HUD の高さへの影響 |
|---|---|---|---|---|
| **候補ア** | **12 x 12** | 4px | **44px** | **無し**（✕ のボタンが高さを決めており、`.tori-btn` は padding 10px＋文字で約38px） |
| 候補イ | 14 x 14 | 4px | 50px | 無し |
| 候補ウ | 16 x 16 | 5px | 58px | 無し（HUD の文字 13px より高いが、ボタンより低い） |

**候補アを推します。** HUD の文字（13px）とほぼ同じ背丈で、行の高さを一切押し上げません。
32x32 を 12px へ縮めるので、`image-rendering` は既存の星と同じ扱いにすれば粗さも揃います。

**減ったぶんの見せ方**は、（i）粒を消す（ii）粒を暗く残す、の二通り。
**(ii) を推します**——残り数と上限3が同時に読め、金の鳥で戻る余地があることも見えるため。
**新素材は要りません。** 暗い側は `filter: brightness(0.35)` などで作れます。

**注意が一つ。** `.tori-hud` は `pointer-events:none` で、`✕` だけが `auto` に戻してあります。
**ライフの粒は押す的ではない**ので、この形のままで問題ありません（作法§10 の当たり判定44pxの
対象外）。

### (3) 無敵1.0秒と、いまの規則の衝突 — **三つあります**

**いまの被弾処理**（`EDA.hitShow:200` / `EDA.hitInvuln:600`・9441-9442行、`recoil()` は 10709行）

```
recoilUntil = now + 200        （仰け反りの絵を出す）
invulnUntil = recoilUntil + 600  ＝ now + 800   （無敵は仰け反りが明けてから数える）
```

**接触の判定**（10388行）

```
if(!frozen && tnow >= joyUntil && !moving && tnow >= recoilUntil && tnow >= invulnUntil)
```

**■ 衝突 その一：数え始めが違う。**
いまは**仰け反りが明けてから**600ms 数えるので、被弾からは**計800ms**。
ご指定は「**被弾直後に1.0秒**」なので、**数え始めを被弾の瞬間へ変える**必要がある。
`hitInvuln` を 1000 にするだけでは、被弾から 1200ms になって指定とずれる。
**`invulnUntil = now + 1000` にするか、`hitInvuln` を 800 にして今の式を保つか**、どちらかを選ぶ形。
**前者を推します**（定数の意味が「被弾からの無敵」で読めるため。ご指定も「1.0秒は定数」）。

**■ 衝突 その二：走ると無敵が消える。**（10653行）

```
/* 走っている間は、攻めの続きも仰け反りも畳む。 */
recoilUntil = 0; invulnUntil = 0; queuedTap = null;
```

**移動を始めた瞬間に無敵が捨てられる。** いまは無敵が見た目の都合だけなので害が無いが、
**ライフが付くと「被弾直後に逃げると無敵を失う」**ことになり、
**逃げた側が損をする**という逆の手触りになる。**ここは無敵だけ残す形へ変える必要がある。**

**■ 衝突 その三：移動中は接触しない。**（10388行の `!moving`）

**移動している間は、そもそも鳥に触れられない。** これは現状の仕様だが、
**ライフが付くと「走り続ければ無敵」**という抜け道になる。
走り斬り（移動中に帯の鳥を捕る）と組み合わせると、**走り回るだけで安全に稼げる**。
**ライフ制を入れるなら、ここを残すか塞ぐかの裁定が要ります。**

**■ 衝突ではないが、確認したい点**

- **息切れ中の接触は有効**（10692行のコメント）で、`recoil()` は `breathUntil = 0` として
  **仰け反りを優先**する（10712行）。無敵1.0秒を入れても、この優先順位は変わらない。**衝突しない。**
- **空中では仰け反りの絵を出さない**（10722行）が、**接触は起こる**（10558行「触られたら仰け反り、
  そのまま落ちる」）。**ライフは減るのに絵が出ない**ことになるので、
  **空中被弾のときライフだけ減る見せ方でよいか**、裁定をいただきたい。
- `hitRadius: 26` は**コメントに「暫定」**とある（9440行）。ライフを入れると当たりの広さが
  そのまま難度になるので、実機で触るときに一緒に見るとよい。

### (4) 連戦モードでライフ0になったときの締め方 — **経路が無い。二つ足りない**

**負けの画面へ入る経路は `finish(false)` 一つだけ**で、呼ばれるのは 10500行の

```
if(!isEndless() && !frozen && left <= 0) return finish(false);
```

**`!isEndless()` で連戦は明示的に外れている。** 連戦は時計を持たないため、
**いまは負けで終わる道が一本も無い**（終わりは ✕ だけ）。

**足りないもの、二つ。**

**① 連戦から `finish(false)` を呼ぶ経路。** ライフ0の判定は難易度に依らないので、
`isEndless()` の外に置く必要がある。

**② `finish()` の中の、連戦向けの分岐。** いま `finish()` は連戦を想定していない。

| 場所 | いまの中身 | 連戦だとどうなるか |
|---|---|---|
| 10860行 | `caught + ' / ' + level.quota + ' 羽'` | `level.quota` が **0** なので「**12 / 0 羽**」と出る |
| 10846行 | `TORI.timeLimit * 1000 - (endAt - now)` で所要を出す | 連戦は `endAt` を毎フレーム先へ押しているので**意味のある値にならない**（負けでは通らないが、勝ち筋を足すなら壊れる） |
| 10849-10855行 | クリアタイムと自己ベスト | **ご指定どおり連戦では出さない**——`win === false` なら `bestLine` は空のままなので、**このままで条件を満たす** |

**つまり、連戦の負け札で直すのは羽数の表示一箇所。**
「12 羽」と出す形（HUD と同じ `isEndless()` の分岐）にすれば足ります。

**なお最高記録の件はご指定どおり据え置きです。** `bestSet()` は `win` のときだけ呼ばれ、
連戦は負けでしか `finish()` に入らないので、**保存も表示も起きません**。
v1399 で撤去した `toriEndlessBest` の後始末（1611行）もそのままで構いません。

### (5) 初級・中級・上級でライフ3が妥当か — **見立て**

**初級は余る、中級はちょうど、上級と連戦は厳しい**——同時羽数が 2/3/4 と増え、速さも 0.30/0.45/0.65 と
倍以上開くうえ、上級だけ追い込みで 1.2倍が乗るため、**同じライフ3でも上級の被弾機会は初級の数倍**になる。

（数字の裏付け：接触は `hitRadius: 26` の一点で起こり、`!moving` の間は起きない。
初級は同時2羽・30秒・ノルマ3羽なので、**触れずに終える回が多い**。
上級は同時4羽・ノルマ7羽で画面に居続ける必要があり、**残り10秒の1.2倍が重なる**。）

**実機で触って決める前提であれば、まず全モード3で通し、上級だけ4へ緩めるかを見るのが早い**と考えます。

### (6) 素材（ライフ0の倒れる絵）について、先にお伝えする点

**ご指定は「704x1524・黒背景」ですが、作法§21 は「元絵は 704x1524 の白背景が現行の形」と
定めています。** 白抜きの道具（`char_export.js`）は**縁から繋がっている白だけを背景とみなす**作りで、
**黒背景の元絵はそのままでは通りません**。

黒背景から抜く手順は別にあり（記憶の「剣士のコマの大きさ合わせ」）、
**縁からの塗りつぶしでしきい値24**、40以上にすると茶色のブーツが地と同じ暗さで千切れる、
という実績が残っています。**どちらで焼くかを決めてから素材をいただくほうが手戻りがありません。**

**もう一点。** この絵は**遊んでいる最中に出るコマ**であって、勝敗の結果絵ではありません。
作法§12 は「一索の鳥は無言。遊んでいる最中の鳥は無言のまま」「**勝敗の結果絵に限り、
絵に含まれる吹き出しを許す**」と定めているので、**この絵には吹き出しを入れないでください。**

**負け絵そのものは既存のまま**（`TORI_LOSE_IMG`）で、結果の札に出ます。
新しい絵は**倒れる瞬間の一コマ**として、その手前に出る、という理解でよいかご確認ください。

---

## 3. 触れていないこと

- `koushu-handan.html` / `check.js` / `ver.txt` / `serifu.txt` は**すべて無変更**（v1417 のまま）。
- **ライフ制の実装には一切着手していない。** 試聴版は本体の外の単独ファイル。
- 版番号は上げていない。`analyze()` / `bestShapeOneSuit` にも触れていない。
- **テスト用のシムなし。**

---

## 4. 次

- **倒れる音の三案から一つ**（A / B / C）。手直しの指定があれば一緒にどうぞ。
- **1322行の修正範囲**（(a) か (b)）。
- **(3) の裁定二つ** — 走り続ければ無敵になる抜け道を塞ぐか、空中被弾でライフだけ減らすか。
- **(6) の確認** — 元絵の背景（白か黒か）と、新しい絵の位置づけ（倒れる瞬間の一コマ）。
- 保留の残りは冒頭の一覧のとおり。
