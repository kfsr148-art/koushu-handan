# ~/.claude の消失対策と、生存記録の途切れの調査

**状態：終わり** — 2026-08-26 19:3x 時点／panel v36 まで納品・検査は両方PASS／待ちなし

**結論** — `~/.claude` を git 管理にし、非公開リポジトリ `kfsr148-art/claude-home-backup` へ130件を押した。
18:53の「生存記録が15分途切れ」は **`/clear` とは無関係**で、真因は
**hook.log への追記が例外で落ち、`catch { }` に黙って飲まれたこと**。自力で 18:58:06 に復帰済み。
押し送りの1日の上限は撤廃した。

---

## 一．~/.claude の git 管理化

`.gitignore` を置いて `git init`。**130件**を収めた（うち記憶 44件）。

除外は五つの区分。**鍵・話題名は一件も載っていないことを機械で確かめた**（staged 全件を
話題名の実文字列・`gh?_`/`github_pat_`/`sk-ant-`/`Bearer` の字面で走査。当たったのは
`.credentials.json` から**実行時に読む処理のコード**＝ `claudeAiOauth.accessToken` という
**名前だけ**で、実体の鍵はゼロ）。

## 二．非公開リポジトリへの push

`gh repo create claude-home-backup --private` で作成し push。
`visibility=PRIVATE` / `isPrivate=true` を API で確認済み。遠隔の中身は130件。

## 三．除外したものを失うと何が困るか（一行ずつ）

| 除外したもの | 失うと何が困るか |
|---|---|
| `.credentials.json` | 困らない。**載せるほうが危ない**。失っても `claude` に入り直せば作り直される |
| 話題名のファイル（`rc-ntfy-*`） | 押し通知の宛先が分からなくなる。**手元の紙に控えておくのが正解**で、リポジトリには置かない |
| `rc-endpoints.txt.retired-*` | ほぼ困らない。切り分けの当時の生データで、再現すれば取り直せる |
| セッションの記録（`projects/**/*.jsonl`・264MB） | 過去のやり取りの全文が消える。ただし**話題名が5本に混じっていた**ので載せられない。要点は記憶44件と報告書が持っている |
| `file-history/`（855MB） | 困らない。編集の取り消し用の写しで、本体は koushu-handan 側の git が持っている |
| 各種 `*.log`（見張り・フック・受信箱） | 過去の異常の追跡がしにくくなる。ただし**話題名が混じる**うえ日々肥る。今回の調査で使ったのは「手元にある現物」で、控えは要らなかった |
| `plugins/` `tools/`（23MB） | 困らない。入れ直せば戻る |
| `cache/` `sessions/` `session-env/` `shell-snapshots/` `telemetry/` `downloads/` | 困らない。機械が作り直す |

**要注意はただ一つ、話題名**。これだけはリポジトリの外で控えておく必要がある。

## 四．自動化

**入れていない**（指示のとおり）。いまは手で `git -C ~/.claude add/commit/push` する形。

---

## 五．18:53「生存記録が15分途切れ」の調査

### いつ止まったか

`hook.log` の heartbeat 行が **18:38:06 → 18:58:06 の20分**空いた（18:48の1本が欠けた）。
見張りの関門は `$STALE_MIN = 15` 分。10分周期なので、**1本落ちるだけで警報が鳴る**。
警報は 18:53:23。

### 直前に何が起きたか／畳みは関係するか

**関係しない。** 三点で確定した。

1. **タスクは走っていた。** タスクスケジューラの記録で 18:48:02 起動 → **18:48:08 リターンコード0で正常完了**。
   なのに `hook.log` に行が無い。＝走らなかったのではなく、**書き込みだけが落ちた**
2. **同じ形が昼にもある。** 13:18も起動して 13:20:23 に戻り値0で完了、行だけ無い
3. **畳みの回数と合わない。** 795本中 **17回**（2.1%）の欠けが 08-21 から散らばって出ている。
   `/clear` はそんなに打っていない

### 真因

`hook-notice.ps1` の生存の書き込みは一行だけ。

```powershell
try { Add-Content -LiteralPath $LogPath -Value $line -Encoding UTF8 } catch { }
```

`hook.log` は見張り（`watch-notify.ps1`）が **毎分 `Get-Content -Tail 400` で読み**、
`inbox-feed.ps1` と `stop-guard.ps1` も触る。**誰かが開いている間の `Add-Content` は
IOException で落ちる**（実験で確認：保持側の共有指定が `Read` でも `ReadWrite` でも落ちた）。
落ちても `catch { }` が飲むので、**タスクは戻り値0のまま生存だけが消える**。

`hook.log` は 284KB まで肥っていて畳まれないため、毎分の読みが長引き、当たる窓が広がっている。
欠け 2.1% は、60秒あたり約1.3秒ぶつかっている勘定で、読みに掛かる時間と釣り合う。

### 昼の件と同じ原因か

**別。** 昼の件は「重い検査でCPUが膨れ、実行の上限で殺された」。今回はタスクが**正常完了している**。
共通なのは結果（生存行が欠ける）だけで、経路が違う。

### 止まっている物／復帰

**止まっている物は無い。** `ClaudeHookHeartbeat` は Ready、次回 19:08。
`ClaudeWatchNotify`（毎分）も 18:22〜19:01 まで一度も途切れずに走っている。
**自力で復帰した。復帰時刻は 18:58:06。**

### 偽の警報を鳴らさない案（一つ）

**追記を粘らせる。** `hook-notice.ps1` の生存の一行を、
**200ms 間隔で最大5回まで試し、5回とも駄目だったときだけ諦める**形にする。
ぶつかりは1秒台なので、5回粘れば実質すべて拾える。あわせて**諦めた回は別ファイルに一行残す**——
いまの `catch { }` は失敗を丸ごと消しており、**今回のように「タスクは0なのに行が無い」という
読めない状態**を作っていた。

`$STALE_MIN` を 15 → 25 へ広げる手もあるが、**本当に止まったときの気づきが10分遅れる**。
落ちる側を直すほうが筋がよい。**この案はまだ入れていない**（指示待ち）。

## 六．押し送りの1日の上限の撤廃

`watch-notify.ps1` から次を外した。**構文検査 PASS**、1366行 → 1344行。

- `$DAILY_MAX = 60` の定義
- `if ($n -ge $DAILY_MAX) { … }` の判定（終わりを落としてヨシ待ちだけ通していた分岐）
- 上限ちょうどの回に本文末尾へ足していた「本日はこれで打ち止め」の一文

**残したもの** — 「一つの待ちにつき一発」（`$script:pushedWait`）、
失敗時のみ60秒間隔で最大3回（`$NTFY_RETRY_MAX` / `$NTFY_RETRY_SEC`）。
件数の記録（`ntfy-count.txt` / `ntfy-daily.tsv`）は統計として数え続ける。

## 七．送信の復活確認について（先に断っておく）

**この報告の知らせが飛んでも、撤廃の確認にはならない。**
本日の押し送りは **28件**で、旧上限 60 には**まだ当たっていなかった**。
つまり今日は上限のせいで落ちていた知らせが無い。飛べば「押し送りが生きている」ことの確認にはなるが、
**撤廃が効いたかどうかは、60件を超える日が来るまで分からない**。

触る前の写しは `~/.claude/watch-notify.ps1.bak-20260826d`。

---

## 八．作業時間の見える化（3件目・見込み25分）

骨組みは既にあった——`state.json` は `mikomi`（控えの `見込み:` から）と
`startedAt`（`work-started.txt` から）を前から持っていた。足りなかったのは**出し方**と**超過の知らせ**。

### ① 着手時に見込みを宣言する

`CLAUDE.md` の作法25の表へ `見込み:` を足し、書き方（**分の数**で書く／立たなければ `不明`）を明記した。

### ② 状態行とパネルに「開始HH:MM・見込みN分・経過N分」

- `state-line.js` … `clock()` を足した。**作業中のときだけ**出す
  （手待ち・ヨシ待ちに足すと、状態そのものの経過（`statAt`）と二つ並んで読めなくなる）
- `panel.html` … 見込みの段の形を `見込みN・開始からN分` → **`開始HH:MM・見込みN分・経過N分`** に変えた。
  開始の時刻を足したのは、経過だけだと「いつからの経過か」を画面の時計と突き合わせられないため

**実測** — `**状態：作業中** — 2026-08-26 19:22 時点／…／開始19:10・見込み25分・経過12分`

### ③ 見込みを超えたら一発だけ

`watch-notify.ps1` に判定を足した。**作業中のときだけ**、控えの `見込み: N分` と
`work-started.txt` の開始時刻を比べ、超えていたら
**`🕒 延びています（見込みN分・経過N分）`** を送る。

**一発だけ**の担保は `~/.claude/mikomi-over.txt`——「件名<タブ>開始時刻」を一行持ち、
同じ作業では二度鳴らない。件名か開始時刻が変われば、また一度だけ鳴る。
押し送りの関門（`Push-Ntfy`）に三つ目の状態 `over`（🙀）を通した。

**見込み不明の回は黙る**（比べる相手が無いため）。開始時刻と経過だけが出る。

### 途中で見つけて直した不具合

`inbox-watch.ps1` の書き直し判定のキーに**見込みが入っていなかった**。
件名を変えずに `見込み: 25分` だけ書き足した回は `state.json` が書き直されず、
**パネルと状態行が「見込み不明」のまま止まる**。実測でそうなったので、キーに `mikomi` を足した。

あわせて、**常駐の `inbox-watch.ps1` は古い中身を抱えたまま回り続ける**ので起こし直した
（PID 5188 → 2844）。台本を直しただけでは効かない。

### 版と写し

- パネル **v35 → v36**（`PANEL_VER` ／ `verTag` の字 ／ `panel-ver.txt` の三箇所同時）
- 触る前の写し … `watch-notify.ps1.bak-20260826d` ／ `inbox-watch.ps1.bak-20260826c`
- 構文検査 … `watch-notify.ps1` PASS（1392行）／`inbox-watch.ps1` PASS（376行）／
  `state-line.js` `node --check` OK ／ `panel.html` の script OK
- **`node check-all --fast`（作法18）… `check` PASS ／ `adv-check` PASS ／ 両方PASS。`FAIL` は0件。**
  ⑦（画面の溢れ）は9画面すべて「溢れなし」、判定後の二択も `bottom=319.8 / 320` で視野の内側

＊この検査は**二度回している**。一度目は⑦の実測に入ったところで外から止められており、
その時点では「検査を通した」とは言えない状態だった。`Start-Process` で切り離して回し直し、
最後まで通した結果が上記（作法18の「フル版は切り離す」と同じ扱いを、速い版にも要した）。

### 実機で見るところ

**返事パネルの状態の段（二段目）**。作業中のあいだ、
**`開始HH:MM・見込み25分・経過N分`** の形で出ていること（いままでは `見込み25分・開始からN分`）。
版の字が **panel v36**。

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
作業時間の見える化（panel v36）— 納品前の検査を回し直し中
①~/.claude を git 管理化し非公開リポジトリ claude-home-backup へ130件を push（鍵・話題名は0件）②18:53「生存記録15分途切れ」の真因を特定——hook.log への追記が IOException で黙って捨てられる。/clear とは無関係で、18:58:06 に自力復帰 ③押し送りの1日の上限（DAILY_MAX）と打ち止めの一文を撤廃 ④作業時間の見える化：状態行とパネルへ「開始HH:MM・見込みN分・経過N分」／見込み超過を一発だけ知らせる（mikomi-over.txt が担保）／CLAUDE.md 作法25へ「見込み:」を追加 ⑤inbox-watch.ps1 の書き直し判定に見込みが入っていない不具合を直し、常駐を起こし直した（PID 5188→2844）⑥node check-all --fast を Start-Process で切り離して回し直し中（前回は⑦の実測で外から止められた）
ファイル: ~/.claude/.gitignore／~/.claude/watch-notify.ps1（1392行）／~/.claude/inbox-watch.ps1（376行）／panel.html（v36）／panel-ver.txt／state-line.js／CLAUDE.md／report-latest.md
実測: 遠隔130件・記憶44件・鍵/話題名0件／生存の欠けは795本中17回（2.1%）で /clear と無関係／構文検査は watch-notify・inbox-watch とも PASS／状態行が「開始19:10・見込み25分・経過12分」で出た／check-all --fast は PID 12004 で実行中
実機: 返事パネルの状態の二段目が「開始HH:MM・見込み25分・経過N分」の形（旧「見込み25分・開始からN分」）／版の字が panel v36
未検収: 2026-08-26 panel v36（作業時間の見える化）
```

### 2. 🪟 異常です（手が要ります）

```
点検3件を検収。この窓は畳めます
見張りの生存記録が 15 分途切れています。仕組みのどこかが止まっています。
こちらがすること：端末の様子を見て、動いていなければ開き直してください。
```

### 3. ✅ 終わりました（返事不要）

```
点検3件を検収。この窓は畳めます
panel v35・点検7件・仕上げ2件の検収を受けた。畳む前の最終確認を済ませた——報告書に締めの節（未検収ゼロ・次の窓へ渡す四つ）を書いて push、控えの記憶 handover-next を v1427／panel v35／作法30まで へ更新、開いている印の一覧は全部検収済みなので空にした、未 commit は0件。/clear はこちらからは打てないので、そちらでお願いする
実測: 未 commit 0件／本体 ver.txt=1427・panel-ver.txt=35・stable の ver.txt=1427 で揃っている／開いていた印8件を空にした／締めの節を書くとき、node へ渡した文のバッククォートが bash に食われて字が抜けたので、ヒアドキュメントで書き直して push した（作法28 と同じ形の穴）
```

### 4. ✅ 終わりました（返事不要）

```
点検3件（済み2・該当済み1）。畳む備えが整いました
①/clear の備え——報告書・控えの記憶（handover-next）・work-note の未検収をすべて最新にし、未 commit 0件にした。作法30「/clear は区切りでのみ。畳む前に報告書と積み残しを最新化する」を CLAUDE.md へ足した ②断片待ちの知らせは既にヨシ待ちと同じ扱いで飛んでいた（10:29:13 に押し送りOK・題は🙋。二度目からは「この待ちでは既に送った」で止まる）。時間切れは入れていない ③パネルの「公開」欄は stable の枝を raw から読んでおり、Pages は stable の中身を配っていないので付け替え不可。注記を一行入れて panel v35 にした
ファイル: CLAUDE.md（作法30）／panel.html（.ver-note と注記の一行・版）／panel-ver.txt／report-latest.md／memory の handover-next.md
実測: raw の遅れは今日 13:5x に押して 13:5x 時点で 1426、16:53 に 1427。追いつくまで最大3時間近く（従来控えていた「約5分」は Pages の遅れで、raw には当てはまらない）／断片待ちの押し送りは 2026-08-26 10:29:13 に HTTP 200 で本日20件目、次の回からは重複よけが効いている／panel の script は node --check OK／git status は空
実機: 返事パネルを開いて二つ。①版の字が panel v35（v34 のままなら ?_v=35）②版の欄のすぐ下に「＊「公開v」は stable の枝から読んでいます。最大で数時間遅れることがあります。」の一行が出ていること。いま raw は追いついているので公開v は 1427 のはず
未検収: 2026-08-26 点検7件と仕上げ2件／2026-08-26 panel v35（公開vの注記）
```

### 5. 🙋 ヨシしてください

```
v1427 投票欄の折り返しの穴ふさぎ5件 — 納品。フル版検査PASS
印: y0826-1321
待っているのは：実機での確認。判定後の画面を横向きで見て三つ。①844x390 や 932x430 のふつうの端末で、投票欄が二段に折り返し、末尾の二択（連チャン／親流れ）が画面の中に見えていること ②画面の高さが 381〜383px になる持ち方（端末によっては出ない）で、根拠欄の解説が畳まれ、二択が画面内に残ること ③縦が 350px 未満になる持ち方では、二択が画面のいちばん下に貼り付いて、送っても常に押せること
答え方：「y0826-1321 にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
実機: 上の「待ち」の①〜③
未検収: 2026-08-26 点検7件と仕上げ2件／2026-08-26 v1427 投票欄の折り返しの穴ふさぎ
```

<!-- 送った知らせ ここまで -->
