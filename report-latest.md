# 押し送りの上限を60件へ／ヨシ待ちは上限でも通す／ntfy.sh 側の枠を調べた

**状態：ヨシ待ち（実機で見てください）** — 2026-08-25 15:0x 時点

**結論** — 上限は **60**（数字一つ）。**上限に当たっても、落とすのは終わりのほうだけ**で、
**ヨシ待ちは通します**。ntfy.sh 側の無料枠は **1日 250通**で、こちらの60は**十分に内側**です。
ただし枠の単位が **IP** なので、同じ回線の他の利用も同じ250を食います。

---

## 1. 上限は数字一つ（後で戻せる）

```powershell
# ~/.claude/watch-notify.ps1
$DAILY_MAX = 60
```

**戻すときはこの一行だけ**を直します。20 に戻せば元の振る舞いです。
上に、なぜ 60 にしたか（実測 約49件＋余裕）を注記として置きました。

## 2. 上限に当たっても、ヨシ待ちは通す

```powershell
if ($n -ge $DAILY_MAX) {
  if ($state -eq 'done') {
    Write-Self ('終わりの押し送りを落とした：本日 ' + $n + '件で上限（' + $DAILY_MAX + '件）。ヨシ待ちは通す')
    return
  }
  Write-Self ('上限（' + $DAILY_MAX + '件）を超えているが、ヨシ待ちなので通す：本日 ' + $n + '件目')
}
```

**落ちるのは終わりだけ**で、ヨシ待ちは何件目でも出ます。
どちらの回も記録に理由が残るので、後から「何件で当たって、何を落としたか」が読めます。

＊これは「上限がヨシ待ちには効かない」形です。暴走したときの歯止めが終わり側にしか無いことになりますが、
　**夜にヨシ待ちが届かないほうが困る**という裁定に従っています。

## 3. 一日の実数を控える

**`~/.claude/ntfy-daily.tsv`** に、送るたび日ごとの実数を足します。

```
日付        合計  ヨシ待ち  終わり
2026-08-25   3      2        1
2026-08-26   1      0        1
```

**合計だけでなく内訳も数えます。** 60 で足りるかは、
「終わりが増えたのか、ヨシ待ちが増えたのか」で打つ手が変わるためです。60日ぶん残します。

＊**数え始めは次の押し送りから**です。今日ぶんを遡って作ることはしていません
（作り物の数字と実測が混ざるため）。**数日ぶん溜まったら報告します。**

## 4. ntfy.sh 側の無料枠 — **1日 250通**。60 は十分に内側

ntfy.sh 自身の口（`https://ntfy.sh/v1/tiers` と `https://ntfy.sh/v1/account`）から、
**いまこの機械に効いている実際の値**を引きました。

| 項目 | 無料（匿名）の値 | こちらの使い方 |
|---|---|---|
| **1日の通数** | **250通** | 上限60 ＝ **枠の24%** |
| 数える単位（`basis`） | **`ip`** | ＊同じ回線の他の利用も同じ250を食う |
| 瞬発（burst） | **60通**、**5秒に1通**ずつ補充 | 送るのは1分に多くて1通。届かない |
| 取り置き | **12時間**（43200秒） | `Cache: no` なので溜めていない |
| 添付 | 1件2MB／合計20MB | 使っていない |
| メール／電話 | 5通／0件 | 使っていない |

**いまの残り**（引いた時点）

```
"messages": 250, "messages_remaining": 242     ← 本日 8通 使用
```

**気づいた食い違い** — こちらの数え（`ntfy-count.txt`）は本日 **13件**ですが、
ntfy 側は **8通**でした。**ntfy の「1日」は UTC 区切り**と見られ、こちらの JST 区切りとずれます
（JST 09:00 より前に送った分が前の日に入る）。**どちらも 250 には遠い**ので実害はありませんが、
数を突き合わせるときは区切りが違うことを踏まえてください。

**上限60 が ntfy 側の枠を超えることはありません。** 仮に IP を他の用途と分け合っても、
250 に対して 60 なら余裕があります。

## 5. 触ったもの

| ファイル | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1` | `$DAILY_MAX` を 60 へ／上限時はヨシ待ちだけ通す／`Ntfy-Tally` を足した |
| `~/.claude/ntfy-daily.tsv` | **新規**（次の押し送りから溜まる） |
| `koushu-handan.html` `panel.html` | **触っていません** |

## 6. 実機で見るところ

前の回のぶんが未検収のままです。**あわせて見てください。**

1. **ntfy** — 14:42:55 前後の二通（😼 と 😽 が3秒差）と、14:46:14 の 😼 が届いたか。
   **題が一行だけか**（絵文字が二つ並んでいないか）。
   **😼 と 😽 が通知一覧の小さな字で見分けられるか**
2. **返事パネル** — 画面のいちばん下の版の字が **`panel v31`**
   ＊v30 のままなら URL に `?_v=31` を付けて一度だけ開いてください
