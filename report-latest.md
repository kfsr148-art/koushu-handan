状態：終わり ／ 2026-08-20 21:24 ／ 枝豆の行が切れる原因は絵文字🫛の未収録（豆腐）と確定。兎の採点1000件も完了。実装には未着手

# 報告：枝豆の注記行が切れる原因と直し方／兎の採点1000件／船長の素材条件

日付 2026-08-20 ／ 本体の版 v1416（**変更なし。実装・修正には着手していない**）

---

## 【1】枝豆の注記行 — **切れているのではなく、絵文字が出ていない**

### 結論

**🫛（さやえんどう）が、この端末のフォントに入っていない。** 出ているのは代替表示の
**「×付きの四角」（豆腐）**で、それが「上下を切られたアイコン」に見えている。
**レイアウトの不具合ではない。**

**決め手。** 同じ描画条件で「絶対に存在しない符号（U+FFFF）」と並べて比べたところ、
**🫛 の描画結果が豆腐の見本と完全に一致した**（画素のハッシュ・大きさ・送り幅がすべて同値）。

| | 描画の大きさ | 送り幅 | ハッシュ |
|---|---|---|---|
| **🫛（いまの印）** | **9x11** | **9.61** | **1361705441** |
| U+FFFF（豆腐の見本） | 9x11 | 9.61 | **1361705441** |
| 🎩（紳士の印） | 18x18 | 20.43 | （別値・正しく出る） |

**🫛 は Unicode 14.0（2021年）**で、この端末（Windows 10 19045）の Segoe UI Emoji に
まだ入っていない。**🎩 は Unicode 6.0（2010年）**なので出る。**紳士だけ無事で枝豆だけ壊れる**のは、
この年代の差がそのまま出ている。

### 実測値（568x320 と 844x390 の両方）

**四つの注記行は、寸法の上ではまったく同じで、どこも切れていない。**

| 行 | 高さ | font | line-height | margin-top | flex | 行の溢れ |
|---|---|---|---|---|---|---|
| 現場猫 | **24px** | 10px | 12.5px | 4px | 0 0 auto | 0 |
| **枝豆** | **24px** | 10px | 12.5px | 4px | 0 0 auto | **0** |
| 紳士 | **24px** | 10px | 12.5px | 4px | 0 0 auto | 0 |
| 人柄 | **24px** | 10px | normal | 4px（844では2px） | 0 1 auto | 0 |

**アイコンの箱と中身**

| 行 | 箱（外寸／内寸） | 中身 | 中身の実寸 | 箱からのはみ出し |
|---|---|---|---|---|
| 現場猫 | 24x24 ／ 22x22 | **画像** | 原寸480x480 → 表示22x22 | なし |
| **枝豆** | 24x24 ／ 22x22 | **絵文字🫛** | **描画箱 15.3x19** | **上 −1.5 ／ 下 −3.5（＝内側）** |
| 紳士 | 24x24 ／ 22x22 | 絵文字🎩 | 描画箱 20.8x19 | 上 −1.6 ／ 下 −3.4（内側） |
| 人柄 | 24x24 ／ 22x22 | **画像** | 原寸358x392 → 表示22x22 | なし |

**はみ出し量はすべて負の値＝箱の内側**に収まっている。`.say-icon` の `overflow` は `visible` で、
そもそも切る指定が無い。親の `.verdict-card` は `overflow: hidden` だが、判定後は
`height:auto / max-height:none` になり、`scrollHeight` と `clientHeight` が一致（差0）で
**カード自体も何も切っていない**。

**枝豆の行だけ違う値** — 探したが、**寸法・行高・余白・flex・overflow のどれも他の三行と同じ**。
**違うのはただ一点、アイコンの中身が「出ない絵文字」であること**だけ。
（描画幅 15.3px は 🎩 の 20.8px より狭いが、これは豆腐の箱が細いためで、切れた結果ではない。）

**視野による差は無い。** 568x320 と 844x390 で、行の高さ・箱の寸法・はみ出し量はすべて同値
（人柄行の margin-top が 4px→2px になる違いだけ。これは既存の指定どおり）。

### 同じ絵文字を使っている他の場所

**🫛 は本体に7箇所**ある。**注記行だけでなく、投票欄のチップと見張り台にも同じ豆腐が出ているはず。**

| 行 | 場所 | 用途 |
|---|---|---|
| 4005 | `tenLeans` の `mark:'🫛'` | **投票欄のチップ**（枝豆には画像が無いので絵文字が出る） |
| 4458 | `sayIconHTML` の `{ eda: '🫛' }` | **注記行の行頭**（今回の件） |
| 7551 | `AV = { cat:…, eda:'🫛', om:'🎩' }` | 見張り台の出題者の印 |
| 1322 | 猫牌の説明文 | 本文中 |

**直すなら、この4箇所をまとめて見るのが筋。** 一箇所だけ替えると、投票欄と注記行で印が食い違う。

---

## 【2】直し方の候補

**同じ端末で候補を実際に描かせて、出るかどうかを確かめた。**

| 候補 | 収録 | 描画 | 判定 |
|---|---|---|---|
| 🫛 さやえんどう | Unicode 14.0 / 2021 | 9x11 | **✗ 豆腐** |
| 🫘 まめ | Unicode 14.0 / 2021 | 9x11 | **✗ 豆腐** |
| 🌱 ふたば | Unicode 6.0 / 2010 | 18x18 | ✓ 出る |
| 🌿 ハーブ | Unicode 6.0 / 2010 | 17x18 | ✓ 出る |
| 🍀 よつば | Unicode 6.0 / 2010 | 18x18 | ✓ 出る |
| 🥬 はっぱ野菜 | Unicode 11.0 / 2018 | 18x18 | ✓ 出る |
| 🥒 きゅうり | Unicode 9.0 / 2016 | 18x18 | ✓ 出る |
| 🟢 みどりまる | Unicode 12.0 / 2019 | 18x18 | ✓ 出る |
| 🎩 シルクハット（紳士） | Unicode 6.0 / 2010 | 18x18 | ✓ 出る |

**豆に近い意味で確実に出るのは 🌱・🌿・🍀・🥬・🥒。**
**🫘（まめ）も同じ2021年組なので使えない**——ここを取り違えると同じ事故を繰り返す。

### 候補と、それぞれの影響

**候補A：出る絵文字に差し替える（🌱 など）**

- **変更量**：字面のみ。4箇所の `'🫛'` を置き換える。
- **寸法への影響**：**無し。** 描画は 18x18 で、箱（内寸22x22）に収まる。
  紳士の 🎩 と同寸になるので、**むしろ二行の見た目が揃う**。
- **行の高さ・二択の位置**：**動かない**（行の高さは箱の24pxが決めており、中身では変わらない）。
- **作法への影響**：§11・§20 の表に枝豆の印として 🫛 は書かれていない（表にあるのは
  「緑 `var(--jade)`」と `eda-mark`）ので、**作法書の書き換えは不要**と見る。
- **難点**：豆そのものの絵ではなくなる。**枝豆という名前と絵が少しずれる。**

**候補B：絵文字をやめて画像にする（現場猫・人柄と同じ形）**

- **変更量**：枝豆の絵を1枚用意し、`sayIconHTML` と投票欄で `img` を出す形へ。
- **寸法への影響**：**無し。** `.say-icon img { width:100%; height:100%; object-fit:contain }` が
  効くので、原寸に関わらず 22x22 に収まる（現場猫は原寸480x480、人柄は358x392 が同じ箱に入っている）。
- **行の高さ・二択の位置**：**動かない。**
- **利点**：**フォントに依存しなくなる**。この種の事故が二度と起きない。
  現場猫・人柄と作りが揃う（いま絵文字なのは枝豆と紳士の二人だけ）。
- **難点**：**素材が要る。** 埋め込みぶん本体が重くなる（現場猫の絵で30KB前後）。

**候補C：文字で置く（「豆」の一字）**

- **変更量**：字面のみ。
- **寸法への影響**：**無し**だが、`font-size: calc(24px * 0.62) = 14.88px` の日本語一字になるので、
  **他の三行の絵と並ぶと文字だけ浮く。**
- **利点**：確実に出る。素材も要らない。
- **難点**：見た目が揃わない。

**候補D：フォント指定を足す** — **効かないので除外。** グリフそのものが端末に無いため、
`font-family` に何を並べても出ない。

### 所見（裁定はお任せします）

**いちばん軽いのは候補A**（字面4箇所の置換）で、寸法にも二択の位置にも影響が出ない。
**いちばん確実なのは候補B**で、フォントの当たり外れから完全に降りられる。
**紳士の🎩も同じ絵文字方式**なので、Bを採るなら紳士も一緒に画像化するか、
枝豆だけ画像にして紳士は絵文字のまま残すか、揃え方の指定もいただきたい。

---

## 【3】兎の採点 — 1000件の結果（打ち切り）

**裁定どおり1000件で打ち切った。50000件へは積んでいない。**

### 点差（最善−最悪）

| | **1000件** | 320件（前回） |
|---|---|---|
| 平均 | **38.23pt** | 39.69pt |
| 中央値 | **32.35pt** | 32.35pt |
| **5pt未満に潰れる割合** | **28.80%** | 27.2% |
| 点差ちょうど0 | 8.70% | 10.0% |
| 20pt以上 | 61.30% | 62.5% |

**320件の結論はそのまま。** 中央値32.35ptは完全に一致し、5pt未満の割合も1.6pt差に収まった。
**点は分離する。**

### 同点（順位が成り立たない件）

| | **1000件** | 200件（前回） |
|---|---|---|
| **最善に並ぶ種類数** | **平均6.08種**（最大13） | 6.16種 |
| **一位が一種だけ** | **9.7%** | 6.5% |
| 点差ちょうど0 | 87件 | 17件 |
| どの切りも0pt | 55件 | 10件 |
| どの切りも100pt | 32件 | 7件 |

**平均11.3種のうち6.08種が一位で並ぶ。** 順位が成り立たないという結論も変わらない。
**裁定どおり順位は出さず、「宣言した切りの点／最善との差／最善に並ぶ種類数」の三つを出す形**が、
この数字に対して素直だと考える。

### 最善の切りの内訳（同点を1/並び数で割った）

| 分類 | **1000件** | 200件（前回） |
|---|---|---|
| 中張・つながり | **38.3%** | 41.5% |
| 字牌・孤立 | **25.1%** | 22.9% |
| 19・つながり | 13.9% | 14.6% |
| 19・孤立 | 9.7% | 8.4% |
| 中張・孤立 | 9.5% | 10.0% |
| 字牌・重なり | 3.5% | 2.6% |

**字牌の孤立が一位タイに入る割合は 72.4%**（920件中666件。200件では68.7%）。
定石と食い違って見えるが、**「字牌が悪い」ではなく「この採点では中張のつながりと区別が付かない」**
という前回の読みが、件数を増やしても保たれた。

### 前回の報告を一点訂正

**「4枚持ちで飛ばした種は一度も起きなかった」と書いたが、これは件数が少なかったためだった。**
1000件では**延べ25回**起きている（320件では0回）。**実装では必ず要る処理**で、
省くと不正な13枚が判定に混ざる。

### 所要時間について一点お断り

**1000件の走りの時間値は、時間の指標としては使えない。** ほかの測定と同時に走らせたため、
ページ内の `performance.now()` が実時間とずれた（ページ内は全種類516ms、実時間は
2175.9秒÷1000＝**2.18秒/件**）。**時間の公式値は、単独で回した100件のほう**
（一枚196.7ms／全種類2225.1ms）を使ってほしい。実時間2.18秒/件はそちらと整合している。

---

## 【4】裁定の記録（前回いただいた分）

実装には着手していないが、次に触るときのために控えた。

- **順位は表示しない。** 出すのは **①宣言した切りの点 ②最善の点との差（pt） ③最善に並ぶ種類数**の三つ。
- **赤5と普通の5は別の宣言**として扱う。**引きを当てる34種の側は据え置き**（赤を混ぜない）。
- **速度の段取り**：判定を押した時点で**全種類の採点を裏で回し始める**。牌を押したら
  **その一枚の点（約197ms）を先に出す**。最善との差と同点の種類数は**裏の計算が終わり次第あとから足す**。
  **裏の計算中に別の牌を押されても壊れないこと**（世代の印を持たせて古い結果を捨てる形が要る）。
- **(2) は1000件で打ち切り。** 50000件へは積まない。

---

## 【5】船長（ネルソン）の絵を兎に替える — 素材の条件

**名前も台詞も地の文も一文字も変えない。絵だけ。見張り台の船長猫は別人なので触らない。**

### 先にひとつ報告 — **差し替えが要るのは実質1枚**

前回「絵は2枚」と報告したが、**そのうち1枚は画面に出ていない**ことが分かった。
`ADV_FACE_IMG.captain`（235x400）へ辿り着く経路を三つとも当たった結果：

| 経路 | `captain` は届くか |
|---|---|
| `sayIconHTML(key)`（4449行） | **届かない**。`saySet` の呼び出し元は人柄七人・`cat`・`eda`・`ai`・`mascot` のみ |
| 一味の並び（6148行） | **届かない**。`pk === 'captain'` のときは `ADV_CAPTAIN_IMG` へ分岐する |
| 投票欄のチップ（8492行） | **届かない**。チップの鍵に `captain` は無い（0件） |

**つまり `ADV_FACE_IMG.captain` は死んでいる。**（`check.js` の ⑥ が拾えていないのは、
入れ物の変数名では参照されているためで、鍵ごとには見ていないから。）

**必要な絵は1枚だけ**にできる。もう1枚も差し替えるか、この機に消すかは裁定をいただきたい。

### 素材の条件

**いま出ている1枚：`ADV_CAPTAIN_IMG`（本体5303行）**

| 項目 | 条件 |
|---|---|
| **ファイル名** | **いまは無い**（本体に base64 で直接埋め込んであり、外部ファイルは存在しない） |
| **提案する名前** | 元絵を **`adv_captain_src.png`** としてリポジトリ直下へ（作法§21「元絵は `*_src.png` で残す」に倣う） |
| **寸法** | **127 x 180**（現行と同じにするのが安全） |
| **形式** | **PNG・8bit・RGBA（色種別6）** |
| **背景** | **完全な透過**（α=0）。**黒背景でも白背景でもない** |
| **縁** | **半透明のぼかしを許す**（現行は全体の8.3%が半透明。四隅はα=0） |
| **絵の置き方** | **絵は縦いっぱい**（上端y=0から下端y=179まで）。左右に余白18px/19px。**足が下端に接する** |

**使われ方が二つあるので、両方で成立する絵にしてほしい。**

1. **船長室の立ち絵**（6287行）— 高さ42%で表示。`image-rendering: pixelated` で縮小され、
   **四方に1pxの暗い縁取り**（`drop-shadow` 四つ）が掛かる。
   **縁が半透明だと、縁取りもぼやける。**
2. **一味の並びの小アイコン**（6148行）— **22x30 に押し込む**（`object-fit: contain`）。
   **縦長の枠なので、横に広い絵だと小さくなる。**

**参考：同じ127x180の枠に入っている人柄七人の立ち絵**（`ADV_CHAR_IMG`）は
**半透明画素が0%（硬い縁）**で、絵の位置も左右いっぱい（x 2..124）・上に18pxの余白がある。
**船長だけ作りが違う**（縁が柔らかく、縦いっぱい）。どちらに揃えるかも指定してもらえると迷わない。

**もう1枚（死んでいる `ADV_FACE_IMG.captain`）を替える場合**

| 項目 | 条件 |
|---|---|
| 提案する名前 | `adv_captain_face_src.png` |
| 寸法 | 235 x 400 |
| 形式・背景・縁 | 上と同じ（PNG・RGBA・完全透過・半透明の縁あり） |

**素材が届くまで着手しない。**

---

## 6. 触れていないこと

- `koushu-handan.html` / `check.js` / `ver.txt` / 素材は**すべて無変更**。この報告書だけが変わっている。
- 版番号は上げていない。`analyze()` / `bestShapeOneSuit` にも触れていない（作法§5）。
- **テスト用のシムなし。** 測定は写しに probe と、一時ディレクトリの単独HTMLで行い、いずれも削除済み。
- 判定カードの行の並べ替えは、**取り下げの指示どおり手を付けていない。**

---

## 7. 次

- **枝豆の印の裁定**（候補A〜Cのどれか）。Bなら素材が要る。紳士の🎩を揃えるかも合わせて。
- **替える場合の範囲**：`'🫛'` は本体に4箇所（投票欄・注記行・見張り台・説明文）。まとめて替えるか。
- **船長の絵**：素材待ち。死んでいる1枚を替えるか消すかの裁定。人柄七人の作り（硬い縁）に揃えるか。
- 持ち越し：**振りの八枚の倍率の裁定**、**目盛り画像を置くか**、**`sizing-review.png` の扱い**。
