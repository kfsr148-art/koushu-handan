# 返事パネルに二つ足した（panel v32）

**状態：ヨシ待ち（実機で見てください）** — 2026-08-25 15:4x 時点

**結論** — **機械が測った様子の一行**と、**写した札の「届いた」印**を入れました。
どちらも**判じられないときは何も出しません**。嘘の印は付けていません。

---

## 1. 機械が測った様子の一行

状態の帯のすぐ下に、**控えの字を読まずに**一行出します。材料は `usage.json` の
`watch.lastStop` ／ `watch.lastResume` ／ `watch.cpu` だけです。

**判じ方** — 最後に鳴ったのが `resume` なら**動いています**、`stop` なら**止まっています**。
どちらも読めなければ**何も出しません**。

```
機械が見た様子：動いています（最後の再開 14:32:15 ／ CPU 4.2% ／ 2分前の値）
```

**「◯分前の値」を必ず添えます。** 見張りは10分ごとに書くので、古いことがあるためです。

**控え由来の状態と食い違うときは、両方を並べます。** 枠の色を橙にして目を引かせ、
**どちらが正しいかは決めつけません**。

```
機械が見た様子：止まっています（最後の停止 14:32:15 ／ CPU 4.2% ／ 2分前の値）
控えでは：作業中 ── 食い違っています。どちらが正しいかは、ここでは決めていません。
```

＊`watch.state` が「異常」のときは突き合わせません（別の話なので）。

### 実測（`panel-mach-probe.js`。7通り）

| 与えた値 | 出たもの |
|---|---|
| 動＋控えも作業中 | `mach` ／ 動いています（最後の再開・CPU・◯分前） |
| 止＋控えもヨシ待ち | `mach` ／ 止まっています |
| **止＋控えは作業中** | **`mach gap`** ／ 両方を並べた |
| **動＋控えはヨシ待ち** | **`mach gap`** ／ 両方を並べた |
| 刻が読めない | **何も出さない**（`mach hide`） |
| `watch` そのものが無い | **何も出さない** |
| `cpu` が `null` | CPU の欄だけ落ちる（他は出る） |

## 2. 写した札の「届いた」印

**置き場所** — 一覧の下に、**畳んだ箱**「写した札 ◯件（届いた ◯件）」を足しました。
押すと開きます。**上の一覧の見え方は v24 のまま**で、写した札は一覧には戻していません。

**印の付け方** — **写した刻より後に `resume` が鳴っていれば**「届いた」と添えます。
写した本文を貼って送れば Claude Code は `resume` で動き出すので、その一本を証拠にしています。

**次のどれかに当たれば、何も出しません。**

- `watch` が読めていない
- `lastResume` が読めない字
- **写した刻を控えていない古い印**（v31 以前は `1` を入れていた）

そのため、**写した刻を控える**ようにしました（`copied[鍵] = 刻`）。古い印はそのまま残り、
印が付かない側へ倒れます。

### 実測（`panel-mach-probe.js`。4通り）

| 仕込んだ印 | 見出し | 届いた印 |
|---|---|---|
| 写したのが再開より**前**（2件とも） | 写した札 2件（**届いた 2件**） | 2件 |
| 写したのが再開より**後**（2件とも） | 写した札 2件 | **0件** |
| 片方だけ前 | 写した札 2件（**届いた 1件**） | 1件 |
| **古い印（刻が無い＝`1`）** | 写した札 2件 | **0件** |

## 3. 変更した行（`panel.html`）

| 場所 | 中身 |
|---|---|
| **84–91** | `.mach`（機械の一行の見た目。食い違いは `.gap` で橙） |
| **93–99** | `.done-box` `#copiedList` `.got`（写した札の箱と「届いた」の字） |
| **196–199** | `#machLine` の置き場（状態の帯の直後） |
| **222–228** | `#copiedBox`（一覧の下。畳んだ箱） |
| **243** | `var allRows`（写した札も含む全件の控え） |
| **430** | `paintCopied(allRows)` を一覧の組み立てから呼ぶ |
| **915–931** | `lastWatch` と `pullUsage` の受け（`paintMach` / `paintCopied` を呼ぶ） |
| **934–941** | `tsOf`（`2026-08-25 14:32:15` を刻へ。Safari 対策で字を割る） |
| **943–988** | `paintMach`（機械の一行の本体） |
| **990–1023** | `paintCopied`（写した札の箱と届いた印） |
| **756–759** | 全部写すで**写した刻**を控える |
| **779** | 一件写すで**写した刻**を控える |
| **1137–1146** | 写した札の箱の開閉（一覧と同じ受け） |
| **230 / 251 / `panel-ver.txt`** | 版を **v32** へ（三箇所そろえた） |

差分は **152行追加・7行削除**。`koushu-handan.html` には触っていません。

## 4. 実機で見るところ

**返事パネル**を開いて、次の四つを見てください。

1. 画面のいちばん下の版の字が **`panel v32`**
   ＊v31 のままなら URL に `?_v=32` を付けて一度だけ開いてください
2. **状態の帯のすぐ下に一行**出ていること。
   「機械が見た様子：動いています（最後の再開 hh:mm:ss ／ CPU ◯% ／ ◯分前の値）」の形。
   **CPU の数字と「◯分前の値」が出ているか**まで見てください
3. 一覧の下に **「写した札 ◯件」の畳んだ箱**があり、**押すと開く**こと。
   写した札が一件も無ければ、この箱は**出ません**
4. 写した札を Claude Code へ貼って送ったあと、しばらく（次の使用量取得＝最大10分）してから
   パネルを開き直すと、その札に **「届いた」**（緑）が付くこと

＊2 の一行は、控えと食い違ったときだけ**二行**になり、**左の縦線が橙**になります。
