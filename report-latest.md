# 被り-1 の調べ — 審査者の材料の総当たり表・被りの洗い出し・空き軸の候補・4000行での検定

**状態：終わり（残り0件）／VAIO 2026-08-29 14:5x**

**結論** — 画面に出る審査者が読む `analyze()` の項目を**行番号つきで総当たり**にした。
**言い換えは既知の三つ（`hasValue`・`shapeNeeded`＝`doraValue`／`totalBlocks`）だけではない。**
新たに **`widthLabel`→`widthGood`**・**`kokushiKinds`→`kokushiSeen`**・**`valueSources`→`hasValue`** の
関数従属が3組、**`acceptTypes`⇔`acceptTiles`（r=0.996）**・
**`kokushiShanten`⇔`defHonors+defTerminals`（r=−0.967）**・**`weakShapes`⇔`fillWeak`（r=0.824）** の
実質同一が3組。審査者どうしでは **軍師と先生が同じ二本の境目**（向聴2以下＝攻／5以上＝守）を見ており、
**お嬢様の票と判定獣の絵**も同じ材料（守り牌の合計）だった。

**厳密に「どこからも読まれていない項目」は0件。** そのうえで**攻守の傾きに一切効いていない項目は14件**あり、
4000行で測ると **`fillRemain`（残り面子枠）が最も強い空き軸**——向聴を揃えても和了率を
**向聴2で15.2pt・向聴3で9.5pt** 動かす。

＊冒頭の一点は指示どおり受けた。**数字は四度とも届いており、13:56 の札は検収済み。**
　「答えが届かなかった」の原因究明はここで打ち切った。**panel v75（畳んだ札の表紙に件名）はそのまま残す。**
　本体（`koushu-handan.html`）には**一字も触れていない**。直しは当てていない。

---

## 1. 画面に出る審査者 × `analyze()` の項目（総当たり・行番号つき）

`analyze()` の返しは **L3054-3062 の33項目**。読み手を、指示の五つ（七人の見立て・判定カード・
根拠欄・集計行・撤退予約線）と、同じ画面に並ぶスイッチ四者の票に分けて並べる。

### 1-1. 七人の見立て

票は `tenLeans()`（L4023-4079）、見立て行は `toneAngleLine()`（L4433-4510）、
金枠は `toneFacts()`（L4242-4432）。**三つとも同じ人柄の担当だが、読む項目は同じではない。**

| 人柄 | 票（`tenLeans`） | 見立て行（`toneAngleLine`） | 金枠の材料（`toneFacts`） |
|---|---|---|---|
| 執事 | **L4048** `a.doraValue >= 1` | L4437-4440（`F.multi`） | **L4251** `a.shapeTiles`／**L4274** `a.yakumanReach`・`a.kokushiSeen`／**L4276** `a.yakuhaiPair` |
| 軍師 | **L4049** `a.baseShanten`・`a.acceptTiles` | **L4446・L4452** `a.baseShanten` | **L4302** `a.directions` |
| ずんだ | **L4050** `a.goodShapes` | **L4463・L4464** `a.weakShapes` | 牌のみ（`analyze()` の返しは使わない） |
| お嬢様 | **L4026** `a.defHonors`＋`a.defTerminals` → L4051 | L4472-4476（`F.lady`） | **L4376** `a.defHonors`＋`a.defTerminals` |
| マダム | **L4034** `a.shapeTiles`（浮き牌の引き算）→ L4052 | L4478-4482 | L4034 と同じ引き算 |
| 一姫 | **L4053** `a.yakumanReach`・`a.kokushiSeen`・`a.yakuhaiPair` | L4484-4500 | 牌のみ |
| 先生 | **L4054** `a.baseShanten` | L4502-4507 | 牌のみ |

### 1-2. スイッチ四者の票（同じ投票欄）

| 道具 | 行 | 読む項目 |
|---|---|---|
| 現場猫 | L4058-4061 | **なし**（猫牌の枚数は画面側で数える） |
| 枝豆 | L4062-4064 | **なし**（対子・暗刻の種類数を手牌から数え直す L4030） |
| 紳士 | L4065-4066 | **なし**（一枚きりの字牌を手牌から数え直す L4031） |
| 兎 | `usagiLean()` **L4015-4021** | **L4017・L4018** `a.totalBlocks`／**L4019** `a.fillWeak`／**L4020** `a.fillGood`・`a.goodShapes`／say は **L4075-4076** `a.fillWeak` |

### 1-3. 判定カード

| 部品 | 行 | 読む項目 |
|---|---|---|
| 判定獣の絵（控え） | **L8411-8414** | `a.verdict`・`a.yakumanReach`・`a.kokushiSeen`・`a.defHonors`＋`a.defTerminals` |
| 判定獣の絵（描画） | **L8594** | 同上 |
| 攻／守の漢字 | **L8391**（`attack`）→ L8595 | `a.verdict` |
| 攻守の一言 | **L8415-8417** → `pickVerdictWord()` L3974-3976 | `a.borderline`・`a.yakumanReach`・`a.kokushiSeen` |
| 虎の台詞 `mascotSay()` | **L4104-4127** | L4106 `a.verdict`／L4108-4109 `a.kokushiSeen`・`a.yakumanReach`・`a.yakumanName`／L4111-4115 `a.shapeOK`／**L4120-4121** `a.goodShapes`・`a.shapeNeeded`・`a.acceptTiles`・`a.doraValue` |
| 虎の親補正 `mascotDealerNudged()` | **L4096-4102** | L4097 `a.isDealer`・`a.kokushiSeen`・`a.yakumanReach`／L4098 `a.acceptTiles`・`a.doraValue`／**L4100** `a.baseShanten`／L4101 `a.shapeOK` |
| 場風・自風の行 | **L8553-8559** | `a.roundWind`・`a.seatWind`・`a.isDealer` |
| 締めの文（`verdict-foot`） | **L8646-8661** | L8649/8656 `a.yakumanName`／L8651/8658 `a.kokushiKinds`・`a.kokushiShanten`／**L8652/8659** `a.doraValue`・`a.goodShapes`／**L8653/8660** `a.doraValue`・`a.shapeNeeded` |

### 1-4. 根拠欄（`reasons` L8471-8549）

| 欄 | 行 | ✓×を決める項目 | 字に出る項目 |
|---|---|---|---|
| ①打点 | L8473-8481 | **L8474** `a.hasValue` | L8475/8477 `a.doraValue`／L8475 `a.valueSources` |
| ②埋まり | L8482-8488 | **L8482** `a.shapeOK` | L8483-8486 `a.fillGood`・`a.fillWeak`・`a.fillRemain` |
| ③伸びしろ | L8489-8500 | **L8490** `a.widthGood` | L8491/8492/8494 `a.widthLabel`／**L8491** `a.acceptTypes`／L8491/8493/8495/8497/8498 `a.acceptTiles`／L8493-8498 `a.baseShanten` |
| ④見える手（攻） | L8501-8528 | **L8522** `a.directions.length` | L8503 `a.kokushiSeen`／L8506/8508/8514/8515 `a.kokushiShanten`／L8514/8515 `a.kokushiKinds`／L8514-8524 `a.directions` |
| ④守る弾（守） | L8529-8548 | **L8533** `a.defHonors`・`stock` | L8531/8535/8538/8541/8545 `a.defHonors`・`a.defTerminals` |

### 1-5. 集計行（`judged-note` L8452-8469）

| 出る字 | 行 | 出どころ |
|---|---|---|
| 向聴 | **L8454** | `a.baseShanten` |
| 受けN枚 | **L8455** | `a.acceptTiles` |
| 猫牌N枚 | L8456 | 画面側（`effectiveCatN`）。`analyze()` の返しではない |
| ドラN枚＋赤N | L8458/8464/8466 | **画面側のグローバル `doraCount`（L1751）と `hand` の赤**。`a.doraCount`・`a.redCount` は**読んでいない** |

### 1-6. 撤退予約線（L8393-8406）

| 行 | 読む項目 |
|---|---|
| L8391/8394 | `a.verdict`（攻めのときだけ出す） |
| L8396 | `a.yakumanReach`・`a.kokushiSeen`（見えていれば線を引かない） |
| **L8397・L8402・L8404** | `a.baseShanten`（`TURN_BY_SHANTEN` で巡目に直す） |

**この欄は `acceptTiles` を読んでいない。** 見出しのコメント（L8392）は
「受け入れ枚数から一段進むのに要する巡目を見積もり」と書いてあるが、
実装が見るのは **`baseShanten` だけ**。字と中身が食い違っている（→ 5節）。

### 1-7. 別画面（参考・指示の五つの外）

| 画面 | 行 | 読む項目 |
|---|---|---|
| 見張り台の答え合わせ `watchReasonLine()` | **L4997-5004** | `a.baseShanten`・`a.acceptTiles`・**`a.redCount`（L5000）**・**`a.doraCount`（L5001）**・`a.yakumanReach`・`a.kokushiSeen` |
| 誤診記録の材料（判定時の控え） | L8277-8283 | `verdict`・`baseShanten`・`yakuhaiPair`・`goodShapes`・`shapeNeeded`・`acceptTiles`・`doraValue`・`widthLabel`・`shapeOK`・`defHonors`・`defTerminals`・`yakumanReach`・`yakumanName`・`kokushiSeen`・`kokushiKinds` |
| 誤診記録の生成 `misdiagCollect()` | L6999-7007 | 同上（控えと同じ15項目） |

---

## 2. 同じ材料を見ている組（総当たり）

### 2-1. コードの上で一意に決まる組（4000行で例外0件）

`core-probe.js` で 4000行に `analyze()` を掛け直し、**「X が決まれば Y が一意に決まるか」**を
全項目の総当たりで数えた。成立したのは5組。

| X（材料） | → | Y（言い換え） | 根拠の行 | 対応 |
|---|---|---|---|---|
| `doraValue` | → | `hasValue` | L2872 `hasValue = doraValue>0` | **既知** |
| `doraValue` | → | `shapeNeeded` | L2880-2883 | **既知**。0/1→4・2→3・3以上→2（実測でも例外0） |
| `valueSources`（要素数） | → | `hasValue` | L2868-2872 | **新規**。打点の内訳の本数がそのまま `hasValue` |
| `widthLabel` | → | `widthGood` | L2817 付近 | **新規**。「狭い」だけ×、他4語はすべて✓ |
| `kokushiKinds` | → | `kokushiSeen` | L2955・L2966 | **新規**。種類数が閾値を跨いだかの二値 |

加えて **`shapeOK` ≡ (`goodShapes` >= `shapeNeeded`)**（L3027・外れ0/4000）。
`shapeOK` は独立した材料ではなく、**ずんだの票（`goodShapes`）と打点（`shapeNeeded`←`doraValue`）の積**。

### 2-2. 別名だが実質同じ数（相関 |r| ≥ 0.60・4000行）

| 組 | r | 意味 |
|---|---|---|
| **`acceptTypes` ⇔ `acceptTiles`** | **0.996** | **新規・最大の被り。** 根拠欄③は同じ行に「有効牌 N種 M枚」と両方を並べているが、**中身は一つの数**（→ 4節で検定） |
| **`kokushiShanten` ⇔ `defHonors`+`defTerminals`** | **−0.967** | **新規。** 国士向聴は「么九字牌の種類数」の裏返しで、**お嬢様の票の材料と同じ物差し** |
| `doraValue` ⇔ `valueSources` の本数 | 0.962 | 2-1 の従属の連続版 |
| `kokushiKinds` ⇔ `kokushiShanten` | −0.944 | 同じ数え上げの表裏 |
| `hasValue` ⇔ `doraValue` | 0.886 | 2-1 の従属 |
| `kokushiKinds` ⇔ `defHonors`+`defTerminals` | 0.883 | 上と同根 |
| **`weakShapes` ⇔ `fillWeak`** | **0.824** | **新規。ずんだの見立て行（L4463）と根拠欄②（L8483）が同じ材料** |
| `shapeNeeded` ⇔ `doraValue` | −0.739 | 2-1 の従属 |
| `goodShapes` ⇔ `shapeOK` | 0.719 | 2-1 の等式 |
| `totalBlocks` ⇔ `baseShanten` | −0.71 | 兎の票の材料と、軍師・先生の材料が近い |
| `doraValue` ⇔ `redCount` | 0.708 | 赤五が打点の主成分 |
| `yakuhaiPair` ⇔ `directions` の本数 | 0.682 | 一姫の票と根拠欄④の✓× |
| `baseShanten` ⇔ `acceptTypes` / `acceptTiles` | 0.666 / 0.652 | 集計行の二つの数が近い |
| `goodShapes` ⇔ `fillGood` / `totalBlocks` | 0.607 / 0.604 | ずんだの票と兎の票 |

### 2-3. 審査者どうしの被り（誰と誰が同じものを見ているか）

| 材料 | それを見ている審査者 | 被りの中身 |
|---|---|---|
| **`baseShanten`** | 軍師の票 L4049／**先生の票 L4054**／集計行 L8454／根拠欄③／撤退予約線 L8397／親補正 L4100 | **軍師と先生は同じ二本の境目**（向聴2以下＝攻・5以上＝守）。違うのは中間帯だけで、軍師は `acceptTiles` で埋め、先生は黙る。**七人のうち二人が同じ物差し** |
| **`doraValue`** | 執事の票 L4048／根拠欄①の✓×（`hasValue` L8474）／虎の {d} L4121／締め文 L8652-8660／`shapeNeeded` 経由で虎の {m} | **執事の票と根拠欄①の✓×は同じ境目**。厳密には執事が `>=1`、`hasValue` が `>0` で、**孤立ドラ1枚（`doraValue`=0.5・L2864）のときだけ食い違う** |
| **`defHonors`+`defTerminals`** | お嬢様の票 L4026／お嬢様の見立て行 L4376／**判定獣の絵 L8413・L8594**／根拠欄④守る弾 L8531 | **お嬢様の票と、羊／鼠の絵が同じ材料**（境目だけ違う。お嬢様は5〜6、獣は2）。さらに `kokushiShanten` と r=−0.967 なので、**根拠欄④の国士の距離とも同じ物差し** |
| **`goodShapes`** | ずんだの票 L4050／兎の票 L4020／虎の {n} L4120／締め文 L8652 | ずんだと兎が同じ数を見ている（兎は `totalBlocks`・`fillGood`・`fillWeak` と混ぜる） |
| **`acceptTiles`** | 軍師の票（中間帯）L4049／虎の {r} L4121／親補正 L4098／集計行 L8455／根拠欄③ L8491 | 根拠欄③は同じ行に `acceptTypes` も出すが r=0.996 で同じ数（2-2） |
| **`shapeTiles`** | マダムの浮き牌 L4034／執事の「攻めの種」L4251 | **マダムと執事が同じ分解を見ている**（マダムは「入らなかった牌」、執事は「入った牌」で、表裏） |
| **`yakuhaiPair`** | 一姫の票 L4053／執事の `yakuHit` L4276／根拠欄④（`directions` 経由・r=0.682） | |
| **`yakumanReach`／`kokushiSeen`** | 一姫の票 L4053／判定獣の絵 L8412／攻守の一言 L8416-8417／虎 L4108／撤退予約線 L8396／紳士の沈黙 L8363 | 六箇所が同じ二つの旗を見ている |
| **`fillGood`／`fillWeak`／`fillRemain`** | 兎の票 L4019-4020／根拠欄② L8483-8486 | 兎の票は `fillGood`・`fillWeak`・`totalBlocks`・`goodShapes` を**四つ混ぜている**（`fillRemain` だけ使わない） |

---

## 3. どこからも読まれていない項目（空き軸の候補）

### 3-1. 厳密な意味では **0件**

33項目すべてが、少なくとも一箇所から読まれている。**`analyze()` に死んだ返しは無い。**

ただし**判定画面（指示の五つ）の中では読まれない**項目が2件ある。

| 項目 | 何を測っているか | 読む所 |
|---|---|---|
| `doraCount` | ドラ表示牌から決まるドラの枚数（赤を含まない） | **見張り台の答え合わせ L5001 だけ**。集計行は画面側のグローバル（L8464/8466）を読み、`a.doraCount` は使わない |
| `redCount` | 手牌に入っている赤五の枚数 | **見張り台の答え合わせ L5000 だけ**。集計行は `hand` から数え直す（L8458） |

### 3-2. 使う定義を「攻守の傾き・✓×を一切決めていない」に広げると **14件**

これが**空き軸の候補**。値としては画面に出るが、**誰の票も動かさず、どの✓×も決めない**。

| 項目 | 何を測っているか | いまの扱い |
|---|---|---|
| **`acceptTypes`** | 引けば手が進む牌の**種類数**（枚数とは別勘定） | 根拠欄③の字だけ L8491 |
| **`fillRemain`** | 和了形（4面子1雀頭）まで**残っている面子枠の数**＝4−確定面子 L2604 | 根拠欄②の字だけ L8483/8485/8486 |
| **`weakShapes`** | **嵌張・辺張の数**（弱形のまとまり） | ずんだの見立て行の字だけ L4463/4464 |
| **`redCount`** | 赤五の枚数 | 判定画面では未使用（3-1） |
| **`doraCount`** | ドラの枚数 | 判定画面では未使用（3-1） |
| **`valueSources`** | 打点の内訳の**名前の並び**（「ドラ2」「赤」「白」など） | 根拠欄①の題の字だけ L8475 |
| **`directions`** | 手なりで見えている**役の名前**（最大3つ） | 根拠欄④の字 L8514-8524 と軍師の染め色の名指し L4302。✓×は本数だけ |
| **`kokushiKinds`** | 手にある**么九字牌の種類数** | 締め文と根拠欄④の字だけ |
| **`kokushiShanten`** | 国士無双までの**おおよその向聴** | 同上 |
| **`yakumanName`** | 見えている役満の**名前** | 締め文と虎の字だけ |
| **`widthLabel`** | 広さを表す**言葉**（非常に広い／広い／ふつう／狭い） | 根拠欄③の字。✓×は別項目 `widthGood` が持つ |
| **`seatWind`** | 自風 | 場風・自風の一行 L8555 |
| **`roundWind`** | 場風 | 同 L8554 |
| **`borderline`** | 判定が**紙一重**で倒れた印（L3037・L3047） | 一言を「際どい」系へ振り分けるだけ L3976 |

---

## 4. 4000行での検定（`core-battle-log.tsv`）

### 4-1. 測り方と、入力が正しいことの確かめ

記録は 1000局×4席＝**4000行**。`core-probe.js`（本体を字面で読み出す取り出し器・**本体は無変更**）で
各行の配牌に `analyze()` を掛け直した。自風は `WIND[(席−親席+4)%4]`、場風は `E`、ドラは 0（記録を作った
`core-battle.js` L124-128 と同じ渡し方）。

**入力の再現が正しいことの確かめ** — 再計算した値と記録の列を突き合わせた。

| 列 | 食い違い |
|---|---|
| `baseShanten` | **0 / 4000** |
| `acceptTiles` | **0 / 4000** |
| `goodShapes` | **0 / 4000** |
| `defHonors` | **0 / 4000** |
| `verdict` | 890 / 4000（22.3%） |

`verdict` だけずれるのは当然で、**記録は 08-28（較正-1 の線引きを当てる前）に採ったもの**。
向聴4以上で幅の関門が消えた分がそのまま出ている。**他の四つが完全一致なので、入力の再現は正しい。**

### 4-2. 地の値（比べる相手）

| 向聴 | n | 和了% | 放銃% | 平均点棒 |
|---|---|---|---|---|
| 2 | 410 | 31.7 | 13.7 | +1067 |
| 3 | 1421 | 25.3 | 12.7 | +478 |
| 4 | 1586 | 18.0 | 13.2 | −42 |
| 5 | 539 | 11.3 | 12.1 | −757 |

＊向聴0（1件）・1（17件）・6（26件）は件数が足りないので層としては使わない。
＊`結果` の内訳は 和了849・放銃516・被ツモ999・流局604・無関係1032。

### 4-3. 五数要約（全4000行）

| 項目 | min | Q1 | 中央 | Q3 | max | 平均 | 相異なる値 |
|---|---|---|---|---|---|---|---|
| `acceptTypes` | 2 | 9 | 16 | 21 | 34 | 15.60 | 33 |
| `fillRemain` | 1 | 3 | 3 | 4 | 4 | 3.42 | 4 |
| `weakShapes` | 0 | 0 | 1 | 1 | 5 | 0.89 | 6 |
| `redCount` | 0 | 0 | 0 | 1 | 3 | 0.29 | 4 |
| `doraCount` | 0 | 0 | 0 | 0 | 0 | 0 | **1** |
| `kokushiKinds` | 0 | 3 | 4 | 5 | 9 | 4.33 | 10 |
| `kokushiShanten` | 3 | 7 | 8 | 9 | 13 | 8.19 | 11 |
| `borderline` | 0 | 0 | 0 | 0 | 1 | 0.017 | 2 |
| （参考）`fillGood` | 0 | 0 | 1 | 2 | 4 | 1.08 | 5 |
| （参考）`fillWeak` | 0 | 0 | 1 | 2 | 4 | 0.98 | 5 |
| （参考）`totalBlocks` | 0 | 3 | 4 | 4 | 6 | 3.74 | 7 |

**`doraCount` はこの記録では判じられない。** `core-battle.js` がドラを 0 で渡しているため、
4000行すべて 0。`valueSources`・`directions`・`yakumanName`・`widthLabel`・`seatWind`・`roundWind` は
数値でないので五数要約を出さない（`widthLabel` は `acceptTiles` の帯分けそのもの）。

### 4-4. 判定を分ける力（向聴を揃えて値ごとに割る）

#### ◎ `fillRemain` — **最も強い空き軸**

| 向聴 | 残り枠 | n | 和了% | 放銃% | 平均点棒 |
|---|---|---|---|---|---|
| 2 | **2** | 152 | **38.2** | 12.5 | **+1815** |
| 2 | 3 | 197 | 29.4 | 13.2 | +559 |
| 2 | **4** | 61 | **23.0** | 18.0 | +841 |
| 3 | **2** | 125 | **32.8** | 9.6 | **+1521** |
| 3 | 3 | 733 | 25.5 | 13.2 | +542 |
| 3 | **4** | 563 | **23.3** | 12.6 | +163 |
| 4 | 3 | 689 | 19.0 | 14.5 | −2 |
| 4 | 4 | 897 | 17.3 | 12.2 | −73 |
| 5 | 3 | 124 | 12.1 | 8.9 | −267 |
| 5 | 4 | 415 | 11.1 | 13.0 | −903 |

**向聴2で15.2pt・点棒974点差／向聴3で9.5pt・点棒1358点差。単調で、向きも素直**
（枠が残っているほど弱い）。向聴4以上では 1.7pt に落ちる。
**いま誰も票の材料にしていない**（根拠欄②に字で出るだけ）。

#### ○ `kokushiShanten` — 効くが、お嬢様の材料の裏返し

| 向聴 | 境（中央値） | 下側 n / 和了% / 点棒 | 上側 n / 和了% / 点棒 | 差 |
|---|---|---|---|---|
| 3 | 9 | 670 / 23.4 / +332 | 751 / 26.9 / +608 | +3.5pt |
| **4** | **8** | 611 / **12.6** / −477 | 975 / **21.4** / +230 | **+8.8pt・707点** |
| 5 | 7 | 158 / 10.1 / −1060 | 381 / 11.8 / −631 | +1.7pt |

**向聴4で8.8pt。** ただし `defHonors`+`defTerminals` と **r=−0.967**（2-2）なので、
**新しい軸ではなくお嬢様の物差しの言い換え**。`kokushiKinds` は同じものの符号違い（向聴4で −10.1pt）。

#### △ `weakShapes` — 2本以上で沈む（単調ではない）

| 向聴 | 0本 | 1本 | 2本 | 3本 |
|---|---|---|---|---|
| 3 | 26.0% / +667 | 25.4% / +392 | 23.4% / +198 | 25.6% / +751 |
| 4 | 18.7% / −54 | 19.9% / +95 | **13.3% / −348** | 17.6% / +1 |
| 5 | 12.2% / −489 | 12.9% / −611 | **7.6% / −1492** | — |

**0〜1本は差が無く、2本で落ちる。** 向聴5で −4.6pt・点棒1003点差。
**「2本以上か」の二値なら軸になるが、連続量としては弱い。**

#### △ `redCount` — 効くが、`doraValue` に既に入っている

| 向聴 | 0枚 | 1枚 | 2枚 |
|---|---|---|---|
| 3 | 25.2% / +336 | 26.2% / **+923** | 17.9% / +303 |
| 4 | 17.0% / **−253** | 20.8% / **+479** | 22.6% / +1229 |
| 5 | 11.4% / −809 | 11.7% / −549 | — |

**向聴4で +3.8pt・点棒732点差。** ただし `doraValue` と r=0.708 で、
**執事の票（`doraValue>=1`）が既に拾っている**。判定画面で読まれていないのは
「もう一度数えている」のではなく「別名で入っている」ため。

#### × `acceptTypes` — **空き軸ではない**（`acceptTiles` の言い換え）

単独では効いて見える（向聴2で中央値割りの和了差 +14.4pt）が、**`acceptTiles` を固定すると単調でなくなる**。

| `acceptTiles` 帯 | `acceptTypes` の幅 | 和了%の動き |
|---|---|---|
| 20〜40 | 5〜13 | 6種28.8% → 9種18.5% → 10種28.9% → 11種22.6%（**上下する**） |
| 40〜60 | 11〜18 | 12種25.4% → 15種21.5% → 17種13.2%（下がるが n=106） |
| 60〜80 | 16〜23 | 19種19.1% → 20種16.3% → **21種26.7%**（反転） |
| 80〜 | 22〜34 | 24種17.4% → 25種15.0% → 26種21.3%（反転） |

r=0.996 と合わせて、**`acceptTypes` は `acceptTiles` を4で割った数に過ぎない。**
根拠欄③が「N種 M枚」と二つ並べているのは、**同じ数を二度書いている**。

#### ○ `borderline` — 件数は少ないが向きがはっきり

| 向聴 | 0（印なし） | 1（紙一重） |
|---|---|---|
| 3 | 1368件 / 24.6% / +417 | **53件 / 41.5% / +2053** |

**紙一重の印が付いた手は、むしろ強い**（+16.9pt・点棒1636点差）。
全体では 1.7%（68件）しか立たず、向聴2・4・5 では層が割れない。
**「際どい」の言葉を選ぶためだけに使っているが、値そのものが良い報せを持っている。**

#### 参考 `fillGood` / `fillWeak`（兎が既に読んでいる）

`fillWeak` は向聴2で 0本34.2%/+1788 → 2本29.6%/−44（点棒1832点差）。
`fillGood` は単調でない（向聴4で 0本17.2% / 3本31.7%）。**兎の票は既にこの二つを見ている**ので空き軸ではない。

### 4-5. 4節のまとめ

| 候補 | 判定を分ける力 | 空き軸として使えるか |
|---|---|---|
| **`fillRemain`** | 向聴2で **15.2pt**・向聴3で **9.5pt**、単調 | **◎ いちばん有望** |
| `borderline` | 向聴3で **+16.9pt**（n=53） | ○ 件数が少ない。印としてなら |
| `kokushiShanten` | 向聴4で **+8.8pt** | △ お嬢様の材料と r=−0.967 |
| `weakShapes` | 「2本以上」の二値で 向聴5に **−4.6pt** | △ 連続量では弱い |
| `redCount` | 向聴4で **+3.8pt** | △ `doraValue` に既に入っている |
| `acceptTypes` | `acceptTiles` を固定すると消える | **× 言い換え** |
| `doraCount` | この記録では全0 | **判じられない**（ドラを渡した記録が要る） |

---

## 5. 調べの途中で見つかったこと（直しは当てていない）

**① `totalBlocks` は七対子の手で `goodShapes`+`weakShapes` と食い違う（493 / 4000＝12.3%）**

L2514 で `totalBlocks = goodShapes + weakShapes` を確定させたあと、
**L3022-3025 の七対子ルート**で `goodShapes` と `weakShapes` だけが上書きされ、
`totalBlocks` は据え置かれる。結果、**七対子が近い手では兎（`totalBlocks`）とずんだ（`goodShapes`）が
別の数を見ている**。意図してそうしたのか、置き忘れなのかは字面からは判じられない。

**② 撤退予約線のコメントと実装が食い違っている**

L8392 のコメントは「受け入れ枚数から一段進むのに要する巡目を見積もり」だが、
実装（L8397-8405）は **`baseShanten` だけ**を `TURN_BY_SHANTEN` に通しており、
`acceptTiles` を一度も読まない。**字のほうが古い。**

**③ 執事の票と根拠欄①の✓×は、孤立ドラのときだけ食い違う**

執事は `a.doraValue >= 1`（L4048）、`hasValue` は `doraValue > 0`（L2872）。
`doraValue` は**孤立ドラ1枚のとき 0.5**（L2864）を取るので、その手だけ
**根拠欄①は✓なのに執事は守りへ入れる**。

---

## 6. 残り

**残り0件。** `orders-open.tsv` の 被り調べ-1〜4 はすべて済。

| 番号 | 項目 | 状態 |
|---|---|---|
| 被り調べ-1 | 審査者 × 項目の総当たり表（行番号つき） | 済（1節） |
| 被り調べ-2 | 同じ材料を共有する組の総当たり | 済（2節・新規6組） |
| 被り調べ-3 | 画面から読まれていない項目 | 済（3節・厳密0件／傾きに効かない14件） |
| 被り調べ-4 | 4000行での分布と検定 | 済（4節） |
