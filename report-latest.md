状態：終わり ／ 2026-08-21 12:39 ／ 抑止の解除が効いていることを対照実験で確認（入力から34秒で送信）。再起動は不要

【保留】
- ~~抑止解除の確認~~ → **決着済み（§4）。環境変数が効いており再起動は不要**
- 鳴きの測定（点数の条件は「三翻以上」と決定済み。この件が片付いてから着手）
- 倒れる音 三案（A/B/C）の選択
- ライフ0の倒れる絵の素材待ち（704x1524・白背景・剣は手から離さず杖のように突く）
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（目盛り画像は `head-proof-*.png`）
- `sizing-review.png` を残すか消すか
- v1419 の実機検収
- タイトルの印が見えたかの目視

# 報告：押し通知の抑止を調べた（実装を読んで確定）

日付 2026-08-21 ／ 本体の版 **v1419**（**リポジトリは無変更**）

**推測は書いていません。** 入っている `claude.exe`（334MB の単一バイナリ）の中の実装を
字面で読み出して確かめた結果です。**分からないものは分からないと書きました。**

---

## 1.【1】(1) 抑止を無効にする設定 — **あります。環境変数です**

**`CLAUDE_CODE_DISABLE_NOTIFICATION_PRESENCE_CHECK`**

実装の該当箇所（読み出したもの、変数名は短縮されたまま）：

```js
if (!s && !q.CLAUDE_CODE_DISABLE_NOTIFICATION_PRESENCE_CHECK && S1s())
  return { ..., disabledReason: "user_present" }
```

- **`q` は環境変数**。**真と評価される値なら何でもよい**（`"1"` で足ります）
- **`s`** は `q.CLAUDE_CODE_REMOTE || cc()` ＝リモートかどうか。**リモートなら在席判定そのものを通りません**
- **`S1s()`** が在席判定（次項）

**`/config` の項目にも `settings.json` のキーにも、この判定を切るものは見当たりませんでした。**
`agentPushNotifEnabled` は別物で、こちらは**押し通知そのものを止める**キー（`config_off`）です。

**入れた設定**（`~/.claude/settings.json`。既存キーは消していません）

```json
"env": { "CLAUDE_CODE_DISABLE_NOTIFICATION_PRESENCE_CHECK": "1" }
```

---

## 2.【1】(2) 「操作中」の判定が見ているもの — **二段構え**

```js
function S1s(){
  let e = terminalFocus();            // "focused"→true ／ "blurred"→false ／ 未報告→undefined
  if (e !== undefined) return e;      // 報告があれば、それが答え（時間は見ない）
  return Date.now() - lastInteractionTime() < 60000;   // 無ければ、最後の操作からの経過
}
```

| 段 | 見るもの | 結果 |
|---|---|---|
| **一段目** | **ターミナルが報告するフォーカス** | 窓が前面なら「操作中」。**経過時間は一切見ない** |
| **二段目**（報告が無いとき） | **最後の操作からの経過** | **60秒未満なら「操作中」**（`dLo = 60000`） |

**しきい値は 60秒**です。

**フォーカスの取り方も確かめました。** Claude Code は画面を戻すときに **`ESC[?1004h`** を書いています
（DEC の 1004＝フォーカス報告の有効化）。対応したターミナルは、窓の出入りのたびに
`CSI I`／`CSI O` を返し、それを受けて `focused` / `blurred` が立ちます。

**いまどちらの段で動いているかは、確定できていません。** ただし**観測は二段目と整合します**——
このセッションの最初の実験で、**2分待ってから出した押し通知は届き**、
**出力の直後に出したものは抑止**されました。一段目（フォーカス）が効いていて窓が前面だったなら、
2分待っても抑止されたはずです。**「二段目が効いている、あるいはその時フォーカスが外れていた」**
のどちらかで、**そこは分かりません。**

---

## 3.【1】(3) リモートデスクトップ越しでも「操作中」と判定されうるか — **段による**

| どちらの段か | 手元を離れていても「操作中」になるか |
|---|---|
| **一段目（フォーカス）が効いている場合** | **なりえます。** 窓が前面のままなら、何時間離れていても「操作中」。**時間を見ないため、放っておいても解けません** |
| **二段目（60秒）の場合** | **なりません。** 最後の打鍵から60秒で「不在」に変わります |

**リモートデスクトップは、ここでは「見ているだけ」でも窓は前面に居続けます。**
一段目が効いている環境なら、**スマホから眺めているあいだ、押し通知は永久に抑止されます。**
今回の症状（届かない状態が続く）とよく合う説明ですが、**一段目が効いているという確証はありません。**

**環境変数を入れれば、この区別は要らなくなります。** 判定そのものを通らなくなるためです。

---

## 4.【2】切ったあとの確認 — **決着しました。効いています**

**あなたの入力から34秒（60秒の壁の内側）で、押し通知が送信されました。**
抑止が働く条件を満たしていたのに通っています。**再起動は要りません。**

### 対照になっている二つの記録

| 時刻 | 入力からの経過 | 環境変数 | 結果 |
|---|---|---|---|
| 11:10:49 | 27秒（内側） | **無し** | **抑止**（`user_present`） |
| **12:39:32** | **34秒（内側）** | **有り** | **送信**（`Mobile push requested`） |

**同じ「60秒の内側」で結果が反転し、間に変えたのは環境変数だけ**です。

### 残る不確かさ（正直に）

**①のフォーカス報告が効いている環境で、たまたま窓が前面から外れていた場合にも同じ結果になります。**
これを潰すには、**環境変数を一時的に外して60秒以内にもう一度出す**（抑止に戻れば確定）という
対照が要ります。**やるかどうかは指示待ちです。**

## 5.【3】フックから同じ知らせを出せるか — **迂回はできますが、同じものは出せません**

**フックは抑止に関係なく発火しています**（記録で確認済み。11:11:16 の `stop` 行は、
押し通知が抑止された直後にもきちんと出ています）。

**ただし、フックから「Claude の押し通知」そのものは呼べません。**
押し通知はツールの呼び出しとして Claude Code の中で処理され、外部のコマンドから叩く口はありません。

**フックからできるのは、別経路の知らせだけです。**

| 経路 | 可否 | 備考 |
|---|---|---|
| 音（いま入っている） | **可** | 抑止と無関係に鳴る |
| ウィンドウのタイトル（いま入っている） | **可** | 同上 |
| Windows のトースト | **可** | 8/20 に見送りと裁定済み。`BurntToast` の導入が要る |
| 自前の HTTP で外部サービスへ | **可** | 別途の受け口が要る（LINE Notify など） |
| **Claude の押し通知そのもの** | **不可** | 外から呼ぶ口が無い |

**環境変数が効くなら、迂回は要りません。** 効かなかった場合に、上の表から選ぶ形になります。

---

## 6. 触れていないこと

- **リポジトリは無変更**（v1419 のまま）。
- **`settings.json` は `env` を足しただけ。** 既存の5キーと `hooks` はそのまま。
- **`claude.exe` は読んだだけ**で、書き換えていません。
- 鳴きの測定には着手していません（**点数の条件は「三翻以上」で承知しました**）。

---

## 7. `hook.log`（最後の20行）

**最後の行が10分以上前なら、仕組みが死んでいます。**

```
2026-08-21 11:10:49  PushNotify    結果=抑止（ターミナル操作中のため送られず）
2026-08-21 11:11:16  stop          音=OK  押し通知=抑止（ターミナル操作中のため送られず）  タイトル=OK
2026-08-21 11:12:16  notification  音=OK  押し通知=抑止（ターミナル操作中のため送られず）  タイトル=OK
2026-08-21 11:18:03  heartbeat     生存
2026-08-21 11:28:04  heartbeat     生存
2026-08-21 11:38:03  heartbeat     生存
2026-08-21 11:45:48  resume        音=-   押し通知=なし  タイトル=OK
2026-08-21 11:48:06  heartbeat     生存
2026-08-21 11:49:35  notification  音=OK  押し通知=なし  タイトル=OK
2026-08-21 11:49:52  PushNotify    結果=送信済み（ターミナル通知＋モバイル要求あり）
2026-08-21 11:58:15  stop          音=OK  押し通知=なし  タイトル=OK
2026-08-21 11:58:15  heartbeat     生存
2026-08-21 11:59:08  notification  音=OK  押し通知=なし  タイトル=OK
2026-08-21 12:08:03  heartbeat     生存
2026-08-21 12:18:03  heartbeat     生存
2026-08-21 12:28:04  heartbeat     生存
2026-08-21 12:38:03  heartbeat     生存
2026-08-21 12:38:58  resume        音=-   押し通知=なし  タイトル=OK
2026-08-21 12:39:20  notification  音=OK  押し通知=なし  タイトル=OK
2026-08-21 12:39:32  PushNotify    結果=送信済み（決着テスト・ターミナル通知＋モバイル要求あり）
```

**11:10:49 と 11:11:16 の二行が、今回の症状そのものです。** 押し通知が `抑止` で送られず、
その直後の `stop` 行にも「押し通知=抑止」と残っています。**切り分けの記録が働いています。**

---

## 8. 次

- **押し通知は直りました。** 届いたかどうかだけ、実機の側から教えてください。
- **残る不確かさを潰すか**（環境変数を一時的に外して60秒以内に再試行し、抑止に戻るかを見る）。指示待ちです。
- そのあと、**鳴きの測定（点数の条件は三翻以上）**に入ります。
