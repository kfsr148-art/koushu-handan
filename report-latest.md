# 点検7件

**状態：作業中** — 2026-08-26 12:26 時点／点検7件（済み5・未了2）。返事話題の読み口が生きていたので塞いだ

**まとめ** — **済み5件／該当なし0件／未了2件**。
③で**実際に外から文字を流し込める口が生きていました**。塞いであります。

---

## 1. ヨシ待ちの ntfy が200以外で終わったときの扱い — **済み**

**直す前** … 送り直しは**ありませんでした**。`Write-Self '押し送り失敗（HTTP …）数えない'` と
記録に残すだけで、その回の知らせは**そのまま消えて**いました。

**直したあと** … **失敗したときだけ**、**60秒あけて最大3回**送り直します。

- 控えは `~/.claude/ntfy-retry.txt`（`回数<タブ>次に試す時刻<タブ>状態`）。**失敗したときだけ置く**
- 見張りは1分ごとに回るので、**眠らずに次の回へ渡します**（ここで眠ると見張りごと止まる）
- **通れば控えを畳む**。3回とも駄目なら諦めて記録に残す
- **成功したあとの催促は0回のまま**（`$REMIND_MAX = 0`）。ここは届かなかった分の埋め合わせだけ
- **数えるのは200のときだけ**（`Ntfy-Bump` は成功の枝にしかない）。失敗は件数に入りません

**実測（失敗を重ねたとき）**

```
1回目 → 控えを置いた（1/3）    2回目 → （2/3）    3回目 → （3/3）
4回目 → 3回で打ち切り（届かなかった）。控えを畳む
5回目 → 新しい失敗として 1/3 から
```

＊一度**回数を数え違えて**いました（3回目で打ち切り＝送り直しは実質2回）。`-ge` を `-gt` へ直し、
　**送り直しをちょうど3回試す**形にしてあります。

## 2. 上限に達する回の本文に打ち止めの一文 — **済み（ただし上限は20ではなく60）**

**上限は 2026-08-25 に 20 → 60 へ上げてあります**（そのとき裁定をいただいた分です）。
ご指示の「20件目」は、いまの作りでは **60件目**に当たります。

**足す先は「控えの本文」**にしました。押し送りの中身は**題の一行だけ**で本文を持たないので、
足しても届きません。**返事パネルの札と `status.md`** に出る本文の末尾へ、
**ちょうど上限の回だけ**次の一文を足します。

> 本日はこれで打ち止め。以後はパネルと status.md を見てください。

＊超えたあとの回には足しません（毎回続けても読まれないため）。
＊ヨシ待ちは上限を超えても通るので、この一文は「**終わりの側が落ち始める**」合図になります。

## 3. 返事話題を読む口 — **生きていました。止めました**

**`inbox-watch.ps1` が15秒ごとに `https://ntfy.sh/<返事話題>/json?poll=1&since=…` を叩いていました。**
拾った文は `inbox.txt` へ入り、「ヨシ」に見えれば**ヨシとして扱われる**作りです。
パネルの送信道具は撤去済みなので、**そこへ書けるのは外から来た者だけ**でした。

**塞いだ内容**

| したこと | 中身 |
|---|---|
| 購読の口を削除 | `inbox-watch.ps1` の 338–339 行（話題の読み）と 343–442 行（巡回と取り込み）を削除。**446行 → 344行** |
| 話題のファイルを畳む | `rc-ntfy-reply-topic.txt` → `.retired-20260826` |
| 常駐を入れ替え | 古い常駐（**08-25 07:06 から回っていたもの**）を止め、新しい中身で起こし直した |
| 残したもの | **`state.json` の書き出し（`Publish-Status`）と置き去りの検査の片付け（`Sweep-Stale`）**。同じ台本にあるため |

**確かめ** — 走っている台本に **`ntfy.sh` の文字は0件**。構文 PASS。

**＊この項の「常駐が二つ走っていた」は誤りでした（2026-08-26 に訂正）。**
数えた命令そのものが `inbox-watch` の字を含み、**自分自身に当たって**いました。
記録の「常駐の見張りを始めます」は 08-25 07:07 と 08-26 12:26 の**二回だけ**で、
その間ずっと**常駐は一つ**です。二重に読んではいませんでした。

＊`inbox-feed.ps1` は**話題を読みません**（`inbox.txt` を窓へ流すだけ）。フック登録はそのままにしてあります。

## 4. push の前に origin/main を取り込んでいるか — **未了（形を変えて満たしている）**

**押す前の取り込みは入れていません。** 代わりに**押して弾かれたら取り込んで押し直す**形です
（`git-push.ps1`）。

```
push → 通らない → git pull --rebase --autostash → もう一度 push
```

**入れなかった理由** — 押す前に取り込んでも、**取り込みと押すあいだに書かれれば同じように弾かれます**。
競争は消えません。一方で押し出しは**一日およそ84回**あり、毎回 `fetch` を足すと通信が倍になります。
**弾かれてから取り込む方が、同じ失敗を同じだけ拾えて、ふだんの費用が要りません。**

＊この形が実際に効くことは 2026-08-26 に確かめています（`--autostash` 無し＝128／有り＝0）。
＊**押す前の取り込みをご希望なら入れます。** 判じきれないので未了としました。

## 5. 状態行を一箇所で作る — **済み**

**直す前** … `state.json` は見張りが機械で書き、`report-latest.md` の先頭は**こちらが手で書いて**いました。
**別々**です。

**直したあと** … **`state-line.js`** を置きました。**`state.json` から一行を組む唯一の場所**です。

```
node state-line.js                  → **状態：作業中** — 2026-08-26 10:35 時点
node state-line.js --json           → 元にした値（stat／subj／at／usage の watch）も出す
node state-line.js --apply report-latest.md   → 報告書の先頭の状態行を、いまの値で書き換える
```

言い換えは**一つだけ**——`手待ち` は報告書では `終わり` と読みます。ほかは `state.json` の字をそのまま出します。

## 6. `panel.html` の取得URLに時刻が付いているか — **済み（もともと付いていた）**

**七つすべてに付いていました。**

```
260  PANEL_VER_URL + '?_chk=' + Date.now()
312  SEEN_URL      + '?_t='   + Date.now()
381  VER_URL       + '?_t='   + Date.now()
386  STB_URL       + '?_t='   + Date.now()
442  NOTICES_URL   + '?_t='   + Date.now()
963  USAGE_URL     + '?_t='   + Date.now()
1106 STATE_URL     + '?_t='   + Date.now()
```

名は `?t=` ではなく **`?_t=`**（版の確認だけ `?_chk=`）ですが、働きは同じです。
`cache: 'no-store'` も併せて付いています。

＊ただし **GitHub Pages の CDN は、押し出しから公開まで約5分かかります**（実測）。
　時刻を付けても**その遅れは縮みません**。縮むのは端末側の取り置きだけです。

## 7. `report-latest.md` の最後の commit と、置き場かどうか — **済み**

```
git log -1 --format="%ci %h" -- report-latest.md
→ 2026-08-26 07:22:29 +0900  d45496c
```

**report-latest.md は今も報告の置き場です。** `status.md` と `notices.json` へ移ってはいません
（あちらは**知らせの写し**で、報告そのものではありません）。

**先頭の状態行も末尾も、いまは付いています。** 直近の中身は
`# 7:16〜7:17 の状態の食い違い — 原因を特定して直した` ／ `**状態：終わり** — 2026-08-26 07:2x 時点` です。

**raw に8月20日付 v1415 の折り返し報告が出ていたのは、raw の遅れです。**
`raw.githubusercontent` は push から数分遅れて追いつきます（08-25 に「raw は push から約5分遅れる」と控えてあります）。
**いま `git log` が指しているのは 07:22 の d45496c** で、中身も新しいものです。

---

# 未了と理由

| # | 未了の中身 | 理由 |
|---|---|---|
| 4 | 押す前の `fetch`＋`rebase` を入れていない | 競争は消えず、通信だけ倍になる。**弾かれてから取り込む形で同じ失敗を拾えている**。ご希望なら入れます |
| 1 | 送り直しが**実地で効く場面**をまだ見ていない | 200以外で終わった回が、今日まだ起きていません。判じ方は五通りで確かめてあります |

# 触ったもの

| ファイル | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1` | ①送り直し（`$RetryPath`・`Retry-Ntfy`）／②上限の回に打ち止めの一文 |
| `~/.claude/inbox-watch.ps1` | ③購読の口を削除（446行 → 344行） |
| `~/.claude/rc-ntfy-reply-topic.txt` | ③ `.retired-20260826` へ畳んだ |
| `state-line.js` | ⑤ **新規**。状態の一行を作る唯一の場所 |
| `koushu-handan.html` `panel.html` | **触っていません** |

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. ✅ 終わりました（返事不要）

```
点検7件（済み5・未了2）。返事話題の読み口が生きていたので塞いだ
①ヨシ待ちの ntfy が200以外のとき、失敗したときだけ60秒あけて最大3回送り直す形を入れた（成功後の催促は0回のまま、数えるのは200のときだけ）②上限の回の控えの本文末尾に打ち止めの一文を足した（押し送りは題の一行だけで本文を持たないため、足す先はパネルと status.md に出る本文）③inbox-watch.ps1 が15秒ごとに返事話題を叩いて取り込んでいたので削除し、話題のファイルを畳み、常駐を入れ替えた④押す前の取り込みは入れていない（弾かれてから取り込む形で同じ失敗を拾えており、毎回 fetch すると通信が倍になる）⑤state-line.js を置き、状態の一行を作る唯一の場所にした⑥panel.html の七つの取得 URL には元から ?_t= が付いていた⑦report-latest.md は今も報告の置き場で、最後の commit は 2026-08-26 07:22:29 d45496c。raw が古く見えたのは raw の遅れ
ファイル: ~/.claude/watch-notify.ps1（$RetryPath・Retry-Ntfy・打ち止めの一文）／~/.claude/inbox-watch.ps1（446行→344行）／~/.claude/rc-ntfy-reply-topic.txt.retired-20260826／state-line.js（新規）／report-latest.md／写しは watch-notify.ps1.bak-20260826c と inbox-watch.ps1.bak-20260826
実測: ③走っている台本に ntfy.sh は0件・構文PASS・止める前は常駐が二つ走っていた（08-25 07:06 と 08-26 10:37）／①失敗を重ねると 1/3→2/3→3/3→打ち切り→新しい失敗として 1/3。一度 -ge で数え違え、送り直しが実質2回だったのを -gt へ直した／⑥七つすべてに ?_t=（版だけ ?_chk=）／⑦git log -1 は 2026-08-26 07:22:29 +0900 d45496c
未検収: 2026-08-26 点検7件（③の塞ぎと①②⑤の実装）
```

### 2. 🙋 ヨシしてください

```
点検7件の指示 — 断片を二つ受領、続き待ち
印: y0826-1031
待っているのは：指示の「以上」の到着。①ヨシ待ちの ntfy が200以外のときの送り直し（失敗時のみ60秒間隔で最大3回・成功後の催促は0回のまま・数えるのは200のときだけ）②上限20件目の本文末尾に打ち止めの一文 ③返事話題を読む口（wait-reply.sh／inbox-watch.ps1／inbox-feed.ps1）が生きていないか＝生きていれば止めてフック登録からも外す ④push 前に origin/main を取り込んでいるか ⑤state.json と報告書の状態行を一箇所で作っているか ⑥panel.html の notices/state の URL に ?t= が付いているか ⑦git log -1 --format="%ci %h" -- report-latest.md の結果と、report-latest.md が今も報告の置き場かを一行で（置き場を続けるなら状態行を復帰）
答え方：「y0826-1031 にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
```

### 3. 🙋 ヨシしてください

```
点検6件の指示 — 断片を受領、続き待ち
印: y0826-1028
待っているのは：指示の「以上」の到着。点検6件（①ヨシ待ちの ntfy が200以外のときの送り直し ②上限20件目の本文に打ち止めの一文 ③返事話題を読む口が生きていないか＝生きていれば止めてフックからも外す ④push 前に origin/main を取り込んでいるか ⑤state.json と報告書の状態行を一箇所で作っているか ⑥panel.html の notices/state に ?t= が付いているか）が、末尾の「以上」なしで届いた。作法23 に従い、着手せず控えて待っている
答え方：「y0826-1028 にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
```

### 4. ✅ 終わりました（返事不要）

```
逆向きの誤りを機械が拾う一行を足した（起きたら気づけるように）
落ちどころの直しを検収いただいた。あわせて「逆向きは起きたら教えてください」に応えるため、機械の側で拾う一行を足した——鍵が run: 以外（＝止まっている扱い）なのに claude.exe の CPU が10%以上あれば、watch-notify.log へ「★逆向きの疑い」と残す。知らせは出さず記録だけ。見張りの要なので作法29 の四段を踏み、構文検査 PASS
ファイル: ~/.claude/watch-notify.ps1（1265-1281 逆向きの拾い）／~/.claude/watch-notify.ps1.bak-20260826b（触る前の写し・1282行）
実測: 七通りで確かめた——idle+CPU35.2%=拾う／idle+CPU0.7%=拾わない／run+CPU40%=拾わない（動いている扱いなので当然）／wait+CPU12.5%=拾う／done+CPU9.9%=拾わない（境目の10%未満）／dead+CPU80%=拾わない（別の異常として扱う）／CPU が読めない(null)=拾わない。物差しは機械全体に対する百分率で、8芯なら一芯まるごとが12.5%
```

### 5. ✅ 終わりました（返事不要）

```
重ねて届いた「状態の食い違い」の指示を取り消し（前の回で済み）
同じ指示が重ねて届いたため実行し直さず、前の回の直しがそのまま効いていることだけを確かめた。新たな手は入れていない
実測: 落ちどころの直し（resume なら run:、それ以外は idle:）は watch-notify.ps1 に入ったまま／触る前の写し watch-notify.ps1.bak-20260826（1272行）も残っている／構文 PASS／07:20 以降に「フックが無いのに run:」は出ていない／inbox.txt は未処理0件
```

<!-- 送った知らせ ここまで -->

---

# 仕上げ2件（2026-08-26 12:3x）

## 1. `report-latest.md` の先頭に状態行を復帰させたか — **済み**

**`state-line.js` に「一言」を足し、状態行を一行で組む形にしました。**

```
**状態：作業中** — 2026-08-26 10:35 時点／点検7件の指示 — 断片を二つ受領、続き待ち
```

- **状態** … `state.json` の `stat` をそのまま（`手待ち` だけ、報告書では `終わり` と読み替える）
- **日時** … `state.json` の `at` から。この機械の時刻で組む（UTC へ寄せない）
- **一言** … `state.json` の `subj`。34字を超えたら「…」で切る（状態行は一行で読めることが値打ち）

`node state-line.js --apply report-latest.md` で写します。**値を作る場所は `state-line.js` の一箇所だけ**です。

## 2. 常駐が二重に走らないようにする — **済み（ただし②の前提は誤りでした）**

`inbox-watch.ps1` の頭に、**同じ台本の常駐が走っていたら先に止める**手を入れました。

**見分けは「命令の字が `-File …inbox-watch.ps1` で終わっているもの」だけ。** 自分（`$PID`）は止めません。

**実測（五通り）**

```
本物の常駐（引用あり）      → ★止める
引用なしの常駐              → ★止める
調べもの（-Command の中に名がある）→ 触らない
中身を見るだけ              → 触らない
別の台本（watch-notify）    → 触らない
```

### ＊**「二つ並走していた」は誤りでした。訂正します**

再発防止を作る過程で、**数え方そのものが間違っていた**ことが分かりました。

`Get-CimInstance … Where-Object { $_.CommandLine -like '*inbox-watch*' }` と打つと、
**その命令自身の命令の字にも `inbox-watch` が入る**ため、**自分に当たります**。
私は今日これを**二度**踏み、そのたび「常駐が二つ」と読み違えていました。

**記録が正しい答えです。** `inbox-watch.log` の「常駐の見張りを始めます」は
**08-25 07:07 と 08-26 12:26 の二回だけ**。その間ずっと**常駐は一つ**でした。
**二重に読んではいませんでした。**

＊したがって 2 は「起きていた事故の再発防止」ではなく、**まだ起きていないことへの備え**です。
　ログオンのたびに起きる作りである以上、前のものが生き残れば二重になり得ます。備え自体は要ります。

## `watch-notify.ps1` は — **該当なし**

常駐ではなく、タスクスケジューラが**1分ごとに起こす**作りです。
多重起動の決めが **`IgnoreNew`** なので、前の回が走っている間は新しい回を起こしません。
**二重にならないことが既に保証されて**います（今日の8分20秒の回で、322＝飛ばしが8回続いたのがその証拠）。
