# 三つの仕掛けが触れ合う点 — 危ういところの洗い出し

**状態：ヨシ待ち（直す前の宣言）** — 2026-08-25 14:3x 時点

見張り（`watch-notify.ps1` / `inbox-watch.ps1` / `notify-record.ps1`）・返事パネル（`panel.html`）・
使用量（`usage.json`）の三つが触れ合う点を調べました。**実測できたもの**と**読みに留まるもの**を分けます。

**いちばん危ういのは、まだ起きていない②です。** 見張りの `git commit` は**index を丸ごと拾う**ので、
私が `git add` してから commit するまでの数十秒に見張りが動くと、**私の変更が「usage: 更新」という名の
commit に呑まれます**。今日の45枚（52ファイル）は、その窓に30秒ほど置かれていました。

---

# 実測できたもの

## ① push の衝突 — 負けるのは「あとから押した側」、失われるものは無い、リトライは効かない

**押す口は三つあり、どれも `git pull` をしません。**

| 台本 | 押すもの | 終了コードを見るか | 時間の上限 |
|---|---|---|---|
| `inbox-watch.ps1:281-283` | `state.json` | **見ない**（`2>&1 \| Out-Null`） | **無し** |
| `notify-record.ps1:212-214` | `status.md` / `notices.json` | **見ない** | **無し** |
| `watch-notify.ps1:1029-1041` | `usage.json` | 見る | 20秒 |

**衝突したときに何が起きるか。** 三つの押し口と私は**同じ手元のリポジトリ**を使っています。
枝が分かれるのではなく、**同じ枝を同時に押しに行く競争**です。今日の実例：

```
私   git push → ! [remote rejected] cannot lock ref 'refs/heads/main':
                  is at 3fdbb97 but expected 31f0135
```

これは**先に見張りが押し切った**という意味で、しかも `3fdbb97` には**私の commit が既に入っています**
（同じ手元のリポジトリなので、見張りは私の commit の上に積んで押した）。
`git pull --rebase` は `Already up to date` を返し、`merge-base` で私の commit が origin に居ることを確かめました。

→ **負けるのは「あとから押した側の push という操作」だけで、中身は失われません。**
　次に誰かが押すとき一緒に運ばれます。

**リトライは効きません。** 三つとも**押し直しの仕掛けを持ちません**。
usage だけは控え（`usage-pub-*.txt`）を更新しないので次の回が出し直しますが、
**残る二つは黙って諦めます**。今日 usage 側だけで **8件**の失敗が記録に残っていました。

```
2026-08-25 06:47:54  使用量：push が失敗（終了コード ）。控えは更新しない
　… 07:27:28 まで計8件
```

＊終了コードの欄が**空**です。`WaitForExit(20000)` のあとの `ExitCode` が読めていません。
　**失敗したことは分かるが、なぜ失敗したかが記録に残らない**状態です。

## ② 見張りの commit が、私の staged を呑む（**まだ起きていないが、起きる**）

三つとも `git add <自分のファイル>` のあと **`git commit -m` を引数なしで**呼びます。
`git commit` は**index に載っているものを全部** commit します。空のリポジトリで実証しました。

```
git add mine.txt        ← 私が add しただけ（まだ commit していない）
git add usage.json      ← 見張りがやること
git commit -m 'usage: 更新'
→ commit に入ったファイル: mine.txt  usage.json     ★ 呑まれた
```

**履歴を50件ずつ遡ったところ、実際に起きた例はまだありません。**
しかし押し出しは 37回/日 → 約84回/日 へ増えています（約17分に1回）。
今日の納品では52ファイルを **30秒ほど** index に載せたままにしていました。

**失われるもの** … 私の変更が「usage: 更新」という**別名の commit** に混ざり、
どの回の納品だったのか履歴から辿れなくなります。消えはしませんが、**戻り先として使えなくなります**。

## ③ 重い作業との重なり — 45枚の写し取りで2〜3倍、**押し前の検査で9分14秒の穴**

`watch-status.log` の分の抜けと、動作記録（TaskScheduler/Operational）を突き合わせました。

| 帯 | 何を回していたか | 見張りの様子 |
|---|---|---|
| 13:14〜13:21 | **写し取り45枚** | 一回が **60〜120秒**（ふだん2〜5秒）。322＝飛ばしが4件 |
| **13:22:58〜13:31:18** | **push（`pre-push` の速い版）** | **一回が8分20秒**。**322＝飛ばしが8件連続**、記録に**9分14秒の穴** |

動作記録そのまま（13:22〜13:32）

```
13:22:58  動作を始めた
13:23:58  ★飛ばした（前の回がまだ走っている）
　… 13:30:58 まで8回連続で飛ばした
13:31:18  動作が終わった      ← 一回が 8分20秒
```

`watch-status.log` も **13:22:03 の次が 13:31:17** で、**9分14秒の穴**です。

**どの段で詰まったかは分かりませんでした。** 足跡（`watch-step.txt`）が**最後の一回しか残らない**ためです。
見張り自身は検査を回しません（押すときも `--no-verify`）ので、
**CPU の奪い合いか、`git` の待ちか**のどちらかですが、いまの記録では決められません。

## ④ 読む側の壊れ方 — 真っ白にはならない。ただし**二つ、嘘をつく**

`panel.html` を手元で開き、三つの JSON への取得を捕まえて壊した中身を返しました
（`panel-broken-probe.js`。公開物には触れていません）。18通り＋正しいとき1通り。

**真っ白にはなりません。** どの壊し方でも本文の字は残り（102〜243字）、版の字も出たままで、
ページの中で例外は**一件も出ませんでした**。

| 読む先 | 壊し方 | 出たもの | 判定 |
|---|---|---|---|
| `usage.json` | 書きかけ／空／空白／HTML／NaN | 使用量の塊が**丸ごと消える** | ○ 古い値は出さない |
| 〃 | **型違い**（配列） | 同上（消える） | ○ |
| `state.json` | 書きかけ／空 | `連絡なし ／ 状態を読めません（…）` | ○ はっきり言う |
| 〃 | 古い `at`（2時間前） | `連絡なし ／ 古い件名（最後は 作業中）… 2時間前` | ○ |
| 〃 | **型違い**（配列） | **`NaN時間前`** と出て、色は**平常（idle）** | **× 嘘をつく** |
| `notices.json` | 書きかけ／空／空白／HTML／NaN | `一覧を読めません（…）` の一行 | ○ はっきり言う |
| 〃 | **型違い**（物） | **「まだ知らせがありません。出ればここに並びます。」** | **× 嘘をつく** |

**危ういのは「JSON としては正しいが、形が違う」ときの二つです。**

- `state.json` が配列だと `d.at` が無く、`NaN時間前` を**平常の色**で表示します。
  読み手には「読めている」ようにしか見えません。
- `notices.json` が配列でないと **「知らせがありません」** になります。
  20件が控えに積まれていても、画面は**平常の空**に見えます。
  ＊これは v28〜v29 で潰した「全部写し済みで空になる」事故と**同じ形**です。

## ⑤ 書きかけがパネルまで届く経路は、理屈の上では開いている

三つの書き出しは**いずれも上書き**で、`WriteAllText` / `Set-Content` を直に呼びます。
**一時ファイルへ書いて置き換える形ではありません**（不可分ではない）。

```
watch-notify.ps1:1007   [System.IO.File]::WriteAllText($UsageJson, $out, …)
inbox-watch.ps1         Set-Content -LiteralPath $StateJson …
notify-record.ps1       Set-Content -LiteralPath $NoticesJson …
```

パネルが読むのは公開ページ（github.io）なので、**git が半端な中身を `add` した瞬間だけ**が窓です。
別々の台本が同時に走れば、その窓は開きます。**起きた証拠はありません。**

---

# 読みに留まるもの（実測していない）

- **リモートが先へ進むと、三つとも黙って永久に失敗する。** どれも `git pull` をしないので、
  手元より origin が先にある状態になると push は通りません。usage は記録に残りますが、
  **`state.json` と `notices.json` は終了コードすら見ない**ので、
  **パネルが古い値を出し続けたまま、誰も気づけません。**
- **時間の上限が usage にしか無い。** `inbox-watch` と `notify-record` の `git push` は**無制限**です。
  通信が詰まればその回が固まり、③の穴と同じ形になります。
  ＊usage 側にこの上限を付けたのは今日（2026-08-25）で、当時の理由がそのまま残る二つに当てはまります。
- **`.githooks/pre-push` は私の push にしか掛からない。** 見張りは三つとも `--no-verify` です。
  見張りが壊れた `notices.json` を押しても、検査は素通りします。
- **ウィジェットは調べられていません。** 台本がリポジトリにも `~/.claude` にも見当たりません。
  名指しいただければ、パネルと同じ検査（壊れた JSON を食わせる）を掛けます。

---

# 直したいこと（宣言）

**①〜⑤のうち、直す値打ちがあるのは四つ**と見ています。番号は上と対応します。

| # | 直すもの | どう直すか | 手 |
|---|---|---|---|
| A | **②の呑み込み** | 三つの `git commit` に **`-- <自分のファイル>` を付ける**。index の他は拾わない | `watch-notify.ps1` `inbox-watch.ps1` `notify-record.ps1` 各1行 |
| B | **①のリトライ無し** | push が弾かれたら **`git pull --rebase` して一度だけ押し直す**。 それでも駄目なら記録に残して諦める | 同3ファイル |
| C | **④の嘘** | `state.json` は `at` が**数でなければ**「読めません」へ。 `notices.json` は**配列でなければ**「読めません」へ（空配列とは分ける） | `panel.html`（版を **v31** へ） |
| D | **③の原因不明** | 足跡を**追記の記録**にする（いまは最後の一回で上書き）。 遅い回がどの段で詰まったか後から読める | `watch-notify.ps1` |

**見送るもの** … ⑤の不可分書き出し（一時ファイル＋置き換え）は、
**起きた証拠が無く、A・B を入れれば窓がさらに狭まる**ため、今回は入れません。
`--no-verify` の素通りも、見張りが押すのは自分の3ファイルだけなので今回は触りません。

**見込み** — A・B で20分、C で15分（パネルは実機確認が要る）、D で10分。合計 **約45分**。

**ご確認ください** — A〜D を入れてよいか。C だけ `panel.html` の版が上がります（v30 → v31）。
