# 較正-1 の線引きを確定し、虎の写しも層別へ揃えた（本体 v1428）

**状態：作業中／VAIO 2026-08-29**

**結論** — 較正-1 の層別の関門（向聴2=25／3=60／4以上は関門なし）を当て、4000行の再採点で
**悪化した層は一つも無い**。あわせて **y0829-tora の案A** で虎の親補正の写し（旧 L4096）を
同じ層別へ揃えた。**文言（`MASCOT_WORDS`）は一字も変えていない**——直したのは
一言を出すかどうかの**条件だけ**。`check.js` の ⑩ も新しい書き方を読めるよう追随させた。
幅の関門の数値を名指しする箇所を総当たりで走査したが、**写し以外には無かった**。

---

## 1. 当てた関門（指示1・3）

`koushu-handan.html` の一箇所だけ。画面の文言・七人の見立て・表示の側には一行も触っていない。

| 行 | 変更前 | 変更後 |
|---|---|---|
| L3041（旧） | `const wideAttack = acceptTiles >= (isDealer ? 20 : 24) && doraValue >= 1;` | — |
| L3040-3043（新・注記） | — | 層別の根拠と、向聴0・1が向聴2の値の流用であることを書いたコメント4行 |
| L3044（新） | — | `const widthGate = baseShanten >= 4 ? Infinity : (baseShanten >= 3 ? 60 : 25);` |
| L3045（新） | — | `const wideAttack = acceptTiles >= (isDealer ? widthGate - 4 : widthGate) && doraValue >= 1;` |

| 層 | 旧・親/子 | 新・親/子 | 備考 |
|---|---|---|---|
| 向聴0 | 20 / 24 | 21 / 25 | 件数1件。線を引けず向聴2を流用（コメントに明記） |
| 向聴1 | 20 / 24 | 21 / 25 | 件数17件。同上 |
| 向聴2 | 20 / 24 | 21 / 25 | 指定どおり25枚 |
| 向聴3 | 20 / 24 | 56 / 60 | 指定どおり60枚 |
| 向聴4以上 | 20 / 24 | 関門なし | `wideAttack` を発火させない |

`narrowDefend`（狭い側・L3036 の 6/8）は据え置き。**親子差も据え置き**とのことなので、
従来の「親は子より4枚甘い」という差をそのまま各層へ持ち込んだ（子の値 − 4 が親の値）。

版は `data-ver` / `verTag` / `ver.txt` の三箇所を 1427 → **1428** に揃えた。

---

## 2. 当てる前の再採点（指示2）

`core-battle-log.tsv` の4000行。うち **7行は国士・役満手前の格上げ**で列の値から verdict を再現できないため、
残り **3993行**で数えた（7行はいずれも defend→attack の上書き）。

### verdict が変わる件数

**890件 / 3993件（22.29%）。向きはすべて attack → defend。** 逆向き（defend→attack）は0件。

| 層 | 変わった件数 | 変わった行の和了率 | 放銃率 | 平均点棒 |
|---|---|---|---|---|
| 向聴0 | 0 | — | — | — |
| 向聴1 | 0 | — | — | — |
| 向聴2 | **0** | — | — | — |
| 向聴3 | 201 | 23.4% | 14.4% | 385 |
| 向聴4以上 | 689 | 18.4% | 11.8% | 18 |

**向聴2で0件**なのは、24→25 の1枚分に該当する行（子で受け24枚ちょうど・親で20枚ちょうど・ドラ1以上・形不足）が
この4000行に一つも無かったため。指定どおり当てたが、この母集団では何も動かない。

### 層ごとの分離（攻 − 守）。悪化した層は無い

| 層 | 件数 | 旧 和了差 | 新 和了差 | 旧 点棒差 | 新 点棒差 | 判定 |
|---|---|---|---|---|---|---|
| 向聴1 | 17 | −11.5 | −11.5 | −360 | −360 | 変化なし |
| 向聴2 | 410 | −3.9 | −3.9 | +830 | +830 | 変化なし |
| 向聴3 | 1419 | +9.1 | **+10.6** | +494 | **+571** | 改善 |
| 向聴4以上 | 2146 | +7.0 | **+14.4** | +657 | **+1159** | 改善 |
| 全体 | 3993 | +8.8 | **+13.4** | +771 | **+1082** | 改善 |

（向聴0は1件で片側しか無く、差が出ない）

### 攻守それぞれの成績（層別）

| 層 | 旧attack | 旧defend | 新attack | 新defend |
|---|---|---|---|---|
| 向聴3 | n759 和了29.5% 放銃12.8% 点710 | n660 20.5% 12.6% 215 | **n558 31.7% 12.2% 827** | n861 21.1% 13.0% 255 |
| 向聴4以上 | n854 20.6% 12.5% 179 | n1292 13.6% 13.1% −478 | **n165 29.7% 15.8% 853** | n1981 15.3% 12.6% −306 |

新 attack の放銃率は 向聴4以上で 12.5%→15.8% と上がるが、和了率が 20.6%→29.7%、
平均点棒が 179→853 と伸びており、**攻めの群として割の良い交換になっている**。

### 移した890行そのものの位置

| 層 | 移動 | 移した行 | 新attack | 新defend |
|---|---|---|---|---|
| 向聴3 | 201 | 和了23.4% 点385 | 31.7% / 827 | 21.1% / 255 |
| 向聴4 | 530 | 和了20.0% 点202 | 30.1% / 882 | 16.7% / −148 |
| 向聴5 | 153 | 和了13.1% 点−673 | （2件のみ） | 11.4% / −754 |
| 向聴6 | 6 | 和了16.7% 点1400 | なし | 19.2% / 350 |

移した行はいずれも**新 attack より新 defend に近い**。降格は成績と合っている。

### 一つだけ添える — 向聴を4/5/6に割ると、向聴5だけ数字が反転する

指示の層（0/1/2/3/4以上）では悪化ゼロだが、**4以上をさらに割ると向聴5で和了差が +2.2 → −11.4 に反転する**。
中身を見ると、**新 attack が2件しか残らない**ためで（向聴5の539件のうち shapeOK は2件だけ）、
その2件が被ツモと流局だったことによる見かけの反転。旧 attack（n155）も和了差 +2.2 と分離はほぼ無く、
**幅の関門は元々この層で何も分けていなかった**。実体のある悪化ではないと判じ、当てた。

### borderline の件数

| | 合計 | narrow（狭くて守） | wide（広くて攻） |
|---|---|---|---|
| 旧 | 958 | 2 | 956 |
| 新 | **68** | 2 | 66 |

956 対 2 の偏りは解消した。

---

## 3. 被り-1（指示4・記録のみ、直しは当てていない）

**申告どおりだった。4000行で食い違い0件。**

| 調べたこと | 食い違い |
|---|---|
| `hasValue` ≠ `doraValue > 0` | **0件** |
| `shapeNeeded` ≠ doraValue だけから決まる値 | **0件** |
| `shapeOK` ≠ `goodShapes >= shapeNeeded` | 0件 |
| `totalBlocks` ≠ `goodShapes + weakShapes` | **493件**（別件・下記） |

### どこで何が同じ数字を見ているか

| 行 | 何が書いてある | 実体 |
|---|---|---|
| **L2867** | `const doraValue = doraEff + redCount + yakuhaiValue;` | **元の一つの数字** |
| **L2872** | `const hasValue = doraValue>0;` | doraValue の言い換え。独立した材料ではない |
| **L2880-2884** | `shapeNeeded` の4分岐 | doraValue だけの関数。しかも `<=0` と `===1` がどちらも4なので**実質3段**（0-1→4／2→3／3以上→2） |
| **L3027** | `const shapeOK = goodShapes >= shapeNeeded;` | 分岐を選ぶのは goodShapes と、**shapeNeeded 経由の doraValue** |
| **L3036** | `narrowDefend` | 見ているのは acceptTiles・isDealer・**doraValue** の三つだけ |
| **L3044-3045** | `wideAttack` | 見ているのは acceptTiles・isDealer・**doraValue**・baseShanten の四つだけ |
| **L8468** | `ok: a.hasValue,` | 画面の「打点の種」の欄。**doraValue と別の材料の顔で並ぶ** |
| **L8469** | `打点 ${a.doraValue} — ...` | 同じ欄の中で、言い換え前と言い換え後が並んで出る |
| **L7113-7114** | ずんだの説明「まとまりが◯つ、必要なのは◯つ」 | 後者（shapeNeeded）は doraValue の関数 |
| **L8647 / L8654** | 「打点 ◯ に対して、良形が ◯ に届かない」 | **同じ数字を二度言っている**（打点＝doraValue、良形の必要数＝doraValue の関数） |

**要するに、borderline を分けているのは `acceptTiles` と `doraValue` の二つだけ。**
画面で「打点の種」「必要な良形」「まとまりの数」と三つの顔で出ているもののうち、
前二つは同じ doraValue、三つ目は判定に関与していない。

### 別件で見つかった — `totalBlocks` が古い値のまま

`totalBlocks` は **L2514** で `goodShapes + weakShapes` として作られるが、
**L3016-3025 の七対子の上書き**（`goodShapes = p2; weakShapes = 0;`）はその**後**に走る。
そのため七対子に寄る手では上書き前の値が残り、**4000行中493行で `goodShapes + weakShapes` と一致しない**
（差1が488件、差2が5件。493件すべて weakShapes=0＝七対子側の行）。

判定には一切効かない（唯一の使い道は **L4017-4018** の `tenLeans` の語り口の分岐）が、
**この一箇所では古い数字を見ている**。台帳の 被り-1 に併記した。直しは当てていない。

---

## 4. 虎の親補正の写しを層別へ揃えた（案A・指示の本体）

### 4-1. `check.js` ⑩ は何がどう落ちていたか（指示1）

⑩ の親補正の節は、`analyze()` 側と虎の写し側を**正規表現で読み取って突き合わせる**作りだった。
落ちていたのは**照合の結果ではなく、読み取りそのもの**。

| 場所 | 中身 |
|---|---|
| `check.js` **L912**（旧） | `const wa = (src.match(/acceptTiles\s*>=\s*\(isDealer\s*\?\s*(\d+)\s*:\s*(\d+)\)/) \|\| []);` |
| `koushu-handan.html` **L3045**（較正-1 で当てた新しい行） | `const wideAttack = acceptTiles >= (isDealer ? widthGate - 4 : widthGate) && doraValue >= 1;` |

**旧 L912 の正規表現は `(isDealer ? 数字 : 数字)` しか読めない。**
較正-1 で三項の中身が数字リテラルから `widthGate - 4 : widthGate` という**式**に変わったため、
一致が空になった。空になると `check.js` **L914** の入口へ落ちる。

```
  if (!nd.length || !wa.length || !cp.length) {          ← L914（旧）
    ng('境目の数値を読み取れない（analyze 側または写し側の書き方が変わった）');   ← L915（旧）
```

FAIL の一行はこれ。**「写しがずれている」ではなく「読み取れない」のほうが出ていた**ので、
⑩ はこの回、写しのずれ（20〜24 と 25／60 の食い違い）を**見てすらいなかった**。
`nd`（狭い側 6/8）と `cp`（写し側）は両方とも読めていた——**落ちていたのは `wa` の一本だけ**。

### 4-2. 当てた直し

**`koushu-handan.html` L4089-4102** — 写しの条件を層別の関門へ揃えた。

| | 変更前 | 変更後 |
|---|---|---|
| コメント（旧 L4088） | `6/8/20/24 は analyze() が使う境目の写し` | `6/8 と幅の関門は analyze() が使う境目の写し` ＋ 層別の内訳4行（向聴2=25／3=60／4以上は関門なし／0と1は2を流用） |
| 関門（新 L4100） | — | `const gate = a.baseShanten >= 4 ? Infinity : (a.baseShanten >= 3 ? 60 : 25);` |
| 判定（旧 L4096） | `return a.shapeOK ? (d <= 1 && r >= 6 && r < 8) : (d >= 1 && r >= 20 && r < 24);` | `return a.shapeOK ? (d <= 1 && r >= 6 && r < 8)`<br>`                 : (d >= 1 && gate !== Infinity && r >= gate - 4 && r < gate);` |

**`MASCOT_WORDS`（L4080-4087）は一文字も触っていない。** 変えたのは
`MASCOT_WORDS.dealer`（「親番なので、基準を少し攻め寄りにしています。」）を**出すかどうかの条件**だけ。

| 層 | 親補正の一言が出る帯（旧） | 帯（新） |
|---|---|---|
| 向聴0・1 | 受け 20〜23枚 | 受け **21〜24枚**（向聴2の流用） |
| 向聴2 | 受け 20〜23枚 | 受け **21〜24枚** |
| 向聴3 | 受け 20〜23枚 | 受け **56〜59枚** |
| 向聴4以上 | 受け 20〜23枚 | **出さない**（`gate !== Infinity` で外す） |

狭い側（`shapeOK` の枝・6〜7枚）は据え置き。`analyze()` の `narrowDefend` を触っていないため。

### 4-3. `check.js` ⑩ の追随

**L907-941**。数字リテラル前提の読み取りをやめ、層別の書き方を両側から読んで突き合わせる形にした。

- `GATE_RE` … `baseShanten >= 4 ? Infinity : (baseShanten >= 3 ? 60 : 25)` を両側から読む
  （写し側は `a.baseShanten` と書くので `(?:a\.)?` で吸収）
- 同じ字面が二箇所にあるので、**`function mascotDealerNudged` の位置で本体を切り分けて**、
  前半＝`analyze()` 側／後半＝写し側として別々に読む
- 親の甘さも字で読む（`analyze()` は `widthGate - 4`、写しは `gate - 4`）
- **向聴4以上を外しているか**も見る（`gate !== Infinity` が写しに無ければ FAIL）

突き合わせる値は5つになった — 狭い側 親6/子8・幅の関門 向聴3=60/向聴2以下=25・親の甘さ4枚。

### 4-4. 総当たりの走査（指示2）— 写し以外に幅の関門を名指しする箇所は**無い**

`acceptTiles` を数値と比べている箇所を全部当たった。**結果は下の一覧がすべて。**

| 行 | 中身 | 幅の関門の写しか |
|---|---|---|
| **L3036** | `acceptTiles < (isDealer ? 6 : 8)`（`narrowDefend`） | `analyze()` 本体（狭い側・据え置き） |
| **L3044-3045** | `widthGate` と `wideAttack` | `analyze()` 本体（今回の層別の関門） |
| **L4100-4102** | 虎の写し | **これが唯一の写し。今回揃えた** |
| L2820-2822 | `acceptTiles >= 7 / >= 4`（聴牌の `widthLabel`） | **違う**。表示の札の物差しで、既に向聴で層分けされている |
| L2824-2827 | `acceptTiles >= 40 / >= 24 / >= 12`（向聴1の `widthLabel`） | **違う**。`24` は数が同じだけの別物 |
| L2830-2833 | `acceptTiles >= 50 / >= 36 / >= 20`（向聴2以深の `widthLabel`） | **違う**。`20` も同上 |
| L4045 | `gunLine = (sh <= 2) ? -1 : (sh === 3 ? 30 : (sh === 4 ? 70 : 9999))` | **違う**。投票欄の傾き（`tenLeans`）で、実測50000回の別の物差し。作法20 で触らないと定めてある。既に層別 |

**虎以外の六人**（現場猫・枝豆・紳士・兎・羊・鼠）—— `catLean`／`edaLean`／`omLean`／
`usagiLean`（L4015-4022）はどれも `acceptTiles` を見ていない。材料は猫牌の枚数・対子と暗刻の組数・
一枚きりの字牌・`totalBlocks`／`fillGood`／`fillWeak` で、**幅の関門とは無関係**。

**判定カード・根拠欄**（L8484-8492「伸びしろ」）—— `a.widthLabel` と `a.acceptTiles` を
そのまま差し込むだけで、数値の関門を持たない。

**集計行**（L7127-7128）—— 「手が進む牌が◯枚残っています」「この向聴数なら『◯◯』の部類です」。
どちらも `analyze()` が付けた札をそのまま出しており、関門の数値を語らない
（L7126 に「広い・狭いの境目は向聴数で変わるので、判定エンジンが付けた札をそのまま使う」と注記あり）。

**撤退予約線**（L8386-8398・仲裁役の言い回し L7212-7245）—— 期限は `TURN_BY_SHANTEN`
（向聴2=3巡／3=6／4=7／5=8）と猫牌4枚で引いており、**受け入れ枚数の関門は使っていない**。

なお L1374・L1428 の解説に出る「**8枚**」は、良形の塊4つ×2枚＝8枚（`shapeNeeded` の話）で、
幅の関門の 8 とは**別の 8**。触っていない。

---

## 実機で見るところ

1. 判定後の画面で、**4向聴の広い手（受け60枚以上・ドラ1以上・良形が足りない手）の判定が「守り」になる**こと。
   いままでは受け24枚で攻めに格上げされていた（較正-1 の関門の効き）。
2. **親番・向聴4以上・良形不足・ドラ1以上**の手で、虎が
   「**親番なので、基準を少し攻め寄りにしています。**」を**言わなくなる**こと。
   向聴2・3では従来どおり出る（帯は 21〜24枚／56〜59枚）。
3. 版の字が **v1428**。

---

## 残り

**残り0件。**

- **較正-1-線引き** … 済（v1428・L3040-3045）
- **較正-1-虎** … 済（v1428・L4089-4102。案Aで写しを層別へ揃えた）
- **虎-1**（⑩ の FAIL の中身を出してから直す） … 済（上の 4-1／4-3）
- **虎-2**（幅の関門を名指しする箇所の総当たり） … 済（上の 4-4。写し以外に無し）
- **虎-3**（v1428 の commit・push と公開側の実読み） … 済（下記）
- **被り-1** … 記録のみ。台帳に積んである。直しは当てない（指示どおり）
- **凍結-0** … 発効中（見張り・通知・台帳の一式。本体と較正は対象外）

---

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. 🙋 ヨシしてください

```
較正-1の線引き確定（層別の幅の関門・v1428）
印: y0829-tora
待っているのは：虎の親補正の写し（L4096 の 20〜24）が層別の関門とずれた件の裁定。案A＝写しを層別へ揃える（文言は変えず条件だけ）／案B＝親補正の一言を向聴3以下に限る。表示の側なので指示3に従い触っていない。check.js ⑩ が FAIL 1件のため v1428 はコミットも push もせず作業ツリーに置いてある
答え方：「y0829-tora にヨシ」で進めます。裸の「ヨシ」は、いちばん新しい待ちへのヨシとして通ります。
実機: 裁定後に push してから。判定後の画面で、4向聴の広い手（受け60枚以上・ドラ1以上・良形不足）の判定が「守り」になる。版の字は v1428
未検収: 2026-08-29 11:09 較正-1の虎の写しの裁定（印 y0829-tora）
```

### 2. ✅ 終わりました（返事不要）

```
較正-1の続き（受け入れ枚数の層別分布と、幅の関門の置き直し案）
較正-1の続きの五項目。(1)acceptTiles を baseShanten 別に五数要約——向聴0=1件／1=17件／2=410件(最小10・中央20・最大72)／3=1421件(14・43・87)／4以上=2151件(20・71・123)。層を混ぜた中央値57は二つの層を重ねた見かけの数で、層で3.5倍離れている。(2)層別五分位の成績——向聴2は単調に良くなり Q3→Q4(受け入れ25枚)で和了32.9%→41.5%・点棒+724→+1652と跳ねる。向聴3は両端だけ効き、Q5(59枚以上)が和了33.0%・放銃7.7%・+1378。向聴4以上は折れ目が無く全区画マイナス(振れ幅543点の山形)。(3)いまの関門の位置——狭い側<8は向聴2以降で0.00%(各層の最小値が既に関門の上・全4000件で発火2件)。広い側>=24は向聴2で43〜57%、向聴3で77〜96%、向聴4以上で99.57%(親100%)が通る。幅が仕事をしているのは向聴2の層だけ。(4)置き直し案(根拠のみ・数値は決めていない)——向聴2は25枚(点棒差1517が最大でいまの24枚とほぼ同じ・据え置き可)、向聴3は60枚(差が508→1077へ倍・いまの24枚は下から77%の位置)、向聴4以上は幅では引けない(85枚で符号逆転)のでドラへ寄せる(588点差)。親子差は親側の件数不足につき据え置きを推奨。(5)材料ごとの発火件数——totalBlocks と hasValue は borderline 判定に一切関与しない(totalBlocks は L4013 の語り口だけ／hasValue は doraValue>0 の言い換えで食い違い0件／shapeNeeded も L2881-2884 で doraValue だけから決まる)。実質の材料は goodShapes・acceptTiles・doraValue の三つ。
ファイル: report-latest.md（書き直し）／~/.claude/inbox.txt（[x]へ）／~/.claude/orders-open.tsv（較正-2〜6を追記）。koushu-handan.html は一行も触っていない
実測: 母集団4000行。shapeOK=1が978件(24.45%)・0が3022件(75.55%)。攻の枝の narrowDefend＝幅のみ2件・ドラのみ713件・両方2件。守の枝の wideAttack＝幅のみ2727件(90.24%)・ドラのみ1081件・両方956件(ドラ成立分の88.4%)。結論を変えている件数は 幅836件(20.90%)・ドラ1771件(44.27%)でドラが2.1倍。層別×ドラ有無の点棒は 向聴2 +974/+1167、向聴3 +241/+758、向聴4以上 -435/+153。
```

### 3. ✅ 終わりました（返事不要）

```
郵便受けの五件（待ち札の食い違いの修理・観察三件の読み直し・較正-1の続き）
(1)待ち札の食い違いを二箇所で塞いだ。watch-notify.ps1 L1160-1183＝同じ印を立て直した回は一覧の件名と待ち先をいまの姿へ差し替える（今までは印が既にあると行を一切書き換えなかった）。stop-guard.ps1 L64-105＝件名を変えたのに待ちが前のままなら終了コード2で差し戻す。y0829-start は閉じた。(2)欠け率＝起点2.1%は関門750秒での値で、実測17/794＝2.14%と一致。前回の1.9%は関門を15分へ変えた別物だった。関門を揃えると 2.14%→2.68% で下がっていない（z=0.57・誤差の範囲）。(3)逆向きの疑い＝控えに待ちを書いた 19:18:48 と stop が鳴った 19:20:57 の間の2分。控え優先の決まりが設計どおり働いた結果で故障ではない。(4)送らなかった回＝24回ではなく31回。人を呼ぶべきだった件は0件（異常6件はすべて自力復帰・最長25分）。(5)borderline の 956対2 は L3038 と L3042 の発火率そのもの。配牌の受け入れは中央値57枚で、関門<8枚は0.05%しか当たらず >=24枚は85%が通る。幅の関門が死んでおり、実質「ドラ1枚以上か」だけで割れている。
ファイル: ~/.claude/watch-notify.ps1／stop-guard.ps1／yoshi-open.tsv／orders-open.tsv／report-latest.md
実測: 修理＝watch-notify 1743→1762行・stop-guard 93→137行、構文エラー0・BOMあり、写し .bak-20260829。判定は6通り＋7通りを写しで確認、全て期待どおり。欠け率＝関門750秒で全域27/1167＝2.31%、起点まで17/794＝2.14%、起点より後10/373＝2.68%。最長の穴は 08-21 19:28→20:08 の40分、起点より後では 08-27 22:08→22:41 の34分。送らなかった31回の内訳＝空題14・異常6・試し5・調べました3・終わりました3。borderline＝shapeOK=1 の978件中 narrowDefend 成立は2件（0.20%）、shapeOK=0 の3022件中 wideAttack 成立は956件（31.63%／幅の関門は2727件＝90.24%が通過）。
```

### 4. ✅ 終わりました（返事不要）

```
中断の後始末と、観察三件の報告
(1)後始末＝壊れている物は無し。台本九本すべて構文OK・BOMあり・関数の重複0、常駐は1本（PID 9040・起動 09:06:33＞台本 09:00:52）で直しは効いている。未着だったのは記録だけで、台帳へ 即時性-1 を済で追加し報告書を追いつかせた。回帰の実測を一本取った（受信箱→着手 30秒）。(2)観察-1＝欠け率 2.1%→1.9%。(3)観察-2＝08-26=40・08-27=19・08-28=54・08-29=6。(4)観察-3＝0件→1件。観察三件を済で閉じた。
ファイル: ~/.claude/orders-open.tsv／report-latest.md
実測: 回帰 10:06:26→10:06:56＝30秒（見張りが 10:06:33 に増分を見つけ 10:06:35 に起こした）。三種の即時性は 09:02:53／09:02:58／09:03:03 に5秒間隔で別々の札として控えへ載り、まとめられていない。生存の欠け＝全域1167本中22回（1.9%）・観察を立ててから360本中7回（1.9%）・本日61本中0回。最長の穴は 08-27 22:08→22:41 の34分。押し送り＝08-25:9／08-26:40／08-27:19／08-28:54／08-29:6（10:08時点）、送らなかった24回はすべて「名乗りなし」。逆向きの疑い＝1件（08-28 19:19:51、resume の75秒後）。
```

### 5. Claude Code watch (test)

```
後始末と観察三件が終わりました。壊れている物は無く、中断した回の直しは全て入っていました（未着は記録だけ）。回帰の実測は受信箱→着手30秒。観察-1 生存の欠け率 2.1%→1.9%（本日0回）。観察-2 押し送り 08-26:40／08-27:19／08-28:54／08-29:6。観察-3 逆向きの疑い 0件→1件（08-28 19:19、resume の75秒後）。未了は0件になり、残りは凍結-0（発効中）だけです。
```

<!-- 送った知らせ ここまで -->
