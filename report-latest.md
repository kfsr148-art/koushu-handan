# 二度目の穴 — 足跡が段を捉え、原因を実験で再現しました

**状態：ヨシ待ち（直しの案のご裁定）** — 2026-08-25 02:30 時点

## 1. 足跡に何が残っていたか（ご指示1）

```
watch-notify.log:1043  前の回は途中で終わっていた → 2026-08-25 01:11:38 / 知らせの判定を終えた
watch-notify.log:1044  前の回は途中で終わっていた → 2026-08-25 01:23:31 / 開始
```

**読み方**

- **01:11:38 の回は「知らせの判定を終えた」まで進んで殺されました。** 次の段は
  「使用量を終えた」なので、止まったのは**使用量の取得（`Take-Usage`）の最中**です。
- **01:23:31 の回は「開始」までしか進めずに殺されました。** 「開始」は走り出してすぐの段です。
- **01:11:38 から 01:23:31 までの約12分は、足跡が一度も書き換わっていません。**
  「開始」の一行すら書けていない＝走り出せてすらいない。

**穴の範囲**

| 記録 | 途切れ |
|---|---|
| 状態行（毎分） | 01:01:20 → 01:25:10（約24分） |
| 生存記録（10分ごと） | 00:58:05 → 01:28:03（30分。01:08 と 01:18 を落とした） |

前回は足跡が無く辿れませんでした。**今回は段まで取れています。**

## 2. そこで何が待たされているのか（ご指示2）

### 段の中で時間の上限が無いもの

`Take-Usage` の中の **`git push`** ひとつです。

```
git -C $UsageRepo push --no-verify -q origin main
```

`curl` には `--max-time 25` が付いていますが、`git push` には何も付いていません。
（`--no-verify` なので pre-push の検査は走らず、走るのは通信だけです。）

### ただし、これだけでは説明がつきません

**01:11:38 より後の12分は「開始」すら書けていません。** 走り出す前の段階で待たされています。
そこで機械の混み具合を疑い、**実験しました**。

`node check-all --fast` を切り離して回す（02:20:53〜02:25:57）。この検査は中で
headless Edge を何度も起こします。

```
02:20:01   （検査の前・定刻）
02:21:50   ← 1分49秒ずれ
02:22:01
02:23:25   ← 1分24秒ずれ
02:24:36
（02:25 は丸ごと欠けた）
02:26:54   ← この回は開始から完了まで68秒かかった
02:27:02   （検査のあと・定刻に戻る）
```

**平常時の1回は 2.6〜2.8秒。検査の最中は 68秒（26倍）。**

### 二度の穴と、こちらの作業の重なり

| | 穴 | その間にこちらがしていたこと |
|---|---|---|
| 一度目 | 23:38〜23:56 | headless Edge の probe を二度、push のたびに pre-push の検査（`node check-all --fast`） |
| 二度目 | 01:01〜01:25 | stable のタグを push（pre-push の検査つき）、`git ls-remote` を20秒おきに13分間 |

どちらも、`resume` から `stop`／`notification` までの**こちらの作業中の時間帯にすっぽり入って**います。

**→ 重い作業が走ると、毎分の見張りが3分の実行上限に届いてしまいます。** タスクの設定が
「多重起動＝IgnoreNew／実行時間の上限＝3分」なので、走りっぱなしの回があるとその間の
引き金は捨てられ、穴になります。

> **再現は部分的です。** 68秒までは伸ばせましたが、20分を超える穴そのものは再現できていません。
> 仕組みの向きは掴めましたが、27分に至る組み合わせまでは詰め切れていません。

## 3. タスクスケジューラの動作記録を有効にする（ご指示3）

管理者の窓が立ち上がる形です。UAC の問い合わせが一度出ます。

```powershell
Start-Process powershell -Verb RunAs -ArgumentList '-NoProfile','-Command','wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true; wevtutil gl Microsoft-Windows-TaskScheduler/Operational | Select-String enabled; Read-Host "Enterで閉じます"'
```

- 立ち上がった窓に「**enabled: true**」と出れば成功です。Enter で閉じます。
- 構文は検査済み。昇格なしだと `Access is denied` で `enabled: false` のままになることも
  確認しています（＝管理者が要ることの裏取り）。
- 有効にすると**起動と終了コードが全て残ります。** 3分の上限で殺されたのか、そもそも
  起動しなかったのかが、次からは記録だけで分かれます。

## 4. 直しの案 — ご裁定をお願いします

| 案 | 中身 | 効き方 |
|---|---|---|
| **甲** | 見張りの実行上限を3分→10分へ延ばす | 重い作業の最中でも殺されなくなる。代わりに本当に固まった回も10分居座る |
| **乙** | `Take-Usage` の `git push` に時間の上限を付ける（切り離して起こし、20秒で見切る） | 段の中の唯一の無制限を潰す。ただし12分の空白の説明にはならない |
| **丙** | 甲と乙の両方 | |

**こちらの見立ては丙です。** 乙だけでは足りず、甲だけでは無制限が残るためです。
ヨシをいただければ入れます。

## いまの版

本体 **v1426** ／ stable **`f3837e8`** ／ panel **v29**。この回はまだ何も変更していません。
