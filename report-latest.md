# 生存記録が18分途切れた件

**状態：ヨシ待ち（実機で見てください）** — 2026-08-25 00:30 時点

## 結論を先に

**引き金は特定できていません。** 切り分けで分かったのは次の三点です。

1. **今日の変更が関わっている証拠はありません**（後述の裏づけ）
2. **タスクスケジューラは生きていました** — 穴の最中にも見張りは起動しています
3. **見張りは起動したのに、記録を一行も書かずに終わっていました** — 最後まで走れていない

2.6秒で終わるはずの走が三回続けて3分（実行時間の上限）を超えた理由は、いまの記録からは
分かりません。**推測で直したことにはしません。**

そのうえで、**辿れなさ**——途中で殺されると手掛かりが一切残らない作り——を直しました。

---

## 1. 今日の変更は関わっているか（ご指示1）

触った三つは、いずれも止まりに繋がる形をしていません。

| 変更 | 中身 |
|---|---|
| `Push-Ntfy` の状態判定 | 文字列の比較だけ。外へ出る処理は無い |
| `Send-Ntfy` の第5引数 | 引数を一つ増やしただけ |
| 使用量の push 間隔 1800秒 | 変えたのは19時台で、穴より**4時間以上前** |

**裏づけ**

- 変更を入れた 23:0x 以降、穴に入る 23:38 までに見張りは**30回以上正常に回っています**
  （`watch-status.log` が毎分刻まれている）
- 穴が明けた 23:56:30 以降も、いまに至るまで毎分刻まれています
- 1回ぶんの所要は平常時 **2.6〜2.8秒**。実行時間の上限（3分）に対して余裕は60倍以上
- 押し送りの判定は 23:56:13 に正しく働いています
  （「押し送りはしない（ヨシ待ち以外の状態: 名乗りなし）」＝異常は送らない）

→ **関わっている証拠は見つかりませんでした。** ただし「関わっていない」ことの証明では
ありません。

## 2. 見張りかスケジューラか（ご指示2）→ **どちらでもありません**

`Windows PowerShell` の記録（イベント400＝エンジン起動）に、穴の最中の起動が残っています。

```
23:38:06  watch-notify   ← 最後に記録を書けた回
23:39:06  watch-notify   ← 起動したが記録は無し
23:43:45  watch-notify   ← 同じ
23:45:07  watch-notify   ← 同じ
23:54:50  watch-notify   ← ここから記録が戻る

23:38:05  hook-notice
23:54:46  hook-notice    ← 23:48 の一本を落としている
```

- **タスクは起動していた** → スケジューラが死んでいたのではない
- **起動したのに記録が無い** → 見張りが最後まで走れていない
- 毎分の引き金は 23:40・23:41・23:42・23:44 が丸ごと欠けています。タスクの設定が
  **多重起動＝IgnoreNew／実行時間の上限＝3分**なので、走りっぱなしの回があるとその間の
  引き金は捨てられます
- **機械は寝ていません**（穴の最中に Windows Update と Defender の記録がある）

## 3. 引き金 — 特定できていません

同じ時間帯にあったこと：

```
23:45:28  Windows Update が更新の取得を開始
23:45:35  Defender の定義更新の導入を開始
23:47:50  Defender の構成が変わった
23:47:51/52  定義の版が更新
23:47:54  導入が完了
```

ただし**穴は 23:38 から始まっており、Defender の更新は 23:45 から**です。時間が合わないので、
これだけでは説明がつきません。

**調べて外したもの**

| 疑い | 結果 |
|---|---|
| 機械の休止 | 記録なし。穴の最中に更新が動いている |
| スケジューラの停止 | 起動の記録が残っている |
| 重い処理 | `Add-Type` 0.36秒／`CIM`（全プロセス）0.05秒／使用量の取得 0.59秒。合計でも1秒未満 |
| PowerShell の 4100 エラー（16秒おき） | `inbox-watch.ps1` から出ており、**穴の前後も同じ間隔**。穴と対応しない（別件として残ります） |

## 4. 直したこと — 引き金ではなく、辿れなさを直しました

いまの作りは、記録の書き出しが**走のいちばん最後の一箇所だけ**でした。途中で殺されると
一行も残らず、どこで止まったのかを辿れません。**それがこの18分で起きたことで、
原因究明を止めているのもここです。**

| 場所 | 中身 |
|---|---|
| `~/.claude/watch-notify.ps1:31-38` | `watch-step.txt`（足跡。一行だけを上書き） |
| `~/.claude/watch-notify.ps1:129-146` | `Step` 関数と、前の回の足跡の拾い上げ |
| 各段 | 開始／窓とプロセスを数えた／`hook.log` を読んだ／控えを読んだ／知らせの判定を終えた／使用量を終えた／完了 |

次に同じ穴が空いたら、`watch-step.txt` に**最後に通った段**が残り、次の回が
「前の回は途中で終わっていた → ◯◯」を `watch-notify.log` へ書きます。

> **お願いが一つ** — タスクスケジューラの動作記録も有効にしようとしましたが、管理者権限が
> 要るため拒否されました。有効にすると起動と終了コードが全て残り、次に同じことが起きた
> ときの助けになります。管理者の PowerShell で次の一行です。
> ```
> wevtutil sl Microsoft-Windows-TaskScheduler/Operational /e:true
> ```

## 5. タスクは動いているか（ご指示・冒頭）

**動いています。起こす必要はありませんでした。**

| タスク | 状態 | 最後の実行 | 結果 | 次 |
|---|---|---|---|---|
| ClaudeWatchNotify | Ready | 00:16:16 | 0 | 00:17:17 |
| ClaudeHookHeartbeat | Ready | 00:08:08 | 0 | 00:18:18 |

`watch-status.log` も毎分刻まれています。

## 6. 実測（ご指示3）

### 生存記録が続けて刻まれること

```
23:58:03  heartbeat  生存
00:08:03  heartbeat  生存
00:18:03  heartbeat  生存   ← 定刻どおり10分間隔
```

毎分の状態行も 00:18:22 から 00:28:14 まで、一分の欠けもなく刻まれています。
足跡は毎回「完了」で終わっており、「前の回は途中で終わっていた」の行は **0本**です。

### 「😼 返事パネル」が iPhone へ届くこと

本番の経路（`Push-Ntfy` を状態 `wait` で呼ぶ）で **00:17:32 に送信、HTTP 200**。
日付が変わったので件数も 2026-08-25 の1件目として数え直されています。
題は「😼 返事パネル」の一行、本文は空、叩くと返事パネルが開きます。

### 足跡の仕掛けが効くこと

途中で殺された回を模して確かめました。

```
前の回が「控えを読んだ」で止まっていた状態で回す
  → watch-notify.log に「前の回は途中で終わっていた → 2026-08-25 00:00:00 / 控えを読んだ」
  → 足跡は「完了」に更新される
正常に終わった直後にもう一度回す
  → 「前の回は…」の行は増えない（1本のまま）
```

## 7. 別件として残るもの

`inbox-watch.ps1` が **16秒おきに PowerShell の警告 4100「システム エラーです」** を
出し続けています（直近6分で23件）。ただし——

- **穴の前後も同じ間隔で出ており、18分の穴と対応していません**
- `inbox-watch.ps1` 自体は正常に働いています（状態の書き出しが続いている）

今回の件とは切り離して、別に扱うべきものと見ています。

---

# 追加：返事パネルの釦の字（panel v29）

| 場所 | 前 | 後 |
|---|---|---|
| `panel.html:394` | 写した印を消して出す | **写した札を出す** |
| `panel.html:193` | panel v28（8月24日） | **panel v29（8月25日）** |
| `panel.html:213` | `PANEL_VER = '28'` | `PANEL_VER = '29'` |
| `panel-ver.txt` | 28 | **29** |

- 案内文「◯件ぜんぶ写し済みです。新しい知らせが出れば、ここに並びます。」は**そのまま**です。
- 動きも**変えていません** — 写した印を消して（`copied = {}` → `saveCopied()`）一覧に出し直す
  （`pull()`）だけで、控え（`notices.json`）には触りません。
- `node --check` 構文OK。

## 実機で見ていただきたいところ

1. 返事パネルの画面下の版の字が **panel v29**
2. 全部写し済みのときの釦の字が「**写した札を出す**」になっている
3. その釦を押すと、写した札が一覧に戻る
4. iPhone に 00:17 ごろ「**😼 返事パネル**」の通知が届いているか（題の一行だけ・本文は空）
