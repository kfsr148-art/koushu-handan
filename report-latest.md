状態：終わり ／ 2026-08-21 01:46 ／ v1419 ライフ制12項目＋本文5箇所を実装。フル版check-all 全PASS

【保留】
- **倒れる音 三案（A/B/C）の選択**（試聴版をお渡し済み。決まり次第 差し込む）
- **ライフ0の倒れる絵の素材待ち**（704x1524・白背景・剣は手から離さず杖のように突く）
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（目盛り画像は `head-proof-*.png`）
- `sizing-review.png` を残すか消すか
- ライフ3の妥当性（実機で触ってからの調整）

# 納品：v1419 一索の鳥にライフ制を入れた

日付 2026-08-21 ／ 版 **v1419** ／ 前版 v1418（`d669d4c`）

**12項目すべてを通しで実装した。** 素材待ちの二つ（**倒れる絵・倒れる音**）は入れておらず、
**後から差し込める形で空けてある**。

---

## 1. 入れたもの（12項目）

| # | 中身 | 場所 |
|---|---|---|
| 1 | **`EDA.life: 3`**（全モード共通） | 定数 9449行 |
| 2 | **無敵を被弾から1.0秒へ**（`hitInvuln: 1000`、起点を `now` に） | 定数 9448行／`recoil()` |
| 3 | `recoil()` で**ライフを1つ減らす**。**走り中と空中は絵を出さない** | `recoil()` |
| 4 | 接触の条件から **`!moving` を外した**（案A） | 主ループ |
| 5 | **走り斬りを接触より先に判定**。捕らえた鳥は `birds` から抜けるので、**その周期の接触には残らない** | 主ループ（塊ごと入れ替え） |
| 6 | 走り出しで**無敵を捨てるのをやめた**（`invulnUntil = 0` を外した） | `zoneGo()` |
| 7 | 空中被弾は**ライフだけ**（落下は止めない。絵も出さない） | `recoil()` |
| 8 | **金の一羽でライフ+1**（上限 `EDA.life`。超過は消える）。ノルマ2羽ぶんは据え置き | `takeBird()` |
| 9 | **ライフ0で `finish(false)`**。全モード。**ノルマ達成の判定より後**に置き、同じ周期なら勝ちを先に見る | 主ループ |
| 10 | HUD に **`#toriLife`**（`EDA_NAGE.tama` を **12x12** で三つ、間隔4px。失ったぶんは暗く残す） | CSS 589-591行／HUD 9599行／`lifePaint()` |
| 11 | 遊び方の本文（**必須5箇所**。⑥の任意は入れていない） | `INFO_DETAIL` |
| 12 | コメント2件（「体力も負け条件も無い」）の書き直し | 定数の見出し／`recoil()` の見出し |

### 実装で決めたこと（補足）

- **描き直しは変わった周期だけ。** `lifeDrawn` に最後に描いた数を控え、`lifeDrawn !== life` の
  ときだけ `lifePaint()` を呼ぶ。毎フレーム DOM を書き換えない。
- **失った粒は消さずに暗く残す**（`filter: brightness(0.35) saturate(0.4)`）。
  残り数と上限3が同時に読め、金の鳥で戻る余地があることも見える。
- **順序の入れ替えは字面の切り貼りではなく、塊の位置を計算して行った。**
  走り斬りの塊と接触の塊を丸ごと入れ替え、接触側の条件と見出しだけを書き換えている。

---

## 2. 実測での確認

**写しに probe を差して、実際に粒が出るかを見た**（本体は無変更・写しは削除済み）。

| | 568x320 | 844x390 |
|---|---|---|
| ライフの枠 | あり | あり |
| **粒の数** | **3個**（暗い 0個） | **3個**（暗い 0個） |
| 粒の寸法 | **12 x 12** | 12 x 12 |
| 枠の幅 | **44px** | 44px |
| **HUD の高さ** | **44px** | **44px** |
| ✕ の右端 | 558（視野568） | 834（視野844） |

**HUD の高さは 44px のまま**で、粒を足しても行が太っていない。
**✕ は `margin-left:auto` で右端に居るので、押しどころも動いていない。**

---

## 3. 検査

**`node check-all`（フル版）**

| | 結果 |
|---|---|
| `check.js` 本体 ①〜㉑ | **全PASS**（✗ **0件**） |
| ⑦ 狭い画面での溢れ（6視野 × 9画面 ＝ 54組） | **PASS**。**`toriend`（結果画面）も6視野すべて溢れなし** |
| ⑤ 死にコード | **PASS**（`lifePaint()` も参照されている） |
| ⑥ 未使用の埋め込み資産 | **PASS** |
| ⑬ 非日本語文字の混入 | **PASS** |
| `adv-check.js` 探偵編の回帰 | **全PASS** |

**判定後の二択の bottom は6視野とも前版と同値**（568x320 で 317.8／844x390 で 387.5／900x381 で 380.5）。
ミニゲーム側の変更が本体のレイアウトへ漏れていないことの裏付けになる。

その他

- **`node --check`**：`<script>` 4ブロックとも構文OK（一時ファイルは切り出して検証後に削除）
- **`serifu.txt` 再抽出**：7593行・**口調の混入0件**、`--check` で一致を確認
- **版表記3箇所**を **1419** に同時更新（`data-ver` / `verTag` / `ver.txt`）
- **テスト用のシムなし。** 確認は写しに probe で行い、写しごと削除した
- `analyze()` / `bestShapeOneSuit` には触れていない（作法§5・§10）

---

## 4. 入れていないもの（素材待ち・後から差し込める）

- **ライフ0で倒れる専用の絵。** いまは**ライフ0でそのまま既存の負け絵の結果札へ行く**。
  差し込むときは `finish(false)` の手前に一コマ挟む形になる。
- **倒れる音。** 三案の選択待ち。鳴らす場所は同じく `finish(false)` の手前。
  **設定の効果音 ON/OFF に従い、追い込みの高さ上げ（`rushPitch`）の対象には含めない**——
  この扱いは既存の悲鳴と同じ。

**どちらも一箇所に差し込むだけで済むよう、ライフ0の判定を `finish(false)` の直前に一本化してある。**

---

## 5. 実機で見ていただきたいところ

- **ライフ3が難易度に合っているか。** 初級は余り、上級と連戦は厳しい見立て（前回の報告 (5)）。
  上級だけ緩めるかどうかは触ってから。
- **走り中に当たったときの手触り。** 絵が替わらないので、**悲鳴と粒の減りだけ**が合図になる。
  分かりにくければ、粒が減るときの点滅などを足す余地がある（いまは入れていない）。
- **無敵1.0秒の長さ。** 被弾から数えるので、前版（実質0.8秒）より少し長い。
- **金の鳥で粒が戻る瞬間**が見て分かるか。

---

## 6. 次

- **v1419 の実機検収。** OK が出れば `stable` を進める（いまは v1416 `d5d5e9f`）。
- **倒れる音の三案の選択**と、**倒れる絵の素材**。届き次第、差し込む。
- 保留の残りは冒頭の一覧のとおり。
