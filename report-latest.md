# 話題の差し替え・断線の後始末・再起動の稽古・仕舞い・矛盾ふさぎ

**状態：終わり（残り4件）** — 2026-08-27 08:55 時点／手待ちの脈（panel v39）まで完了

**結論（手待ちの脈）** — 手待ちの3時間半、`state.json` が一度も書かれず**赤帯だけが育っていた**。
パネルの注記は「15分おきに心拍を出す」と書いていたが、**その心拍は実装されていなかった**——
`$script:lastStatAt` は控えてあるのに、どこからも使われていない。**30分の脈**（`$PULSE_SEC`）を
入れ、対になる**赤帯の閾値を 20分→45分**（**panel v39**）へ揃えた。**赤帯は本当に止まった時だけ出る。**
前の報告で出し損ねた**③④の答え・締め-6 の状態・残り欄**は、下の十一-2 に書いた
（**④は差2件ではなく差1件**が正しい）。

**結論（矛盾ふさぎ）** — 前の回に入れた「`done:` で時計を落とす」直しが効かなかったのは、
**走っていた常駐が直しより前の起動だった**ため。`inbox-watch.ps1` は**立ち上げたときの中身のまま
走り続ける**ので、直しても走っているプロセスには効かない。起こし直し（**PID=2256**）、
`CLAUDE.md` の作法29 へ**⑤「常駐を直したら起こし直す」**を足した。
「最終確認」が凍るのは**書く場所が分かれていたのではなく**、書き直す条件に**控えの中身が
入っていなかった**ため——**控えの指紋**を足し、件名を変えずに中身だけ直した回でも `at` が
動くことを実測した。控えが古い回に前の回の値が並んでいた件は、**五欄すべてを `stale` を見る形へ**揃えた。
「写した札」と「届いた」の差2件は、**打ち止め帯には当たらない**（押し送りは通っている）。
パネルは **v38**（「全部写す（N件）」・0件は押せない）。

**結論（確認）** — 03:19 に済んだ件で超過が鳴ったのは、**閉じる側が無かった**ため。
打ち直す条件はあったが、**作業が終わったときに開始の時計を落とす処理が無い**まま残っていた。
**「終わり」を出した回で時計を落とし、打ち直さない**形にした（`inbox-watch.ps1` **382→398行**）。
`ClaudeHomeBackup` の初回は **03:30:30 に走り、結果0・7件を押した**（`稽古-1` 済み・残り4件）。

**結論（仕舞い）** — 締めの6件は**全て済み**。修理を一件入れた——
**同じ件の待ちを立て直すとき、印を切り直さず元の印と知らせを使い回す**形にした。
同じ件かの見分けは「**件名と待ち先の二つが揃って一致するか**」。
`watch-notify.ps1` **1564→1594行・構文OK**（写し `.bak-20260827`）。

**結論（稽古）** — **再起動の稽古を通した。手が要ったのは二つだけ**——**Windows へのサインイン**（22:11:20）と
**Claude Code の窓を開くこと**（22:36:20）。見張り四つと常駐は**すべて自分で戻った**（最も遅くて 22:23:42）。
戻りが遅く見えたのは電源断のぶんではなく、**起動 21:55 からサインイン 22:11 までの16分**が
効いている。実測の時系列と ⓪サインインの段を `RESTART.md` へ書き足した。
**新しい未了が一件**——`ClaudeHomeBackup` の**初回の見届け**（08-27 03:30。まだ刻限が来ていないだけで異常ではない）。
時間の表示は「終了予定HH:MM」へ揃え（**panel v37**・検査は両方PASS）、話題名の差し替えも完了済み。

---

## 一．時間の表示を「終了予定HH:MM」へ（panel v37）

**書くのは分、出るのは時刻。** 控えには今までどおり `見込み: 25分` と書くが、
表に出るのは**開始に足した結果の時刻**。読む側が毎回足し算しなくて済むようにした。

| 出どころ | 出る形 |
|---|---|
| 状態行（`state-line.js`） | `開始19:10・終了予定19:35・経過12分` |
| パネル二段目（`panel.html` v37） | 同上 |
| 超過の知らせ（`watch-notify.ps1`） | `終了予定19:35を過ぎています（経過37分）` |

見込みが立たない仕事は **`終了予定 未定`**。`CLAUDE.md` 作法25 にも
「書くのは分・出るのは時刻」を明記した。版は三箇所同時（`PANEL_VER` ／ `verTag` ／ `panel-ver.txt`）。

**`node check-all --fast` … `check` PASS ／ `adv-check` PASS ／ 両方PASS。**

## 二．`usage.json` の `watch` に時計の欄

**採った形** — `started`（開始HH:MM）／`ends`（終了予定HH:MM・立たなければ `null`）／
`elapsed`（経過分）の三つに加え、**ウィジェット向けの短い形 `span`（`"〜19:35"`／未定は `"〜未定"`）** を併記。
字数に余裕のある側は三つを組み、無い側は `span` だけ読めばよい。

出すのは**作業中のときだけ**で、ほかの状態では四つとも `null`。
材料は状態行・パネル・超過の知らせと同じ（`work-started.txt` と `work-note.txt`）——
**四箇所で同じ時刻が出ることが要**なので、別の測り方を持ち込まない。

**実測** — 手待ちの回で `{"state":"手待ち","started":null,"ends":null,"elapsed":null,"span":null}`。
欄が出ることと、作業中以外では黙ることを確かめた。

## 三．「終了予定 未定」のまま30分

未定の仕事は**過ぎる相手が無い**ので超過の判定に掛からず、放っておくと何時間でも黙る。
経過が30分（`$UNDEF_MIN`）に達したら **`終了予定が未定のままN分が経ちました`** を
**一度だけ**送る。

**一発だけの担保** — 済んだ印（`mikomi-over.txt`）の鍵に**形の名（`over` / `undef`）も入れた**。
入れないと、未定で一度鳴った作業にあとから見込みを足しても鍵が同じで二度目が出ない。

**分かれ方の実測**

```
見込み25分・経過12分 → 黙る      見込み無し・経過12分 → 黙る
見込み25分・経過37分 → over      見込み無し・経過30分 → undef
                                  見込み無し・経過90分 → undef（鍵が同じなので二度目は出ない）
```

## 四．控えの自動化（一日一回）

`git-push.ps1` の並びへ **`home-backup.ps1`** を足し、日課のタスク **`ClaudeHomeBackup`**
（毎日 03:30・次回 8/27 3:30・上限10分）で回す。押す口（`Git-Run` / `Git-Note`）は
`git-push.ps1` のものをそのまま使い、記録も `git-push.log` に揃えた。

- **一日一回の担保** … `home-backup-last.txt` に押した日付を持つ。同じ日なら何もせず返る
- **変更が無い日も日付は刻む**。刻まないと、変更の無い日に毎回 git を起こすことになる
- **通らなかった日は刻まない**。次の回でもう一度試させる

**人が見ていない経路なので、鍵の混入検査を通ってからでないと押さない。**
拾った顔ぶれを話題名の実文字列と鍵らしい字面（`gh?_` / `github_pat_` / `sk-ant-`）で走査し、
**一件でも当たれば拾ったものを戻して押さず、記録に残す**。`accessToken` のような
「名前」は本文に出るので数えない（実体だけを見る）。

**実測** — 印を今日にした空回しで記録が1行も増えないこと（＝二度押さない）を確かめ、
印を消した本番で `押した：控え: 2026-08-26（25件の変更）` が出た。

## 五．復元の稽古

`claude-home-backup` を一時フォルダへ clone し、手元の `~/.claude` と突き合わせた。

```
clone した件数 : 136        手元の追跡件数 : 136
木のハッシュ   : ff518b7a7d5b5be4216148fb21152806eb99efb1（両方とも同じ）
```

**完全一致。** 追跡している中身に漏れはない。

**戻らないもの（＝意図して除外しているもの）は、次の一件だけが本当に困る。**

| 戻らないもの | 失うと何が困るか |
|---|---|
| **話題名（`rc-ntfy-*`）** | **押し通知の宛先。リポジトリの外に控えが要る唯一のもの** |
| `.credentials.json` | 困らない。`claude` に入り直せば作り直される |
| `projects/`（264MB） | セッションの全文。**話題名が5本に混じる**ので載せられない。要点は記憶44件が持つ |
| `file-history/`（855MB） | 編集の取り消し用の写し。本体は koushu-handan の git が持つ |
| 各種 `*.log` | 過去の追跡がしにくくなるだけ。**話題名が混じる**うえ日々肥る |
| `plugins/` `tools/` | 入れ直せば戻る |
| `cache/` `sessions/` `session-env/` `shell-snapshots/` `telemetry/` `downloads/` | 機械が作り直す |
| `.last-cleanup` ほか | 回ごとに書き換わる屑 |

## 六．話題名の作り直し（差し替え完了）

**差し替えは済んだ。** 新しい名前を乱数で作り、`.gitignore` の `rc-ntfy-*` に当たる
ファイル名で保存し、iPhone 側で購読してもらったうえで送り先を入れ替えた。

**名前は黒い窓の画面にだけ出した。** この報告書にも、パネルにも、`notices` にも、
リポジトリにも書いていない。

| 手順 | 実測 |
|---|---|
| 除外の確認 | `git check-ignore -v` で3本とも `.gitignore:13 rc-ntfy-*` に当たる。`git status` にも出ない |
| 送り先の入れ替え | `rc-ntfy-topic.txt` を新しい名前で置き直した |
| 古い名前の退避 | `rc-ntfy-topic.txt.retired-20260826` へ改名（消していない） |
| 試験送信 | **HTTP 200**。公開側（`?poll=1`）でも題と話題名を確認 |
| 本番と同じ形での再送 | 題だけ・本文は幅ゼロ空白・`Cache: no` で **HTTP 200** |
| 残存の確認 | 送り先として使う場所に古い名前は無い。残っていたのは `projects/*.jsonl` 6本と `watch-notify.log` だけで、**どれも `.gitignore` の除外に入っている** |

**画面に出した名前と、20:15 に作って控えておいた名前は別物になった。**
20:15 のぶんは、こちらが読み直さずに作り直してしまったため使われていない。
購読してもらったのは**画面に出したほう**なので、そちらを送り先に入れ、
20:15 のぶんは `rc-ntfy-topic-new.txt.retired-20260826-unused` へ退避した（消していない）。

**一件だけ、ntfy に残る形で送ってしまった。** 最初の試験送信は本番と違う形
（`Cache: no` 無し・本文つき）で打ったため、ntfy 側に12時間ぶん残る。
`ntfy.sh` に消す口は無いので、**時間切れを待つ**（2026-08-27 09:02 ごろに消える）。
二度目は本番と同じ形で打ち直した。

## 七．断線の後始末（20:30〜21:00 の帯）

### 1. 状態の閉じ忘れ — 直した

**原因（一行）** — 断線で窓が落ちた回に、見張りが件名を置き字
「Claude Code は動いていません」に差し替えて**作業の時計を打ち直し**、窓が戻ったあとも
その 20:31 の時計が残っていたため、21:03 に「終了予定 未定のまま31分」が鳴った。

| 何を見たか | 実測 |
|---|---|
| 知らせの記録 | `watch-notify.log` 21:03:17 `未定のまま30分を一発知らせた：経過31分` |
| 置き去りの時計 | `work-started.txt` … `subj=Claude Code は動いていません` ／ `at=20:31:38` |
| 鍵の動き | 21:02:38 に `wait:` → `run:` へ。作業中になった途端、古い時計が読まれた |

**閉じた物** — `yoshi-open.tsv` から `y0826-topic` を落として空に、
`work-started.txt` と `mikomi-over.txt` を削除。状態は手待ちへ。

**再発しない手** — `inbox-watch.ps1` に**門を一つ**入れた。

```powershell
if ($alive -ne 0 -and ($prevSubj -ne $subj -or $startedAt -eq 0)) {
```

窓が無い回（`$alive -eq 0`）は**時計に触らない**。置き字の件名で作業が始まったことにならず、
窓が戻った回に本当の件名で打ち直される。**376行 → 382行。`PSParser` 構文OK。**
触る前に `.bak-20260826` を取ってある（作法29 の順）。

### 2. 断線の帯に積まれた重複指示 — 該当なし

**受信箱には一件も積まれていなかった。** `inbox.txt` は **2026-08-23 17:32 から動いておらず**、
未処理は **0件**。「話題名の表示」「`topic-note.txt` へ変える」「その取り消し」は、
どれも受信箱には残っていない。

**この窓に届いたのは「新しい話題名をこの画面に一行で出して」の一件だけ**で、それは実施済み。
ほかは実行し直していない（**取り消し扱い**）。

**積まれなかった理由も分かった** — 受信箱の常駐（`inbox-watch.ps1`）が、**窓と一緒に落ちていた**。

| 何が | いつから止まったか |
|---|---|
| 常駐（PID=2844） | 20:29 の稽古前の記録には居た。20:31 以降は不在 |
| `inbox-watch.log` | 最後の行が 20:26:51 |
| `state.json`（公開側） | **20:31 で止まったまま**。返事パネルの状態の一行もそこで固まっている |

常駐は**タスクではなくスタートアップ**（`ClaudeInboxWatch.vbs`）で起きるので、
**落ちたら次のログオンまで戻らない**。この見分け方と手で起こす一行を `RESTART.md` へ足した。

### 3. 台帳の残り（全件）

| 番号 | 項目 | 状態 |
|---|---|---|
| 積み残し-1 | 目盛りの実戦較正 | 未了：着手の合図待ち |
| 観察-1 | 生存の欠け率の報告（起点 795本中17回＝2.1%） | 未了：2026-08-29 |
| 観察-2 | 押し送り件数の報告（起点 08-25=9件・08-26=33件） | 未了：2026-08-29 |
| 観察-3 | 逆向きの疑いの件数の報告（起点 0件） | 未了：2026-08-29 |
| 稽古-1 | `ClaudeHomeBackup` の初回実行の見届け | 未了：2026-08-27（初回が 03:30。走らなければ不具合） |
| ~~断線-5／締め-5~~ | ~~再起動の稽古~~ | **済：22:50（下の五を見よ）** |

**今日の枠で未了のまま残っている物は無い。** 観察の三件は**3日後が期日**、
積み残し-1 は**着手の合図待ち**、稽古-1 は**明日の刻限待ち**。どれも今日の分ではない。

### 4. 旧話題名の後始末 — 済み

旧話題名のファイルは `rc-ntfy-topic.txt.retired-20260826` へ改名済み（消していない）。
**送り先として読む所は4箇所**（`watch-notify.ps1` ／ `inbox-watch.ps1` ／ `home-backup.ps1` の
計4行）で、すべて `rc-ntfy-topic.txt`＝新しい名前を指す。旧名が残っていたのは
`projects/*.jsonl` 5本・`watch-notify.log`・退避した `retired` 2本の**計8件だけで、
どれも `.gitignore` の除外の中**。除外の外には一件も無い。

### 5. 再起動の稽古 — 済み

**手が要ったのは二つだけ。** 残りはすべて自分で戻った。

#### 時系列（実測）

| 時刻 | 何が起きたか | 自動／手動 | 出どころ |
|---|---|---|---|
| 21:53頃 | `shutdown /r /t 120` を発令 | **手動**（前の窓） | 前の報告 |
| **21:55:47** | OS 起動 | — | `System` ログ 6005 |
| 21:56:13 | Winlogon 到達（ロック画面まで） | 自動 | `System` 7001 |
| **22:11:20** | **Windows へサインイン** | **手動** | `TerminalServices` 21／22 |
| 22:14:27 | `inbox-watch.ps1` が起動（PID=4480） | 自動（サインインが引き金） | `Win32_Process` の起動時刻 |
| 22:16:15 | 「常駐の見張りを始めます」 | 自動 | `inbox-watch.log` |
| **22:18:21** | `ClaudeHookHeartbeat` 復帰 | 自動 | `hook.log` |
| **22:23:42** | `ClaudeWatchNotify` 復帰 | 自動 | `watch-notify.log` |
| 22:32:32 | `ClaudeSweepChecks` 復帰 | 自動 | タスクの `LastRunTime` |
| **22:36:20** | **Claude Code の窓を開く** | **手動** | `claude.exe` の起動時刻 |
| 22:37:16 | `state.json` の更新が再開 | 自動 | ファイルの更新時刻 |

#### 自動で戻った物 — 五つ全部

| 何が | 戻った時刻 | 空いた穴 | 引き金 |
|---|---|---|---|
| 常駐 `inbox-watch.ps1` | 22:14:27（記録は 22:16:15） | — | スタートアップの vbs（**ログオン**） |
| `ClaudeHookHeartbeat` | 22:18:21 | **30分01秒**（21:48:20→22:18:21） | 繰返 PT10M ＋ `StartWhenAvailable` |
| `ClaudeWatchNotify` | 22:23:42 | **31分28秒**（21:52:14→22:23:42） | 繰返 PT1M ＋ `StartWhenAvailable` |
| `ClaudeSweepChecks` | 22:32:32 | — | 繰返 PT20M ＋ `StartWhenAvailable` |
| `state.json`（公開側） | 22:37:16 | 断線の 20:31 から**2時間6分** | 常駐が戻ったため |

**前回いちばん確かめたかった「常駐はログオンで戻るか」は、戻ると確かめられた。**
断線で落ちた常駐（PID=2844）は次のログオンまで戻らなかったが、今回のサインイン（22:11:20）で
3分後に新しい PID=4480 が上がっている。

#### 手動だった物 — 二つだけ

| 何が | 時刻 | 忘れるとどうなるか |
|---|---|---|
| **Windows へサインイン** | 22:11:20 | **常駐もタスクも一切上がらない。** ロック画面のままでは全部止まったまま |
| **Claude Code の窓を開く** | 22:36:20 | 見張りは動くが、仕事が進まない |

#### 分かったこと（四つ）

1. **遅れの正体は電源断ではなくサインイン待ち。** 起動 21:55 → サインイン 22:11 で**16分**空いた。
   「立ち上げたのに何も動かない」と見えたら、**まずサインインしたかを疑う**。
2. **`StartWhenAvailable` の追いつきは即時ではない。** サインイン 22:11 から見張りの復帰 22:23 まで
   **12分**。毎分の繰返しでもこれだけ待つ。**急かして手で起こさない**。
3. **`ClaudeHomeBackup` はまだ一度も走っていない——が、これは異常ではない。**
   `LastRunTime` が未実行の印 `1899-11-30`、`LastTaskResult=267011`（`SCHED_S_TASK_HAS_NOT_RUN`）。
   最初「控えの自動押し出しが効いていない」と見たが、**調べ直して取り下げた**——
   タスクを作ったのが **08-26 20:13:56**、日次の刻限が **03:30** なので、**初回は 08-27 03:30**。
   まだ来ていないだけだった。台帳の `稽古-1` は**初回の見届け**として残す
   （**08-27 を過ぎても走っていなければ本当の不具合**）。
   ＊押す枝は `master`（本体の `main` と違う）。台本 `home-backup.ps1` の側は正しく `master` を指している。
   ＊**電池では走らない設定**なので、電源を抜いたまま 03:30 を迎えると飛ぶ。
4. **`ClaudeRemoteControlCheck` は実在しない。**`RESTART.md` の §4 の表に載っていたが、
   実際のタスクは四つ（`WatchNotify` ／ `HookHeartbeat` ／ `SweepChecks` ／ `HomeBackup`）。
   接続の見分けの印が付けられず「接続=不明」しか書けなかったため 2026-08-22 に止まっている。**表を直した。**

#### `RESTART.md` へ書いたこと

`C:\Users\user\.claude\RESTART.md`（リポジトリの外・Claude Code が消えても読める）へ、
実測に合わせて次を足した。

| 直した所 | 何を |
|---|---|
| §4 の表 | `ClaudeRemoteControlCheck` を落とし、`SweepChecks` と `HomeBackup` を足した（三つ→**四つ**） |
| 自動で戻るものの表 | `HomeBackup` が一度も走っていないことの注記 |
| **稽古の実測の表** | 上と同じ時系列（11行）をそのまま |
| **⓪ サインインの段を新設** | 手で戻す順の**①の前**。「これを忘れると何も戻らない」／サインイン後は**12分待つ** |

**手で戻すものの順番書き（`RESTART.md` の写し）**

| 順 | やること | 確かめ方 |
|---|---|---|
| **⓪** | **Windows にサインインする** | **忘れると何も戻らない。**済んだら12分待つ。`watch-step.txt` の時刻が進めば戻っている |
| ① | `cd C:\Users\user\Desktop\mahjong\koushu-handan` → `claude -c --remote-control` | 窓が開く。`-c` で前の会話を継ぐ |
| ② | Remote Control が繋がったか見る | `watch-notify.log` の押し送りが通る。駄目なら ① を `--remote-control` 付きで打ち直す |
| ③ | 台帳を読んで「残りN件、続けます」と申告（作法32） | `orders-open.tsv` の四つ目の欄が「済」で始まらない行を並べる |
| ④ | 受信箱に溜まった指示を読む | `inbox.txt` の `[ ]` の行 |
| ⑤ | 持ち越しの二つを見る（**消さない**） | `ntfy-pending.txt` ／ `ntfy-retry.txt`。次の見張りの回が片付ける |
| ⑥ | 版が揃っているか見る | `git status --porcelain` が空／`ver.txt`・`panel-ver.txt`／`git log -1 stable` |

＊**常駐が上がったかの見分けは `state.json` の時刻**。動き出していれば上がっている。
　止まったままなら、スタートアップの `ClaudeInboxWatch.vbs` を `wscript` で手で起こす。
　＊今回は手で起こす必要は無かった（サインインで自動的に上がった）。

### 6. 凍結の宣言 — 発効中

**本日までの仕掛けをこれで確定**とする。見張り・通知・台帳の一式には、数日間
**新しい機能を足さない**。仕事は**不具合の修理**と、**仕込み済みの観察報告**だけ。
観察の初回は **2026-08-29**（生存の欠け率／`ntfy-daily.tsv`／逆向きの疑い）。
**麻雀本体と較正の作業は凍結の対象外。**

＊今回の `inbox-watch.ps1` の門は**不具合の修理**なので、凍結には当たらない。

## 八．仕舞いの3件（2026-08-27 03:19〜）

### 1. 締め-1 と締め-6 の状態 — どちらも済み

| 番号 | 状態（一行） |
|---|---|
| 締め-1 | **済み。**`yoshi-open.tsv` に `y0826-topic` は無く（0件）、再発しない門は `inbox-watch.ps1` の302行に入っている（382行・`PSParser` 構文OK） |
| 締め-6 | **済み。** 凍結は台帳の `凍結-0` が**発効中**、記憶にも `freeze-2026-08-26` として残っている。初回の観察は 2026-08-29 |

**追加の実施は不要だった。**

### 2. 修理 — 同じ件の待ちで印を切り直さない

**何が起きたか** — 2026-08-26 **21:51:06** と **21:52:14**、**1分8秒の間に**同じ件名で
`y0826-2150` と `y0826-2151` の二つの印が立ち、**同じ内容の知らせが二度出た**。

**なぜ** — 控えに `印:` を書かない回は、印を**最後のフックの時刻**から `yMMDD-HHmm` の形で組む。
時刻が一分ずれれば別の印になる。知らせの鍵（`$key`）も同じ材料（`wait:<フックの時刻>`）だったため、
**印と鍵が揃って変わり、二度目が新しい待ちとして出た**。

**直した形** — 印を組む前に `yoshi-open.tsv` を見て、**同じ件なら元の印をそのまま使う**。
**鍵も印から組む**（`wait:<印>`）ようにしたので、印を使い回せば知らせも出ない。

> **同じ件かの見分け方（一行）** — **件名（`件名:`）と待ち先（`待ち:`）の二つが揃って一致すれば同じ件**。
> 片方では足りない——**件名は続きの回でも同じまま**で、**待ち先は文言を直せば変わる**。

そのため `yoshi-open.tsv` に**四つ目の欄（待ち先）**を足した。
**三欄しかない古い行は、待ち先を比べようが無いので件名だけで同じ件とみなす**（後方互換）。
読み手（`inbox-watch.ps1`）は一つ目の欄しか見ないので、欄が増えても壊れない。

**写しで確かめた6通り**（本体には仕掛けを入れていない）

| 場合 | 結果 |
|---|---|
| 三欄・件名一致 | 元の印を使い回す |
| 四欄・件名と待ち先が一致 | 元の印を使い回す |
| 四欄・件名一致・**待ち先違い** | **新しい印**（別の件と見る） |
| 件名が違う | 新しい印 |
| 一覧が空 | 新しい印 |
| 待ち先の前後に空白 | 使い回す（`Trim()` で吸収） |

**手順は作法29のとおり** — 写し `.bak-20260827` を取る → 直す → `PSParser` 構文OK →
**1564行 → 1594行**（足した31行・置換1行。関数の重複なし）。膨らみ方の事故（`$'`）は起きていない。

＊`yoshi-open.tsv` に残る `y0826-2150` ／ `y0826-2151` は、この不具合そのものの残骸。
　**手で落とさない**——年限3日（`OPEN_KEEP_DAYS`）で 08-29 に自分で落ちる。

＊効き目が出るのは**次にヨシ待ちで止まる回**から。今回は待ちが無いので通らない。

### 3. 台帳の残り — 5件（`稽古-1` だけではなかった）

| 番号 | 項目 | 状態 |
|---|---|---|
| 積み残し-1 | 目盛りの実戦較正 | 未了：**着手の合図待ち** |
| 観察-1 | 生存の欠け率の報告（起点 795本中17回＝2.1%） | 未了：**2026-08-29** |
| 観察-2 | 押し送り件数の報告（起点 08-25=9件・08-26=33件） | 未了：**2026-08-29** |
| 観察-3 | 逆向きの疑いの件数の報告（起点 0件） | 未了：**2026-08-29** |
| 稽古-1 | `ClaudeHomeBackup` の初回実行の見届け | 未了：**2026-08-27 03:30:30**（03:23 時点でまだ来ていない） |

＊`凍結-0`（発効中）と `再起動-1` ／ `再起動-2`（`断線-5` へ統合）は、
　四つ目の欄が「済」で始まらないので機械の列挙には出るが、**待っている仕事ではない**。

## 九．確認の3件（2026-08-27 03:44〜）

### 1. 済んだ件で超過が鳴った — 閉じる側を入れた

**何が起きたか** — **03:19:44**、済んで検収も終えた件で
**「再起動の稽古の報告・終了予定23:11を過ぎています（経過272分）」** が鳴った。

**なぜ** — **閉じる側が無かった。** 開始の時計（`work-started.txt`）を**打ち直す**条件は
入れてあったが（件名が変わったとき／時計が空のとき）、**作業が終わったときに落とす処理が無い**。
そのため 22:46 に打たれた時計が、作業が終わり検収も済んだあとも残り続け、
**03:19 に別の作業で動き出した瞬間**、まだ件名を書き換える前だったので古い時計がそのまま読まれた。

＊締め-1 の門（`$alive -ne 0`）が効いていなかったのではない。あちらは**窓が落ちている回に
　打ち直さない**ための門で、**終わった回に落とす**のは別の穴だった。**塞ぐ場所が違う。**

**直した形**

```powershell
if ($key -like 'done:*') {
  Remove-Item $startPath            # 開始の時計を落とす
  Remove-Item 'mikomi-over.txt'     # 一発だけの印も落とす
  $prevSubj = ''; $startedAt = 0
}
if ($key -notlike 'done:*' -and $alive -ne 0 -and (...)) {   # done: の回は打ち直しもしない
```

> **再発防止（一行）** — **「終わり」を出した回（鍵が `done:`）で開始の時計を落とし、打ち直さない。**
> 次に作業が始まった回で本当の開始が打たれる。

**落とすのを `done:` の回だけに限った理由** — 状態は「作業中」と「手待ち」を**毎分行き来する**ので、
「作業中でない回に落とす」形にすると経過そのものが数えられなくなる。
**打ち直しもしない**のは、終わった時刻が次の作業の開始として残ると、そのぶんずれて数え始めるため。

**写しで確かめた6通り**（本体には仕掛けを入れていない）

| 場合 | 結果 |
|---|---|
| 終わりで止まる（`done:`） | **時計を落とす**・打ち直さない |
| 済んだ後に作業を再開（`run:`・時計が空） | **打ち直す**（← 03:19 の事故はここで防がれる） |
| 作業が続いている（件名も同じ） | 触らない |
| 件名が変わった | 打ち直す |
| 窓が落ちている（`$alive -eq 0`） | 触らない（締め-1 の門） |
| ヨシ待ち（`wait:`） | 触らない |

**手順は作法29のとおり** — 写し `.bak-20260827` → 直す → `PSParser` 構文OK →
**382行 → 398行**（足した17行・置換1行・関数の重複なし）。

＊いまの時計は正常（`at=03:24:57`）。03:46:37 の「終了予定03:44・経過21分」は
　**見込み20分の作業が21分かかった**という正しい知らせで、事故ではない。

### 2. 締め-1 と締め-6 の状態（一行ずつ）

| 番号 | 状態 |
|---|---|
| **締め-1** | **済み。**`yoshi-open.tsv` に `y0826-topic` は**0件**、窓の落ちた回に打ち直さない門は `inbox-watch.ps1` に入っている。**ただし「終わった回に落とす」穴は残っていた**ので、上の九-1 で塞いだ |
| **締め-6** | **済み。** 凍結は台帳の `凍結-0` が**発効中**、記憶にも `freeze-2026-08-26` として残る。仕事は**修理と観察報告だけ**で、観察の初回は **2026-08-29** |

### 3. 台帳の残り（全件）

| 番号 | 項目 | 状態 |
|---|---|---|
| 積み残し-1 | 目盛りの実戦較正 | 未了：**着手の合図待ち** |
| 観察-1 | 生存の欠け率の報告（起点 795本中17回＝2.1%） | 未了：**2026-08-29** |
| 観察-2 | 押し送り件数の報告（起点 08-25=9件・08-26=33件） | 未了：**2026-08-29** |
| 観察-3 | 逆向きの疑いの件数の報告（起点 0件） | 未了：**2026-08-29** |

**残り4件。** `稽古-1` は下の九-4 で済んだ。
＊`凍結-0`（発効中）と `再起動-1` ／ `再起動-2`（`断線-5` へ統合）は、四つ目の欄が「済」で
　始まらないので機械の列挙には出るが、**待っている仕事ではない**。

### 4. `ClaudeHomeBackup` の初回 — 走った

> **03:30:30 に実行、結果 0（成功）。03:31:34 に「押した：控え: 2026-08-27（**7件の変更**）」** — commit は `a1d550c`。

次回は **08-28 03:30:30**。押した日付の印（`home-backup-last.txt`）も `2026-08-27` に更新されている。
**控えの自動押し出しは効いている。** `稽古-1` はこれで済み。

## 十．パネル v38 と矛盾ふさぎ4件（2026-08-27 04:20〜）

### 0. パネル v38 — 「全部写す」に件数を出した

釦の字を **`全部写す（N件）`** にした。**N の数え方は一覧に出している札と同じ**（`shown`）——
写した札は一覧から落ちる作りなので、画面に見えている枚数と釦の数が食い違わない。
**0件のときは押せなくし、「写す物なし」** と出す。版は三箇所同時（`PANEL_VER` ／ `verTag` ／ `panel-ver.txt`）。

**写しに probe を差して駆動した実測**（作法14。本体には仕掛けを入れていない）

| 場面 | 釦の字 | 押せるか | 一覧の件数 |
|---|---|---|---|
| 読み込み直後 | **全部写す（20件）** | 押せる | 20（**一致**） |
| 全部写した後 | **写す物なし** | **押せない** | 0 |

**`node check-all --fast` … 両方PASS。**

### 1. `done:` の直しが効いていなかった — 常駐が古い台本のままだった

**何が起きたか** — 03:56 に「終わり」を出した件が **04:31 でも「作業中・終了予定04:10」** のまま残り、
**04:30 に超過が鳴った**。前の回に入れたばかりの「`done:` で時計を落とす」直しが効いていない。

**なぜ** — **走っていた常駐が、直しより前の起動だった。**

| | 時刻 |
|---|---|
| 常駐 `inbox-watch.ps1` の起動 | **08-26 22:14:27** |
| 台本を直した時刻 | **08-27 03:46:59** |

`inbox-watch.ps1` は**立ち上げたときの中身のまま走り続ける**。ファイルを直しても、
走っているプロセスには**一切効かない**。毎分呼び直される `watch-notify.ps1` とは扱いが違う。

**やったこと** — 古い常駐を止め、残っていた `work-started.txt` と `mikomi-over.txt` を落とし、
`ClaudeInboxWatch.vbs` 経由で起こし直した。**PID=2256・04:48:30 起動**（台本 04:47:50 より後）で**1本**。

**`CLAUDE.md` の作法29 へ⑤を足した**

> 5. **常駐（`inbox-watch.ps1`）を直したら、走っている常駐を起こし直す**

＊効いたかの確かめ方は「**常駐の起動時刻 ＞ 台本の更新時刻**」。数えるときは
　**`-File …inbox-watch.ps1` で終わる**ものだけを当てる。ゆるく数えると**自分の診断コマンドが
　引っかかり、二本走っているように見える**——台本の側も同じ注意書きを持っており、**今回そのまま踏んだ**。

### 2. 「最終確認」が凍る — 書く場所は一箇所だった

**確認の結果** — `state.json` を書くのは **`inbox-watch.ps1` の `Publish-Status` ただ一箇所**。
`state-line.js` は**読む側**で、書いてはいない。**寄せる先は既に一箇所だった。**

**凍る本当の理由** — 書き直すかどうかを

```
$stat | $subj | $todo | $startedAt | $mikomi
```

の突き合わせだけで決めていた。**控えの中身が入っていない**ので、
**件名も状態も変えずに控えだけ直した回——つまり報告を書いた回——には書き直されない。**
`at` は書き直したときしか動かないため、そこで凍る。

**直した形** — 突き合わせへ**控えの指紋**（`work-note.txt` の中身の MD5 頭8桁）を足した。
**「開始:」の行は除く**——回のはじめに機械が刻むので、入れると毎回書き直しになり、
「変化があったときだけ」の決まり（2026-08-23 の394件の反省）が崩れる。

**実測**

| 試し | `at` |
|---|---|
| 件名を切り替えた回 | 1787773713 → **1787773770**（動いた） |
| **件名は変えず控えの中身だけ直した回** | 1787773770 → **1787773804**（**動いた**） |

＊昨夜 20:31 で数時間凍っていたほうは、**常駐が落ちていた**ぶん（別件・既報）。

### 3. 控えが無い回に、当てにならない値が出ていた

**何が起きたか** — 「この回は控えを書いていません（中身は当てになりません）」と断りながら、
その下に**前の回のファイル欄・実測欄がそのまま並んでいた**。

**なぜ** — ⑧そのままだけが `stale` を見ており、**ファイル・実測・実機・未検収は見ていなかった**。

**直した形** — **五欄すべてを `-and -not $note.stale` で揃えた**。
`done:` の知らせと、入力待ち（`notification`）の知らせの**両方**に同じ穴があったので両方直した。

＊**ファイルだけは、控えが古くても `git` の差分から拾える。** こちらは当てになるので出すが、
　前の回の控えと取り違えられないよう **`（git の差分から）` と出どころを添える**。

### 4. 「写した札」と「届いた」の差2件

**打ち止め帯には当たらない。** 押し送りは通っており（**HTTP 200**・本日3件目）、
`notices.json` も **20件**が 04:30:04 まで揃っている。**送れなかった知らせは無い。**

**「ここまで読んだ」由来でもない。** `seen.json` は `upto: 0` ＝この仕掛けは使われておらず、
「チャットで読んだ」の札は**0件**。

**残る説明は「届いた」の判じ方そのもの。** 印が付くのは
**`lastResume`（`usage.json`）が、その札を写した刻より後**のときだけ。
`lastResume` は「写した本文を送って Claude Code が動き出した」ことの証で、
**04:29:35 で止まっている**（`hook.log` の `resume` も同じ）。したがって差の2件は次のどちらか。

| 見分け | その2件の時刻 | 何が起きているか |
|---|---|---|
| **写しただけで送っていない** | 今日の新しい時刻 | 本文を送って Claude Code が動けば付く |
| **v31 以前の古い印** | 数日前 | 刻が `1` で入っており判定から外れる。印を消して出し直せば消える |

**どちらかはパネル側で分かる**——「写した札」を開いて、印の付かない2件の**時刻**を見る。
＊この2件は**端末の `localStorage` にしかない**ので、VAIO 側からは中身を読めない。

## 十一．手待ちの脈と、出し損ねた答え（2026-08-27 08:49〜）

### 1. 手待ちの間に赤帯だけが育つ — 脈を入れた

**何が起きたか** — 05:10 から**3時間半**、手待ちの間に `state.json` が一度も書かれず、
パネルの「連絡なし」の赤帯だけが育った。**手が空いているだけなのに、止まっているように見える。**

**なぜ** — 書き直すのは「中身が動いたとき」だけ。手待ちの間は状態も件名も動かないので、
`at` が凍る。パネルは `at` の古さで赤帯を出すため、**20分（`STALE_SEC`）で赤帯が点く**。

**この仕掛けは、あるつもりで無かった。**`panel.html` の注記はこう書いていた——

> 見張りは「変化があったときだけ」流す作りで、変化が無い間は15分（900秒）おきに
> 心拍を一本だけ出す。

**その心拍は実装されていない。**`$script:lastStatAt`（最後に書いた時刻）は前から控えて
あったのに、**どこからも使われていなかった**。注記だけが実装を先取りして書かれていた形。

**直した形** — 中身が同じでも、**間が空いたら時刻だけ打ち直す**。

```powershell
$PULSE_SEC = 1800   # 30分
if ($line -eq $script:lastStat -and ($now - $script:lastStatAt) -lt $PULSE_SEC) { return }
```

**閾値も対で揃えた（panel v39）** — 脈が30分なので、閾値が20分のままだと
**脈を打っているのに赤帯が出る**。`STALE_SEC` を **1200 → 2700（45分）** へ。

| | v38 まで | v39 |
|---|---|---|
| 脈（`$PULSE_SEC`） | **無し** | 30分 |
| 赤帯の閾値（`STALE_SEC`） | 20分 | **45分** |
| 手待ち3時間半のとき | **赤帯が出る** | 出ない |
| 本当に止まったとき | 20分で赤帯 | **45分で赤帯** |

**二つは対で決まる値。片方だけ変えない。** 赤帯を早く出したいときは、**脈のほうも一緒に縮める**。
＊代償として、本当に止まったときの気づきが 20分→45分に延びる。**赤帯の意味を
　「手が空いている」から「本当に止まっている」へ寄せたぶんの引き換え。**

**手順は作法29のとおり**（今日足した⑤も踏んだ）— 写し `.bak-20260827c` → 直す →
`PSParser` 構文OK（**413→426行**・足した14・消えた1）→ **常駐を起こし直す**
（**PID=1044・08:51:24**、台本 08:50:29 より後・1本）。`node check-all --fast` **両方PASS**。

### 2. 前の報告で出し損ねた答え

**報告が②の途中で切れていた。** ③④と「残り」欄、締め-6 の状態を改めて出す。

#### ③ 控えが無い回の欄 — 五欄すべてを `stale` を見る形へ揃えた

⑧そのままだけが `stale` を見ており、**ファイル・実測・実機・未検収は見ていなかった**。
「この回は控えを書いていません（中身は当てになりません）」と断りながら、その下に
**前の回の値がそのまま並んでいた**。五欄を揃え、`done:` の知らせと入力待ちの知らせの
**両方**を直した。

＊**ファイルだけは、控えが古くても `git` の差分から拾える。** こちらは当てになるので出すが、
　前の回の控えと取り違えられないよう **`（git の差分から）`** と出どころを添える。

#### ④ 写した札20件・届いた19件 — 差は1件

**前の報告の「差2件」は誤り。実機の数字は 20件と19件で、差は1件。**

**打ち止め帯には当たらない。** 押し送りは通っており（HTTP 200）、`notices.json` も20件が
揃っている。**送れなかった知らせは無い。**「ここまで読んだ」由来でもない（`seen.json` は `upto: 0`）。

**残るのは判じ方そのもの。** 印が付くのは **`lastResume` がその札を写した刻より後**のときだけ。
20件を「全部写す」で一度に写せば刻はほぼ同じなので、**19件に付いて1件だけ付かない**のは、
**その1件だけ写した刻が違う**ことを意味する。

| 見分け | その1件の時刻 | 何が起きているか |
|---|---|---|
| **いちばん最後に写した1件** | ほかの19件より後 | 写したあと Claude Code がまだ動いていない。本文を送れば付く |
| **v31 以前の古い印** | 数日前 | 刻が `1` で入っており判定から外れる。印を消して出し直せば消える |

**パネルの「写した札」を開き、印の付かない1件の時刻をほかの19件と見比べれば分かる。**
＊この印は**端末の `localStorage` にしかない**ので、VAIO 側からは中身を読めない。

#### 締め-6（凍結宣言）の状態 — 一行

> **済み。** 凍結は台帳の `凍結-0` が**発効中**、記憶にも `freeze-2026-08-26` として残る。
> 見張り・通知・台帳へ**新しい機能を足さない**。仕事は**修理と観察報告だけ**で、観察の初回は **2026-08-29**。

＊今回の脈も、③④の直しも**不具合の修理**なので、凍結には当たらない。

#### 残り — 4件

| 番号 | 項目 | 状態 |
|---|---|---|
| 積み残し-1 | 目盛りの実戦較正 | 未了：**着手の合図待ち** |
| 観察-1 | 生存の欠け率の報告 | 未了：**2026-08-29** |
| 観察-2 | 押し送り件数の報告 | 未了：**2026-08-29** |
| 観察-3 | 逆向きの疑いの件数の報告 | 未了：**2026-08-29** |

<!-- 送った知らせ ここから -->

## 送った知らせ

直近に ntfy へ送ったものを、新しい順に五件まで。**要約せず、送った本文をそのまま写しています。**

### 1. 🕒 延びています

```
矛盾ふさぎ4件（常駐の起こし直し・最終確認の凍り・控えなしの欄・写した札の差）
終了予定05:55を過ぎています（経過218分）
```

### 2. ✅ 終わりました（返事不要）

```
矛盾ふさぎ4件（常駐の起こし直し・最終確認の凍り・控えなしの欄・写した札の差）
①走っていた常駐が 08-26 22:14 起動＝台本の直し（03:46:59）より前だったので、止めて時計と mikomi-over を落とし、新しい台本で起こし直した（PID=2256・04:48:30）。CLAUDE.md 作法29 へ⑤「常駐を直したら起こし直す」を足した ②state.json を書く場所は inbox-watch.ps1 の一箇所だけで、state-line.js は読む側。凍る原因は「変化が無ければ書き直さない」条件に控えの中身が入っていなかったこと。突き合わせへ控えの指紋（開始行を除く MD5 頭8桁）を足した ③控えが古い回に ファイル・実測・実機・未検収 が stale を見ずに出ていた。五欄とも stale を見る形へ揃え（done: 側と notification 側の両方）、ファイルだけ git 差分から拾って出どころを添える ④写した札と届いたの差2件は打ち止め帯に当たらない（押し送り HTTP 200・notices 20件・seen は upto=0）。届いたは lastResume>写した刻 で判じるので、写した後に動かしていない2件か v31 以前の古い印 ⑤パネル v38（全部写す（N件）・0件は押せない）⑥push まで着地——控え 68b9235／本体 ff303ef（作法29の⑤・報告書の十）と panel v38。背後の push は ⑯ の途中で止められたが、見張りの自動 push が先に運んでおり追跡は揃っている
ファイル: ~/.claude/inbox-watch.ps1（398→413行）／~/.claude/watch-notify.ps1（1594→1607行）／CLAUDE.md（作法29 に⑤）／panel.html・panel-ver.txt（v38）／report-latest.md（十の節）／写しは両方 .bak-20260827b
実測: 両方 PSParser 構文OK・関数の重複なし。常駐 PID=2256・04:48:30 起動（台本 04:47:50 より後）・1本のみ。state.json の at は 件名を切り替えた回（…713→…770）と 件名を変えず中身だけ直した回（…770→…804）の両方で動いた。パネルは probe で 20件→「全部写す（20件）」／写した後→「写す物なし」・押せない。check-all --fast 両方PASS
実機: ①返事パネルの版の字が panel v38 で、釦が「全部写す（N件）」になっていること（写す物が無いときは「写す物なし」で押せない）②現況の「最終確認」が、報告を出すたびに新しい時刻へ動くこと
```

### 3. 🕒 延びています

```
確認の3件（超過の閉じ忘れ・仕舞いの答え・HomeBackup の初回）
終了予定04:10を過ぎています（経過39分）
```

### 4. ✅ 終わりました（返事不要）

```
確認の3件（超過の閉じ忘れ・仕舞いの答え・HomeBackup の初回）
①03:19:44 の「終了予定23:11を過ぎています（経過272分）」の原因は、開始の時計を打ち直す条件はあったが「終わったときに落とす処理」が無かったこと。inbox-watch.ps1 に done: の回で work-started.txt と mikomi-over.txt を落とし、打ち直しもしない形を入れた（締め-1 の門は「窓が落ちた回に打ち直さない」ためのもので、塞ぐ場所が違っていた）②仕舞い-1と3の答えを報告の本文に書いた（締め-1・締め-6 の状態を一行ずつ／台帳の残りを全件）③ClaudeHomeBackup の初回は 03:30:30 に実行・結果0・7件（a1d550c）。稽古-1 は済み
ファイル: ~/.claude/inbox-watch.ps1（382→398行・写し .bak-20260827）／~/.claude/orders-open.tsv（確認-1・2・3 を積んで済へ・稽古-1 も済へ）／report-latest.md（九の節を追加・先頭も差し替え）
実測: PSParser 構文OK／足した17行・置換1行・関数の重複なし／判定を写しで6通り確認——done:=時計を落とす・打ち直さない／済んだ後の再開=打ち直す／作業継続=触らない／件名変更=打ち直す／窓落ち=触らない／ヨシ待ち=触らない。いまの時計は at=03:24:57 で正常。03:46:37 の「終了予定03:44・経過21分」は見込み20分に対する正しい知らせ
実機: 次に「終わり」で止まったあと、しばらく置いてから新しい作業を始めた回で、古い件名の超過知らせが鳴らないこと
```

### 5. 🕒 延びています

```
仕舞いの3件（締めの状態・印の使い回しの修理・残りの列挙）
終了予定03:44を過ぎています（経過21分）
```

<!-- 送った知らせ ここまで -->






















