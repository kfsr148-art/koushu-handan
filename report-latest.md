状態：作業中 ／ 2026-08-20 23:45 ／ 一索の鳥のライフ制の下調べ（見込み00:15終了）。1322行の範囲指定は保留のまま

【保留】
- **1322行の説明文の修正範囲**（この報告の§1。(a) 1番だけ／(b) 枝豆の4つ＋地の文）← いま待っているもの
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（目盛り画像は焼いた。§2 のとおり）
- `sizing-review.png` を残すか消すか（中身は §3 に一行で書いた）

# 報告：説明文と実行時の食い違いの洗い出し／目盛り画像の設置／sizing-review.png の素性

日付 2026-08-20 ／ 本体の版 **v1417**（1322行の修正はヨシ待ちのため未着手）

---

## 1.【1】説明文と実行時の食い違い — **17件見つかった**

### 照合のやり方

説明文の中の「…」の引用を機械で拾い、**同じ文字列が実行時のコードに在るか**を突き合わせた。
絵文字と空白は落として比べ、数字（「4組」など）は伏せて雛形でも見た。
照合した範囲は**猫牌の説明（1322行）と読み物パネル（`INFO_DETAIL` 4612〜4659行）**。
拾った引用は25件、うち**17件が実行時に見当たらない**。

### ご指示の1322行 — 枝豆のくだりは**4件すべて食い違っている**

ご指摘は1件だったが、同じ欠陥が4つ並んでいた。

| # | 説明文（1322行） | 実行時の文言 |
|---|---|---|
| 1 | 「🌱枝豆も**本判定と一致、ダブルヨシ**」 | `N組。揃い` |
| 2 | 「🌱枝豆は**攻めと言ったが、本判定は守り**──珍しい食い違い」 | `N組。だが本判定は守——珍しい食い違い` |
| 3 | 「🌱枝豆も**薄い、守りヨシ**」 | `N組。薄い——守り` |
| 4 | 「🌱枝豆は**守りと言ったが、本判定は攻め**──珍しい食い違い」 | `N組。薄いが本判定は攻——珍しい食い違い` |

加えて**地の文に「ここまでは常にダブルヨシが出る計算になります」が1件**。

**1・3・地の文の計3箇所は §11 の専有語（「ヨシ」は現場猫だけ）にも触れている。**
`check.js` の ⑨ が通っているのは、⑨ が見張る対象に説明文が入っていないため。

### 直す案（ヨシ待ち）

実行時の字面へ合わせる。**実行時の文言は一文字も変えない。**

- 1 →「🌱4組。揃い」
- 2 →「🌱4組。だが本判定は守——珍しい食い違い」
- 3 →「🌱0組。薄い——守り」
- 4 →「🌱0組。薄いが本判定は攻——珍しい食い違い」
- 地の文「常にダブルヨシが出る計算」→「常に一致が出る計算」

**範囲の指定をお願いします。(a) 1番だけ ／ (b) 枝豆の4つ＋地の文（推奨）。**
版は v1418 へ上げ、フル版 check-all まで回す。見込み約25分。

### 今回は直さない残り（一覧）

**ご指示どおり、挙げるだけで手を付けていない。**

**■ 現場猫のくだり（1322行）— 3件**

| 説明文 | 実行時の文言 |
|---|---|
| 「本判定と一致、ダブルヨシ」 | `猫牌N枚、指差し確認——ダブルヨシ！` |
| 「ヨシと言ったが危ない手」 | `⚠️現場猫アラート：ヨシと言ったが、危ない手` |
| 「逃げ腰だが実は行ける手」 | `⚠️現場猫アラート：逃げ腰だが、実は行ける手` |

いずれも**頭の「⚠️現場猫アラート：」と読点が落ちている**だけで、意味は合っている。
「ダブルヨシ」は現場猫の言葉なので §11 の問題は無い。

**■ 紳士のくだり（1322行）— 2件**

| 説明文 | 実行時の文言 |
|---|---|
| 「🎩わたしの見立ても本判定と一致」 | `一枚きりの字牌N枚。わたしの見立ては攻め／守り——一致です` |
| 「🎩見立ては攻／守でしたが、外れました。本判定は…」 | `見立ては攻／守でしたが、外れました。本判定は攻／守です`（🎩は付かない） |

**下の1件は中身がほぼ合っている**（行頭の🎩が説明文だけに付いている）。上の1件は要約になっている。

**■ 用語の言い換え（1322行）— 7件**

「数牌（スウハイ）の孤立牌（コリツハイ）1枚」「形が整う割合」「形の整った面子（メンツ）候補」
「良形（リョウケイ）の塊が4つ以上」「必要数4」「実用上ほぼ消える境目」「理屈の上でも不可能になる境目」

**これらは実行時の文言の引用ではなく、説明のための言い換え**（鉤括弧を強調に使っている）。
**食い違いではないので、直す対象ではないと考える。**

**■ 読み物パネル — 1件**

4620行「誰でも気軽に」。これも実行時の引用ではなく、説明の中の言い回し。**対象外と見る。**

### まとめると

**本当に直すべき食い違いは、枝豆の4件（＋地の文1件）と、現場猫の3件、紳士の1件。**
残る9件は言い換えや強調で、食い違いではない。

---

## 2.【2】目盛り画像を置いた — `head-proof.js`

**`asset-proof.js` と同じ流儀**で、リポジトリ直下に道具と二枚組を置いた。**push 済み**（`0b43586`）。

| | |
|---|---|
| 道具 | **`head-proof.js`** |
| 出力 | **`head-proof-dark.png`**（本番と同じ暗背景 `#0d1b16`）／**`head-proof-light.png`**（白地） |
| 大きさ | 2636 x 2486 ／ 各1.6MB |
| 中身 | **振りの八枚**（u / ur / r / dr / d / dl / l / ul）＋**基準の三枚**（idleRight / breathR / action）の計11枚 |
| 目盛り | 元画像の画素で **10px ごとの細線、50px ごとに太線と数字**。倍率2倍 |
| 添えたもの | **`EDA_FOOT` の足元の行**を赤い横線で一本引いてある |

**素材は本体の字面から拾っている**（`EDA_WHIP` / `EDA_NAGE` / `EDA_FOOT` を `const … = {` の
ブロックから正規表現で取る）。**実行時の内部は覗いていない**——`asset-proof.js` と同じやり方で、
作法§14・§15 に沿う。焼くのは headless の Edge。

**道具の頭に、なぜ要るかを書いた。** 振りの八枚は弧が頭に重なるため色による自動判定が効かず
（髪が影に沈み、弧が金髪の色域に入る）、`char_export.js` の「顔比」も全身の上から5分の1の帯で
最も広い行を測る作りなので八枚では弧を拾う。**目盛りを重ねて目で読むしかない**、という経緯。

**走らせたときの締めに読み方を出す。**

```
読み方：頭＝耳を除いた髪と顔の輪郭の、いちばん広い行の幅。
　　　　輪郭のぼかしは数えず「芯」で読む（外まで取ると2〜3px多く出る）。
　　　　基準の三枚は idleRight 52 ／ breathR 49 ／ action 59（2026-08-20 の目視読み）。
```

**作法書（§21）への追記はしていない。** §21 は `asset-proof.js` を名指しているので、
`head-proof.js` も並べて書くのが筋だが、**頼まれていないので触っていない**。要否をご指示ください。

---

## 3.【3】`sizing-review.png` とは

**2026-08-19 02:06 に作った、剣士の全コマを本番と同じ暗背景（`#0d1b16`）で足元を揃えて
横一列ずつ並べた「大きさ比べの一覧」**（2366 x 5140・4.7MB・未追跡のまま）。

直立・斬撃・降下・上り・走り・投げ・喜びが段ごとに並び、**同じ姿勢の候補が横に何枚も置いてある**。
v1411（`c93ac8a`。体の見かけの物差しへ変えた回）で**倍率を目で決めるために使った確認用シート**と
見てまず間違いない——日付も作業時期と合う。ただし**生成元のスクリプトがリポジトリに無い**ので、
用途の断定まではできない（画像の中身と日付からの推定）。

**`git` には一度も入っていない。今回も消していない。** 残すか消すかの裁定をお願いします。

**なお、今回置いた `head-proof.js` が、この用途の一部を引き継げる。** あちらは道具が残るので、
いつでも焼き直せる。`sizing-review.png` を消しても、同じものが要るなら道具から作れる形にはなった
（ただし `sizing-review.png` は全コマ、`head-proof` は11枚なので、そのままでは代わりにならない）。

---

## 4. 触れていないこと

- `koushu-handan.html` / `check.js` / `ver.txt` / `serifu.txt` は**すべて無変更**（v1417 のまま）。
- 版番号は上げていない。`analyze()` / `bestShapeOneSuit` にも触れていない。
- **`sizing-review.png` は消していない。**
- 1322行の修正は**ヨシ待ちのため未着手**。
- **テスト用のシムなし。**

---

## 5. 次

- **1322行の修正範囲の指定**（(a) か (b)）。いただければ v1418 として着手する。
- 保留の残り三件は冒頭の一覧のとおり。
- v1417 の実機検収もお待ちしています。
