状態：終わり ／ 2026-08-20 19:20 ／ 書き込み失敗の調査（原因はWriteツールの防護。権限・パスは正常）と、読み直し確認の決まりを追加

# 報告：v1416 の追検証 — ⑦の効きを前後で確かめ、フル版を通した

日付 2026-08-20 ／ 対象 v1416 ／ **本体は無変更**

裁定でいただいた【1】案Bの二本立てと【2】⑦への視野追加は、**v1416（`d5d5e9f`）で既に実装済み**だった。
そのため今回コードは一行も変えていない。**版番号も上げていない**——中身が同じまま版だけ進めると、
利用者に実質同一の新版が配信されるため。

やり残していたのは【2】の後半、「**新しい視野を入れた ⑦ が、修正前の実装なら落ちること**」の確認。
前回の報告は帯の bottom を v1415／v1416 で並べた実測表までで、検査そのものを旧版に当てていなかった。
そこを埋めたのが今回。

---

## 0. 書き込み失敗の調査（この回の用件）

画面に `Write(report-latest.md) └ Error writing file` が出た件を調べた。**結論から言うと、
ディスクへの書き込みは失敗しておらず、報告書の中身は正しく入っていた。**

**エラーの正体。** 失敗したのは Write ツールの一回の呼び出しで、本文は

```
File has been modified since read, either by the user or by a linter.
Read it again before attempting to write it.
```

**書き込みが拒まれたのではなく、「最後に読んだ内容と実ファイルが食い違っている」という
ツール側の防護**が働いたもの。直前に状態行を Bash（`node -e`）で二度書き足していたため、
ツールが把握している内容と実体がずれていた。その直後に Bash のヒアドキュメントで書き直しており、
そちらは成功している。

**権限・パス・ロックを実地で確かめた**（推測で切らないため）。

| 調べたもの | 結果 |
|---|---|
| パス | `C:\Users\user\Desktop\mahjong\koushu-handan\report-latest.md`（正しい） |
| 読み取り専用属性 | `IsReadOnly: False` / `Attributes: Archive` |
| 権限 | `VAIO\user` に `FullControl` |
| 実地の書き込み | 追記・復元とも**成功**（6684バイトへ復元） |
| 他プロセスのロック | 排他オープン（`ReadWrite`/`None`）**成功** |

**どれも問題なし。** 原因は書き込み手段の食い違いだけだった。

**中身が入っていることを三方向で確認した。**

| 確認先 | 先頭行 |
|---|---|
| ローカル（19:00:41 更新・135行） | `状態：終わり ／ 2026-08-20 18:59 ／ …` |
| コミット `c0d56d4` | 同じ |
| GitHub の raw URL | 同じ（見出しも「v1416 の追検証」） |

**手当て（今後の決まり）**

- **`report-latest.md` の書き込みはヒアドキュメントに一本化する。** Write ツールと Bash を
  同じファイルに混ぜない。混ぜると必ずこの防護に当たる。
- **「更新済み」と書く前に必ず読み直す。** push のあとに raw URL を取り直し、書いたはずの内容が
  返ってきたことを確かめてから知らせを出す。
- **失敗したときは知らせに「報告書の更新に失敗」と明記する。** 成功したことにして流さない。

---

## 1. 裁定の内容が入っていることの確認

| 項目 | 現物 | 場所 |
|---|---|---|
| 【1】案Bの二本立て | 入っている | `koushu-handan.html:1142-1143` |
| `orientation: landscape` の残置 | 残っている | 同上 |
| 【2】⑦の視野に 900x340・900x381 | 入っている（900x381 は速い版にも） | `check.js:626-627` |
| ⑦の合格条件 | **未変更** | — |

```css
@media (orientation: landscape) and (min-width: 844px) and (min-height: 350px) and (max-height: 380px),
       (orientation: landscape) and (min-width: 844px) and (min-height: 384px) {
```

```js
const VIEWS = FAST ? [[568, 320], [900, 381]]
                   : [[568, 320], [667, 375], [844, 390], [932, 430], [900, 340], [900, 381]];
```

---

## 2. ⑦の効き — 修正前は落ちる

v1415（`c3edeb3`）の本体を一時ディレクトリへ取り出し、**現行の** `check.js` をフル版で当てた。
`check.js` は旧版を指定して回せる作りになっている（`check.js:20` に「旧版に当てて検査そのものの
効きを確かめるために要る」と明記）。

**結果：⑦ FAIL。落ちたのは追加した二視野だけ。**

| 視野 | judged の結果 |
|---|---|
| 568x320 | 溢れなし |
| 667x375 | 溢れなし |
| 844x390 | 溢れなし |
| 932x430 | 溢れなし |
| **900x340** | **✗ 8件** |
| **900x381** | **✗ 8件** |

**既存の4視野は素通りしていた。** つまり v1415 の不具合は、追加前の ⑦ では原理的に捕まえられない。
視野を足したことで初めて見えるようになった、という関係がそのまま出た。

**同じ走りで①も落ちているが、これは想定内。** `check.js` の `ver.txt` の読み先はリポジトリ直下に
固定されているため、v1415 の本体（`data-ver="1415"`）と直下の `ver.txt`（1416）が食い違う。
旧版を当てるときは必ずこうなる。**見るのは⑦だけ。** 他の18項目は全て PASS だった。

---

## 3. ⑦の効き — 修正後は通る（【1】＝フル版 check-all）

現行 v1416 に `node check-all`（**フル版**）。

| | 結果 |
|---|---|
| `check.js` 本体 ①〜㉑ | **全PASS**（✗ 0件） |
| ⑦ 狭い画面での溢れ（**6視野 × 9画面 ＝ 54組**） | **PASS**（54 / 54 を実測） |
| ⑬ 非日本語文字の混入（ゼロ基準） | **PASS** |
| `adv-check.js` 探偵編の回帰（即死罠・時間切れ） | **全PASS** |

**判定後の二択（連チャン／親流れ）の bottom は全視野で視野内。**

| 視野 | 二択の bottom | 視野の高さ | 余白 |
|---|---|---|---|
| 568x320 | 317.8 | 320 | −2.2 |
| 667x375 | 345.3 | 375 | −29.7 |
| 844x390 | 387.5 | 390 | −2.5 |
| 932x430 | 382.0 | 430 | −48.0 |
| **900x340** | **327.8** | 340 | **−12.2** |
| **900x381** | **380.5** | 381 | **−0.5** |

**追加した二視野は、落ちていた側から余白のある側へ移った。** 900x340 は v1415 で +4.3px 画面外
だったものが −12.2px に、900x381 は +1.5px 画面外だったものが −0.5px になっている。

### 余白の薄い場所（引き続きの注意）

- **900x381 は −0.5px。** 一段に落ちたぶんで収まっているだけで、余白は半画素しかない。
  ここは**速い版にも入れてある**ので、レイアウトを触れば毎回引っかかる。
- **844x390 は −2.5px、568x320 は −2.2px。** どちらも既存の視野で、従来から薄い。

---

## 4. 版表記と serifu.txt（【2】）

**版表記3箇所は一致していた。**

| 箇所 | 値 |
|---|---|
| `koushu-handan.html` の `data-ver` | 1416 |
| `koushu-handan.html` の `verTag` div | v1416 |
| `ver.txt` | 1416 |

**`serifu.txt` は本体と一致していた。** `node serifu-extract.js --check` が「一致しています」を返した。
指示どおり `node serifu-extract.js` で再抽出も回したが、**差分ゼロ**（7593行・**口調の混入 0件**）。
台詞を触っていない回なので当然の結果だが、確認として回してある。

---

## 5. 触れていないこと

- `koushu-handan.html` / `check.js` / `ver.txt` は**無変更**。この報告書だけが変わっている。
- `analyze()` / `bestShapeOneSuit` には触れていない（作法§5）。
- **テスト用のシムなし。** ⑦を旧版に当てるための取り出しは一時ディレクトリで行い、
  本体には何も入れていない。`window` に診断口も残していない（作法§15）。
- 作業ツリーには未追跡の `sizing-review.png` が残ったまま。この回では扱っていない。

**見送った件：Windows のトースト通知フック。** スマホへの知らせが通るようになり、
報告書の先頭に状態行も入って気づく経路が二つになったため、導入しないことになった。
`BurntToast` は**インストールしていない**（`Get-Module -ListAvailable` の読み取りのみ実行）。
`~/.claude/settings.json` も**未編集**。

---

## 6. 次

- **v1416 の実機検収。** OK が出れば `stable` を v1416 へ進める（作法§19。いまは v1377 `3aa4b5d` を
  指したまま）。**どの版が検収済みかはこちらでは決めない**ので、実機で確かめた版を言ってほしい。
- 申し送りの**振りの八枚の焼き直し**は指示待ちのまま。
- 投票欄の五人目（鳥）の材料は「手役の芽」案では**引けない**と出た（前回の測定）。別の材料で
  測り直すなら指示をください。
- `sizing-review.png` を残すか消すかの裁定。
