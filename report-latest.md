状態：終わり ／ 2026-08-20 22:48 ／ v1417 枝豆の印を🫛→🌱に替えた（7個）。フル版check-all全PASS、四行24pxのまま、二択も不動

【保留】
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（u57 / ur55 / r54 / dr55 / d51 / dl54 / l57 / ul47）
- 頭の幅の目盛り画像をリポジトリへ置くかどうか
- `sizing-review.png` を残すか消すか

# 納品：v1417 枝豆の印を 🫛 から 🌱 へ替えた

日付 2026-08-20 ／ 版 **v1417** ／ 前版 v1416（`d5d5e9f`）

**枝豆の注記行が「上下に切れて」見えていた件の手当て。** 切れていたのではなく、
**🫛 がこの端末のフォントに無く、代替表示の四角（豆腐）が出ていた**（前回の報告で確定済み）。
裁定どおり**案A**を採り、出る絵文字へ置き換えた。

---

## 1. 入れた変更

**`koushu-handan.html` の 🫛 を 🌱 へ。7個すべて。他は一文字も触っていない。**

| 行 | 場所 | 個数 |
|---|---|---|
| 1322 | 猫牌の説明文（読み物パネル） | **4** |
| 4005 | `tenLeans` の `mark`（**投票欄のチップ**） | 1 |
| 4458 | `sayIconHTML` の `{ eda: … }`（**注記行の行頭**） | 1 |
| 7551 | 見張り台の `AV = { … eda: … }` | 1 |

置換後、**`🫛` の残りは0個、`🌱` は7個**。一括置換を使ったが、対象がこの文字だけであることを
数で確認してから行っている（作法5の「全件を意図的に置き換える」）。

**🫘 は使われていなかった。** 本体・`check.js`・作法書・台本・`adv-check.js`・`serifu-extract.js`・
`README.md` のいずれも**0件**。替える対象は無い。

**作法書は変更なし。** §11 は名前（現場猫・枝豆・紳士・兎）だけ、§20 の表は「緑 `var(--jade)`」と
`eda-mark` だけで、**枝豆の絵文字はどこにも書かれていない**（`CLAUDE.md` の 🫛 も0件）。
直す箇所が無かったので触っていない。

---

## 2. ご指定の確認 — **四行とも24pxのまま、二択も動いていない**

### 注記四行の高さ（変更前 → 変更後）

| 行 | 568x320 | 844x390 |
|---|---|---|
| 現場猫 | 24px → **24px** | 24px → **24px** |
| **枝豆** | 24px → **24px** | 24px → **24px** |
| 紳士 | 24px → **24px** | 24px → **24px** |
| 人柄 | 24px → **24px** | 24px → **24px** |

**行の top / bottom も一致**（568x320 で 59/87/115/143、844x390 で 92.6/120.6/148.6/174.6）。
font-size・line-height・margin-top・flex・overflow もすべて前と同値。

### アイコンの描画

| | 変更前 🫛 | 変更後 🌱 | 紳士 🎩（対照） |
|---|---|---|---|
| 描画箱 | **15.3 x 19**（豆腐） | **20.8 x 19** | 20.8 x 19 |
| 箱からの上はみ出し | −1.5 | **−1.5** | −1.5 |
| 箱からの下はみ出し | −3.5 | **−3.5** | −3.5 |

**🎩 と完全に同寸になった。** 箱（内寸22x22）の内側に収まっており、はみ出しは無い。
**画面を撮って目でも確認した**——豆腐の四角が双葉の絵に替わっている。

### 二択（連チャン／親流れ）の bottom — **全視野で不動**

| 視野 | 変更前 | 変更後 |
|---|---|---|
| **568x320** | 317.8 | **317.8** |
| 667x375 | 345.3 | **345.3** |
| **844x390** | 387.5 | **387.5** |
| 932x430 | 382.0 | **382.0** |
| 900x340 | 327.8 | **327.8** |
| 900x381 | 380.5 | **380.5** |

**一つも動いていない。** 行の高さはアイコンの箱（24px）が決めており、中身が絵文字でも画像でも
変わらないため。

---

## 3. 検査

**`node check-all`（フル版。表示に関わる回のため）**

| | 結果 |
|---|---|
| `check.js` 本体 ①〜㉑ | **全PASS**（✗ 0件） |
| ⑦ 狭い画面での溢れ（6視野 × 9画面 ＝ 54組） | **PASS** |
| **⑬ 非日本語文字の混入（ゼロ基準）** | **PASS**（🌱 は絵文字として通る） |
| `adv-check.js` 探偵編の回帰 | **全PASS** |

その他

- **`node --check`**：`<script>` 4ブロックとも構文OK（一時ファイルはスクラッチパッドに切り出し、検証後に削除）
- **`serifu.txt` 再抽出**：`node serifu-extract.js` を実行。**7593行・口調の混入0件**。
  `--check` で本体との一致を確認。差分は**版表記の1行だけ**（台詞は変わっていない）
- **版表記3箇所**を **1417** に同時更新（`data-ver` / `verTag` / `ver.txt`）
- **テスト用のシムなし。** 測定と撮影は写しに probe で行い、写しごと削除した。本体には何も入れていない
- `analyze()` / `bestShapeOneSuit` には触れていない（作法§5・§10）

---

## 4. 範囲外の気づき（触っていません）

**1322行の説明文に、古い言い回しが残っている。**

> 一致すれば「🌱枝豆も本判定と一致、**ダブルヨシ**」

実行時の文言は `6組。揃い` / `薄い——守り` で「ヨシ」を使っておらず、**説明文だけが昔のまま**。
§11 で「ヨシ」は現場猫の専有語と定めた際、実装は直したが説明文が取り残された形に見える。
`check.js` の ⑨ は通っているので検査上は問題にならないが、**読み物としては食い違っている。**
**ご指示の範囲外なので手を付けていない。** 直すなら別途どうぞ。

---

## 5. 記録した決まり（実装はしていない）

- **兎の採点の仕様** — 順位は出さず「①点 ②最善との差 ③最善に並ぶ種類数」の三つ。
  赤5と普通の5は別宣言（34種側は据え置き）。**4枚持ちの牌は34種から除外し、分母もその分減らす**。
  裏で全種類を先に回し、押した一枚の点を先に出して、あとから差と種類数を足す（再入で壊れないこと）。
- **ヨシ待ちの決まり** — 状態行を「ヨシ待ち」にして push。**5分おきに知らせを出し直す（上限12回）**。
  待つあいだも手を止めず、保留を積んで別の項目を進める。保留一覧は状態行のすぐ下。
- **長い処理の決まり** — 十分以上かかるものは、状態行に**見込みの終了時刻**を書く。
  十分ごとに「まだ回っています。残り約◯分」を一度出す。ヨシ待ちの再送とは別物。

---

## 6. 次

- **v1417 の実機検収。** OK が出れば `stable` を v1417 へ進める（いまは v1416 `d5d5e9f`）。
- 保留の四件は冒頭の一覧のとおり。
