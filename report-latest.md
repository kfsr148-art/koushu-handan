# 「版 —／最終確認 —」の原因と直し（panel v33）

**状態：ヨシ待ち（実機で見てください）** — 2026-08-25 21:2x 時点

**結論** — 原因は特定して直しました。ただし **v32 の巻き添えではありません**。
この抜けは **panel v18 から入っていた**もので、**`state.json` が古くなって「連絡なし」になったときだけ**
表に出ます。ご覧になった時点がその状態だった、というのが実際のところです。

**ついでに二つ目の取りこぼしも見つけて直しました**（開いた直後、「公開 v」が最大15秒ぶん未確認のまま）。

---

## 1. 原因

`pullState()` の中で、**「連絡なし」の早戻りが `paintFacts()` より前**にありました。

```js
if (sec > STALE_SEC) {          // 20分より古ければ「連絡なし」
  paintState('lost', '連絡なし', …);
  return;                       // ← ここで戻るので…
}
…
paintFacts(d.at || 0);          // ← 版と最終確認を塗るのは、この先だった
```

**連絡なしの間は `paintFacts` が一度も走らず**、`#fVer` と `#fSeen` は
HTML に書いた初期値「版 —」「最終確認 —」のまま残ります。

**panel v32 のせいではありません。** `git log -S` と `git show a4025ee:panel.html`（v30）で確かめました。

| 版 | `paintFacts` の位置 | 早戻りの位置 |
|---|---|---|
| **v30**（a4025ee） | 959行 | 941行 ← **既に前** |
| v32 | 同じ形 | 同じ形 |

`paintFacts` が入ったのは **panel v18**（`eefa76b`）で、そのときから**この順**でした。
v32 で差し込んだ一行（`#machLine`）は `.facts` の**外側**にあり、塗る筋には触れていません。

**なぜ今まで気づかなかったか** — 連絡なしは滅多に出ないうえ、出ているときは
状態の帯の赤い「連絡なし」に目が行きます。**いちばん知りたい「最後に見えていた時刻」が
横棒になる**という、いちばん困る形の抜けでした。

### 実測（手元で `state.json` を50分前にして再現）

| | 直す前 | 直したあと |
|---|---|---|
| 状態 | 連絡なし | 連絡なし（変わらず） |
| `#fVer` | **`版 —`** | **`本体 v1426 ／ 公開 v1426`** |
| `#fSeen` | **`最終確認 —`** | **`最終確認 20:23`**（＝最後に見えた時刻） |

ふつうの回（連絡ありのとき）も変わらず出ることを併せて確かめました。

## 2. もう一つの取りこぼし — 「公開 v未確認」が最大15秒

公開ページを **WebKit で実際に開いて**通信を覗いたところ、
`raw.githubusercontent.com` の `stable/ver.txt` は **HTTP 200 で中身も読めている**のに、
画面は「公開 v**未確認**」のままでした。

原因は**塗る回数**です。版の取得は投げっぱなしで、**塗るのは状態の取得（15秒おき）のときだけ**。
開いた直後は最初の `paintFacts` が**版より先に**走るため、
**最大15秒ぶん「未確認」**が出ます。stable は別の宿（raw）で遅れやすく、取り残されがちでした。

→ **版が届いたら、その場で塗り直す**ようにしました（`reFacts()`）。

## 3. 変更した行（`panel.html`）

| 行 | 中身 |
|---|---|
| **341–348** | `reFacts()` を足した（版が届いたら、その場で塗り直す） |
| **354・355・359・360** | `pullVer()` の四つの落ち（成功・失敗×2本）から `reFacts()` を呼ぶ |
| **1086–1097** | **`paintFacts(d.at || 0)` を「連絡なし」の早戻りより前へ移した**（これが本題） |
| **1112** | 元あった `paintFacts` の呼び出しを消した（上へ移したので二重になる） |
| **230 / 251 / `panel-ver.txt`** | 版を **v33** へ（三箇所そろえた） |

差分は **23行追加・8行削除**。`koushu-handan.html` には触っていません。

**v32 の二つ（機械の様子の一行・写した札の箱）は、v33 でも同じに動くことを確かめました**
（`panel-mach-probe.js` の11通り、すべて前回と同じ結果）。

## 4. 実機で見るところ

**返事パネル**を開いて、次の三つを見てください。

1. 画面のいちばん下の版の字が **`panel v33`**
   ＊v32 のままなら URL に `?_v=33` を付けて一度だけ開いてください
2. 機械の様子の一行の下が **「本体 v1426 ／ 公開 v1426」「最終確認 hh:mm」**（横棒でない）
3. **開いた直後から**「公開 v」に数字が出ていること（「未確認」が居座らない）

＊いちばん確かめたいのは **2 が「連絡なし」のときも出るか**です。
　状態の帯が赤い「連絡なし」になっている場面に出くわしたら、そのとき
　**版と最終確認が横棒に戻っていないか**を見てください。そこが今回の直しどころです。
