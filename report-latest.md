状態：終わり ／ 2026-08-21 03:02 ／ 知らせを三経路にした（押し通知・音・タイトルの印）。hook.log に追記も入れた

【保留】
- **本物の Stop でタイトルが変わるかの目視**（この応答が終わった瞬間に「●終わり」が付くはず）
- 倒れる音 三案（A/B/C）の選択
- ライフ0の倒れる絵の素材待ち（704x1524・白背景・剣は手から離さず杖のように突く）
- 船長の絵の素材待ち（127x180・PNG/RGBA・完全透過・足が下端接地）
- 振りの八枚の倍率の裁定（目盛り画像は `head-proof-*.png`）
- `sizing-review.png` を残すか消すか
- v1419 の実機検収

# 報告：知らせの経路を三つにした（押し通知・音・ウィンドウのタイトル）

日付 2026-08-21 ／ 本体の版 **v1419**（**リポジトリは無変更**。触ったのは `~/.claude/` の中だけ）

---

## 0. 先に — **前回の音のフックは、本物の発火が確認できました**

前回「次の応答で確認する」とお伝えした件です。記録にこう残っていました。

```
Notification 2026-08-21 02:39:35   ← 手で走らせたぶん
Stop         2026-08-21 02:39:38   ← 手で走らせたぶん
Notification 2026-08-21 02:48:07   ← 本物
Stop         2026-08-21 02:48:16   ← 本物（前回の応答が終わった瞬間）
Notification 2026-08-21 02:49:17   ← 本物
```

**フックは実際に発火しています。** 設定の書き換えは、`/hooks` を開かずにそのまま反映されました。
確認用の一時的な記録（`hook-check.log`）はもう使わないので、本式の `hook.log` へ切り替えました。

---

## 1.【1】ウィンドウのタイトルに状態を出す

| 場面 | 印 |
|---|---|
| 入力待ち・権限確認待ち（`Notification`） | **★待ち** を先頭に付ける |
| 作業完了（`Stop`） | **●終わり** を先頭に付ける |
| **作業を再開したら**（`UserPromptSubmit`） | **印を外す** |

**印は先頭に足すだけで、元のタイトルは残します**（`★待ち ◐ Session …` の形）。
付け替えのときは、前に付いている印を先に落としてから足すので、二重にはなりません。

### 分かったこと — **Claude Code 自身がタイトルを書き戻す**

子プロセスからタイトルを書き換えられることは確かめました（同じコンソールを引き継ぐため）。
**ただし、一度書くだけでは消えます。** 作業中は Claude Code が ◐ の回転を出すために
タイトルを更新し続けており、こちらの印がすぐ上書きされました（実測で確認）。

**対策として、0.25秒おきに8回、計2秒ぶん貼り直しています。**
待ちに入った直後の最後の一回に負けないための保険です。
**待っている間は Claude Code が更新を止めるので、そのまま残るはず**——ここは目で見ていただきたい点です。

---

## 2.【2】発火の記録 — `~/.claude/hook.log`

**追記のみ。消しません。** 一行はこの形です。

```
2026-08-21 03:01:44  notification  音=OK  押し通知=なし  タイトル=OK
2026-08-21 03:01:48  stop          音=OK  押し通知=なし  タイトル=OK
2026-08-21 03:01:51  resume        音=-   押し通知=なし  タイトル=OK
```

| 欄 | 中身 |
|---|---|
| 日時 | `yyyy-MM-dd HH:mm:ss` |
| どのフックか | `notification` / `stop` / `resume` |
| 音 | `OK`（鳴った）／`NG`（例外）／`-`（鳴らさない場面） |
| **押し通知** | 直近120秒に押し通知を出していれば、その**結果**。無ければ `なし` |
| タイトル | `OK`（書き換えた）／`NG`（書き換えられない） |

**押し通知の欄の埋め方。** フックからは押し通知が見えないので、
**こちらが `PushNotification` を呼ぶたびに、同じ記録へ一行書きます。**

```
2026-08-21 03:05:00  PushNotify    結果=送信済み（モバイル要求あり）
```

フック側は直近120秒のこの行を拾って欄に入れます。
**これで「送っていないのか、送ったが届いていないのか」が一つの記録で切り分けられます。**
押し通知を出していれば `PushNotify` の行が必ず残るので、**行が無ければ送っていない**、
**行はあるのに届いていなければ経路の問題**、と読めます。

---

## 3. 作りと、踏んだ穴

**設定には長い一行を書かず、スクリプト一本にまとめました** — `~/.claude/hook-notice.ps1`。
`-Kind notification|stop|resume` で呼び分けます。**追加のモジュールは使っていません**
（音は Windows 標準の wav を `Media.SoundPlayer` で鳴らすだけ）。

**踏んだ穴：`.ps1` は BOM 付きで保存しないと日本語が壊れます。**
Windows PowerShell 5.1 は BOM の無い `.ps1` を ANSI として読むため、
最初に書いたときは「★待ち」が化けて**構文エラーで三つとも終了コード1**になりました。
BOM を付け直したら通りました。**記憶にも残しました。**

**`args` の配列で渡し、シェルは通していません。** この端末は Git Bash があるので既定シェルが
bash に倒れ、`shell: "powershell"` は PowerShell 7（`pwsh`）を指しますが入っているのは 5.1 です。
実行ファイルと引数を分けて渡す形なら、どちらの事情も踏みません。

---

## 4.【3】確かめたこと

**フックと同じ引数で三つとも走らせました。**

| イベント | 終了コード | 所要 | 音 | タイトル | 記録 |
|---|---|---|---|---|---|
| Notification | **0** | 4463ms | OK | OK | 一行増えた |
| Stop | **0** | 4364ms | OK | OK | 一行増えた |
| UserPromptSubmit | **0** | 522ms | －（鳴らさない） | OK | 一行増えた |

所要は「音1.3秒＋貼り直し2秒＋起動」で辻褄が合っています。resume は貼り直さないので短い。

**本物の発火。** 前回の記録で `Stop` も `Notification` も本物が発火することは確認済みです（§0）。
**今回の追加ぶん（タイトルの印）は、この応答が終わった瞬間に「●終わり」として出るはずです。**
**そこだけは目で見ていただきたい**——こちらからはタイトルバーが見えません。
`hook.log` にも一行増えるので、次の応答で記録の側は確認して報告します。

---

## 5.【3】追加前の `settings.json`（前回からの差分）

前回の報告で全文を載せた5キーはそのままです。今回変えたのは `hooks` の中身だけで、
**`Notification` と `Stop` の音は消さず**、スクリプト経由に付け替えたうえで
**`UserPromptSubmit` を足しました**。

```
変更前： hooks = { Notification（音のみ）, Stop（音のみ） }
変更後： hooks = { Notification, Stop, UserPromptSubmit } すべて hook-notice.ps1 へ
```

既存5キー（`autoUpdatesChannel` / `skipDangerousModePermissionPrompt` / `theme` /
`inputNeededNotifEnabled` / `agentPushNotifEnabled`）は **5/5 残っていることを読み直して確認**しました。

---

## 6. 触れていないこと

- **リポジトリは無変更**（v1419 のまま）。
- **押し通知はそのまま。** 三経路とも出します。
- 追加のモジュールは入れていません。
- `hook.log` は**追記のみ**で、消していません。

---

## 7. 次

- **タイトルに「●終わり」が付いたかを見てください。** 付かなければ、貼り直しの回数か間隔を伸ばします。
- 次の応答で `hook.log` の本物の行を確認して報告します。
- 保留の残りは冒頭の一覧のとおりです。
