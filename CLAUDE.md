# 作法

このリポジトリで作業する際は、以下の作法を必ず守ること。

## 1. 進行手順

**宣言 → ヨシ → 実装 → 納品** の順を厳守する。

1. **宣言** — 何をどう変更するかを先に説明する
2. **ヨシ** — ユーザーから承認語「ヨシ」を受け取る
3. **実装** — 承認後にはじめてコードを変更する
4. **納品** — 変更結果を報告する

承認語「**ヨシ**」を得るまで、実装に着手してはならない。

## 2. 一応答一操作

1回の応答につき、操作は1つだけ行う。複数の変更をまとめて実行しない。

## 3. 頼まれていない改善を足さない

依頼された範囲だけを実装する。リファクタリング、命名の整理、コメント追加、
最適化などの「ついでの改善」は、明示的に頼まれない限り行わない。

## 4. バージョン表記の同時更新

`data-ver` 属性・`verTag` div・`ver.txt` の3箇所は、**必ず同時に更新する**。
どれか一つだけを更新してはならない。

- `koushu-handan.html` の `data-ver` 属性
- `koushu-handan.html` の `verTag` div
- リポジトリ直下の `ver.txt`

`ver.txt` は更新確認が読みに行くファイル。ここが古いと、新版を置いても
利用者に配信されない。

## 5. 変更禁止

以下は変更不可。触れてはならない。

- `analyze()`
- `bestShapeOneSuit`

## 6. 納品後の commit と push

納品の後、**必ず GitHub に commit と push まで行う**。

commit メッセージは、**v番号と変更内容を一行で書く**。

## 7. 詳細な作法は SKILL.md を読む

`koushu-handan.html` に手を入れる前に、必ず
`.claude/skills/koushu-handan-dev/SKILL.md` を読むこと。

上記1〜6に加えて、以下が規定されている。

- 編集は str.replace で、置換前に grep で一意性を確認する
- 構文検証は `<script>` を一時 `.js` に切り出して `node --check` にかける
- キャラのセリフは、書く前に既存を grep して口調を合わせる
- テスト用のシムは納品前に除去し、除去したことを報告に明記する
- 納品前チェックリスト

## 8. 実行時の確認は求めない

以下は、いちいち確認を求めずに実行してよい。

- ファイルの読み取り・検索
- git 操作（add / commit / push / status / log / diff）
- このリポジトリ内のファイル編集

ただし **1の「宣言 → ヨシ」の手順のみは維持する**。
何をどう変更するかを宣言し、承認語「ヨシ」を得てから実装に入ること。
確認を省略してよいのは実行手段であって、着手の承認ではない。

## 9. 報告の後は待たない

段取りをまとめて宣言し、その全体に「ヨシ」を得た場合、**各件の報告のあとに承認を待たず次の件へ進む**。
承認は冒頭の1回でよい。件ごとに止まると、まとめて頼んだ意味がなくなる。

一件ずつ頼まれた場合は、これまでどおり件ごとに「宣言 → ヨシ」を踏む。

## 10. 当たり判定は外箱44px方式

指で押す的は **44px 以上**、的同士の間隔は **8px 以上**。見た目を小さくしたい場合も、
**箱そのものを44pxにする**（外箱＝当たり判定、中身＝見た目、という入れ子にする）。

`::before` / `::after` で箱の外へ判定を広げる手は、**`overflow: auto` の箱の中では使わない**。
はみ出した判定ごと切り取られて、押せなくなる。投票欄（`.vz-chip`）がこの形。

例外は、`overflow` の箱に入っていない要素だけ。判定獣（`.verdict-mascot`）は、
箱を44pxにすると中央の列が縦に太ってカードが画面から溢れるため、疑似要素で判定だけ広げている。

## 11. 「ヨシ」は専有語

「ヨシ」「ダブルヨシ」は**現場猫の言葉**。

この決まりが掛かるのは、**マスコット六体**——スイッチ四者（**現場猫・枝豆・紳士・兎**）と
判定獣（攻の**虎**／守の**羊・鼠**）。六体のうち**ヨシを言ってよいのは現場猫だけ**で、
残る五者の側には書かない。**兎も「ヨシ」を言わない**——兎の台詞は「攻めだ。」「守りだ。」の
言い切りで、現場猫の言葉は借りない。`check.js` の ⑨ が見張っている。

**名簿の外**：探偵編の猫たち（ワトソン猫・茶色猫・警察猫・電話猫・船長猫）はマスコットではなく、
物語の登場者。名簿には入れない。

**この決まりの対象外**：**人柄七人・誤診記録・探偵編**。ここは物語と人柄の領分なので、
「ヨシ」が出ても違反ではない（一姫の台詞にある6件がこれに当たる）。

## 12. マスコットの作法

- **判定獣** — 攻めは**虎** 🐅（国士が見えていれば龍 🐉、役満手前なら鳥 🐦‍🔥）。
  守りは**羊** 🐑（字牌＋端牌が2枚以上）／**鼠** 🐭（それ未満）。この対応を崩さない。
- **一索の鳥は無言** — 吹き出しも文字も持たない。勝っても負けても喋らせない。
  - *例外*：**遊んでいる最中の鳥は無言のまま**で、ここは変えない。
    **勝敗の結果絵に限り、絵に含まれる吹き出しを許す**（負け絵の「まだまだだね」がこれ）。
    許すのは**絵の中に描かれている吹き出し**だけで、文字を別に描き足すことはしない。
- 遊んでいる最中の画面に現場猫は出さない。

## 13. ミニゲームの数は「同時◯羽」と「ノルマ◯羽」を書き分ける

一索の鳥には数が二種類ある。混ぜて書かない。

- **同時◯羽** — 画面に同時に飛んでいる羽数（`birds`）
- **ノルマ◯羽** — 勝つために捕まえる数（`quota`）

## 14. 検査は「写しに probe」方式

検査のために**本体へ仕掛けを入れない**。本体の写しを一時ディレクトリに作り、
そこへ探索用の一片（probe）を差し込んで駆動し、実行後に写しごと消す。

- 駆動は headless の Edge / Chrome ＋ `--dump-dom`
- 結果は base64 の目印つきで DOM に置き、吐かれた HTML から拾って読む
- ブラウザが無い環境では、その検査だけ飛ばす（まとめで PASS と紛れないようにする）

## 15. window に診断口を残さない

検査の都合で内部を覗きたくなっても、`window` に診断用の口を生やさない。
`analyze()` の内部も公開しない。**画面の文字とボタンで辿る**か、字面の照合で済ませる。

探偵編の回帰（`adv-check.js`）は、状態が読めない前提で画面だけを辿って書いてある。

## 16. 画面に収まっているかの合格条件

`check.js` の ⑦ が見張る。合格の条件は次のとおり。

- はみ出し・重なり・横溢れが無いこと
- **判定後は、末尾の二択（連チャン／親流れ）の bottom が視野の高さ以内**であること

判定後の画面は縦に送れる作りなので、**縦の溢れは咎めない**。
また、**送れる箱**（`overflow-x: auto/scroll`。投票欄など）と、
**「…」で切る箱**（`text-overflow: ellipsis`）の横溢れは数えない。切れていることが読み手に分かるため。
`overflow: hidden`（何の合図もなく切れる）は数える。

## 17. 台詞に触れた納品は serifu.txt の再抽出まで

キャラの台詞を足した・変えた回は、**`serifu.txt` の再抽出まで含めて納品**とする。
口調の混入検査も併せて行う。抽出し忘れると、台本と本体が静かにズレる。

再抽出は **`node serifu-extract.js`** を回す。手で書き換えない。版表記も行番号も口調の混入も、
このスクリプトが本体から組み直す。**版番号を上げてから回すこと**（版表記は `data-ver` を読む）。
いまの `serifu.txt` が本体と合っているかは `node serifu-extract.js --check` で見る。

## 18. 納品前は `node check-all` を回す

`check.js`（本体）と `adv-check.js`（探偵編の回帰）を続けて回す入口。
片方が落ちても両方回り、どちらかが FAIL なら終了コード1になる。

**速い版とフル版がある。**違いは ⑦（画面の溢れ）の実測だけ。

| | 回し方 | ⑦の範囲 | 目安 |
|---|---|---|---|
| 速い版 | `node check-all --fast` | 9画面 × **568x320 のひと視野** | 2〜3分 |
| フル版 | `node check-all` | 9画面 × **4視野** | 約9分 |

- **通常の納品と push 前は速い版。** いちばん狭い視野は溢れがいちばん出やすいので、
  画面の取りこぼしを作らずに時間だけ縮む。
- **フル版を回すのは、台詞を触った回・レイアウトを触った回・バッチ末尾のまとめ報告の前。**
  視野ごとに効く指定（`@media` の分岐）が絡むため、狭い視野だけでは足りない。

**⑬（非日本語文字の混入）は既知例外なしのゼロ基準。** キリル・ハングルなど、日本語でも
記号でも絵文字でもない字が**1件でも出たら FAIL**。`Ａ` と `А` のような見た目の同じ字は
目では気づけないので、例外表を作らずゼロで通す。

**フル版は `Start-Process` で切り離して回す。**フル版は10〜12分かかり、前面で回すと10分の
上限に当たり、背後の仕事として回すと途中で止められる（2026-08-24、四回続けて⑯前後で切断）。

```powershell
Start-Process -FilePath (Get-Command node).Source -ArgumentList check.js `
  -WorkingDirectory C:UsersuserDesktopmahjongkoushu-handan `
  -RedirectStandardOutput <出し先> -RedirectStandardError <誤りの出し先> -PassThru
```

起こしたら出し先のファイルを見て進み具合を確かめる。`adv-check.js` は30秒ほどなので前面でよい。

push の前は `.githooks/pre-push` が速い版を自動で回す。使うには一度だけこれを設定する。

```
git config core.hooksPath .githooks
```

## 19. 検収の済んだ版に `stable` を進める

`stable` タグは「**実機で確かめて、問題なしと言われた版**」を指す。
納品しただけ・検査を通しただけの版には付けない。

検収OKが出たら、その版へ進める。

```
git tag -f stable <その版のcommit>
git push -f origin stable
```

いまどこを指しているかは、こう見る。

```
git log -1 --format="%h %s" stable
```

**どの版が検収済みかは、こちらで決めない。** 実機で確かめた版を言ってもらってから進める。
不具合が見つかった版を指したままにしないこと（戻り先として使えなくなる）。

## 20. 人柄七人の担当

七人は同じ13枚を別々の材料で見る。**見立て行（助言欄の一行）と、金枠で光る牌は、必ず同じ材料を指す。**
どちらか片方だけ変えない。

| 人柄 | 見立て行 | 光る牌 | 印の名 |
|---|---|---|---|
| 執事 | 掛け持ち点検 | 盾・雀頭・攻めの種のうち二役以上を兼ねる牌 | `multi-mark` |
| 軍師 | 鳴きの適性 | 役牌の対子／染めに寄せる色／どちらも無ければ最も厚い色の数牌 | `naki-mark` |
| ずんだ | 待ちのかぶり（二度受け・筋被り） | かぶっている二つのまとまり | `dup-mark` |
| お嬢様 | 盾の寿命 | 盾のうち、切る当てに入っている牌 | `safe-mark` |
| マダム | 浮き牌 | どのまとまりにも入らない中張牌 | `float-mark` |
| 一姫 | 強形 | 五連形・四連形・中ぶくれ（無ければ端にかかるターツと字牌の対子） | `edge-mark` |
| 先生 | 雀頭 | 雀頭になれる牌 | `head-mark` |

材料は `toneFacts()` が判定のたび一度だけ組み、`window._lastFacts` に控える。
**人柄チップを押すたびに数え直さない。** 台詞（`toneAngleLine()`）も印もここを見る。

**スイッチ四者のうち三者は牌を光らせる。** 人柄の担当ではないので**金枠は使わない**。見張り台の
答え合わせと同じ色・同じ光らせ方（`outline` ＋ぼかし）で出す。

| 道具 | 光る牌 | 色 | 印の名 |
|---|---|---|---|
| 現場猫 | 猫牌（1〜9の孤立牌。実物の猫牌も一枚と数える） | 青 `rgba(120,170,220)` | `cat-mark` |
| 枝豆 | 二枚以上ある種類の牌 | 緑 `var(--jade)` | `eda-mark` |
| 紳士 | 一枚きりの字牌 | 赤 `#c0392b` | `om-mark` |
| 兎 | **光らせない** | — | — |

**兎だけは牌を光らせない。** 材料が牌ではなく残り枠の埋まり方（`totalBlocks` /
`fillGood` / `fillWeak`）だからで、判定獣を対象外にしたのと同じ理由——まとまりを光らせると
13枚の大半が光り、印として働かない。投票欄には四人目として一票だけ立つ。

**点くのは常に一つだけ。** `toolMark` が持つ。道具を点ければ人柄の金枠は消え、
同じ印をもう一度押すか人柄を押せば戻る。虎（判定獣）が喋る間は両方消える。
**判定獣は対象外**——まとまりは13枚の大半が光り、印として働かない。

**投票欄の傾き（`tenLeans()`）は別物。** あちらは実測50000回で引いた攻守の物差しで、
見立て行を振り直しても触らない。

弱形（嵌張・辺張）に使った牌は `analyze()` から出てこないため、同じ順位付けの分解を
`toneBlocksOneSuit()` に写してある。**片方だけ直さないこと。** `check.js` の ⑭ が
条件と再帰の引数を字面で突き合わせて見張っている。

## 21. 素材の作法

剣士（`EDA_WHIP` / `EDA_NAGE`）まわりの絵を触るときの決まり。散らばっていた注意をここへ集めた。

**元絵はリポジトリ直下に `*_src.png` で置く。** 704x1524 の白背景が現行の形。
`kishi_*` / `hoshi_*` と同じ「**原版として残す**」扱いで、消さない——値の測り直しや
コマの切り直しの戻り先になる。

**同名での作り直しを禁じる。** 差し替えるなら元絵の名を変える。同じ名前で中身だけ替えると、
どの版から焼いた素材なのかが後から辿れなくなる。

**透過素材の点検は、暗背景と白地の両方で目視する。**
`node asset-proof.js` が32枚を足元揃えで二枚組に焼く（`asset-proof-dark.png` ＝ 本番と同じ
`#0d1b16`／`asset-proof-light.png` ＝ 白地）。**素材を差し替えたら、この二枚を見るまでが検収。**

数値では出ない。v1399 で見つかった刀身の欠け——銀の刀身・白い耳・白いタイツが
「明るさ＝不透明度」の白抜きで落ちていた——は、平均・割合・指紋のどの数値検査もすり抜けた。
暗背景の一覧に並べて初めて分かる。白地の側は、輪郭に白い縁が残っていないか（アンマットの
取りこぼし）を見るために要る。

**白抜きに明るさを使わない。** `char_export.js` は、画像の縁から繋がっている白だけを背景とみなし、
人物の内側はどれだけ白くても不透明のまま残す。縁2画素だけ半透明にして馴染ませ、そこにだけ
アンマットを掛ける。**元絵の縁には 219〜235 の薄い枠が一画素だけ回っていることがある**ので、
縁の一周は色を問わず背景の種にする（残すと切り出しが元絵いっぱいに広がる）。

**斬撃の弧は旧法で薄く焼いてある。** いま本体に入っている弧を新しい道具で焼き直すと濃く出る。
弧を持つコマ（`airSlash*` と `u/ur/r/dr/d/dl/l/ul`）を焼き直したら、必ず暗背景で新旧を見比べること。
道具の冒頭にも同じ注意を置いてある。

**内側の白い袋（腕と胴の間・脚の間・弧の内側）の透過は、目視で指定して行う。**
黒で塗るのではなく透過にする——画面では暗い背景がそのまま見えるので黒く塗ったのと同じ見え方になり、
鳥が剣士の後ろを通るときも隙間から見えて自然になる。

**この判定を `char_export.js` に入れてはならない。** 輪郭に囲まれた白い服（タイツ・耳・刀身）と
機械では区別が付かず、v1399 の刀身の欠けと同型の事故になる。袋に番号を振った図を出して人が決める。

**`EDA_FOOT`（足元の行）と体軸（`EDA_WHIP_AXIS` / `EDA_AXIS`）は、機械の測定が迷走したら
目視読みが最終。** 角度の自動判定は既に止めてある（刃と弧を取り違えるため）。
`char_export.js` が書き出す目盛りつきの確認画像を見て、目で読んだ値を入れる。

## 22. Service Worker は入れない

**オフライン化のための Service Worker は採用しない。**`ver.txt` 方式の更新と衝突し、
古い本体を掴み続ける事故の温床になる。将来これを検討したくなったら、まずこの一行を読むこと。

更新の経路はこう組んである——起動のたびに `ver.txt` を `cache:'no-store'` ＋
`?_chk=<時刻>` で取りに行き、版が新しければ `?_v=<新版>` を付けて本体へ跳ぶ。
`ver.txt` が無い環境では本体の `data-ver` を読む方式へ落ちる。この二段のどこにも
中間のキャッシュを挟まないことが前提になっている。

## 23. 指示は「以上」で閉じる

チャット側からの指示は、末尾に **「以上」** の行を付けて送られる。

**「以上」で終わっていない入力が届いたら、作業も長い返答もしない。**
「**断片を受領、続きを待ちます**」と**一行だけ**返して待つ。届いた断片はそのまま控える。

**「以上」が届いた時点で、控えた断片を順に繋ぎ、一つの指示として実行する。**

単独で完結する短い入力（「ヨシ」「もう一度試す」など）は、この限りではない。

**なぜこうするか** — iPhone の Code タブに複数行の文を貼ると、**複数件に割れてキューへ
積まれる**。2026-08-24 18:47 の実測で、一つの指示が**6件**に割れて届いた。
断片ごとに丸ごとの応答が走ると、時間も使用量も食う。断片への返答を一行に抑えるのは、
そのため。**割れた断片の一つだけを見て動くと、指示の後半を読まずに着手することにもなる。**
